server/translated/
server/fonts/
server/nextwork/
server/__pycache__/
server/utils/__pycache__/
server/zip/
server.zip
# 密钥文件不入库（模板见 server/config/config.json.example）
server/config/config.json
server/config/config.json.task
logs/*.log
*.staging
*.backup

# Python 缓存（项目根及任意层级，含 tools/）
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 开源红线：版权译文/个人数据/交换目录 不入库（见 docs/OPEN_SOURCE_CHECKLIST.md）
inbox/
out/
segflow/
ledger/
tools/table_pipe/*_response*.tsv
对照抽查点.md
docs/
zotero-pdf-2-zh-*.xpi

# 开源红线二期: 含本机路径的工具/文档 (本地保留, 暂不开源)
# [2026-09-20 v28.30] tools/tests/ 与 tools/sync_patches.ps1 已脱敏入库:
#   本机路径一律改为 sys.prefix / PDF2ZH_PYTHON / PDF2ZH_VENV_SITE 派生(不含用户名),
#   公开 clone 跑 tools/tests/run_all.py 应当全绿(被测对象缺失的套件自动 SKIP)。
#   契约: 入库脚本里不得出现 C:\Users\<某人>(用户身份) 或焊死到某个 venv 的绝对路径。
#   项目默认根 D:\zotero-pdf2zh 作兜底缺省是既有约定(有 P2Z_PROJ 覆盖), 不算违规。
logs/
UPGRADE_RUNBOOK.md
server/md-translate/md2pdf.py
tools/seam_review.py
tools/seams_report.py
tools/strategist.py
tools/tests/test_strategist.py
zotero-pdf-2-zh-*.xpi

# 密钥红线: 含真实 key 的配置 (模板 *.example 不受影响)
server/config/config.json
server/config/config.toml
server/md-translate/config.json

# 用户数据红线: 概念链接规格里有本机的译文锚文本(模板见 user_links.example.json)
#   [2026-09-26 发布准备] 原先是入库的; 里面唯一那条真条目带着一篇论文的译文片段,
#   属本项目自订的"绝不放版权译文"红线 -> 移出库(盘上那份一个字节没动)。
/user_links.json

# 本机路径与个人文件 (开源红线): 本地保留不入库
server/config/local_paths.json
# [2026-09-22 v28.67] tools/dedouble_sweep.py 解除本机保留、已入库:
#   v26.7 那版写死三个本机路径且 import 即改真库; 经 P3 重写(__main__ 守卫 + 路径全参数化
#   + 写库前先落备份)后文件内已无本机盘符/用户名 -> 公开分发(旧版见历史 5293526)。

# 解压残留与运行时状态 (上游 zip 重下会再带回来, 只忽略不清理)
.DS_Store
__MACOSX/
server/config/package_update_state.json

# 本机探针/临时产物 (xpi 解包试验、声音试听; 代码不引用, 防漏提交)
state/

# [2026-09-25 v33] 渲染期取证件与面板运行时标记不入库:
#   verify/ 下是逐页像素快照 PDF (本机已 77 MB), 复现件留在盘上即可, 不进版本库;
#   .panel_vendor 是面板写的 18 字节运行时标记, 不是源码。
verify/
tools/table_pipe/.panel_vendor
