# zotero-pdf2zh 自定义改动记录

> 记录相对官方默认版本的**全部自定义改动**，用于升级/重建环境后对照还原。
> 最后更新：2026-08-17

---

## 一、服务器配置改动

### 1. `D:\zotero-pdf2zh\server\config\config.json`（与 `.example` 同步修改）

| 项目 | 值 | 官方默认 | 原因 |
|---|---|---|---|
| `translators` 新增 **silicon** 段 | `SILICON_API_KEY`=sk-zepsr...（见文件）、`SILICON_MODEL`=`deepseek-ai/DeepSeek-V3.2` | 无此段 | 接入硅基流动翻译 |
| `translators` 新增 **openailiked** 段 | 百炼 dashscope base_url + Key + `qwen-plus` | 无此段 | 备用翻译服务（普通论文可用） |
| `translators.deepseek` | `DEEPSEEK_API_KEY`=sk-df02...、`DEEPSEEK_MODEL`=`deepseek-v4-flash` | 默认 null / 默认模型 | 已填 Key（注：`deepseek-v4-flash` 在官方 API 不存在，官方服务实际不可用） |
| `NOTO_FONT_PATH` | `D:\zotero-pdf2zh\server\fonts\NotoSerifCJKsc-Regular.otf` | Linux 路径 `/app/SourceHanSerifCN-Regular.ttf` | **中文方块字的根本修复**（Windows 上默认路径无效） |
| `PDF2ZH_LANG_FROM` / `PDF2ZH_LANG_TO` | `English` / `Simplified Chinese` | — | 英译中 |

### 2. `C:\Users\<user>\.config\PDFMathTranslate\config.json`（pdf2zh 1.x **实际读取**的位置）

- 官方默认：空/未创建（pdf2zh 1.x 不读服务器目录的 config.json）
- 我们写入：`NOTO_FONT_PATH`（同上 D 盘字体）+ `openailiked` 段（百炼）

### 3. `D:\zotero-pdf2zh\server\config\config.toml`

| 项 | 值 | 说明 |
|---|---|---|
| `bing` | `true` | 内置免费翻译兜底 |
| `[translation] lang_in / lang_out` | `en` / `zh-CN` | 英译中 |
| `qps` | `200` | 限速放宽 |
| `ignore_cache` | `true` | 忽略缓存 |
| `[pdf] formular_char_pattern` | `(\||\+|-|=|\d|\.|\$|\[|\]|\{|\}|%|,|/|√|×|÷|≥|≤|≈|[\u0080-\ufaff])` 等 | 公式保护正则 |
| `[pdf] formular_font_pattern` | `(CM.*\|TeX.*\|.*Math\|.*Ital\|STIX\|MS.*\|Times-Italic)` | 公式字体正则 |

---

## 二、服务器代码改动

### 4. `D:\zotero-pdf2zh\server\server.py`

- **`prepare_path()`**（约 967-996 行）：`.example` **不再覆盖**已有 config 文件（官方：每次启动覆盖 → 自定义 Key 全丢）
- **`translate_pdf()`**（约 1016-1060 行）：
  - 公式保护参数改用 **`-f`（--vfont）/ `-c`（--vchar）**（官方用 `--formular-*-pattern`，pdf2zh 1.x **不识别** → 无效）
  - 自动追加 **`--skip-subset-fonts`**（避免字体子集化导致公式乱码）
  - **移除 `--compatible`**（Windows 上 pikepdf 权限报错）
- 新增服务映射：`'openailiked': 'openaicompatible'`

> **2026-09-01 修复**：`-f`/`-c` 曾在上次同步中丢失（记录仍写着"已改"，实际代码里没有），本次重新补回。
> 实现改为从 `config.toml` 的 `[pdf]` 段读取 `formular_font_pattern` / `formular_char_pattern`（值为 `"null"` 则跳过），
> 读取失败仅告警不中断。**已验证**：pdf2zh 1.9.11 的 `create_parser()`（第 60-73 行）确实支持 `-f`/`-c`；
> `--config` 经 `pdf2zh.py:263-264` 的 `ConfigManager.custome_config` 生效。

### 5. `D:\zotero-pdf2zh\server\utils\config.py`

- **`update_config_file()`**（约 163-184 行）：插件传**空值时不覆盖**已有配置，只把映射 key 加入保留列表（官方：直接覆盖 → Key 被清空）
- **POLISH 键保护**（2026-08-29 v6，配合润色钩子）：`update_config_file()` 的 pdf2zh 分支在"删除旧 key"前插入白名单逻辑——①从 `server/config/config.json` 对应 service 段读取所有 `POLISH_*` 预置键补写进目标 envs 并加入 translator_keys；②删除循环因此不会清掉它们。**根因**：官方逻辑每次翻译按插件请求重建目标 translator 的 envs，未携带的 key 一律删除 → 预置的 POLISH 配置每翻一篇就被清空 → 润色钩子静默失效（已实际发生：CLAP 论文翻译时钩子未生效，且该次实际走的是 openailiked/百炼而非 silicon）。**此文件改动需重启 Server 才生效**（config.py 在服务器进程内运行）

> **2026-09-01 修复（BUG）**：上述"空值保护"**只在 `pdf2zh_next` 的基本参数分支真正生效**，
> 其余 3 个 `else` 分支都漏了 `translator_keys.append(...)`，导致空值虽然不覆盖，
> 但该 key 会被随后的"删除不在保留列表中的 key"循环清掉 —— **补丁形同虚设，配置照样丢**。
> 本次在以下 3 处补上 `append`：
> 1. pdf2zh 分支 · 基本三参数（`apiKey`/`apiUrl`/`model`）
> 2. pdf2zh 分支 · `extraData`
> 3. pdf2zh_next 分支 · `extraData`
>
> **影响面**：此前插件对任一配置项传空值，该配置的已有值就会被清空。
> POLISH 之所以没丢，是靠上面那条单独补丁救的；其他键（如 `GEMINI_MODEL`、`DEEPLX_ENDPOINT`
> 等插件可能不传的）一直在裸奔。

### 5b. `D:\zotero-pdf2zh\server\utils\environment_lifecycle.py`（2026-09-01 新增）

- **`maybe_prompt_existing_user_update()`**（约 890 行）：**冻结翻译环境更新**，函数开头直接 `return`，不再弹出 y/n 询问。
  - **背景**：原逻辑靠 `not sys.stdin.isatty()` 静默跳过——后台 `launch.ps1` 启动时无 tty，所以这个询问从未出现；
    但一旦前台运行 `python server.py` 排障，就会开始询问。
  - **风险**：一旦回答 y，`transactional_install_or_update()` 会以 `-I`（`--ignore-installed`）
    **强制重装**，把 site-packages 内的自研补丁覆盖成官方原版。
    `venv.json` 的版本钉死（`==1.9.11`）只能防"升版本"，**防不住"同版本强制重装"**。
  - **注意：不可用 `--enable_winexe true` 达成此目的** —— 该参数会让 `server.py:1151` 改用
    `winexe_path` 指向的 `pdf2zh.exe` 执行翻译，**完全绕过 venv 内的润色补丁**
    （默认路径是 `pdf2zh-v2.6.3` 的 exe，与钉死的 1.9.11 不是同一个东西）。
  - **恢复**：删除该函数开头的 `return` 即恢复上游行为（原逻辑已注释保留备查）。

### 5c. 更新冻结全面强化（2026-09-02，用户要求"想办法先关闭更新"）

- **`server/config/venv.json` 全量钉死**：此前仅 `pdf2zh==1.9.11`、`numpy==2.2.0`、`pdfminer.six==20250416` 有版本号，
  其余（`pdf2zh_next>=2.9.0,<3.0.0`、`pypdf`、`PyMuPDF`、`flask`、`toml`、`packaging`）全部浮动。
  现按当前安装版本全部钉死：`pdf2zh_next==2.9.0`、`pypdf==6.16.1`、`PyMuPDF==1.25.2`、
  `flask==3.1.3`、`toml==0.10.2`、`packaging==26.2`（conda 与 uv 两个分支同步修改）。
  **理由**：浮动版本在环境重建/强制重装时会装最新版，造成行为漂移；
  特别是 `PyMuPDF`（字体渲染）与 `pypdf`（PDF 解析）直接参与翻译链路。
- **`server/requirements.txt` 钉死**：该文件每次启动都经 `uv run --with-requirements` 处理，
  原先全部浮动（`flask`/`toml`/`pypdf`/`argparse`/`PyMuPDF`/`packaging`），现钉死到当前安装版本。
  （`argparse` 为标准库占位，保持原样。）
- **更新口子核对结论**（至此全部关闭）：
  1. Server 源码更新 → `launch.ps1` 的 `--check_update false` ✅
  2. 翻译环境更新询问 → `environment_lifecycle.py` 开头 `return`（5b）✅
  3. `venv.json` 版本钉死 → 本次全量补齐 ✅
  4. `requirements.txt` 浮动 → 本次钉死 ✅
  5. `venv.json`/其他 config 被上游覆盖 → `prepare_config_files` "用户值优先" ✅
- **`config.json` 新增 `POLISH_DOMAIN`**（silicon 服务 envs）：领域标签注入润色 prompt，
  消解术语歧义（EMNLP 2025 实证）。当前值 `topology optimization / soft robotics`，
  **每篇论文领域不同，翻译前按需修改**；留空/删除该行 = 关闭。
- ⚠️ **上游预警**：server 启动日志显示作者公告——"项目将全面重构并预计 9 月发布新版本"。
  9 月升级上游前务必先打 git tag，`patches/` 重放将面临真正考验。

---

## 三、第三方库补丁（pdf2zh venv）

### 6. `D:\Users\<user>\anaconda3\envs\zotero-pdf2zh-venv\Lib\site-packages\pdf2zh\translator.py`

- **`set_envs()`**（约 63-78 行）：只合并**非空**配置值（配合第 5 项）
- **润色钩子**（2026-08-29 新增，自研）：
  - `BaseTranslator.translate()`（约 106-113 行）：`do_translate` 之后调用 `self.polish(text, translation)`（若存在），异常时回退初译
  - `OpenAITranslator` 新增 `_load_polish_glossary()` 与 `polish()`（约 477-537 行）：二次润色方法，带 3 段上下文窗口（译名/指代一致）+ 术语表强制注入 + `{{vN}}` 占位符保护（润色破坏占位符即回退初译）
  - **反思模式升级**（2026-08-29 二次迭代）：`POLISH_REFLECT=1` 开启"审校批判-修订"两段式（初译→分【忠实性】【流畅性】两组输出建议→按建议修订，批判为"无明显问题"时短路跳过修订）；`POLISH_GUIDELINE` 注入翻译指南文本（语域/术语/风格约定）至每次润色 prompt
  - **人审报告**（2026-08-29 三次迭代）：`POLISH_REPORT_DIR` 指定报告目录，每次翻译任务生成 `审校报告_时间戳_进程号.md`，每段四栏对照（原文/初译/审校建议/自动修订）追加落盘，供人工终审；批判 prompt 增设第三组【存疑】——术语表译名与语境冲突的条目**不自动修改**、保持初译现状并写入报告交人工裁决；修订 prompt 相应禁止落实存疑组
  - **v4 加固**（2026-08-29 四次迭代）：① 启动自检——translator 初始化时向服务器控制台 print `[润色钩子] 已加载 | 反思=x | 锚点断言=x | 人审报告=x | 术语表=x | 指南=x`，钩子因环境重建/升级丢失时立即可见（防静默失效）；② 上下文窗口改 `threading.local` 线程隔离（property `_polish_context`），并发翻译时段落顺序不再互串；③ 【存疑】条目用正则单独抽取，落盘独立小文件 `存疑清单_时间戳.md`（人审疲劳解法：终审只看这份裁决术语冲突）；④ 存疑清单不含忠实性/流畅性内容，主报告仍完整保留三组
  - **锚点断言**（2026-08-29 五次迭代，批判者不可审问题的确定性缓解）：`_anchor_check()` 润色后做不经 LLM 的确定性校验——①数字保真 ②不译词保留（术语表 target==source 条目）。初译完好→回退初译；初译同违规→保留修订但报告标注。`POLISH_ANCHOR=0` 可关闭，默认开
  - **锚点调优**（2026-08-30 六次迭代，首次实战 Exton 2001 后校准）：①数字检查剔除**文献引用括号**（`[15–17, 24]` 区间端点曾大量误报）②数字缺失降级为**弱信号只记录不回退**（存在合法数字转换如 "1630 hours"→"16时30分"，实战中 11 处"初译同违规"绝大多数为误报；不译词丢失仍为强信号回退）③存疑点为"无"的段落不再写入存疑清单。测试 4 项全通过
  - `OpenAITranslator.__init__` 新增配置（约 447-460 行）：`POLISH=1`、`POLISH_REFLECT=1`、`POLISH_GLOSSARY`（术语表 CSV）、`POLISH_GUIDELINE`（指南 txt）、`POLISH_MODEL`（可选润色模型），开启项分别计入缓存参数与未开启缓存隔离
  - 生效范围：所有 OpenAI 系服务（openailiked/silicon/deepseek/zhipu/gemini 等）；Google/Bing 等无 LLM client 的服务不受影响
  - 离线单元测试全部通过（v1: 4 项；v2 反思模式: 4 项——两段式调用次数/指南注入/无问题短路/占位符保护/非反思回退）
  - 配套改动：config.json 与 `.example` 的 openailiked/silicon 段新增 `POLISH: "1"`、`POLISH_REFLECT: "1"`、`POLISH_GLOSSARY: D:/zotero-pdf2zh/server/glossary/terms.csv`、`POLISH_GUIDELINE: D:/zotero-pdf2zh/server/glossary/guideline.txt`；新建 `glossary\terms.csv`（三列术语表，UTF-8 无表头）与 `glossary\guideline.txt`（通用学术翻译指南模板，每篇论文可改）

### 7. `D:\Users\<user>\anaconda3\envs\zotero-pdf2zh-venv\Lib\site-packages\pdfminer\encodingdb.py`

- **`get_encoding()`**（约 122-138 行）：新增 **`C{xx}` glyph 名回退解码**
  - 规则：老式 Type1 字体 `Encoding/Differences` 里的 `/C{xx}`，xx 即 Adobe StandardEncoding 码
  - `xx < 128` → `chr(xx)`（ASCII）；`xx >= 128` → `std2unicode[xx]`
  - **核心修复**：解决老 PDF 抽不出文本 → 全部变成 `(cid:N)` → pdf2zh 当公式不翻译 → 输出仍是英文

### 8. `D:\Users\<user>\anaconda3\envs\zotero-pdf2zh-venv\Lib\site-packages\pdfminer\pdffont.py`（2026-08-30 新增）

- **字体字符校正表**（`_apply_font_char_fixes()`）：
  - **根因**：Exton 2001 等 Elsevier 老 PDF 的 AdvTimes/AdvPS 系列字体用自定义编码，`C{xx}` 名的语义与 ASCII 不完全对齐，部分码位（±、−、分隔符）落在 Differences 之外回退 Standard 解码 → 系统性错字：`±→I`、`−→)`、`分隔→Æ`。此损伤发生在抽取层，翻译与钩子无法修复
  - **实现**：`PDFSimpleFont.__init__` 末尾与 `PDFType1Font.__init__` 内置编码重建后，应用 `server/config/font_char_fixes.json` 校正表（键=字体名子串，规则=[码位, 当前错误解码, 正确字符]）
  - **防误伤**：仅当该码位当前解码 == 记录的错误解码时才替换（Times-Roman 的真字母 I 未受影响）
  - 校正表现有规则：AdvTimes 73:`I→±`、AdvPSSPS-AS 2:`)→-`、AdvPSMP1 1:`Æ→,`
  - **验证**：重抽 Exton PDF，Æ 归零、负号/± 复原、真字母 I 保留
  - **扩展方式**：遇到其他老 PDF 的同类错字 → 在 JSON 里加规则即可，无需改代码

---

## 四、环境层

### 更新冻结策略（2026-08-29 制定）

| 项 | 状态 | 说明 |
|---|---|---|
| pdf2zh | 钉死 `==1.9.11`（venv.json 原本已钉） | 深度补丁环境，禁止浮动 |
| pdf2zh_next | 钉死 `==2.9.0`（原 `>=2.9.0,<3.0.0` 已改） | 防止 venv 重建时静默升级 |
| Server 启动更新检查 | `--check_update false`（launch.ps1 已改；手动启动命令也须带此参数） | 默认 True 且会询问 y/n，按 y 会用上游源码**覆盖 server.py/utils 自定义改动** |
| 翻译环境更新询问 | environment_lifecycle 每版本询问一次"是否更新翻译环境"，**一律答 n** | 同意会重装 site-packages，冲掉 translator.py/encodingdb.py 补丁 |
| Zotero 插件 xpi | 只手动安装 | 不自动升级 |

**更新决策流程**：想更新时先让 AI 拉取上游 release notes，对照本文件检查 ①是否触碰已补丁文件（translator.py/encodingdb.py/server.py/utils/config 处理）②是否修复了我们正踩的已知 bug ③新功能是否值得重放补丁。三问通过才执行更新 runbook：git 提交快照 → 更新 → 从 patches/ 重放补丁（若官方文件结构变化则人工重新移植）→ 跑离线测试 → 翻一篇 PDF 验证 → git 打 tag。

| 项目 | 说明 |
|---|---|
| conda 环境 | `zotero-pdf2zh-server`（Python 3.12），服务器专用 |
| tencentcloud-sdk-python / -tmt / -common | **固定 3.1.121**（新版删了 `TextTranslateRequest`，pdf2zh 1.x 报 ImportError） |
| 中文字体 | `D:\zotero-pdf2zh\server\fonts\NotoSerifCJKsc-Regular.otf`（Noto Serif CJK SC） |

---

## 五、启动命令（服务器）

```powershell
$env:PYTHONUTF8=1; D:\Users\<user>\anaconda3\envs\zotero-pdf2zh-server\python.exe D:\zotero-pdf2zh\server\server.py --env_tool=conda
```

- 必须在**用户自己的终端**启动（沙箱环境无权限写 `translated\` 目录）
- 启动前确认无残留旧 python 进程（否则旧配置可能覆盖新配置）

---

## 六、Zotero 插件设置（当前生效）

| 项 | 值 |
|---|---|
| 翻译引擎 | `pdf2zh` |
| 翻译服务 / LLM API | `silicon` |
| 模型 | `deepseek-ai/DeepSeek-V3.2` |
| API Key | 留空（服务器 config.json 已存） |
| 服务器地址 | `http://localhost:8890` |

---

## 七、翻译质量问题（已知残留）

- 作者/关键词分隔处显示 `Æ`（老 PDF 字体 C225 字形 = 逗号，pdfminer 解成 Æ）——不影响正文
- 个别数字标点错位（如 `第0.天1630时`）——pdf2zh 公式保护拆分的固有问题
- 少量 `(cid:)` 残留（版权行等特殊字形）——老 PDF 字体所致

## 2026-09-02 23:20 润色钩子配置防覆写修复
- 问题: POLISH/POLISH_REFLECT 键在插件推送配置时被置 null, 导致 silicon 通道润色钩子静默失效(成功翻译但无人审报告)
- 修复: server/utils/config.py 预置保护逻辑改为从 config.json.example(稳定托管默认)读取 POLISH_* 键, 每次提交强制回填, 不再依赖被反复覆写的 config.json
- 同时回填 config.json: POLISH=1, POLISH_REFLECT=1

## 2026-09-03 00:15 新增 tools/pre_check.py 翻译前体检器
- 功能: 提交翻译前用 pypdf 零成本体检——参考文献页检测(行首条目式[n]特征,自动推荐skipLastPages)、扫描页检测、公式密集页预警、加密/损坏预检、疑似伪中文字符提示
- 背景: 参考文献页双栏小字号版面解析失败(9/2 TO-SoFiT 事故)与卡死任务(16分钟白等)均可由此提前拦截
- 依据: BabelDOC ACL 2026 论文的 Untranslated Blocks 质检指标 + zotero-pdf2zh issue#64 官方解法(pdf2zh_next+OCR)
- 用法: venv python tools/pre_check.py <PDF路径>, 报告写入 translated/review/
- 已验证: TO-SoFiT(推荐skip=1,与人工结论一致) / Exton 2001(字体表损坏老PDF,正确提示伪字符风险)

## 2026-09-03 00:20 新增 tools/post_check.py 翻译后质检门禁
- 功能: mono 译文 vs 原始 PDF 逐页对比, 三项断言——汉化率(原文实质内容页译文汉字占比<5%报警)、引用完整性([n]标号逐页对账,超容差=版面错乱)、占位符残留({vN}未回填=公式回填失败); skipLastPages 跳过的原文保留页自动识别为预期行为不误报
- 输出: review/翻译后质检_*.md, 门禁判定 PASS/FAIL, 退出码 0/1 供自动化联动
- 指标依据: BabelDOC ACL 2026 论文 Untranslated Blocks 质检指标
- 已验证: 正向(今日TO-SoFiT合格产物=PASS) + 反向(英文原文伪造译文=FAIL,全部正文页报警)

## 2026-09-03 00:32 server.py 集成自动质检钩子（自动化质检流程上线）
- 实现: _execute_translate_job 成功后起 daemon 线程运行 _run_auto_qc——依次执行 tools/pre_check.py(翻前体检)与 tools/post_check.py(质检门禁), 全部失败安全(try/except+timeout 300s), 不阻塞响应
- 解释器: _qc_python() 从 env_manager._existing('pdf2zh') 取托管 venv python(含 pypdf); 不可用则回退 sys.executable。注意 uv 基础解释器无 pypdf, 不能直接用 sys.executable
- 触发条件: 产物中存在 *-mono.pdf
- 端到端验证: API 直提交(缓存秒回)→ 钩子触发 → review/ 自动生成 翻译前体检+翻译后质检 两份报告, 门禁 PASS
- 已知事项: 手工 Start-Process 启动服务时 stdout 未重定向, 🧪质检日志不可见; 用 logs/launch.ps1 启动则完整可见

## 2026-09-03 01:00 全项目代码全面检查
- 方式: 四路并行逐行审查(server.py/utils核心/执行工具链/工具脚本与补丁), 关键疑点运行时实证
- 结果: 89项发现(严重5/中等27/轻微57), 报告见 代码检查报告_20260903.md
- Top5严重: 1)config.json并发读写无锁(POLISH事故根源) 2)venv.py无线程同步 3)config_migration把OS异常当配置损坏覆写用户配置 4)同名任务并发文件互相覆盖 5)post_check原文保留页豁免可误放行文献页失败
- 修复路线图见报告第五节(P0约23行改动即可消除两大并发事故源)

## 2026-09-03 13:07 P0 修复落地(源自代码检查报告) + 抓获并修复真正的 POLISH null 元凶
- P0-1: utils/config.py——update_config_file 全程持 _config_write_lock 进程锁, json/toml 两处写入改用 _atomic_write_text 原子写(方法拆为 update_config_file 薄包装 + _update_config_file_locked)
- P0-2: utils/config_migration.py——_migrate_json/_migrate_toml 异常分类收窄: 仅 ValueError(解析失败)才备份+恢复默认, OSError 跳过迁移保留原文件; _backup_invalid 失败时放弃覆写
- P0-3: utils/task_manager.py——get_active_tasks_list/get_history 返回 deepcopy 快照, 修复 SSE 在任务完成瞬间 RuntimeError 断流
- P0-4: server.py 质检钩子 endswith 兼容 pdf2zh_next 的 .mono.pdf 命名
- 🔴真凶落网: pdf2zh 子进程的 ConfigManager 会把 --config 指向的文件注册为自己的配置并在启动时规范化写回(mtime=子进程启动秒), POLISH 正是被它置 null——服务器加锁/原子写/预置注入都拦不住子进程这最后一笔
- 终极修复: server.py translate_pdf 改为传 config.json.task 副本给子进程, 主配置永不被子进程触碰
- 端到端验证: 恢复 POLISH=1/REFLECT=1 → 提交翻译(缓存) → 主配置 POLISH=1 REFLECT=1 完好存活, 钩子已加载(反思=开), 自动质检双报告生成

## 2026-09-03 13:58 中度(🟡)问题批量修复（源自代码检查报告 27 项，本轮处理 26 项）

### server/utils/config.py
- #11 字体写入提前: NOTO_FONT_PATH 是全局键, 移到 config_map 判空之前, bing/google 等无映射服务不再静默忽略字体; 仅字体变更时也落盘
- #12 裁剪参数容错: pdf_w_offset/pdf_h_offset/pdf_offset_ratio/pdf_white_margin 加 _int_opt/_float_opt 守卫, 非数字回退默认(不再 500)
- #13 apiKey 日志掩码改用 _safe_log_value, 非字符串值不再 len() 崩溃(json/toml 两处)
- #14 TOML pool_max_workers 不再写字符串 "null"(TOML 无 null, 新消费者 int() 会崩), 未设置时删键走默认
- #16 删除两处遗留调试 print(self.__dict__)/print(translator.keys())
- #17 运行期配置自愈: _load_config_with_heal——运行中 config.json/toml 损坏时自动调 migrate_config_file 备份+恢复后重读, 失败抛业务错误而非裸 500
- #15 补注释明示 POLISH_* 为有意托管(插件 UI 改不动, 以 config.json.example 为准)

### server/utils/venv.py
- #18 引擎识别: get_command_and_env/_resolved_command 新增 engine 形参(调用方 execute.py 显式传), 兜底只嗅探命令首项; 旧实现全命令行 join 子串嗅探, 文件名含 pdf2zh_next 的 PDF 会误判引擎
- #19 缓存命中降级: ensure_env 命中缓存只做路径存在性检查, 不再每次翻译 fork `conda info --json`(数百 ms~秒级, 超时还可能误入重装)
- #20 get_conda_bin_dir 三态(False/str/None)统一为 Optional[str]

### server/utils/execute.py
- #21 子进程看门狗: 新增 TaskTimeoutError + 总超时(默认3h, PDF2ZH_TASK_TOTAL_TIMEOUT)/空闲超时(默认30min, PDF2ZH_TASK_IDLE_TIMEOUT); POSIX 按无输出判定, Windows 按进度推进判定(监视器未解析到进度时不启用空闲判定, 防误杀无控制台长任务)
- #23 Windows 控制台监视器进程级唯一(_windows_console_helpers_active 事件): 并发任务不再串读同一屏幕缓冲互相覆盖进度; 后来任务退化为无实时进度
- #25 PTY 解码器修复: 真正的非法字节用 replace 强制前进并清 leftover(旧实现非法字节永久卡住, 输出/进度解析停死)
- #26 tqdm 分支命中后补 return, 不再被 legacy 正则二次覆写进度

### server/server.py
- #3 MAX_CONTENT_LENGTH=500MB; base64 解码后校验 %PDF 魔数, 伪装/截断内容不落盘
- #4 skipLastPages≥总页数时抛业务错误(旧代码生成 -p 1-0 / 1-0 非法参数); -p 与页码拆为两个 token
- #5 产物文件名 stem 改用 os.path.splitext(两处), 不再 .replace('.pdf','') 误伤 a.pdf.b.pdf 类名字
- #6 pdf2zh_next 零产物/仅一产物时给业务错误信息(旧代码 retList[1] 裸 IndexError)
- #7 /translate 校验失败(非 origin)已落盘的上传件自动删除, 不再留孤儿文件
- #8 data URL 前缀大小写/缺 MIME 变体兼容(按 ;base64, 标记截断)
- #9 启动交互 input() 捕获 EOFError: 隐藏窗口/后台启动默认继续(旧代码直接 traceback 中断启动)
- #10 冗余布尔判断清理(3处); babeldoc 跳过 compare 补日志
- 自动质检线程加 _QC_SEMAPHORE(上限2), 批量翻译不再瞬时起多个 pypdf 进程

### server/utils/ 其他
- #22 auto_update: 通知/更新检查移到 server.py daemon 线程(旧代码 GitHub 被墙时同步阻塞启动 1~2 分钟); 四处网络超时 30s→8s, NOTICE_TIMEOUT 8→6; 后台发现新版本仅提示(更新走 manage_packages.py)
- #24 cropper.merge_pdf 异常路径 finally 关闭文档句柄(Windows 句柄占用→PermissionError); TB→LR 合并失败检查 None 返回并抛业务错误
- #28 deepseek_thinking.strip_thinking_fields_from_config 改行级精准删除(旧实现 toml 全量重写丢全部注释)
- #27 config_migration._merge_managed_requirements 保留用户钉版: 包名带版本说明符的托管包不再被 release 默认覆盖, 未钉版仍跟随 release

### tools/ 与补丁
- #30 pre_check: 加密 PDF 定向提示(先查 is_encrypted + 尝试空口令; 用户口令 PDF 给"先解密"提示), 旧代码在 len(pages) 处崩、加密预检分支不可达
- #31 post_check: 页数计算移入 try, PDF 异常退出码固定 2(基础设施), 不再 traceback 退码 1 冒充质量 FAIL
- #33 term_verify: 超时/URLError/连接重置也退避重试 2s/4s/8s(旧实现仅 429/5xx 重试)
- #32 润色缓存指纹: POLISH_MODEL/POLISH_DOMAIN/FORMULA_MAP/FORMULA_HINT + terms.csv/guideline 文件(mtime+size+md5)全部登记为缓存键——改配置或编辑术语表后重翻不再命中旧缓存。补丁源 patches/pdf2zh_translator.py 与 venv 部署版 translator.py 两处同步修改

### 验证
- 全部 13 个改动文件 py_compile 通过
- 单测: 用户钉版 pdf2zh==1.9.5 在迁移合并后保留(默认约束/额外包不丢); PTY 非法字节两 chunk 内自愈不卡
- pre_check 回归: TO-SoFiT 6 页正常, 推荐 skip=1 结论不变(退出码0)

### 生效方式
- server.py/utils/tools 改动: 需重启翻译服务(8890)后生效
- translator.py(venv): 下次翻译新起 pdf2zh 子进程即生效, 无需重启
- 遗留: 严重项 🔴2 venv RLock / 🔴4 任务级工作目录 / 🔴5 post_check --skip-last 豁免参数化 仍未做(P1)

## 2026-09-03 14:15 P1 严重项收尾（体验导向：并发安全 + 质检不误判），服务已重启生效

### 🔴2 venv.py 环境操作线程锁
- VirtualEnvManager 新增 self._env_lock = threading.RLock(); ensure_env/install_packages 改为薄包装全程持锁(原方法体改名 _ensure_env_locked/_install_packages_locked)
- 效果: 并发翻译任务的"发现环境→检查依赖→安装修复"成为原子操作, 杜绝两个 uv/pip 同时写同一 site-packages 导致环境损坏; RLock 可重入, 环境就绪后缓存命中仅几条路径判断, 无性能影响

### 🔴4 server.py 同名任务幂等去重（替代 per-task 目录的最小侵入方案）
- 新增模块级 _SUBMIT_LOCK + _INFLIGHT_FILES(fileName->taskId): translate() 在落盘上传文件**之前**登记, 同文件名并发请求直接复用进行中的任务
  - 新插件(异步): 返回 {status:accepted, taskId:复用ID}, 插件轮询同一任务拿结果
  - 旧插件(同步): _wait_for_task_payload 轮询等待复用任务结束(2s 间隔/30min 上限), 返回同一产物; 复用任务失败则登记本任务重译; 超时但任务仍活则交回任务列表(绝不覆盖正在被读取的输入)
  - worker 包 _guarded_worker try/finally 释放登记; 校验失败/异常路径同步释放; 释放校验 taskId 防误删后来者占位
- 效果: 重复点击/异步重试不再截断输入文件、不再产物互覆盖、不再 Windows 撞句柄; 任务完成后的重提不受影响(正常重译覆盖旧产物)

### 🔴5 post_check 跳页豁免参数化（封堵文献页失败误放行）
- post_check.py 新增 --skip-last N: 汉化率断言仅豁免"末尾连续 N 页且确无汉字"的原文保留页; 豁免区外的无汉字英文页一律判 FAIL(旧逻辑仅凭 [n] 密集+无汉字特征豁免, LLM 原样回显文献/跳页数不足时静默放行)
- 无参数时不豁免并给🔵提示; 报告头注明跳页豁免状态
- server.py 自动质检钩子把任务实际 skipLastPages 透传给 post_check(pdf2zh/next 两引擎统一)

### 验证
- py_compile 通过(venv.py/server.py/post_check.py); venv 锁与 server 模块导入正常
- post_check 对比实测(TO-SoFiT skip=1 产物): 无参 → 第6页 FAIL+提示(漏洞封堵); --skip-last 1 → 第6页豁免 PASS
- 去重逻辑单测: 进行中任务可匹配/他文件不误伤/失败后立即释放
- 服务已重启(旧 PID 6632/6192 → launch.ps1 新进程), /health 200, 配置迁移正常(config.json 无需修改, POLISH 托管配置存活)

### 至此代码检查报告处置情况
- 🔴严重 5 项: 全部修复(1/3 于 P0 轮, 2/4/5 本轮)
  - [2026-09-20 口径订正] 🔴4「同名上传/产物确定性文件名致并发互相覆盖」**只覆盖 `/translate`**
    （`_SUBMIT_LOCK` + `_INFLIGHT_FILES` 登记在 translate 的落盘之前）。`/crop`、`crop_compare`、
    `compare` 三个端点（server.py 的 `process_request` 处）**至今没有同名在途去重** ——
    同一文件并发提交仍会各自落盘同一路径。另: 2026-09-07 新增的
    `_completed_task_id_for_file`（历史已完成同名任务复用）**不在原报告视野内**，
    同名重提在去重之外还有第二层，且默认返回**旧 taskId + 旧产物**（要真重译须 `force=true`）——
    风险形态从"并发互相覆盖"变成"静默拿到旧结果"，比前者更隐蔽。
- 🟡中等 27 项: 已修复 26 项(#1/#2 在 P0 轮完成); #29 API Key 环境变量化属基础设施变更, 待用户决策
- 🔵轻微 57 项: 未处理(死代码清理 execute2.py/execute3.py/utils/record.py 约 600 行无引用, 删改无运行收益且有误删风险, 暂保留)

## 2026-09-03 15:05 译文机械伤清洗上线（连字符 + 半角标点残留，双保险三层）

背景: 对比豆包翻译锐评发现的两个高频观感伤——① PDF 断行连字符烤进中文("鱼类-式""模型 - 化"); ② 半角标点残留/双标点("变形,产生" "至关重要,，")。修法采用确定性正则为主(100%执行、零API成本), 不依赖 LLM 自觉。

### 三层实现(patches/pdf2zh_translator.py 与 venv 部署版 translator.py 双份同步)
- 第1层(主力): OpenAITranslator 新增静态方法 _clean_cjk_typography, 确定性正则清洗, 幂等:
  - 汉字间连接符(允许中间空白)整删; 汉字邻接半角 ,;:?! 转全角; 汉字后句点(后随非字母数字)转句号
  - 汉字前紧贴半角逗号/分号转全角; 含汉字的半角括号对转全角
  - 标点归一: 连续同类收敛(",，"→"，") + 错序对修正("，、"→"、" "。，"去逗)
  - 负面清单不触发: 数字小数点(3.12)/数字范围(1-3)/{vN}占位符/纯英文内部标点(规则均要求至少一侧紧邻汉字)
  - 插桩两处: do_translate 返回前(不开润色也生效) + polish() 出口(占位符保护之后, 兼治回退初译场景)
- 第2层(辅助): 润色 prompt(反思修订版+普通版)均加"排版要求"规则, 让 LLM 从源头少产出并主动合并断行词
- 第3层(缓存): __init__ 登记 add_cache_impact_parameters("typo_clean","v1")——旧缓存自动失效, 重翻即走新逻辑, 无需手动清缓存

### 已知边界(非缺陷)
- "鱼类-式"清洗后为"鱼类式"而非"鱼式"(正则不做语义删字, 语义级合并交由第2层 LLM 规则)
- 全角括号前如原有空格保留("和 （见图 3）"), 无害

## 2026-09-06 (晚) vfont 公式字体正则误吞正文斜体段 + 术语表补 in-silico

### 问题(英文残留段根因, 排查链: 布局分类→缓存→字体三层排除后锁定)
- 现象: Melhani 论文注记7正文、3.6/3.7 节部分段、1.1 节标题("1.1. Motivation and Context")等十余处整段英文未译
- 排除过程: ① 布局模型分类复现实验 → 该区域判为 plain text(可译), 排除; ② sqlite 缓存检索 → 无该段记录(从未提交翻译), 排除"LLM 半译"; ③ 审校报告零命中 → 确认未进翻译流程; ④ 段落字体检查 → 全部为 NimbusRomNo9L-ReguItal(正文斜体)
- 根因: config.toml [pdf].formular_font_pattern 含 `.*Ital`, 意图保护数学斜体(CMMI 等), 但同时匹配 **NimbusRomNo9L-ReguItal 等正文斜体字体** → 整段字符被判公式 → 纯占位符段落 → converter 跳过翻译 → 原文直渲染。LaTeX \textit 段落(注释体、\emph 强调、斜体小节标题)全部中招
- 修复: formular_font_pattern 移除 `.*Ital` → `(CM.*|TeX.*|.*Math|STIX|MS.*|Times-Italic)`。数学斜体实际由 CM.*(CMMI/CMSY/CMEX) 覆盖; Times-Italic 保留防 Word 排版数学
- 生效时机: server 在**任务提交时**读 config.toml(server.py translate_pdf 内 toml.load), 无需重启
- ⚠️ 遗留地雷(未动, 待验证): 同正则中 `CM.*` 会匹配 CMR10(纯 LaTeX 默认正文字体)——用 LaTeX 默认字体的论文会整篇被吞。建议将来改为 `CM(MI|SY|EX)` 精确匹配数学三族, 改前需实测

### 术语表增补
- terms.csv 追加 `in-silico,计算机模拟`(修标题"in-silico benchmark"被直译为"硅基准验证"的误译)
- 注意: 术语表指纹变更使全部缓存失效, 下次重翻为全量; vfont 修复使斜体段以新文本提交(旧缓存按段落文本键, 不冲突)

## 2026-09-07 v21 架构级修复(P0 五项, 基于管线全景数据流分析 + 上游配方调研)

> 背景: Melhani 净稿验收发现三残留 —— ① API 直提任务 Zotero 不挂载; ② 标题仍"硅基准";
> ③ 机械伤 43 处(vfont 修复副作用)。深挖 converter/translator/server 全链后统一治理。

### 调研结论(上游对照)
- 官方 ADVANCED.md 推荐 vfont `(CM[^RT].|MS.|.Ital)`: **官方是 `.Ital`(单字符前缀, re.match 锚定开头),
  我们 8 月部署抄成 `.*Ital`(任意前缀) → 误吞 NimbusRomNo9L-ReguItal 正文斜体** —— 一字之差
- 官方推荐 vchar 不含 `, . -` 等普通标点; 我们 config 多加了(从官方模板抄入未按 1.x 调优)
- zotero-pdf2zh issue #87({vN} 占位符残留): 零回复无官方修复, 本项目 v20+渲染层整形为自研领先
- 实测: `re.match(r".Ital","NimbusRomNo9L-ReguItal")`=False(安全), `r".*Ital"`=True(被吞)

### P0-1 server.py: 去重加查 history(挂载根治)
- 插件挂载=自己持有 taskId 轮询的会话协议; 外部直提任务插件无感知 → 死信
- 新增 `_completed_task_id_for_file()`: 活跃任务查不到时查 history(同名+success+有 fileList)
- 插件重提已完成文件 → accepted+历史taskId → 轮询 /api/history 命中 → **秒级下载挂载, 零重跑**
- 旧插件同步协议走 `_wait_for_task_payload` 的 history 分支, 同样直达产物
- 注: history 为内存态, server 重启后退回重跑路径(缓存兜底)

### P0-2 translator.py: 初译 prompt 注入术语表("硅基准"根治)
- 根因: 术语表只进润色 prompt, 初译默认模板对术语一无所知; 润色修订被占位符保护
  回退后, 终稿落回"从未见过术语表"的初译(标题段占位符密集高发)
- 修法: 覆写 `OpenAITranslator.prompt()`, user 消息前注入**归一化匹配**的术语条目
  (`In{v3}Silico` 剥离占位符后能匹配 `in-silico`); 不开润色同样生效
- getattr 防御 init 时序(prompt 指纹计算早于 _polish_* 属性赋值)
- 缓存: prompt("") 空文本注入为空 → prompt 指纹不变; polish_glossary_fp 已在缓存键

### P0-3 converter.py: 渲染层混合占位符首尾标点整形(机械伤根治)
- 根因: vchar 含 `\d` 和标点 → 数字+标点连成混合 var 段(`", 2006"` `"3 ("` `"(3)"`),
  v20 只消除孤立纯标点占位符 → 混合段原字体回填时首尾半角标点夹汉字
- 新增 `_split_mixed_placeholders()`: 混合段首尾标点抽出整形(句读转全角, 连字符/方括号
  删除, 圆括号转全角), 主体重开 var 条目原字体回填; **中段标点不动**(小数/千分位天然安全)
- 安全边界: 纯标点段交 v20; 含其他结构(真公式)不动; 邻接非汉字不动;
  **负数守卫**(`-5` 的连字符不删); "Fig. 3" 型保守不整形; 整形异常不阻塞(退化为 v20)
- 12/12 单测全过(含负数守卫/引用编号全角化/左英文不触发等边界)

### P0-4 config.toml: vfont 官方化(排 CMR10 地雷 + 斜体正名)
- `(CM.*|TeX.*|.*Math|STIX|MS.*|Times-Italic)` →
  `(CM[^R]|MS.M|XY|MT|BL|RM|EU|LA|RS|LINE|LCIRCLE|TeX-|rsfs|txsy|wasy|stmary|.*Mono|.*Code|.Ital|.*Sym|.*Math|STIX|Times-Italic)`
- `CM[^R]`: CMR10(LaTeX 默认正文)不再吞; `.Ital`: 正文斜体放行, TimesItal 型保留;
  Times-Italic 显式保留(`.Ital` 单字符前缀匹配不到完整名)
- 9 用例字体语义验证全过(Nimbus-ReguItal 放行/CMMI 保留/CMR10 放行/txsy 保留)
- 影响面: 仅斜体段(~20 段)缓存 miss 重翻, 其余段落 sstk 不变缓存命中

### P1-5 translator.py: 批判限流+修订瘦身(token -40%)
- 批判 prompt 加输出纪律: 每组最多 3 条、每条 ≤60 字、严禁复述原文整句
  (此前人均批判输出 2.5K token, 审校报告 266 段 1.2MB, 多为低价值复述)
- 修订调用不再重复注入前文 context(批判阶段已看过), 省 1-2K 输入/次

### 部署状态
- venv 与 patches 镜像 MD5 同步(converter/translator/server 三对)
- server 已重启(health ok); venv 子进程每次任务自动加载新版
- 待验证: Melhani 重翻(斜体段真翻译 + 渲染整形全量生效) → Zotero 重提秒挂载(P0-1)
- 作者名/单位音译问题(页眉块)不在本次范围, 属翻译策略问题

### 验证
- 15 个构造用例全过(含小数点/数字范围/公式占位符/纯英文括号等全部误伤陷阱), 幂等性验证(函数跑两遍结果一致)
- py_compile 双份通过
- 生效方式: 无需重启 8890 服务, 下次翻译新起 pdf2zh 子进程即生效; 已翻旧文重翻会因 typo_clean 版本号自动绕过旧缓存

## 2026-09-06 12:10 修复 9-03 引入的翻译启动即崩 bug（Event 误用为锁）+ 插件升级配套验证

### 事故
- 用户升级 pdf2zh 插件 4.0.3→4.1.7 后首次翻译报错: 'Event' object has no attribute 'acquire', 任务直接失败
- 根因: 9-03 修复#23(控制台监视器单实例)时把 threading.Event 当互斥锁用——Event 没有 acquire 方法, 应为 Semaphore。当日仅做 py_compile 与导入验证, 未跑真实翻译路径, 运行时 AttributeError 无法被静态检查发现, 埋雷三日

### 修复(server/utils/execute.py)
- _windows_console_helpers_active: threading.Event → threading.Semaphore(1)
- finally 中的 .clear() → .release()
- 单实例语义不变: 首个任务 acquire(blocking=False) 成功挂监视器, 并发任务拿不到自动降级"无实时进度", 任务结束 finally 释放

### 附带发现与修复: 同文件并行编辑竞态
- 本次对 execute.py 的两处修改并行提交发生覆盖(后写者胜, 先写修改丢失但工具报成功), 导致磁盘上短暂存在"Event+release"混合状态
- 已改为串行强制写入并逐字节读回验证; 随后用 Python 显式 UTF-8 全量核对 execute.py 的 9-03 全部修改点(看门狗/TaskTimeoutError/PTY增量解码/监视器喂狗/单实例)完整无缺
- 教训记入: 同一文件的多处修改必须串行执行; 静态编译通过≠运行时正确, 关键路径需真实调用验证

### 验证
- Semaphore 语义单测: 互斥(持有期间他人获取失败)/释放后可再取/20线程并发仅1赢家(非阻塞语义正确)
- py_compile 通过; 服务重启(PID 7920), /health 200
- 配套背景: 插件已升级 4.1.7(xpi 2026-09-06 11:41), server 4.1.7 补丁版配套, 等待用户重试翻译验证全链路

## 2026-09-06 12:25 修复 @retry 装饰器错位（9-03 并行编辑竞态第二案），插件升级后首次翻译失败事故

### 事故经过
- 用户升级插件后首次翻译(EKF论文 50段, 异步协议accepted已正常生效), 但每段译文在 LLM 调用成功后统一报错: OpenAITranslator._clean_cjk_typography() takes 1 positional argument but 2 were given, converter 捕获后 raise, 产物必然大面积未翻译
- 根因: 9-03 向 OpenAITranslator 插入 _clean_cjk_typography 时, 恰好插在 @retry(tenacity RateLimit 重试) 与 def do_translate 之间——retry 错装到 _clean_cjk_typography 头上, tenacity 包装破坏 staticmethod 语义(类字典中变回 function), 实例调用 self._clean(x) 被绑定为 (self, x) 两参数 → TypeError; 同时 do_translate 丢失 RateLimit 重试
- 当日单测从类上访问调用(类函数访问不触发绑定)故未暴露; 实例调用路径今天才第一次真实走到
- 与当日 execute.py 的 Event 误用同源: 均为 9-03 并行编辑同文件多处的竞态/错位伤害, py_compile 与单次调用单测均无法发现

### 修复(patches 与 venv 部署版同步, 脚本化串行写入+读回断言)
- @retry 块移回 def do_translate 之前(恢复 RateLimit 重试), @staticmethod _clean_cjk_typography 保持在 _polish_context 之后(staticmethod 语义恢复)
- 运行时权威验证: 类字典条目 isinstance(staticmethod)=True; 实例调用(object.__new__)成功且清洗效果正确(标点/连字符/括号转换无误伤); do_translate 恢复 tenacity 包装(.retry 属性存在)
- 双文件 py_compile 通过(venv 侧沙箱拦 pyc 缓存写入无碍, import 成功即语法证明)

### 处置
- 终止坏任务子进程(其加载旧代码每段必炸, 产物无效), server 重启清空任务, 现等待用户重新提交翻译验证全链路
- 流程教训强化: 对既有装饰器函数插桩必须检查装饰器归属; 同文件多处修改一律串行+读回断言; 关键路径验证必须覆盖"实例调用"与"类调用"两种形态

## 2026-09-06 13:55 补齐 server.py 镜像同步（9-03 遗漏）
- 9-03 14:12 对 server/server.py 的改动(质检并发上限、同名任务幂等去重等)未同步进 patches/server_server.py 镜像, 镜像停留在 9-01 版(1307 行 vs 实际 1537 行)
- 按 UPGRADE_RUNBOOK 流程 Copy-Item 重新同步, 双文件 MD5 一致(B542F0A8...); 至此 patches/ 下 4 个镜像(translator/converter/pdfminer×2/server)全部与运行环境一致
- translator/pdfminer 镜像经 MD5 核对本已同步, 无需处理

## 2026-09-06 14:05 term_verify.py 兼容 v18 humanize 审校报告格式
- 现象: 对 Melhani 新版审校报告跑存疑术语提取, 得 0 个术语
- 根因: v18 报告 humanize 改版后, 段落头 `## 存疑段落 N`→`## 段落 N`, 存疑块标记 `**存疑点**`→`【存疑】`, 术语写法新增 `术语/表达："xxx"` 变体; parse_doubt_md 仍按旧格式解析
- 修复: 切块与存疑块正则双格式兼容; 术语正则允许关键词与引号间 ≤12 字符修饰; 含 {vN} 占位符的存疑项跳过(占位符切分存疑非词汇表对象)
- 附带教训: openalex_search 的 --context 必须用**短英文领域词**(如 state estimation); 传长中文描述会污染全文检索词导致 0 召回(实测纯术语 5321 条→加长 context 0 条)

## 2026-09-06 14:50 修复润色输出绕过机械伤清洗的漏洞（Melhani 验收发现）
- 验收发现: Melhani mono 全文残留 汉字后半角标点 868 处 + 汉字间连字符 410 处(摘要页 17 处), 三关中第二关不达标; 一三关 PASS(门禁 PASS, dual 100=2×50 页对齐)
- 根因: do_translate 内的清洗只保护初译; translate() 的 polish 钩子返回值(最终入库稿)绕过清洗, 润色 LLM 重写时重新引入半角标点/连字符
- 修复(translate() 两条路径全覆盖, 清洗幂等):
  1. polish 返回值后补一遍 _clean_cjk_typography 再入缓存
  2. cache.get 命中路径同样过清洗——v1 缓存终稿读取时即修复, 无需重译
  3. typo_clean 缓存键维持 v1 不递增(升 v2 会作废 Melhani 全部缓存, 与路径2矛盾); patches 与 venv 同步部署(MD5 CF17D601...), 运行时验证 4 项全 PASS
- 效果: 重翻 Melhani 时全部段落缓存命中, 零 LLM 成本, 产物即净稿; 未来新翻译从源头干净

## 2026-09-06 19:30 纯标点占位符消除（机械伤第三层根因：cid 标点被判为公式占位符）

### 现象与排查（第二关残留 201+414 的真凶）
- 14:50 修复后重翻 Melhani, 半角标点 868→201(-77%) 但连字符 410→414(纹丝不动), 清洗层三路径全覆盖却清不掉
- 排查链: 清洗函数单测全过 → 缓存扫描 2795 条中文译文 Melhani 相关零残留(仅他文档 42 条) → 字体甄别: 残留标点全为 NimbusRomNo9L(原文字体), CJK 为 LXGW(译文字体) → PDF 字符解剖: "滤波-的""九-室"连字符用原文字体渲染 → 缓存铁证: "Reif{v68}G{v69}unther{v70}Yaz{v71}Unbehauen" 连字符全部是 {vN} 占位符
- 根因: 原文 PDF 的连字符/逗号/句点/括号以 (cid:NN) 形式被 pdfminer 解出(字体 ToUnicode 缺失), 命中 converter vflag 首条规则 `re.match(r"\(cid:", char)` 被判为公式 → 包成 {vN} 占位符 → LLM 围绕占位符行文(如"九{v1}室") → 渲染时原字体标点回填到汉字之间。该字符在占位符内部, 翻译层/清洗层/缓存层均不可见
- Tavily 检索结论: 上游 1.x 无相关修复(官方已冻结转 2.0/BabelDOC 重写), 需自研; 学术 dehyphenation 文献针对英文抽取场景不适用

### 修复(converter.py, 翻译后·排版前)
- 位置: receive_layout 内 executor.map 之后、新文档排版之前, 对每段译文跑 _strip_punct_placeholders
- 规则: {vN} 内容(经 (cid:NN)→chr 归一)属于孤立半角标点集合 {- , . ; : ( )} 且紧邻汉字/全角标点(至少一侧)才处理——句读类(,.;:)转全角, 连字符/括号直接删除; 随后收敛重复全角标点("先,，"→"先，")
- 安全边界: 公式内容(数字/字母/数学符号)不匹配; 数学减号 U+2212 属 Sm 类不在集合; 英文/数字邻接不触发("50{v}%的"不误伤); 未引用 var 条目不渲染无副作用
- 关键设计: 修改发生在**翻译之后**, 翻译缓存键(sstk 原串)不变 → 已缓存段落全部命中, 重翻只付排版时间(约 10 分钟), 零 LLM 成本; 旧缓存条目同样受益
- 部署: venv converter.py 与 patches/pdf2zh_converter.py 镜像同步(MD5 一致), import 验证通过; 无需重启 8890, 新任务子进程即生效

### 验证（2026-09-06 19:47 三关全过）
- 重翻 4bb8b017 全缓存命中, 11 分钟落盘(19:46), 零 LLM 成本——"翻译后修改"设计兑现
- 第一关: post_check 门禁 PASS(引用对账 116/112 容差内, 占位符零残留)
- 第二关: mono 全文 汉字间半角标点 201→**0**, 汉字间连字符 414→**0**(摘要页 0+0), 双清零
- 第三关: dual 100 = 2×50 页对齐
- 至此机械伤三层防线闭合: ①译文清洗(9-03/9-06) ②润色路径覆盖(14:50) ③占位符标点消除(本轮)

## 2026-09-10 v22 渲染层邻接标点去重（机械伤第四层：占位符标点 × LLM 自加标点叠加）

### 现象（v21 净稿终验发现, 目检 + span 级取证排除提取假象）
- "（（EKF））" 型双括号 ×50（全角 35 + 半角 7 + 闭括号类）、"，。" ×14、句中句点（"继承自[1]。的"）若干
- 缓存 3784 条零双括号、零"，。"邻接 → 叠加发生在渲染回填层

### 根因（证据链闭环）
- 原文括号/标点被保护为纯标点占位符（如 {v100}="(" {v101}=")"）；LLM 重排句式时**自加一套全角括号/句读**
- 渲染层 v20/MIX 回填把占位符标点转全角 → 与 LLM 自带标点相邻叠加
- 实例：缓存"滤波器{v100}（EKF{v101}）" → 渲染"滤波器（（EKF））"；"可观测性{v55}。"({v55}=",") → "，。"

### 修复(converter.py + translator.py)
- v20 _strip_punct_placeholders: 映射标点与相邻字面标点**半/全角等价**(_NB2FULL)比较，
  同向括号/同类句读相邻 → 占位符标点删除；句读后跟句末标点("，。") → 删除
- MIX _split_mixed_placeholders: 首尾映射标点与相邻字面标点同类即逐个剥离
- translator _clean_cjk_typography ④扩充: "，。"→"。"、同向全角括号"（（"→"（"（缓存内 9 条 + 新译预防）
- 已知残留（有意不修）: 句中句点"继承自[1]。的"类（句序重排后句点失去句末地位, 局部规则无法无歧义判定）; 
  半角"((cP，c2))"LLM 自笔误 ×7（真实嵌套括号 "((" 存在误伤风险, 保守不动）; 斜体标题 2 个
  （4.5 节标题、附录 C.1 Lemma）+ arXiv 页脚样板（页 2）不译

### 事故记录与教训: 渲染验证请求漏传 config → Bing 重译 341 段
- 过程: 为验证渲染修复, 用临时文件名 -rev 重提时 JSON 只传了 fileName/fileContent/asyncJob
  → Config 缺省 service=**bing**(utils/config.py L119)、targetLang=zh-Hans、无润色 → 341 段缓存键全新 → Bing 重译 4.5 分钟
- 症状: 进度卡"开始翻译 0"数分钟（与缓存命中 25 秒预期不符）; "计算机仿真（in-silico）"退化为"in silico 实验"未按术语表翻译
- 修复: 补全 config（service=silicon/targetLang=zh-CN/threadNum=4/noWatermark/mono/dual/skipSubsetFonts）重提 -rev2 → **25 秒全缓存命中**
- 教训: **直提 /translate 必须带全插件同等 config 字段**, 否则引擎静默回落 bing; 缓存键含全部 params, 引擎不同即全量 miss
- 清理: bing 版 -rev 三件与 -rev2 临时件已删除; silicon v22 产物以 rev2 覆写正式 Con-mono/Con-dual（历史 taskId 0cfe7064 挂载即修复版）

### 验证（2026-09-10 14:12 rev2, silicon/DeepSeek-V3.2 全缓存命中, 25 秒）
- 双全角括号 35→**0**, 闭括号类 15→**0**, "，。" 14→**1**, "。。" 0, 半角"((" 7(残留, 见上)
- 术语: 计算机仿真（in-silico）✓ 硅基准 0; dual/mono 50 页对齐
- 部署: venv 与 patches 镜像同步; 子进程免重启生效
- 待办: Zotero 重提原文件名 → P0-1 秒挂载(服务端已验证 0.02s 返回历史 taskId)

## 2026-09-11 v23 缓存键规范化 + 术语表按段指纹（灵敏度测试驱动的命中率工程）

### 背景: 灵敏度测试实测两类全量失效源
- T1: terms.csv +1 行 dummy → 前3页 23 段全 miss(232.9s) —— polish_glossary_fp 整表指纹
- T2: vchar +1 字符(撇号) → 23 段全 miss(200.7s) —— var 全局编号级联平移
- 基线 T0: 无扰动 0 新增(12.1s), 缓存确定性极强; bing 事故遗留行已甄别(id 有空洞, 行数差才是真增量)

### 修复一: cache.py 占位符编号规范化(级联失效根治)
- _canon_seq: {vN} 全局编号 → 段内出现序规范编号, 缓存键与全局编号解耦
- _remap: 命中后按 双方 seq 把译文 token 从存入时编号重映射到当前编号;
  LLM 自造 token 原样保留(与旧行为一致); 乱序/交叉引用 token 因绑定"原文出现序"天然正确
- get() 四级查找: 新形态规范键 → 新形态原文键(防御) → 旧形态规范化索引 → 旧形态原文键
- _legacy_index: 旧条目以 raw 文本入库, sqlite 精确匹配找不到"规范化相等、原文不等"的行
  —— 惰性载入本引擎全部旧行 canon 化(4千行 <1s), 命中即回写规范形态
- set(): 统一规范化入库; 写回迁移时译文规范化必须用"存入时原文"的 seq(行内 token 与当前编号无关)
- 零迁移成本: 4171 条旧条目经索引继续命中, 不重译; 新形态键不产生重复行(索引即迁移)

### 修复二: translator.py 术语表按段指纹(整表失效根治)
- BaseTranslator 新增 _cache_key_suffix(text) 钩子, translate() 传入 cache.get/set
- OpenAITranslator 覆写: 与 prompt() 注入同一套 _norm 匹配, 只哈希"本段命中术语";
  未命中段用显式空标记 #g23:- ; init 时整表 fp 移出静态键 + snapshot_legacy_key 留旧形态回查
- 语义: 表变更后, 含变更术语的段落才重译, 其余段落按段指纹不变继续命中
  (首次变更因新形态条目尚不存在仍全量 miss 一次, 自第二次变更起收益兑现)
- ModelScope/Zhipu 继承 OpenAI 自动获得; AzureOpenAI 独立继承不受影响

### 修复三: server utils/config.py service 缺省回落告警
- 请求缺 service 字段回落 bing 时大声打印告警(bing 事故教训的最后一环)

### 验证(全部通过)
- 单元 10/10: 平移命中/重映射/乱序往返/交叉引用/自造token/旧形态回查/表变更不误命中/后缀隔离/纯文本
- 集成(临时库+真实缓存样例6段): v22兼容6/6 + 平移+3命中重映射6/6 + 规范化写入后再平移+5 6/6 + 表局部变更语义 ✅
- 真实四轮: R1 无扰动 12s 全命中(0 LLM) / R2 复渲染 4s 0新增 / R3 vchar+撇号 12s 0新增
  (v22 同扰动 23段/200.7s; 撇号未实际命中文本, 文本变化级联由集成②覆盖) / R4 还原 0新增
- 部署: cache.py 首次纳入镜像(pdf2zh_cache.py), translator/config 镜像同步; 子进程免重启生效

## 2026-09-12 v23.1 测试入库: tools/tests/ 回归套件 (工程评估落地第一项)

### 背景: 工程质量评估发现"仓库内 0 个测试文件"——v23 的单元/集成测试全部活在 Temp 目录, 清理即失传
### 交付
- tools/tests/run_all.py: 聚合运行器 (venv python, 三套件独立进程, 汇总退出码)
- test_mirrors_sync.py: 镜像 MD5 门禁(8文件) —— v21 漏同步类事故改为回归阶段拦截
- test_cache_canonical.py: 缓存规范化单元 10 例
- test_cache_integration.py: v22→v23 迁移集成 5 例 (合成数据, 临时库, 不碰真实缓存)
- 约定: 全 stdlib 无 pytest 依赖; PDF2ZH_VENV_SITE/PDF2ZH_PYTHON 环境变量可覆盖路径;
  灵敏度测试(真实 LLM 消耗)不入回归套件, 方法论与脚本结论见 2026-09-11 条目
### 首轮: 23/23 PASS (8 镜像 + 10 单元 + 5 集成)
### 用法: venv python tools\tests\run_all.py

## 2026-09-12 v23.2 协议契约文档 + 镜像同步脚本 (工程评估落地第二/三项)

- patches/PROTOCOL.md: {vN} 跨层协议唯一权威描述 —— 生命周期(创建/翻译缓存/渲染整形/回填)、
  六条不变量(I1-I6)、三条隐形通道(_tls.formula_map 等)、五条改动守则、已知边界
- tools/sync_patches.ps1: venv/server 运行侧 → patches/ 单向同步 + MD5 断言 (UTF-8 BOM,
  PS5.1 无 BOM 中文注释会炸解析); 用法 powershell -File tools\sync_patches.ps1
- 完整闭环: 改补丁 → sync_patches.ps1 → run_all.py 回归 → 改动记录
- 经验: .ps1 含中文必须带 BOM, Write 工具默认无 BOM 需 python 补写

## 2026-09-12 v23.3 force直通道 + 跨页断句根因确诊与缓存手术流程 (Cactaceae 实战)

### 用户实测发现
- "三个成功的配子"被译成"三倍于。.." —— 根因 = 长段落跨页(刊页198/199/200)被段落组装器
  切成多个缓存段, LLM 拿到半句残文("a selfer has, on average, three successful" 截断于页尾)。
  上游 1.x 已知局限, 非补丁回归; BabelDOC 版式解析对此有改善, 属迁移收益之一

### 缓存手术流程(确立并实战验证)
- 定位坏段(SQLite LIKE 译文关键词) → 人工修正译文(UPDATE 单行) → 强制重渲染 → 页面抽验
- 修复链实战: 修4273("三倍于。.."→"三个成功的") → 重渲染发现空转 → 全杀重启 → 删行真重译
  (65s, 新输出"三次成功的") → 微调4401("三个成功的") → force 渲染(16s) → 终验 三倍于=0

### server.py: force 直通道
- 请求带 "force": true 跳过三类去重(活跃/在途/历史), 强制真渲染
- 动因: 缓存手术修了库, 同名重提被历史去重拦成空操作(4s 返回旧产物), 修复永远落不到产物
- 附带修复: force 路径补登记在途文件名, 维持并发防覆盖语义

### 重大环境发现: Windows SO_REUSEADDR 多实例静默共存
- 反复 launch.ps1 "重启"实为叠加实例: 新实例"成功"绑定已占用 8890 端口, 旧实例持续服务
- 症状: 历史去重一直命中旧任务 / "重启后"历史仍还在 / 进程列表多个 python
- 正确重启姿势: 全杀 python/pdf2zh 进程 → launch (已写入项目记忆; launch.ps1 后续可加防重入探测)

### 验证
- py_compile 通过; 镜像同步 8/8; 回归 23/23 全绿; 实战终验: 三倍于=0, 三个成功的=1, 跨页衔接通顺

## 2026-09-12 v23.4 跨页断句前瞻上下文(瓦片错落) — 页内版落地 + 跨页版架构性不可行确认

### 用户提案
- 在两段之间再次切片, 让切片包含中间的缝(瓦片错落堆叠), 使 LLM 看到断句两侧内容

### 落地形态: 前瞻上下文注入 (直接重叠会重复渲染, 改为只读参考)
- converter.py: 检测"前段无句末标点 + 后段小写开头"续接对(允许跳过页眉页脚小段,
  前向最多 3 段), 把后段开头 ~120 字符经 _tls.look_ahead 注入 worker
- translator.py: _inject_look_ahead 把前瞻作为 [Context note] 追加 user 消息
  (只译本段/同点收住/不译续文); _cache_key_suffix 把前瞻指纹编入缓存键
- 适用: **页内断句**(sstk 按页重建, 同页续段可见); prompt 注记不影响锚点断言
### 架构确认: 跨页断句前瞻不可行
- 诊断日志实证 sstk 按页重建(每页索引归零), 跨页对分属两次 receive_layout,
  渲染完第 N 页时第 N+1 页尚未解析 —— 流式架构下跨页前瞻原理上不可能
- 跨页断句正式解 = 缓存手术(v23.3 流程); BabelDOC 版式解析为迁移收益
### 实战 (Cactaceae A/B)
- 删人工修复行 → 无前瞻重译 → "三倍于"(复现猜测式错译) → 手术修为"三个成功的"
- force 渲染 16s 终验: 三倍于=0, 三个成功的=1, 跨页衔接通顺
### 回归: 33 项全绿 (8镜像 + 10缓存 + 5集成 + 10前瞻)


## 2026-09-12 v24a 文档摘要前置 (A档: "通读全文"意识的工程落法) + Cactaceae 实战修复

### 背景
- 用户架构洞察: 切片为并行+版面锚定而生, 但缺文档级上下文; "AI 需要通读全文"
- 业界对照(检索): GIST 式术语注入(已内置) / 文档级上下文 / TM 模糊匹配 / RAG
  —— 文档级采用"分层读"而非"通读着翻": 草稿层(切片并行) + 通读校对层

### A 档实现: 文档摘要前置
- converter.py: 首页派发翻译前引导 `_tr.summarize_document(首页文本前4000字符)`
  (流式架构下第 1 页翻译时全文未解析, 首页=标题+摘要+引言, 信息密度最高)
- translator.py `summarize_document`: 1 次 LLM 调用生成 ≤120 字中文文档画像;
  **落盘 ~/.cache/pdf2zh/docsummary/<md5>.txt**, 同文档永远复用同一份
  —— 摘要稳定是缓存键稳定的前提(每次 LLM 生成会漂移, 漂移=整篇重译)
- fp 经 add_params("doc_summary_fp") 进静态键; prompt() 每段前置 [Document context]
- 成本: 每篇 +1 次摘要调用 + 每段 ~150 token 画像; 换取全局术语/领域一致性

### 配套: cache.py 参数形态阶梯泛化 (prev_params_json)
- 事故: doc fp 首次加入使键演化, 旧阶梯只有一层 legacy 回查 → Cactaceae 全文重译(601s)
- 修复: snapshot_prev_key() 登记"上代形态"; get() 按形态队列 [当前→prev→legacy]
  逐级回查(规范化索引+直查+回写迁移) —— 键演化不再触发全文重译
- test_docsummary: USERPROFILE 重定向沙盒隔离 —— 回归曾清空真实摘要目录,
  致同文档键漂移整篇重译(第二次事故, 双重教训: 测试必须与真实数据隔离)

### Cactaceae 实战终态
- 多实例竞态(Windows SO_REUSEADDR)确诊: 三实例并存轮流入客, 历史去重时灵时不灵;
  正确重启 = 全杀 python/pdf2zh → launch → 验证 history 为空
- 术语表 Cactaceae→仙人掌科 入库: 仙人学科 1→0, 仙人掌科 81→88→100 全文统一
- 跨页接缝 "三倍于/三次成功的" → "三个成功的": 缓存手术 2 行 + force 渲染 16s
- 终验: 双括号 0 / 三倍于 0 / 仙人学科 0 / 仙人掌科 100 / dual 对齐
### 提交: 013a0e2 (含 v23.4 后全部累积); 回归 42/42

## 2026-09-12 v24b 军师层落地 — 文档级通读校对 + 人工裁剪精准写回 (v24b.1)

### 架构: 军师(文档级协调者) × 队员(分段worker)
- converter.py: 渲染时逐页追加 (raw, trans) 流水到 sidecar
  ~/.cache/pdf2zh/segflow/latest.jsonl (首页截断, _segflow_fresh)
- tools/strategist.py: 分块通读(42k 字符/块, 相邻块重叠 1 段) → LLM 只报四类
  局部视角盲区: 跨段指代/接缝断裂/术语漂移/文风突变; 最小编辑格式 find/replace
  (严禁整段重写), 锚点校验 {vN} 多重集一致 (--allow-drop 放宽为子集)
- 写回: UPDATE cache WHERE original_text=canon(raw) → force 重渲染生效

### v24b.1 两处关键修复 (Cactaceae 实战发现)
1. **canon 序号重排 (防占位符整体错位)**: 库内 translation 以段内出现序
   0-based 存储, sidecar 是渲染时 current 序号 (S6 sidecar 以 {v1} 开头而
   库内 {v0}; S2 乱序 {v11}...{v10}) —— apply 原样写回会让 get() 回映射把
   占位符整体错位一格(公式/英文窜位)。修复: 写库前经 raw 的 seq 映射重排
   (canon_remap), 单测 5 例覆盖。
2. **--from-report 人工裁剪模式**: 军师只看译文看不到占位符背后的英文字形,
   会提出与续段/字形内容重复的建议 —— 22 条建议经 raw 对照+邻接复核仅 16 条
   可用。裁剪清单 all-or-nothing 载入, 逐条重校验(find 唯一+锚点)。

### 军师建议 6 拒 6 改实录 (人工审计价值实证)
- 拒 S8 "三个成功的→三个成功的配子": S12 raw 开头就是 gametes, 插入=渲染重复
- 拒 S18 "Arias→Arias et al.": S20 raw 开头 "et al b" 续接, 插入=重复
- 拒 S12 "插入字面3:2": {v6}/{v7} 就是数字 3/2 字形, 插入=渲染 "33:22"
- 拒 S2 "交配杂交系统→交配系统": raw 实有 hybridization, 原译忠实
- 拒 S35 "传粉系统→花柱多态性系统": raw=pollination systems, 过度改写
- 改 6 条: LLM 建议搬移软连字符/句点垃圾占位符 (hermaphro{v25}dites 类),
  正解是删除 —— 改写为 allow-drop 删除版 (S34/87/127/130/133)

### 系统性行为发现: typo_clean 规则②a 强转 (接受残留)
- "这种{v6}:{v7}优势" 写库成功, 渲染仍为 "这种3：2优势" (全角):
  _clean_cjk_typography 规则②a 的 lookbehind 含 "}" (2026-09-10 为
  "占位符后逗号"加的), {vN} 闭括号后的半角冒号必被强转全角
- 结论: {vN} 后紧跟的半角标点在缓存里改不出半角渲染; S12 修正的实际价值
  = 确保 {v6}/{v7} 字形原位(防 33:22), 全角冒号为可接受残留
- 潜在隐患备案: 服务器进程 _legacy_canon_index 为惰性内存缓存, 外部改库
  不使其失效; 本次 15/16 直查命中未踩雷, 若日后手术后修复不渲染且
  直查形态不匹配, 需全杀 python 重启重建索引

### 应用与终验
- 应用: --from-report 裁剪清单(16条) --apply --allow-drop → 写回 53 行
  (含参数形态阶梯历史形态行, 全部同步修正)
- force 渲染 21s (全缓存命中, 零 LLM token); 终验 24/24:
  16 条修正 15 条渲染实证 + S12 无字形重复; 拒绝项 3 条维持原状;
  无 {v 泄漏 / 仙人掌科≥80 / 仙人学科 0 / 三倍于 0 / 双括号 0
- 镜像: converter/translator v24a 改动曾漏同步, sync_patches.ps1 补齐 8/8
### 回归: 全部通过 (含 strategist 15 单测)
### 提交: f32cb86 (军师层 + v24b.1 修复 + 镜像补齐)

### 收尾复查补录 (文档一致性)
- PROTOCOL.md §3 隐形通道补录 segflow/latest.jsonl (converter → strategist):
  行内序号是渲染时 current 序号而非库内 canon 序号, 写回前必须 _canon_seq 重排
- run_all.py docstring 聚合清单补 4/5/6 (lookahead/docsummary/strategist)
- strategist.py 用法行改为绝对解释器路径 (仓库无 venv 目录)

## v24b.2 军师层·占位符图例 + 报告/修复分离 (2026-09-12)

### 动机 (对 v24b 实战失效率的归因)
v24b Cactaceae 实测 22 条建议 6 条被人工拒绝, 归因分两类:
- **字形盲区 (真需视觉)**: S12 军师插入字面 "3:2", 而 {v6}/{v7} 本就是数字
  3/2 字形 → 渲染 "33:22"。军师只拿到译文字符串, 永远看不见占位符背后。
- **规则缺失 (与视觉无关)**: S8/S18 在段尾补"配子/et al." , 但续段 raw 开头
  就是 gametes/et al. → 跨页切分造成的重复; 侧车里文本信息本来就有。
结论: 要"看"的只有字形一类, 而字形本就在 converter 的 var[] 里 —— 补导出即可,
成本为零, 不必为此上多模态模型 (VLM 需逐页出图, 且治不了结构问题)。

### 改动
- converter (venv 侧为源, 见下"顺序教训"): 段序导出的每行新增 `vars`
  = 该页 {vN} → "".join(c.get_text() for c in var[N]), 仅导出该页用到的号;
  var[] 是页内局部表, 故图例随页携带 (同号跨页含义可不同)
- strategist.py: `legend_of()` 生成逐段图例, 随段注入 prompt
  ("[S12] 译文…\n⟨占位符内容: {v6}=3 {v7}=2⟩"); 纯标点占位符过滤 +
  单值截断 30 字符 (见下"成本实测")
- strategist.py: prompt 新增三条硬约束 —— 严禁把图例字面写进 find/replace;
  严禁在段首/段尾增删内容 (跨页接缝不得靠补字"接上"); find 不得跨越 {vN}
- strategist.py: **报告/修复分离** —— 无 find/replace 但有 idx+issue 的条目
  收进 reports 列表 (跨页断句这类缓存层无修复出口的问题), 只打印不写库,
  落盘到 strategist_report.json 的 "reports" 字段, 供人工做缓存手术

### 成本实测 (Cactaceae 154 段)
- 全量图例占译文总量 **81.4%** (最长单段 1984 字符) → 过滤纯标点 + 截断后 **24.9%**
  (最长 802); 图例是 prompt 输入增量, 不影响缓存键
- S12 图例实测输出 `{v6}=3 {v7}=2` —— 正是当时误判的那两项, 盲区消除

### E2E 实测
- force 重渲染 Cactaceae: 14.9s, 缓存行 delta 0 (零 LLM token)
- 侧车 34 页 / 154 段, **34 页全部带非空图例**; 抽样 p0 vars={0:10, 1:ı´, 2:','}
  p2 表格页图例为大段表头文本 (已截断)

### 顺序教训 (踩坑备案)
sync_patches.ps1 的方向是 **venv/server 运行侧 → patches/** (单向)。
本次先改了 patches/pdf2zh_converter.py, 随后 sync 用 venv 旧内容覆盖之,
改动静默丢失 (镜像回归仍全绿 —— 只比对两侧相等, 不校验内容新旧)。
正确顺序: 先改 venv/server 源, 再 sync, 再回归。

### 回归: 全部通过 (含 strategist 23 单测: +图例 6/图例噪声 2)

## 2026-09-12 v26-L1/L2 军师层两类可机械识别漏损的压缩 (页边界标记 + 排版残渣占位符预标注)

### 动机 (对 Cactaceae 22 条建议失效率的归因)
22 条建议 → 16 条采纳 → 真零人工可用仅 **10 条 (45%)**。漏损集中在两处:
- **页边界被当成语段边界**: 军师敢给页尾残句"补字接续", 补的字与下一页首行原文重复
  (实测 idx 8: "三个成功的" → "三个成功的配子")
- **锚点按占位符多重集全等**: "该删的排版残渣占位符"删一个就整条被丢弃, 只能人工代删

两项都是**零缓存影响**的纯军师线改动 (不碰翻译路径, 不产生缓存键变化)。

### L1 页边界标记
- `converter.py` 侧车 rec 增加 `page` 字段 (`_segflow_pageno` 自增)。依据:
  `receive_layout` 每页回调一次 → 调用次序即页序 (首页判定 `_segflow_fresh` 已在用同一假设)
- `strategist.py`: `load_segments` 载入 `page`; `build_chunk_text` 在跨页两段之间插
  `PAGE_BREAK_MARK`; prompt 声明"页边界≠语段边界, **禁止段首尾补字接续**, 断句一律用 B 格式报告"
- 老侧车无 `page` 键 → 不插标记, 行为与旧版一致 (失败安全)
- 分块重叠 1 段的既有机制天然保住跨页标记: 块边界恰落在页边界时, 重叠段仍是上一段,
  标记插在它之后而非丢失

### L2 排版残渣占位符预标注
- `_DROPPABLE_GLYPHS` 只收渲染层本就会**改写或删除**的字形: 连字符族 (渲染层直接删) +
  句读与圆括号 (渲染层转全角/判冗余后删)。**方括号不收** —— 渲染层原样保留 `[n]` 引用标号, 删掉会丢引用
- `is_junk_glyph` / `droppable_of`: 只依赖侧车已带的 `vars` 图例, 零 PDF 重解析;
  `(cid:NN)` 按 `chr(NN)` 还原; 空白/空字形视为残渣; 老侧车无 `vars` → 空集 (退回"一律不得删")
- `tokens_ok(..., droppable=set)`: 只对集合内的占位符放宽为**子集**, 其余仍要求多重集全等;
  新增占位符与占位符复制在任何模式下都禁止; 人工兜底 `--allow-drop` 优先级更高
- 写回侧两处调用方 (`llm_review_flow` / `load_curated`) 均已接 `droppable=droppable_of(seg)`
  —— 接之前 L2 只是"空转" (prompt 说了可删, 锚点仍按全等拦)
- prompt 增补 `⟨可删占位符: …⟩` 说明与"未列出的占位符仍必须原样保留"

### 安全边界实测 (把 22 条原始建议按三种口径重放)
| 段 | strict | droppable | allow-drop | 缺失的占位符 |
|---|---|---|---|---|
| S2 | ✗ | ✗ | ✓ | {v15}(渣), **{v19}** |
| S12 | ✗ | ✗ | ✓ | **{v25}** |
| S35 | ✗ | ✗ | ✓ | 10 项, 全非渣 |

结论: 这三条**本应拦下**的建议在全局 `--allow-drop` 下会被静默放行 (删掉的是承载真实字形/英文的
占位符), L2 的收窄子集把它们挡住了 —— **L2 严格优于全局 `--allow-drop`**。

### 已知边界 (接受)
渲染层对 `.,;:()` 的行为是"转全角 / 判冗余后删", 与"直接删除"不完全等价; 极端情况下 LLM 可能
删掉一个成对的圆括号占位符。取此宽度的理由: 军师只在**改写该句**时才行使删除权, 且 prompt 已限定
为排版残渣。

### 回归
`tools\tests\run_all.py` 全绿: 镜像 8 + 缓存单元 10 + 迁移集成 5 + 前瞻 10 + 摘要 9 +
**军师 47** (23 → +24: droppable 锚点 6 / 渣字形 8 / 可删集 2 / 送审文本组装 7 / 页号载入 1) = **89 项**。
镜像同步已完成 (`patches\pdf2zh_converter.py` = `E3A96113`)。

### 真验收 (2026-09-12 17:32 重启后实测)
重启纪律执行到位: kill 全部 python/uv 进程 → `launch.ps1` → `/api/history` 为空
(全新实例, 无历史去重)。

**L1 · 侧车页号 (converter 侧)**: force 重渲染 Cactaceae **16.6s / 0 LLM token** (全缓存命中);
侧车 34 页全部带 `page`, 序列 1..34 连续, 154 段, 行键 = `[page, pageid, segs, vars]`。

**L1 · 军师行为转变 (真实 LLM, 2 块 / 约 25 分钟)**: 建议 47 条 → **采纳 1 条 / 只报告 25 条 (16 段)**,
其中 **12 条是"接缝断裂"且明确点名下一页段号** (S8→S12, S18→S20, S35→S37, S78→S80,
S87→S89, S114→S116, S125→S127 …)。对照基线报告 (22 条采纳 → 人工裁到 16 条, 其中 6 条被人工推翻):
**军师不再给页尾残句补字, 改为准确报告** —— 那 6 条"需人工推翻"的有毒产出被清零。
唯一采纳项 S26 (page=6) 首尾 20 字未变, L1 不变量成立。

**结论修正**: C3 的"自动可用率 45%"这个指标选错了分母。军师的定位是**发现者而非修复者** ——
跨页断句在缓存层无修复出口 (只能缓存手术 / 上游 BabelDOC), 它正确的产出形态就是"准确的待手术清单"。

**L2 · 本轮未被触发**: 0 条采纳依赖 droppable (唯一采纳项 `strict=True`)。本轮 LLM 未产出
"删排版残渣占位符"型建议, 所以"5 条人工删占位符 → 零人工"的验收在**本样本上无法复现** ——
既未证实也未证伪, L2 保持零缓存影响、零风险的待命状态。安全边界仍在工作:
S148/S150/S152 (参考文献段) 共 12 条 "锚点失败 (replace 改动了占位符)" 被正确拦下
(LLM 想改参考文献译名却动了承载英文标题的占位符)。

**新发现并修复 · 报告噪声**: 25 条只报告项里 **9 条 issue 仅为 "B"** —— 模型把 prompt 的
格式编号当成了 issue 内容回吐 (且 idx 已被同段的内容条目覆盖)。修复: prompt 增一行约束 +
`is_format_marker()` 代码侧兜底丢弃 (+2 单测, 回归 49 例)。

**踩坑备案 (重要)**: 军师报告必须与产生它的那份侧车**同批**使用。本次重渲染前, 旧报告 (14:05)
与侧车 (15:19) 已不同源, 22 条里 **14 条 `find` 在新侧车 0 命中** —— 旧报告已归档为
`strategist_report.v24b2_baseline.json`。

**成本**: 军师是分钟级批处理 (2 块 / ~25 分钟), 不适合交互式等待。

### 回归
`tools\tests\run_all.py` 全绿 (军师单测 **49**)。

---

## 2026-09-12 v26 后续 军师接缝清单落地为缓存手术 (跨页断句 3 处)

### 清单对账 (更正上一节"12 条接缝断裂"的统计口径)
上一节把"issue 里出现'接缝断裂'字样"当成了"断言断裂"。逐条对账 (前段尾 + 后段头实文对照):
- **断言断裂 7 条** (S8→S12 / S18→S20 / S35 / S78→S80 / S87→S89 / S114→S116 / S125→S127) —— **全部属实, 0 误报**
- 另 5 条 (S81 / S103 / S119 / S130 / S136) 军师自己的结论是"句子完整": 其中 S103 / S130 / S136
  确实完整, **S81 / S119 实为断开却被判完整 (漏判)**
- 即接缝判定**精确率 7/7, 召回 7/9** —— 清单的价值在"命中准", 人工必须**补漏**

### 手术选取 (判定依据: 只看段首会误判, 必须前段尾 × 后段头对照)
| 接缝 | 处置 | 理由 |
|---|---|---|
| S87→S89 | **改** S87 尾 `采用间接估算法` → `间接估计值` | 拼接后为"采用间接估算法来自…"语法不通; 原文 indirect estimations 是名词短语 |
| S78→S80 | **改两段**: S78 尾补 `相关的植物` + S80 头去 `相关植物{v0}的`, 改 `自交不亲和性已根据…` | 原文当时为 "…or from genetically related plants. Self-incompatibility has been classified…", 现有译文把两句并成一句; 双段同改后既不悬空也不重复 |
| S8 / S125 | 不改 | 补尾必与下段开头字面重复, 双改又改变语义; 且拼接本就可读 |
| S18→S20 | 不改 | 断在参考文献作者名 (`; Arias` \| `et al. 2005b)`), 原文格式即如此, 双改风险 > 收益 |
| S114→S116 | 不改 | 拼接"方差较大 + 与近交衰退相关联"本就通顺 |
| S35→S71 | **改** (见下节"追加手术") | 首轮误判为"续接被表格隔开、收益低"; 实为跨 **7 页**接续, 且 S71 头是截断误译 |
| S103 / S130 / S136 | 不改 | 军师判对, 句子完整 |

**关键认识**: 跨页断句的"手术"**不是补字** —— 补字必然与邻段重复 (v26-L1 已从 prompt 层禁止)。
能干净修复的只有两类: (a) 前段尾因**误译**导致拼接不通 (S87); (b) 后段头**把两句并成一句** (S78+S80)。

### 写回与 canon 保护
- 路径: `strategist.py --from-report <curated> --allow-drop --apply` (find 唯一 + 锚点重校验, all-or-nothing)
- 写回 **10 行** (3 段 × 各参数形态), `UPDATE _translationcache SET translation=? WHERE original_text=<canon 原文>`
- canon 重排生效实证: 侧车 S87 用 `{v97}`, 库内写成 `{v66}` —— 若不重排, 渲染时占位符会整体错位 (v24b.1 老坑)
- **L2 首次真实用途**: S80 的 `{v0}`='.' 属可删字形, 删除被放行; 但该段 `{v1}{v2}` (连字符) 在既有译文里
  本就缺失, 不在 `droppable_of` 集合内, 故本条仍需 `--allow-drop` 才过闸 —— 记录为 L2 的边界案例

### 真验收 (19:31)
重启纪律: 精确 kill 服务器进程树 (uv 24212 → py 6772 → py 33288) → `launch.ps1` → `/api/history` 为空。
force 重渲染 Cactaceae: **全缓存命中, 0 LLM token**。
- 侧车 (19:31:51, 34 页 / 154 段): 三处修正全部进入渲染输入
- PDF 输出层 spot-check (mono):
  - p17 尾 `…或来自遗传上相关的植物` → p18 首 `自交不亲和性已根据种群内花部形态（同型与异型）…` ✔ 跨页成句
  - p19 尾 `…首先，间接估计值` → p20 首 `来自种群遗传学研究的数据…` ✔ 跨页成句
  - 旧串 `采用间接估算法` / `相关植物的自交不亲和系统` 在输出中 0 命中 ✔

### 口径固化 (军师定位)
`strategist.py` 头部 docstring 增"定位 [v26]"段: 军师是**发现者而非修复者**, 跨页断句在缓存层
没有修复出口, 正确产出形态是"准确的待手术清单"。评估指标应为**清单准确率**, 而非"建议自动可用率"。

### 配套工具: `tools/seams_report.py` (接缝体检)
把"发现跨页断句"从"等军师跑 25 分钟 + 人工过清单"压缩成一条命令:

```powershell
$PY = "D:\Users\<user>\anaconda3\envs\zotero-pdf2zh-venv\python.exe"
& $PY tools\seams_report.py --with-report
```
- **输入**: 侧车 `latest.jsonl` (逐页导出, 含 `page`)
- **逻辑**: 只取"正文段"(剥掉 `{vN}` 后仍有实义字符), 相邻正文段若跨页即为一条接缝 ——
  中间隔多少表格/页脚段都会跨越 (S8 与 S12 之间隔着页脚 + 整页 Table 10.1)
- **标记**: 前段译文尾无句末标点 -> 疑断; 后段原文以小写续接 -> 疑续; 两条同时成立标 `[!]`
- **交叉**: `--with-report` 显示军师是否已点名该段

Cactaceae 实测 (154 段 / 34 页, 正文段 73):
- 输出 25 处跨页接缝, 其中 `[!]` **8 处**
- 与人工逐条核对结论吻合: 军师断言断裂的 7 条**全部命中**, 外加 S94 (表格→正文过渡) 一处假阳性
- 额外捞出一条**人工绝不可能找到**的远距离接续: `S35(p8) -> S71(p16)` 隔 35 段 ——
  p9~p15 全是表格与图页, 被"非正文段"过滤后两侧正好相邻, 文本上确实是同一句被切开
- 弱标记 `[~]` 12 处全部落在图注 / 参考文献 / 表格过渡上, 人工一眼可排除

**注意**: 这是**结构症状清单**, 不等于质量清单。已手术修好的条目 (S78 / S87) 仍会出现在 `[!]` 里 ——
因为原文仍被页边界切开, 只是译文已能跨页读通。是否已处理请对照本节记录。

### 追加手术: S71 截断误译 (seams_report 的首个战果)
首轮判 S35 时我只看到"续接被 p9–p12 表格隔开", 结论记成"收益低、不改" —— **依据是错的**。
新脚本一次运行就把实情摆出来了:
- `S35(p8)` 原文尾 `…in which plants with female flowers have short`
- `S71(p16)` 原文头 `styles and male flowers have long styles with an atrophied stigma`
- `S36~S70` **无任何正文段** (全是表格/页脚/占位符) —— 中间隔 **7 页**

即同一句被切开, 而且 S71 头把孤立的 `styles` 猜成了"雌花花柱长", 与 S35 尾的
"雌花植株具有短花柱"**直接矛盾** (与"三倍于"完全同类的截断误译)。

- 手术: S71 头 `雌花花柱长；雄花花柱亦长，但柱头退化` → `雄花具有长花柱，柱头退化`
  (`styles` 是前句残余、不再重译, 直接从"雄花"起句; 只改文字不动占位符, **strict 模式直过**)
- 写回 **3 行**; 重启 + force 重渲染全缓存命中
- PDF 输出层验证: p8 尾 `其中雌花植株具有短花柱` → p16 首 `雄花具有长花柱，柱头退化（del Castillo…）` ✔
  旧串 `雌花花柱长` / `雄花花柱亦长` 在输出中 0 命中 ✔

**方法论结论**: 跨页接续的"距离"可以远超直觉 (实测 7 页), 靠人工翻页对照不可行 ——
这类问题必须按"**正文段相邻**"配对, 而不是按"物理页相邻"配对。

## 2026-09-12 v26.2 接缝运维工具化 (体检 → 台账 → 专项审查 → 渲染验收)

### 起因
跨页断句的处置已跑通 4 次 (S8 / S71 / S78+S80 / S87), 但每一步都靠临时手段:
- **发现**: 军师全文通读 25 分钟, 且它对"这条到底断没断"只能给文字描述;
- **记账**: 哪几条已手术全靠人记, 重渲染后清单照旧把已修条目列成 `[!]`, 容易重复动手;
- **验收**: 每次都临时写 pdfminer/pypdf 脚本捞关键词。
本轮把这条链路固化成命令。

### 新增闭环
| 步 | 命令 | 做什么 |
|---|---|---|
| 发现 | `tools/seams_report.py` | 侧车零成本捞出跨页接缝, 标 `[!]`/`[~]`, 高噪折叠 |
| 裁决 | `tools/seam_review.py` | 接缝四行对照送一次 LLM, 判类型 + 给最小编辑建议 |
| 手术 | `strategist.py --from-report <file> --apply` | 人工裁剪后写缓存 (find 唯一 + 锚点校验) |
| 验收 | `tools/verify_render.py` | 新串落到哪页 / 旧串是否 0 命中 |
| 记账 | `seams_report.py --mark <段号> --note <说明>` | 下次运行标 `[已手术]` |

### 1. 手术台账 (seams_report.py 升级)
- 位置 `~/.cache/pdf2zh/segflow/surgery_log.json`
- **指纹只对 `raw` 取 md5** —— 手术只改译文, 指纹不变, 台账在反复重渲染后依然有效;
  换文档则指纹不同, 不会误标 (Cactaceae = `92601357970d`)
- 同步登记既有 4 次手术 (共 5 条):
```powershell
& $PY tools\seams_report.py --mark 8   --note "S8 尾截断误译: '三倍于' -> 拥有三个成功的(配子)"
& $PY tools\seams_report.py --mark 71  --note "S71 头截断误译: 雌花花柱长 -> 雄花具有长花柱,柱头退化"
& $PY tools\seams_report.py --mark 78,80 --note "S78尾补'相关的植物' + S80头去'相关植物{v0}的'"
& $PY tools\seams_report.py --mark 87  --note "S87: 采用间接估算法 -> 间接估计值"
```
- 效果: 25 条接缝里 5 条带 `[已手术 日期: 说明]`, 上一节末尾那句"是否已处理请对照本节记录"
  的人肉提示可以退休

### 2. 折叠规则修了两处误判 (假阳性降级)
折叠的本意是"把人工一眼可排除的项藏起来", 但它自己也会误伤 —— 实测两处:

| 问题 | 原判据 | 实测反例 | 修正 |
|---|---|---|---|
| S71 被判"文献条目"而折叠 | 汉字**占比** < 35% | `雄花具有长花柱，柱头退化 del Castillo 和 GonzalezEspinoza` 占比 29% —— 而它正是跨 7 页那条的尾段 | 改汉字**绝对计数** < 8 才判文献 |
| `S2(p1)->S5(p2)` 被当接缝 | 无 | 后段是章节标题"引言" | 实义长度 < 12 且无句末标点 -> 标题 |

另加一条兜底: **台账登记过的接缝永不折叠** —— 人工已确认它是真接缝, 不能因为
"段短/含拉丁学名"这类表象再被藏起来。折叠数 14 -> 6 (S35→S71 因此显形)。

### 3. 渲染层验收 (tools/verify_render.py, 新增)
- 判据: 关键词先做**去空白归一化**再匹配 —— PDF 提取常把一行切成多行、词间塞空格,
  原样子串匹配会假阴性 (实测 `雄花具有长花柱，柱头退化` 跨行)
- 默认取 `server/translated` 最新 `*-mono.pdf`; 退出码 全通过 0 / 有 FAIL 1
- 本轮四处手术一次性验收 **9/9 通过**:
```
[OK] 期望串 '雄花具有长花柱' -> p16×1        [OK] 禁止串 '雌花花柱长' -> 0 命中
[OK] 期望串 '间接估计值' -> p19×1 p27×1      [OK] 禁止串 '采用间接估算法' -> 0 命中
[OK] 期望串 '三个成功的' -> p2×1             [OK] 禁止串 '三倍于' -> 0 命中
[OK] 期望串 '或来自遗传上相关的植物' -> p17×1  [OK] 禁止串 '雄花花柱亦长' -> 0 命中
```

### 4. 接缝专项审查 (tools/seam_review.py, 新增)
把"军师全文通读"换成"只读接缝": 每条只送 前段原文尾/译文尾 + 后段原文头/译文头
四行对照 + 图例 + 可删占位符; 15 条接缝约 12K 字符, 一次调用返回 (军师需 25 分钟)。
- 类型四选一: 截断误译 / 头并句 (可修) | 物理切开 / 排版如此 (只报告)
- 产出 `~/.cache/pdf2zh/segflow/seam_review.json`, 与军师报告同构, 可直接
  `strategist.py --from-report <file> --apply`, 也可 `seams_report.py --with-report --report <file>` 交叉
- **脚本自身不写缓存**; 校验未过的建议自动降级为 reports (附原因, 只损便利不丢信息);
  接缝专属约束 `idx` 必须是该接缝的前段或后段, 防止模型改到无关段
- 台账已登记的接缝默认跳过 (`--redo` 重审); 分块按字符预算, 每条独立无需重叠

实测 (Cactaceae 15 条): **0 修正 / 15 报告** —— 手术已完成, 剩余确为物理切开与文献排版。
**需记录的局限**: `S81→S83` 人工已认定是断开 (后段头 `autogamy treatments` 被并进下一句),
模型判成"物理切开" —— 与军师漏判落在**同一处**。即"后段头并句"是模型稳定漏的一类,
结论"是否需要手术"虽与人工一致, 类型判定并不精确。清单仍须人工过一遍 (与 v26 口径一致)。

## 2026-09-12 v26.3 接缝判定的硬信号 (悬空功能词) + 一处设计缺陷修正

### 起因: S81 的漏判不是"模型不够聪明"
模型把 `S81 (p18)` 判成"物理切开、无错误"。但它的前段**原文**尾是:

```
...found ca{v132} fruit set in
```

`in` 后面不可能没有宾语 —— 这是句子被页边界硬切断的铁证, 比"译文尾没有句号"硬得多。
此前 prompt 只教模型看"译文尾是否像残句", 而 `发现结实率约为{v132}` 读起来自成一句,
模型没有理由怀疑; 真正的证据在原文里, 只是没人提示它去看。

### 判据 (零成本, 纯字符串)
前段原文尾若落在介词/连词/冠词/系动词上 (`in/on/of/to/and/.../is/are/was/were`),
即为"悬空功能词"。实测 25 条接缝 **3 条**带此信号:

| 接缝 | 原文尾 | 性质 |
|---|---|---|
| S81→S83 | `...fruit set in` | **模型漏判那条** |
| S87→S89 | `...indirect estimations were` | 同一类型, 已手术 (当初人工正是靠 `were` 发现的) |
| S130→S132 | `...Jiménez-Sierra {v66} In` | `In general` 被切开, 译文已正确合并, 良性 |

即精确率 2/3, 且**恰好覆盖漏判**。落地两处:
- `seams_report.py`: 清单行加 `[悬空词:in]` 标注 (给人看)
- `seam_review.py`: 送审文本里显式标注 + prompt 禁止把带标注的接缝判成"物理切开" (给模型看)

### 实测效果 (重跑 15 条送审)
- **S81 从"物理切开"纠正为"截断误译" ✔** —— issue 写的是"前段原文尾被硬切断于介词
  'in'... 后段原文头 'autogamy treatments' 表明前句应为 'found ca. fruit set in
  autogamy treatments'", 判断准确
- **代价 (召回换精确)**: S114 / S130 被连带判为"截断误译" —— 而人工已认定 S114 拼读通顺、
  S130 译文已正确合并。两条都因锚点校验失败停留在 reports 层, **不写库**, 人工多读两行即可排除
- S81 的**修复建议**本身不合格 (锚点失败: 模型试图把 `{v132}` 展开成字面数字), 同样降级为报告 ——
  "分类准确"与"能否干净修复"是两件事, 前者是军师的职责, 后者受架构限制 (跨段语序需重排)

### 一并修掉的设计缺陷: `idx` 字段本身有歧义
上一轮模型把 `idx` 填成了 `1` (接缝序号), 于是"改前段还是后段"整件事失败:
`校验未过: idx=1 不属于该接缝(S81/S83)`。**段号本不该由模型表达** ——
改由模型输出 `which` (`前段`/`后段`/`无`), 段号由脚本按接缝推出, 这类错误从设计上消失。
另加固两道: `find == replace` (模型只复述一遍) 与锚点越权删除, 均在 `validate` 拦下。

## 2026-09-12 v26.4 裸花括号脱壳 + 术语按多数派归一 (Cactaceae 豆包对照后的本地修)

### 路线: 豆包当参照, 不进渲染链
豆包译文只作"语境与译笔"的对照物(整篇入语境、逐页取译文), 本地缺陷仍走既有流程
(补术语表 → 缓存手术 → force 重渲染 → verify_render 抽验)。互补定位: 豆包补"读感",
本链补"版面保真 / 可复现 / 可手术 / 术语硬约束 / 离线批量"。

### 术语按多数派归一 (terms.csv + terms.babeldoc.csv)
- 追加 `herkogamy,雌雄异位`、`breeding system,繁殖系统` 两行 (两文件同步, 防启动覆写)
- 机制红利(v23 按段指纹): 加词只作废"含该词的段"——本文档 22 段重译, 其余全命中;
  纯 `Cactaceae` 命中的段指纹不变 → **S7/S8 的手工修复不被覆盖**
- 重译后实测: 雌雄异位 17 / 繁殖系统 29; 旧变体 雌雄蕊异位、繁育系统、育种系统 **全为 0**

### 缓存手术 (备份 `cache.v1.db.bak-20260912-211403`)
- **S7** (id 4271/4406/4432/4472 四种参数形态): 数字/方括号错位
  (`…保护联盟[59IUCN(红色名录中的)个物种…64CITES(附录I中的)个物种]`) → 重排为
  `…保护联盟{v21}IUCN{v22}红色名录{v23}，{v24}个物种…`
- **S8** (活行 id 4473): 字面花括号 3 处 (`{Lovett{v4}Doust与Lovett{v5} Doust{v6}}`) 去壳
- 两条硬断言: 新串 `{vN}` 多重集 ⊆ 原串; `replace` 在段内唯一

### v26.4 代码修复: 裸花括号脱壳 (`_clean_cjk_typography` ⑤)
- 根因: 源文本里的真花括号已被 converter 的 vchar 字符表(`{`/`}` 在表内)保护成 `{vN}`
  占位符, 故译文里"非 `{vN}`"的花括号**必是** LLM 模仿占位符语法自造的伪影
  (实例: p28 参考文献把仙人掌科写成 `{仙人掌科}`×7, 纯文本渲染出可见花括号)
- 做法: 按 `{vN}` 切分保护占位符, 其余 `{`/`}` 一律删除、内容保留 →
  占位符多重集与 polish 锚点断言均不受影响
- **放在读取路径**: 不改任何缓存键(`typo_clean` 仍 v1) → 旧缓存零重译即刻生效,
  全库同类残留一并消失
- 边界: 段落无汉字时函数早退(既有设计) → 纯拉丁段落的裸括号不受此规则保护, 留待观察

### 新增工具 `tools/force_rerender.py`
固化"全插件同等 config"(engine/pdf2zh, service=silicon, targetLang=zh-CN, threadNum=4,
skipLastPages/noWatermark/mono/dual/skipSubsetFonts, asyncJob) + `force:true`, 轮询到结束并
打印产物路径 —— 把 2026-09-10"漏传 config → 静默回落 bing 全量重译 341 段"的坑封死在工具里。
默认 900s 上限: 重译 22 段实测约 39 分钟(含 polish 反思与多次"polish 破坏占位符"回退),
远超该值属正常, 需自行加大 `--timeout`。

### 验证
- 回归 `tools/tests/run_all.py`: **全部通过**
- 渲染 1 (术语归一后, 22 段重译): 21:23:50 → 22:02:23
- 渲染 2 (v26.4 生效后, 全缓存命中): **45 秒**, 产物 mono/dual 重写
- `verify_render` 12 项全 OK: 期望 `雌雄异位`(9 页)、`国际自然与自然资源保护联盟`;
  禁止 `[59IUCN` / `64CITES` / `{Lovett` / `{仙人掌科}` / **`{`(全文 34 页 0 命中)** /
  `雌雄蕊异位` / `繁育系统` / `育种系统` / `仙人学科` / `三倍于`
- 镜像同步 8 文件 MD5 全 PASS

## 2026-09-12 v26.5 successful gamete 语境修复 (待定: 该条术语该不该全局生效)

### 用户给出的语境判断 (领域知识, 非模型自推)
进化遗传学/繁育系统语境里 `successful gamete` 的"成功"= **能参与受精并把亲本基因传给子代**,
不是"细胞活着"。Fisher 的 3:2 自交优势正是这个含义: 自交个体平均 3 个可传递基因的配子
(2 个自交 + 1 个外送), 异交个体只有 2 个。中文植物学文献惯例译 **可育配子**;
"成功配子"生硬直译, "存活配子"错误(存活 ≠ 可育)。

### 处置
- 术语表两文件追加 `successful gamete,可育配子`
- 缓存手术: p3 段尾 `三个成功的` → **`三个可育`**(4 种参数形态行 4401/4405/4433/4473)。
  注意尾词之所以留"可育"不带"配子": 下一段(p4)段首自带"配子"(原文页首就是 `gametes`),
  写成"三个可育配子"会渲染成"…三个可育配子配子…"重复
- p4 段(id 4566 家族)因术语加词换键 → 自动重译, 得到"只有两个**可育**配子"
- 渲染 100 秒(1 段重译 + 其余全缓存命中)

### 验证 (verify_render)
- 期望: `拥有三个可育` p2×1、`可育配子` p4×1
- 禁止: `成功配子` / `三个成功的` / `三倍于` **全 0 命中**
- 跨页接缝说明: 源版式把 Table 10.1 整页插在句子中间(p2 尾 → p3 表格 → p4 首),
  故"三个可育"与"配子，"分列两页属**原版式**, 连读语义正确, 不是缺陷

### 待定 (需要用户裁决)
该术语目前是**全局硬约束**: 任何文档里出现 `successful gamete` 的段都会被强制写成"可育配子"。
若判断它只在本文语境成立, 应改走"文档级术语表"(需改代码)或撤出术语表只留缓存手术。

### 本轮顺带发现 (未处理)
p4 "多个多肉植物科，**例如。g.,**龙舌兰科" —— `e.g.,` 残留(译出"例如"又原样留下 ".g.,")。

---

## v26.5 (2026-09-12) 豆包译文采纳管线 M1 打通 (p2-p4 端到端)

### 路线背景
用户确定"豆包当翻译引擎、本地管版面"的路线。豆包桌面版 = 内置 Agent 平台,
本地侦察确认: Chromium 147 内核 (D:\LenovoSoftstore\Install\doubao), 109 个内置技能包,
支持自定义连接器 (HTTP/STDIO)。基础对话免费不限次, 工作任务(Agent)模式免费额度极少
(实测一个 PDF 即耗尽, 等 12h+), 连接器仅在 Agent 模式挂载 → **主路线 = 基础对话 + 粘贴**,
连接器留作有额度时的省事选项。

### 新增工具 (tools/)
- **doubao_bridge.py** — 本地 MCP 桥 (STDIO, 零依赖, 自写 JSON-RPC):
  list_inbox / get_payload / submit_result 三工具, 操作 inbox|out 目录。
  豆包「技能·连接器」注册: 命令=venv python, 参数=本脚本。回环测试通过。
- **seg_export.py** — 侧车→编号段落包: 过滤纯字形段(页眉/整页表格),
  {vN}→真实字形还原, PDF 断词连字符修复 (后随小写删连字符 "obser- vation"→
  "observation"; 后随大写保留 "Lovett- Doust"→"Lovett-Doust", 首版一刀切
  误伤真连字符导致回锚光标级联冲过头, 已修), 跨页续接检测合并 (段尾无句末
  标点+下段首小写, 中间隔整页表格也能抓到: p2#4+p4#0), 产 payload + manifest。
- **seg_import.py** — 豆包译文→校验+回锚: #S编号切块 (容忍 markdown),
  编号对账, ⋮ 对账, **回锚 = 只锚高价值字形 (核心=字母数字), 纯标点字形按设计
  丢弃不参与搜索** (否则 '-' 找不到本尊会偷走后面真标点的位置, 光标级联),
  核心命中后向两侧吸收与字形值一致的标点 (全角兼容), 产 imported.json。
- **seg_inject.py** — imported.json→缓存库: ①重编号 (侧车页级 {v27}↔缓存段内
  0基 {v0}, 按 token 顺序映射); ②文档作用域 (translate_engine_params LIKE
  '%docsummary:3768fd6fce999176:bc4a2947%' —— "Introduction" 原文匹配会命中
  13 行跨 13 个文档, 文档指纹后缩到 1 行, 防误伤); ③前置断言 (缓存现行译文
  == 侧车 trans 重编号, 证明行集正确) + 占位符多重集 ⊆ raw。
  9 段 13 行写入, 备份 cache.v1.db.bak-20260912-232520。

### M1 结果 (p2-p4, 9 段 6153 字符)
豆包基础对话粘贴 (6640 字符 payload 一次发完, 零额度消耗) → 回复 2133 字
→ 校验 PASS (8/8 段, ⋮ 唯一, 数字/拉丁全回锚) → 注入 13 行 → force 渲染
81s→45s (全缓存命中, 零 LLM) → verify_render **13/13 全过**:
- IUCN 59 种 / CITES 64 种数字归位 (比 v26.3 手工手术更干净: "红色名录收录
  {v47} 个物种")
- Fisher 3:2 = "平均拥有三个可育配子", 豆包自己把 ⋮ 放在 "可育|配子" 正确处
  (我曾误判为词中断裂而 --shift 修错方向, 实为检查脚本 find('⋮') 在已切分
  段上返回 -1 的 bug; 撤销移位后正确)
- 术语: 繁殖系统/交配系统/异交率/雌雄异位 全对
- 旧变体全 0: 成功的配子/成功配子/[59IUCN/{Lovett/雌雄蕊异位/繁育系统

### 已知残留 (不阻塞, 记台账)
1. 整页大表格 (p3, p9-14) 保持英文原文 —— 纯字形段不翻译的既有特性,
   post_check 汉化率断言因此 FAIL (渲染前旧报告同样 FAIL, 非本次回归)。
   表格要不要翻译 = M2 决策点 (需要拆字形, 动版面)。
2. 豆包自己的标点与字形标点并存处会双标点: "（Holsinger 1992).）"
   (v71 后的 "）、" 只吸收了全角左括号, 右侧 "）、" 因 '、' 不等于 ',' 未吸收)。
   改进方向: 吸收匹配改逐字符增量式。
3. "Arias et al{v40}a" 类 al. 缩写点丢失 (渲染 "et al2005.a") —— 微观外观。

### 未提交
M tools/(doubao_bridge|seg_export|seg_import|seg_inject).py,
inbox/、out/ 交换目录, 改动记录本节。

---

## v26.6 (2026-09-13) 豆包采纳管线 M2：p5–p20 全量落地

### M2a (p5–p9)
9 段 / 10565 字符 → 豆包 4376 字 → 10/10 回锚 → 注入 10 行 → 45s 渲染 → 验收 9/9。
新增工具能力: **数字千分位弹性回退**（豆包写 2000 / 字形 2,000，正则 `2[,， ]?000` 命中后仍以字形原值渲染）。
首版回退有**吞字 bug**（命中长度≠core 长度，替换区间按 core 计算多吞一字"个"）——已修
（match_len 取实际命中长度）。教训: 弹性匹配的替换区间必须用实际命中 span。

### M2b (p16–20)
12 段 / 14676 字符（p16→20 四条跨页接缝全合并）→ 豆包译文 → 落盘时修正 4 处 ⋮ 边界
（逗号前/词中/句尾空段 → 移到自然边界）→ 注入 10 行 → 渲染。
新增工具能力: **纯符号字形丢弃**（'/' 等无字母数字核心不参与搜索——此前 '/' 会在全文找
远处斜杠, 光标级联冲头, 一条斜杠毁掉整段 8 个锚点）。
实战确诊三类语序重排/全角化字形, 手术定点放置: v36='98'（英文"28 out of 98"→中文
"98 个属中有 28 个", 数字对调）、v107='(1981).'（55 与 Ross(1981) 对调）、
v15='s)' v17='x'（公式下标 (ws)/(wx) 拆字形成, 中文全角括号需整组替换）。

### 已知残留 (p18/p20 外观级)
- "Ross（1981）。对" —— 字形 (1981). 未锚定(语序重排), 但年份可见无内容损失
- "（wx的" —— 渲染层邻接去重吃掉一个全角括号 (v22 已知家族)
- 改进方向: 全角容忍的 core 正则 + 语序重排字形的成对放置算法

### 进度
- 豆包译文已覆盖: p1(待) p2–p8, p16–20; 表格页 p3, p9–15 自动跳过; 参考文献页 p28–34 按政策不翻
- 剩余批次: B4 = p1+21–22 (~4.5k), B5 = p23–27 (连续链, ~10k)

## v26.7 (2026-09-13) 豆包采纳管线 B4：p1 + p21–22

### 接缝核对先行
实测纠正一个预判: p22 尾 "See text for further details" 是完整句（"xt" 只是 text 的词内断行，
导出器断词修复处理），p23 以标题 "Inbreeding Depression" 开头——p22→p23 无断链。
B4 按原计划 p1+21–22 执行，全批零跨页合并（无 ⋮，对豆包最省事）。

### seg_export 两处修复（离散页码暴露）
1. `--pages 1,21-22` 混合写法直接崩溃：原解析只支持单区间 `2-4` 或纯列表 `2,3,4`，且列表被当作
   连续区间取首尾。改为逐 token 解析（单页/区间混合）。
2. 跨页合并只看"顺序相邻"不看页码：p1 尾段以字形 {v40} 收尾（'}' 非句末标点）+ p21 头段
   "where..." 小写开头，会把**相隔 20 页**的两段错误焊成 ⋮ 合并段。
   加"页码物理相邻（pb == pa+1）"守卫——跨页续接只可能发生在相邻页之间。

### B4 走账
10 段 / 4491 字符 → 豆包 → 10/10 解析；FAIL 3 段全是既有家族：
- p1#2：2% 与 2,000 **语序对调**（raw 先 2% 后 2,000，中文先说"约 2000 个物种"再说 2%），
  前向光标锚不上 → 手术补 {v11}
- p21#2/p21#3：公式字形被豆包写成 LaTeX（`\(w_{SS}\)` 等）。关键认知：**LaTeX 字面量会被
  原样印上页面**（pdf2zh 不解释 LaTeX），必须清除并按 raw 字面量（ws|' w'|'w'|' w'）逐字
  对位重建 token 序列，渲染结果 "wss= ws/(wx+ ws)," 与原版一致
- 额外清理 4 处**字形收尾标点与中文标点叠用**：{v36}/{v47}/{v72} 的字形值自带 ")."，中文
  "）。" 叠上会渲染成 ").）。" → 删中文标点留字形；{v94}='(1876),' 后同理删一个 "）"
手术自检脚本：每段高价值 token 恰好一次、任何 token 不得重复（与 seg_inject 多重集断言对齐）。

### 沙箱教训（环境级，非管线）
seg_inject 打印"已更新/PASS"但复核查库 0 命中：TRAE 沙箱把**工作区之外**（C:\Users\<user>\.cache\）
的写入作废，伴生退出码 1 + sqlite3 pycache 报错。排查结论：首次执行实际已落库（10/10 行复核
字节级一致），第二次执行被丢弃。此后对缓存库的操作：加 `PYTHONDONTWRITEBYTECODE=1`，
**以查库复核为准，不以打印为准**。

### 进度
- 豆包译文已覆盖: p1–p8, p16–22; 表格页 p3, p9–15 自动跳过; 参考文献页 p28–34 按政策不翻
- 验收: 16/16（12 期望串全命中 p1/p21/p22；4 禁止串——旧机翻摘要/旧公式行/LaTeX 残留——0 命中）
- 剩余: B5 = p23–27（连续长句链，必须整批），之后全书终验

## v26.8 (2026-09-13) 豆包采纳管线 B5：p23–27 收官 + 全书终验

### B5 走账
15 段 / 15785 字符（p23→27 长句链 4 条 ⋮ 合并）→ 豆包 → 落盘修 3 处 ⋮ 边界
（S5 "较大⋮，" 逗号前 / S7 "类群⋮。" 句号前 / S12 "总⋮体" 词中；S10 "克隆⋮生长"
与原文 clonal|growth 分页断点精确镜像，保留不动）。
导入 FAIL 4 段 = 公式/统计字形家族，其中新增关键认知：**'¼' 是 PDF 字体 cmap 对 '='
的抽取形**，字形值带 '¼' 时 token 渲染即 '='。
- 23#4：v17='s' 误锚进 "Charle**s**worth" 且 LaTeX 残留 → 整段重建
  （"式中 ws 为自交后代平均适合度，wx 为异交后代平均适合度…"）
- 23#6：n=27（清 LaTeX）/ d=0.97（v84='¼' 补位 → "d=0.97"）/ 0.57（v92 补位）
- 24#3：n=49（清 LaTeX）
- 27#1：统计括号重建（%P=62.98 / A=2.18 / Ho=0.16，"H"+v19='o¼0.16).' → "Ho=0.16)."）

### 通用去叠（新工具 tools/dedouble_sweep.py）
B5 验收发现 ").）。" 叠用是**历史批次系统性问题**（全书抽验 53 处），于是把 B5 手术
中人工执行的去叠判断固化为工具，对全部 5 个批次的注入行机械清扫：
规则 = 字形值含 ')' 删其后紧跟的 '）'；run 尾 ',' 删 '、''，'；尾 '.' 删 '。'；
缩写点（run 不含 ')'）不删；缺 token 段（如 18#1 v107 未锚）不触碰。
M1 18 处 / M2a 55 处 / M2b 63 处 = **136 处一次清零**（B4/B5 手术时已手工处理，0 处）。

### 全书终验（11/11，零容忍口径）
期望串跨批次全命中：p1 摘要 / p1 作者 / p4 可育配子 / p18 "共 98 个属" /
p24 传粉综合征 / p27 遗传多样性 / p27 结尾句。
禁止串全零：LaTeX '\('、叠用 ').）。'、**'成功的配子'（最初触发整个项目的误译）**、
'cid:'（抽取失败标记）。
豆包译文覆盖: p1–p8, p16–27 = 正文全部；表格页 p3, p9–15 按政策跳过；
参考文献 p28–34 按政策不翻。**本书翻译收官。**

## v26.9 (2026-09-13) 成品打磨：表格页回填与定点中文化 + 链接勘察

### 表格页回填（新工具 tools/backfill_pages.py）
发现成品中整页表格被重渲染打碎（旋转表头/单元格成竖排散字）：p3, p9–15 共 8 页
零可译段（段落全是 {vN} 占位符），翻译管线不碰它们, 但渲染仍会重新排版。
修复 = 零可译段页用原版 PDF 对应页回填（pypdf 按页替换, 页数 1:1 校验）,
验收 = 回填页文本与原版逐字一致、中文页不受影响。

### 表格定点中文化（新工具 tools/table_zh.py, 路线 B）
**产品决策：已执行后回退。** 用户目检后判定"不翻译效果更好"——依据成立：
表格是"查"不是"读"(物种名/代码/数字/问号本无语言障碍), 词级替换产出半翻译
混合文本, 且 Noto 对撞原版 Times 字体、基线微错位, 把出版级表格变补丁。
成品定版 = 表格页纯原版(backfill 后停), table_zh.py 工具保留供选用。
正文采纳是"整段替换"所以成立; 表格页是碎片字形, 没有"整段"可采纳 ——
**工具没错, 用错场景**。
原版表格是 landscape 旋转排版 —— V1 正立插入方向错误, V2 改为 **rawdict 字符级
bbox + 行方向向量**(dir=(0,-1) → rotate=90, 锚点取 bbox 右下) 修正。
匹配两级：短语（Table 10.2 / No data / Fruit set / self-incompatibility …15 条）
优先, 单词（subfamily/melittophily/treelink…约 90 词, 含 ﬂ 连字变体）全词匹配;
物种名/作者缩写/亚科代码(Pac/Opt)/SC SI GSI CE MM/数字/'?' 不在词表 => 自动保护。
成品 8 页共 333 处替换（p3 ×116, p9 ×70, p10 ×49 …）, 渲染图目检通过
（表 10.1/10.2 表头、蜂媒/蝙蝠媒、乔木状/灌木状、颜色词、合计 全部按表格方向就位;
 and/or 连接词保留为已接受的 V1 残留）。

### 链接勘察（成品=mono 的隐藏彩蛋比预想好）
- 原版 328 个 NAMED 链接（表格引文→参考文献书内跳转）: **mono/dual/成品 全部保留**
  （逐页分布与原版一致）, 168 个命名目标树完整带过来, **0 失效**。
- 缺陷：译文页链接**热区矩形错位** —— 重排后文字位置变了, 链接框还钉在原坐标
  （抽查 116/116 错位, 框内文字已非锚文本）。表格回填页天然对齐。
- 已执行方案甲（新工具 tools/relink_pages.py）：从原版同页同矩形提取锚文本 →
  在译文页重搜 → 链接矩形搬到新位置（同页多命中取距原中心最近者）。
  结果：254 重定位 / 0 未命中 / 73 回填页自带对齐；核心比对终验 **319/328 精确
  对齐（97.3%）**, 余 9 处为"2005a"年份+上标组合的边界差（热区落在年份数字,
  上标 a 在毫米外, 点击跳转全部正常 —— 跳转目标由链接名决定, 未受影响）。
  注意：relink 只可对"backfill -> table_zh 后"的成品跑一次（锚文本取自原版
  同矩形, relink 过的矩形已搬移不可作输入）。成品构建链定版：
  **渲染(pdf2zh) -> backfill(表格回填) -> relink(热区+防碰撞) -> resolve_links(GOTO 化) -> appendix_species(物种附录+学名链接, 可选) -> style_links(可见样式) -> 交付**
  （table_zh 为可选步骤, 本产品未采用）。dual 版为质检并行产物, 未做 relink（原版页在旁, 链接天然可用）。
- 收尾（新工具 tools/resolve_links.py）：NAMED 链接依赖命名目标树, **Edge 等简易
  阅读器不执行 NAMED 跳转**, 且成品保存后 41 条目标损坏(page=-1)。按"成品与原版
  页码 1:1"用**原版目标树**解析, 全部 328 条改写为显式 GOTO(目标页+坐标写死),
  0 失败。跳转分布: p28–34 参考文献 305 条, 图表交叉引用 23 条 —— 任何阅读器可点。

## v26.10 (2026-09-13) 物种中文名：对照附录 + 学名悬停注释

### 需求与红线
拉丁学名不译但读者不懂 —— 产品方案: 书末对照附录 + 正文点击查看(原地不跳转)。
红线: **查无通行中文名如实标注, 绝不编造**; 原书拼写疑误(如 Astrophitum 应为
Astrophytum)原样收录加注。

### 数据 (out/species_zh.json, 68 物种)
表 10.2 全表结构化解析 + 正文出现页统计。中文名经联网核查(iPlant/PPBC、多肉联萌
对照表、中文维基等), 分级 ●高 ◐中 ○低; 32 个有通行名(高置信 20+), 36 个暂未查到。
纠错案例: F. wislizenii 通行名是**金赤龙**(王冠龙是 F. glaucescens —— 印证
"不查证就翻译=编造"红线)。

### 工具
- tools/species_extract.py: 学名提取(先用斜体字体特征 —— 实测本书全文无斜体,
  改为表 10.2 结构化解析 + 词典匹配)。
- tools/appendix_species.py: ① 书末追加附录页(第 35 页, 不插页不动页码):
  68 条学名-中文名-置信度, 双栏排版; ② 全书 171 处学名位置挂链接。
- 验收: 35 页 / 链接 499(328 引文 GOTO + 171 学名→附录行) / 附录页渲染目检通过。
- **两轮交互形态迭代(产品决策)**: 首选 Text 注释气泡 —— 用户阅读器渲染成巨型
  图标遮挡正文; 修成 5.5pt 小 /Rect 后用户进一步指出 **"原本的超链接就能达到
  这个效果"**(其阅读器悬停/点击链接会弹目标页预览浮窗) —— 终版 = 删除全部
  注释, 学名位置改挂隐形 GOTO 链接指向附录对应行: 悬停预览中文名(原地不跳转),
  点击才翻页, 链接本身零遮挡。教训: **优先复用阅读器原生机制(链接), 自造注释
  形态的兼容性成本高**。
- **链接可见性恢复（新工具 tools/style_links.py）**: 用户对比发现译文页"少了超
  链接" —— 量化定位: 链接框 328 个全在, 丢的是**锚文本的蓝色**(原版链接框内
  蓝/总 = 4/4、5/5, 成品 = 0/3、0/6 —— 重排后字形全部变黑)。修法 = 译文页每个
  链接矩形底部画 0.8pt 蓝色下划线(329 条, 回填页自带蓝字跳过), 链接的通用视觉
  语言, 不遮挡字形。教训: **链接=矩形+可见样式, 二者缺一读者就认为没有链接**。
- **遮挡迭代**: 首版注释图标在用户阅读器里被渲染成巨型气泡遮挡正文 —— 修法 =
  注释 /Rect 钉成 5.5pt 小点(图标随矩形缩放) + 脚本可重跑(先清旧注释, 附录页
  已存在则跳过) + 附录页自身不挂注释。教训: **注释图标的呈现尺寸由阅读器决定,
  跨阅读器产品必须显式钉小 /Rect**。

## v26.11 (2026-09-16) 超链接"乱跳"根因与修复 (三层) + 锚文本染蓝

### 现象与判据
用户: "超链接是一一对应的吗? 为什么会乱跳"。先分清三件事 ——
**数量对应**(328 = 原版 328, 一一对应) ≠ **热区对应**(锚文本找得到) ≠
**落点正确**(跳过去真是那条文献)。前两项此前已验, 第三项从未验过, 正是"乱跳"
所在。新增 tools/verify_links.py 做端到端语义校验: 取锚的(作者姓+年份)到落点
y±45 里找, 并额外试探镜像位置(h-y) —— 命中镜像即坐标系方向反了。

### 根因 1: 坐标系镜像 (全部 328 条落点整体翻面)
`resolve_names()` 返回的目标 y 是 **PDF 底部原点(向上)**, 而 `insert_link` 需要
**fitz 顶部原点(向下)** —— 未转换则落点全页镜像。实锤: CR135(沃瑟) 原样 y=601
落在 p34 空白处, 镜像 y=65 正是 "Waser NM (1983) The adaptive nature of floral
traits..." 那条。修法: `fitz_y = page.height - ty`。

### 根因 2: 排版漂移 (修完镜像仍有半数落点在别条文献上)
目标坐标取自**原版同一页**, 但成品文字被重排/翻译(参考文献页尤其: 标题中文化、
行距 10pt→11.8pt、条目行数被压缩), 同一个 y 在两版指向不同段落。修法: 用原版
目标处的**行文本(作者姓+年份)**到成品目标页重新定位同名行。文献页标题已中文化,
**唯一稳定键就是"作者姓+年份"**(作者名与年份不译) —— 顺带一个坑: 原版行首的
`Guzma´n`/`Lime´nez` 类重音变体在两版写法不同, 匹配前须 fold(去重音去非字母)。

### 根因 3: 行距歧义 (±1 行不确定)
目标 y 与行顶常有 4~8pt 偏差, 而文献页行距仅 10pt —— y 常落在两行之间, 取"最近
行"会取到相邻条目。修法 = **候选投票**: 原版 y±26pt 内枚举候选条目(最多 6), 逐个
到成品页做精确匹配, 命中者中取离原坐标最近的。窗口从 26 扩到 40 反而更差(相邻
条目被误选) —— 实测保留 26/6。
兜底: 文本键全不中时用**行序比例映射**(两版条目顺序一致, 只是行数被中文压缩,
按"第几行/总行数"等比映射), 最后才回退坐标转换。

### 实测 (Cactaceae 2009, 328 链接)
| 版本 | 语义命中率 | 回退坐标 |
|---|---|---|
| 仅修镜像 | 51.8% | — |
| + 行文本重定位 | 79.2% | 27 |
| + 候选投票/行序映射 | **79.5%** | **0** |
基线: 同一套校验跑**原版自身** = 93.8%(失配 19 条为校验器假阴性: 作者名被拆行、
左邻词误取)。成品 0 条镜像命中, 328 条全部有确定落点; 剩余 62 条无法用"作者+
年份"复验, 主因是成品文献页中英残片混排导致作者名被拆(如 Calderon-Urrea 拆成
"Cal" + "onUrrea A"), 属校验器的严格性而非落点错误。
- 成品构建链(定版 6 步): 渲染 → backfill(表格回填) → relink(热区+防碰撞) →
  resolve_links(GOTO 化+落点重定位) → appendix_species(物种附录/注释) → style_links

### 链接可见性 v2: 锚文本染蓝 (替代下划线)
v26.10 的 0.8pt 蓝色下划线被用户否决("像划底线, 不美观")。改为**取原字体、在原
origin 原位叠绘蓝色字形**: 从成品页自身字体资源 `extract_font` 抽出锚文本字体
(本书为子集字体 AdvP6975), 只在链接矩形内的字符上重绘 `(0,0,0.75)` 蓝 —— 与黑字
完全重合覆盖, 视觉即原版蓝字, 零线条零遮挡。实测 251 段/1004 字, 新 span 的
origin 与原字**逐位相同**(如 73.5,151.3), 颜色 0000bf; 抽不到字体则跳过(保持黑字)。
教训: **补可见性优先"还原原文样式", 而不是叠加新视觉元素**。

---

## v26.12 (2026-09-16) 落点语义定位 v2 + 8 个成品工具参数化

### 修正 v26.11 的结论: 原版目标树本身不可信
v26.11 把剩余失配判为"校验器严格性"。实测推翻:
- 原版 p13 的 12 条 NAMED 链接里有 **4 条不同引文(2005) + 1 条(2004) 指向同一坐标**
  (p31, y=113); 而原版 p31 该处文本是 "Molina Freaner ... (2004)" 一条, 原版 p31
  通篇没有 "Casas" —— 即**原版自己就跳错**, 且错得不罕见。
- 两个假阴性来源: 成品文献页里作者名连字符被吞(Molina-Freaner → MolinaFreaner);
  校验窗口 45pt 内常找不到年份(条目跨 2~4 行)。

### resolve_links v2: 不信目标坐标, 信语义键
1. `left_author()` 取锚矩形左侧同行最近实词(引文写作 "(Casas 2005)", 锚只盖年份);
   `key_from_name()` 对自带语义的命名目标("Nobel 1988")直接采用 —— 比猜邻词可靠。
2. `find_ref_entry()` 拿(作者姓, 年份)在**成品参考文献区**(ref_start() 定位, 本书 p28)
   全书搜条目: 从作者行起**向后 5 行内**找年份(按 y 距离 14pt 会漏跨行条目), 取
   "作者-年份距离最近"者, 同距离优先原版目标页 —— **可改页**, 反过来修正原版错目标。
3. 失败才退回 v26.11 的目标页内候选投票 → 行序映射 → 坐标转换。
实测: 语义定位 105 条(其中 **16 条改页**), 目标页内 190, 行序映射 33, 回退 0。

### verify_links 归一化(消除假阴性)
- 作者/年份比较前 `fold()` 去重音去非字母, 归并两版排版差异。
- 容忍 style_links 造成的锚文本重复('19901990'): 去掉年份后若只剩年份, 仍算引文锚。
- 命中率: 56.2% → 83.8% → **92.1%**(命中 279 / 可校验 303, 镜像 0, 非年份锚 25)。

### redact 替换方案实测否决
想让染蓝从"叠绘"变"替换", 试 `add_redact_annot(text=...) + apply_redactions()`:
文本替换本身成功, 但 **apply_redactions 会清掉该页全部链接(实测 18 → 0)**; 它跑在
挂链接之后, 故不可用。保留叠绘, 并把"文本层重复一次"写入 style_links 文档与 README。

### 8 个成品工具参数化(可移植性)
- 三个脚本此前连 argparse 都没有, 且把 `D:\zotero-pdf2zh\...` 写死在源码里, 外人
  clone 后无法运行: `appendix_species`(--pdf/--data/--font, 新增 --redraw 重绘附录)、
  `species_extract`(--pdf/--out)、`table_zh`(--pdf/--font/--pages)。默认值改为
  **按脚本位置推导的仓库相对路径**, 换文献用参数覆盖。
- `backfill_pages` 的 `import json` 从函数定义后移到文件头。
- 文档与实现对齐: appendix_species 的 docstring 还写着"隐形 GOTO 链接", 实际已是
  4pt 弹窗注释; 附录导语"点按学名可跳转到本页对应条目"改为"可弹出本条目中文名"。
- README 新增"出成品: 6 步"章节(顺序约束 + 三个乱跳坑), 增量清单补齐 8 个工具。
- 幂等验证: appendix_species --redraw 复跑仍 35 页 / 171 注释; 整链重跑 328 链接不变。

## v26.13 (2026-09-16) 学名注释: 从"黄色方块"改为荧光笔 (Highlight)

### 症状
阅读器(Edge/Zotero)里学名位置出现**黄色实心方块把学名遮住**, 与 v26.10 的设计意图
("矩形钉成 4pt 小点, 无可见图标")完全相反。

### 根因
`page.add_text_annot(pt, tip, icon='Note')` 产生的是 **Text 便签注释**(/Subtype /Text)。
PDF 规范里 Text 注释的**默认外观就是黄色(/C [1 1 0])小块**。我们只把 /Rect 压到 4pt
想让它"不可见", 但阅读器按默认外观渲染 —— 尺寸再小也是**不透明黄块**, 还带描边,
正好盖在学名上。4pt 不是"看不见", 而是"看成一个 4pt 的黄点"。

### 修法: 换注释类型, 不调尺寸
改用 **Highlight 注释**(`add_highlight_annot(rect)`):
- `/C [1 1 0]` + `/CA 0.35` → 半透明黄底纹, 学名从底色下透出(荧光笔);
- `set_info(content=tip)` 保留弹出内容("中文名 / 拉丁学名 / 置信度 / 注"), 点击即弹、
  原地不跳转 —— v26.10 的功能一条不丢;
- 幂等清理同时删 `PDF_ANNOT_TEXT` 与 `PDF_ANNOT_HIGHLIGHT`, 旧方块可一键清除。

### 验证
- 重建后直接读成品: `annots {'Highlight': 171}` —— 全部 Highlight, **零残留 Text**;
  抽样 `color {'stroke': [1.0, 1.0, 0.0]}` / `opacity 0.35` / `content_len 58` ✓
- 人眼验收(Edge)通过: 淡黄荧光笔, 字清晰可读, 点击仍弹中文名。
- 附录页已存在故跳过重绘, 成品仍 35 页; 改动前成品备份 `out/成品/_bak_注释改造前.pdf`。

### 教训
"把注释矩形缩到极小让它不可见"是**错的** —— Text 便签的可见性不由 /Rect 决定, 而由
阅读器的默认外观决定。要让注释"轻", 换**注释类型**(Highlight), 而不是缩尺寸。

---

## v26.14 (2026-09-16) term_verify 检索源可插拔化 + 接入 Zotero 本地库/维基百科/维基数据

### 背景
用户问"如何让用户使用时专业词汇可以被查询"。结论: **不能靠我们预置释义**(太局限),
而要让项目里的 LLM 在**生成时**接上可插拔搜索工具现场查证。项目其实已有这一层的骨架
——`tools/term_verify.py`(存疑术语联网查证器), 但接口是**硬的**。

### 改造前的接口形态(硬的)
| 位置 | 现状 | 阻碍 |
|---|---|---|
| 常量 | `OPENALEX_API` 写死 | 源地址不可配 |
| 检索 | `openalex_search()` 是**唯一**实现 | 无抽象层 |
| 入口 | `main()` 无 `--source`, 调用点写死函数名 | 无分发 |
| 报告头 | 写死 `"- 查证数据源：OpenAlex（…）"` | 加源后报告会说谎 |

好消息: **判定层已解耦** —— `llm_judge_relevance(term, titles, context)` 只吃一个
字符串列表, 不关心标题来自哪个源, 所以加源**不用碰 LLM 判定逻辑**。

### 改造(三处 + 一个顺手的重构)
1. **抽源接口**: 每源一个函数, 统一签名
   `search(term, max_results, timeout, context, max_retries) -> 契约`,
   注册进 `SOURCES`; 契约新增可选键 `hints`(源特有补充线索)与每条的 `source` 字段。
   无被引概念的源把 `year/cited` 置 `None`, 报告自动省略该项。
2. **加 `--source`**(默认 openalex, 向后兼容): 逗号多选 / `all` 全源;
   `resolve_sources()` 校验未知源名并返回退出码 2。
3. **参数化报告头**: `build_report(..., sources, dropped)`, 数据源描述查 `SOURCE_DESC`。
4. **顺手重构**: 原先内联在 `openalex_search` 里的退避重试(429/5xx + 网络类错误,
   2s/4s/8s)抽成 `_http_json()`, 供所有源复用 —— 避免新源各抄一遍重试逻辑。
5. **新增三个检索源**:
   - `zotero`(Zotero 7 本地 API): 命中**本地 PDF 全文索引**, 返回**原文上下文摘录**,
     是唯一**真零网络**的源 —— 详见下节。
   - `wikipedia`(MediaWiki 全文检索): 条目摘要给出**定义性语境**, 与 OpenAlex 的
     "用法证据"互补; 语义召回噪音大, 用"标题含全部实词=强, 否则要求术语出现在摘要里"
     两级过滤。
   - `wikidata`(wbsearchentities → wbgetentities): 取 **zh 标签**, 这是最直接服务
     "译名裁决"的源(如 herkogamy → 雌雄异位), 以 `hints` 呈现中文标签/定义/别名。
     红线: 中文标签只是**证据不是裁决**, 质量参差(繁简混用/冷门概念无中文标签), 仍须人工确认。

### 实测发现的硬约束: 维基系在国内网络不可直连
| 主机 | DNS 解析 | TCP 443 | 结论 |
|---|---|---|---|
| `en.wikipedia.org` | **157.240.17.35**(Facebook 的 IP) | False | **DNS 污染** |
| `www.wikidata.org` | 103.102.166.224(正常) | True | TCP 通但 **TLS 握手超时 → SNI 阻断** |
| `api.openalex.org` | 104.20.26.229 | True | ✓ 正常(实测 1.1s) |
| `127.0.0.1:23119`(Zotero) | — | True(Zotero 启动后) | ✓ 正常(实测 0.2–0.4s, 不出网) |

本机虽有 Clash 配置(`127.0.0.1:7897`)但 `ProxyEnable=0`, 且实测 7897 端口无人监听
(Clash 未运行)。→ **两个新源在本机当前环境下全部不可用**, 属"零部署但需代理"。
`urllib` 自动读取 `HTTPS_PROXY`, 故启用代理后无需改代码即可用。

### 顺带修掉的缺陷(由多源改造引入)
多源模式下若某源被阻断, **每个术语**都要在它身上耗尽 `timeout×max_retries + 退避`
(约 46s)。40 个术语 × 2 个死源 ≈ 一小时的纯空转。
→ 新增 `preflight_sources()`: 开跑前用探针词 `science` 真实调一次各源(短 timeout、
不重试), 不可达的直接剔除并在报告里**如实写明**, 把"每术语一次静默超时"变成
"开跑时一句提示"。全部源都不可达则不产出报告、退出码 2(与既有设计原则一致)。

### 新增 `zotero` 本地库源(唯一真零网络)
- 端点 `http://127.0.0.1:23119/api/users/0/items`; 前置: Zotero 7 运行中, 且
  `设置→高级` 勾选了「允许本机其他应用与 Zotero 通信」(已确认本机 prefs 里已开)。
- 关键在 `qmode=everything`: 命中的是**本地 PDF 全文索引**, 不只是标题/作者 ——
  所以能给出"术语在原文里怎么用"这种最强证据, 而 OpenAlex 只能给标题级。
- 全文命中返回的是 `attachment`/`note` 条目(其 title 是**文件名**不是文献名), 须按
  `parentItem` 回查父条目换成真实元数据; 再对附件调 `/items/<key>/fulltext` 取全文
  (实测一本 52KB), 用 `_context_snippet()` 截术语前后各 150 字作摘录。
- 同一篇文献在库里常存有多个副本/附件 → 按标题归一化去重, 且**带摘录的优先保留**。
- 错误文案分流: WinError 10061 映射成"Zotero 未运行, 或未开启「允许本机其他应用与
  Zotero 通信」", 其余加 "Zotero 本地 API: " 前缀 —— 让用户照着做而不是看堆栈。

实测:

| 项 | 结果 |
|---|---|
| 延迟 | 0.2–0.4s/术语(本机回环, 不出网) |
| herkogamy | 命中本地正在翻译的专著《Reproductive Biology of Cactaceae》+ Desert Plants 2009 |
| 去重 | 修复前 3 条全是同一本书(多副本+多父条目), 修复后 2 条 |
| 端到端 | `--source zotero,openalex` 前 3 个术语 3/3 成功、5.9s, 报告正确显示 `[zotero]`/`[openalex]` 标签与摘录 |

### 验证
- `py_compile` 通过; `--help` 正确列出 `--source`。
- 真实存疑清单(`存疑清单_20260901_215301_15016.md`, 40 个术语)端到端:
  `--source all --limit 4` → 预检剔除 wikipedia/wikidata, 实际用 openalex,
  4/4 成功、7.0s, 报告头如实标注"已剔除不可达源"。
- 向后兼容: 不带 `--source` 时报告头与条数格式与改造前一致(多源才加 `[源名]` 前缀)。
- 边界: 非法源名 → `[ERROR] 未知检索源 bogus（可选：…）` 退出码 2;
  仅 `--source wikidata`(全灭) → 提示需设 `HTTPS_PROXY`, 退出码 2。
- 离线单测(真源不可达, 用假源验分支): 跨源去重(4 条合并为 3)、
  单源失败仍 `ok=True` 且 error 汇总、全源失败 `ok=False`、预检剔除死源、
  报告多源渲染(源名标签 / 无被引省略 / 摘要 / 补充线索) —— 全部断言通过。
- `zotero` 源直测: herkogamy / breeding system / dichogamy 全部 `ok=True`, 0.2–0.4s,
  均截出原文上下文。
- `zotero` 辅助函数单测: `_zotero_get` 的 10061 映射与一般错误前缀、`_context_snippet()`
  截取长度与"找不到术语返回空串"、报告头括号配平(`（`/`）` 各 2) —— 全部断言通过
  (`ALL_OK`)。

### 未做(明确挂账)
- **SearXNG 容器源**: 用户决定暂不接入(省内存)。
- **术语在线(termonline.cn, 全国科技名词委)**: 本想作为国内可直达的权威中文术语源,
  实测该站是纯前端 SPA(HTML 仅 3.2KB, 无服务端渲染), 未找到公开 JSON 接口,
  需逆向其内部 POST 接口才可用 —— 未做, 待定。
- **维基两源未对真实 API 响应验证**: 本机不可达(`wikipedia` DNS 污染 / `wikidata`
  SNI 阻断), 解析代码按 MediaWiki 官方 API 文档编写, 并已用**同构假数据离线**验证
  合并/渲染分支; docstring 顶部与 `SOURCE_DESC` 均已标注 ⚠️ 未验证 —— 待代理可用时复核。

---

## v26.15 (2026-09-16) term_verify 首次全量实战 + 按术语形态修四处

### 背景
v26.14 把检索源做成了可插拔, 但四源**只用前 3 个术语试过**。这次拿最富的一份真实
存疑清单(`存疑清单_20260901_215301_15016.md`, TO-SoFiT 软体机器鱼论文, 40 术语)
跑完整流程, 用实测数据决定还该修什么。

### 首轮结果(改造前)
`--source zotero,openalex` → 40/40 成功, 146.8s, 报告
`术语查证报告_20260916_142003.md`。数据: zotero 条目 128 / openalex 125 /
带原文摘录的强证据 38 条 / 29 个术语拿到了**本论文原文句子**。

**验证了这层是有用的**: 29/40 术语直接从本地 PDF 全文索引里取回本论文原文句子
(如 `eroded design` 给出 "the strain constraint to the eroded design [9, 13]") ——
回答"这词在**本文**里指什么", 这是 OpenAlex 给不了的; 而 OpenAlex 的价值集中在
被引数(如 `topology optimization` → 被引 5759 的权威专著), 正好对上存疑清单里
"是否领域通用译法"的疑问。

**同时也暴露四处**(按实测证据修):

### 修复(P1~P4)
1. **中文术语不再走 zotero 源**(`SKIP_CJK_REASON`)。实测 Zotero 全文索引对汉字是
   **字符级松散匹配**: 单独查 `拓扑优化` 返回 10 条, 9 条完全无关(仙人掌科、卡尔曼
   滤波、Attention Is All You Need、降雨建模…); 而这 10 条要逐个回查父条目 + 取全文
   才判得出来, 纯空转。更关键的是中文术语在本地库里唯一的强命中来自**我们自己产出的
   译文 PDF** —— 循环证据。实测 7 个中文术语(占"zotero 全弱"10 个中的 7 个)全无有效产出。
   跳过了什么**写进报告头**, 不计作失败。
2. **存疑清单里的模板占位串直接丢弃**(`JUNK_TERM_RE`)。实测 40 个"术语"里有 2 个是
   「中文译名（flow contrast）」「中文译名（英文原文）」—— 来自"首次出现应处理为
   中文译名（英文原文）"这条规则的措辞被当成了术语名。`parse_doubt_md(path, stats)`
   经 `stats["junk"]` 回传丢弃数, 报告如实写明。
3. **短缩写加提示**(`_is_short_abbrev`)。实测 `CM` 召回 ICD-9-CM 疾病编码(被引 10840)、
   `cm⁻¹` 波数这类完全无关的高被引条目, `TO` 也几乎全噪音 —— 这类术语不是查不到,
   而是**必须换全称或加领域限定词**, 故在术语块顶部直接给出提示。
4. **译文命中标记为循环证据**。库里同时存有 pdf2zh 产出的中文译文 PDF, 附件全文就是
   译文本身; 用英文术语仍能命中(术语往往保留未译), 截出的上下文却是中文句子。
   `zotero_search` 用 `CJK_RE` 判断摘录语言, 打 `translated` 标记, 报告明示
   "属循环证据（拿旧译法验证旧译法），不可作为译名依据"。

### 顺带修掉一个真 bug(重名覆盖)
`main()` 里预检剔除的源清单叫 `dropped`, 而 LLM 过滤分支内部**也叫 `dropped`** ——
`--llm-filter` 启用时会把报告头那行"已剔除不可达源"污染成 LLM 剔除的条目。
内层改名 `llm_dropped`。

### 复跑对比(同一份清单)
| 指标 | 改造前 | 改造后 |
|---|---|---|
| 术语数 | 40 | **38**(丢 2 假术语) |
| zotero 条目 | 128 | **63**(−51%, 去掉的全是中文假召回) |
| 带摘录的强证据 | 38 | 32 |
| zotero 全弱术语 | 10 | **2**(仅剩 `CM`/`compliance mechanisms` 这类英文短词) |
| 循环证据标记 | 0(未识别) | 1(已标出) |
| 耗时 | 146.8s | **126.0s** |

掉的 6 条强证据全部来自中文术语命中**译文**的循环证据, 属该走的。

### 验证
- `py_compile` 通过; 23 项离线单测全过(`ALL_OK`): 假术语 40→38 且 junk=2、
  不带 stats 向后兼容、短缩写判定 5 例(含 `TO-SoFiT`/`PneuNet` 不误判)、
  中文跳过 zotero 且英文不跳过、全被跳过时 `ok=False` 且**未发起任何调用**、
  循环证据只标译文条不误标原文条、短缩写提示只给缩写、
  跳过说明/junk 行/头行跳过计数进报告、**无跳过时头行与改造前逐字一致**(向后兼容)。
- 端到端复跑与上表一致。

### 已知取舍
- 中文术语跳过 zotero 的代价: 库里若真收藏了**中文文献**, 这类术语就查不到了
  —— 报告头已写明跳过了什么, 需要时改走其它源或人工在 Zotero 里检索。
- 循环证据只判"摘录含汉字"; 若译文 PDF 的该段落恰为纯英文(术语整段未译)则会漏判。

---

## v26.16 (2026-09-16) term_verify 即席查证入口（--term）

### 背景：原计划的"接进管线"被实测否掉
用户诉求是"给项目里的 LLM 接一个类似 tavily 的搜索工具"。计划把 term_verify
挂在 `server.py` 的自动质检钩子旁（翻译完自动查存疑清单）。**动手前先量了产出**，
29 份存疑清单按日统计：

| 日期 | 份数 | 术语合计 | 单份最多 |
|---|---|---|---|
| 08-30 | 1 | 14 | 14 |
| 09-01 | 1 | 38 | 38 |
| 09-02 | 1 | 0 | 0 |
| **09-06** | 8 | **59** | 23 |
| 09-07 ~ 09-12 | 18 | **5** | 5 |

逐段核对过 09-12 的清单: 57 个"存疑点"**全部写着"无存疑"** —— 不是解析器跟不上
格式, 是内容上真的没有术语级存疑了(术语表成熟 + v21 初译注入 + v23 缓存指纹 +
v24a 文档摘要 + 军师层 → 一致性上去了)。

**结论: "翻译后自动查证"收益趋零, 且会把每次白跑的返回码 1 记成 QC FAIL(负收益)。
真正的用法是"人卡在某个词上时立刻取证据"** —— 故改为加即席入口。

### 改动
1. **新增 `--term`**（`action="append"`, 可重复给多个）: 术语直接来自命令行,
   不必先有存疑清单。输入去重、保持顺序, 空串忽略。
2. **`--term` 与清单路径互斥**: 同时给则报错退出 2（避免"到底查哪份"的歧义）。
3. **即席模式缺省源为 `zotero,openalex`**（都零部署、零代理, 且本地库命中率最高）;
   清单模式缺省仍为 `openalex`, **与改造前逐字一致**（向后兼容）。
   `--source` / `TERM_VERIFY_SOURCE` 优先级不变, 显式指定总是覆盖缺省。
4. **报告头分流**: 有清单文件时仍写"- 源清单：`xxx.md`"; 即席模式写"- 术语来源：即席查证（--term）"。
5. **即席模式报告同时打到屏幕**——这是"现在就想要答案"的用法, 不必再去开文件;
   文件仍照常落盘（目录不存在时退回当前目录, 不为了写报告去建目录）。

### 验证(实测)
- `py_compile` 通过; 互斥检查报错退出 2。
- 即席实跑 `--term herkogamy --term herkogamy --term "breeding system"`:
  去重成 **2 个术语**、源为 `zotero + openalex`、耗时 6.0s、
  报告头写"术语来源：即席查证（--term）"、**报告已回显屏幕**、文件落
  `server/translated/review/术语查证报告_20260916_143629.md`。
  zotero 源直接给出本地 PDF 原文, 如 breeding system 的
  "Studies on breeding systems cover functional and morphological floral traits..."。
- 向后兼容实跑 `存疑清单_20260912_113157_44848.md --limit 1`:
  `[INFO] 检索源: openalex`（缺省未变）、头行仍是"- 源清单：`xxx.md`"。

### 未做(明确挂账)
- **自动挂钩子未做**: 不是做不了, 是实测收益趋零。若将来存疑术语量回升
  （如换新领域论文、术语表覆盖不足时）, 再挂 `server.py::_run_auto_qc_locked`
  也不迟; 届时须让"零术语"静默跳过且**不记 FAIL**。
- **MCP 工具化未做**: 8788 的 `local_mcp_server` 源码不在本仓库, 属跨项目改动。

### 附: README 补记
此前 README **一字未提 term_verify**（新用户发现不了）。本次补: 增量清单加一行、
架构图后加"旁路"指引、新增「术语查证：先拿证据再裁决」整节（两种用法 / 数据源前置条件表 /
读报告三注意）、已知限制加一条（维基两源未验证 + 中文术语跳过 zotero）。
实测过 README 里写的命令: `--help` 渲染正常; 无参数确实取到最新清单
（0 术语 → WARN + 退出码 1, **不发任何网络请求**）。

---

## v26.17 (2026-09-16) 把查证能力交给豆包: MCP 桥新增 search_term 工具

### 背景: 原始诉求的真正落点
用户最初的诉求是"`zoteropdf2zh` 里有 LLM, 只是缺个类似 tavily 的搜索工具"。
本轮查证发现: **那个 LLM 就是豆包, 而豆包连进来的入口 `tools/doubao_bridge.py`
本来就在本仓库里**(零依赖手写 STDIO MCP 桥, 原暴露 list_inbox / get_payload /
submit_result 三个工具)。所以"接上搜索工具"不需要新起服务, 也不用碰 8788,
**往这个桥里加一个工具即可** —— 上一版记的"8788 源码不在本仓库"那条挂账因此绕开。

### 关键前提(先核实再动手)
`term_verify` 能不能被 STDIO MCP 复用, 取决于它**会不会往 stdout 写东西**
——stdio 桥的 stdout 是 JSON-RPC 通道, 混一行 `[INFO]` 就废了。核实结论:
**23 处 `print()` 全部在 `main()` 内**(行 1123~1298), 模块级与各搜索函数零输出。
故复用其函数(`resolve_sources` / `preflight_sources` / `search_multi` /
`build_report`)是安全的, 且**连判定与渲染逻辑都不用重写**。

### 改动(全在 `tools/doubao_bridge.py`)
1. 新增 `tool_search_term(args)`: 参数 `term`(必填) / `sources`(默认 `zotero,openalex`) /
   `max_results`(默认 5, 上限 10)。返回**证据文本**(复用 `build_report`, 头两行写
   "术语来源：MCP 工具 search_term"), **不落文件**——豆包每问一次就往 `review/` 丢一份报告是污染。
2. **延迟导入** `term_verify`: 桥启动开销保持为零, 且该模块若缺失只影响这一个工具。
3. 注册进 `TOOLS`(含给模型看的中文 description)与 `_DISPATCH`; 模块 docstring 三工具→四工具。
4. **`max_retries=1`(交互式快速失败)** —— 见下。

### 顺带发现: OpenAlex 限流会把交互式工具卡 16.7s
冒烟测试时中文术语那次耗时 **16.7s 且 0 命中**。单测直连定位到根因:
**`HTTP 429: Too Many Requests`**(当天手测打得太密, IP 级短时饱和), 而 4 次重试 +
2s/4s/8s 退避正好 ≈16.6s。**连英文词也一样**(herkogamy 同样 16.6s/429)。
- 额外实测: 给 `OPENALEX_EMAIL` 进"礼貌池"后**仍是 429**(UA 已带 mailto 确认生效)
  → 说明这次是短时饱和而非缺 mailto。
- 处置: 仅对 MCP 这条交互路径设 `max_retries=1`(快速失败), 批量模式保持 4 次不变。
  效果: herkogamy 从"16.7s 后失败"变为 **0.5s 拿到 zotero 的 2 条原文命中**,
  且 OpenAlex 会在预检阶段就被剔除、报告头如实写"已剔除不可达源"。

### 验证(真实 JSON-RPC over stdio, 7 项)
用一个临时脚本拉子进程真跑(测完已删), 逐项断言:
- A `initialize` → serverInfo `pdf2zh-bridge` / 协议 2024-11-05 ✓
- B `tools/list` → 4 个工具, 含 `search_term` ✓
- C `search_term {term:herkogamy, max_results:3}` → `isError=false`, 1.4s, 命中 5 条,
  报告头"术语来源：MCP 工具 search_term" ✓
- D 非法源 `nosuchsrc` → `isError=true`, 提示"未知检索源…(可选: zotero, openalex, wikipedia, wikidata)" ✓
- E 空 `term` → `isError=true`, "term 为空" ✓
- F 中文术语 → 正常返回, 不炸(zotero 按设计跳过, 报告头写明) ✓
- G 旧工具 `list_inbox` 仍可用(13 个文件) ✓
- **隐含断言(最关键)**: 测试用 `json.loads` 读每一行 stdout —— 任何一行非 JSON 立刻抛异常,
  即"stdout 洁净"是被真实校验过的, 日志确认只出现在 stderr。
- 二轮复跑(改 max_retries 后): herkogamy 0.5s / 中文术语 0.0s, 全部断言仍过。

### 附: README
- 可选自动化通道段: 工具清单补 `search_term`, 并点明"让豆包定稿时自己查证据"
- 增量清单 `doubao_bridge.py` 行: 补"三件套 + `search_term` 术语证据检索"
- 「术语查证」节新增子节「让豆包自己查（MCP 工具 `search_term`）」: 注册前提、
  参数、**建议用英文原词查**、限流/不可达的处置、只出证据

### 未做(明确挂账)
- **`OPENALEX_EMAIL` 未配置**: 配了能进礼貌池(本次实测未能立刻解 429, 但属正确做法),
  批量模式下 429 仍会按 4 次重试硬扛(单源最坏 ≈16.6s/术语) —— 需要时再给
  `_http_json` 加"429 直接放弃并记入 error"的分支。
- ~~豆包侧未实测~~ → 见下节, 同日补测完毕。

### 附: 豆包首次实战 —— "描述即接口"(同日补记)

**首战没调。** 用户问"查一下 herkogamy 这个术语", 豆包用**自身知识 + 自带联网搜索**答了
(引用 CSIRO / PMC / Herbarium), 全文没有"术语来源：MCP 工具 search_term"那行。

排查顺序(先排除连接问题, 再怀疑措辞):
1. 用户最初反馈"有连接器但看不到工具" → 先疑"没连上/列表是旧的";
2. 用户贴回连接器里**四条工具名与描述** → 证明**桥是好的、豆包看得见**;
3. 结论: 问题不是"调不到", 而是**它不觉得需要调**。

**根因**: 原描述只说"定稿时拿不准某个术语该怎么译就调它", 没讲清**这件事豆包自带的能力干不了**。
对"herkogamy 是什么意思"这类问题, 它自身知识 + 联网搜索已经够用, 自研工具在它眼里
只是一个**可替代的竞争项**, 于是被跳过。

**处置(只改措辞, 逻辑一行未动)**:
1. `search_term` 的 description **61 字 → 312 字**, 补三个要素:
   - 讲清"你干不了": "这不是通用搜索——**你自带的联网搜索拿不到用户收藏的 PDF 原文,
     所以别用自己的知识或联网搜索代替它**";
   - 讲清做什么: ① 用户库 PDF 全文里摘原文上下文 ② OpenAlex 标题与被引数;
   - 给触发词: "**查证 / 找证据 / 看看原文里是怎么用的 / 这词在文献里怎么用的**"。
2. 给 `get_payload` 加**串联钩子**: "若某段里有拿不准的术语, 先用 `search_term`
   取该术语在用户库里的原文证据再定译法" —— 让豆包在处理译文时自然想到它。

**二轮实测(用户重启豆包后): 调用了。** 依据:
- 返回**两条本地库原文证据**, 都出自《Reproductive Biology of Cactaceae》
  (书里 "H Herkogamy, D Dichogamy" 缩写表; Desert Plants 2009 的
  "Herkogamy and dichogamy seem to be common");
- 并主动说明"**OpenAlex 源因 429 限流未返回, 不影响本地证据**" ——
  预检那行如实传达给了模型, 降级路径在对话里也是可读的。

**改后回归**(临时脚本, 测完已删): 4 工具齐全; description 四个关键词命中;
`get_payload` 含 cross-hint; `search_term` 仍 0.6s 命中本地库 2 条, `isError=false`。

**教训**: 把自研能力交给 LLM 时, **description 就是接口**。"能跑"(协议层)与"会被调"
(决策层)是两件事 —— 后者要求描述里明确写出"**幻觉替代品干不了什么**"。
仅说"什么时候可以用"不够, 必须说清"**你不该用自带能力代替它**"。
这条已同步进 `PIPELINE_STORY.md` 教训第 6 条。

**附: README 补充说明** —— 「让豆包自己查」节新增一小节
「和豆包自带联网搜索的区别（以及什么时候该调它）」: 五维对比表
（语料来源 / 能否查正在译的这本 / 给什么 / 可复现 / 联网隐私）+ 判断标准
"**不是这词难不难, 而是答案是否必须来自你自己的语料**" + 明确二者互补不互替
（豆包强在覆盖面与新近性），并点明工具描述里那句"你自带的联网搜索拿不到用户收藏的 PDF 原文"
正是为此而写。

## v26.18 (2026-09-16) 429 命中 Retry-After: 不再空撞退避阶梯

### 起因: 清挂账时反而测出真缺陷
v26.17 挂着的"`OPENALEX_EMAIL` 未配置"本轮清掉: 机器上可见的唯一身份是提交里那个
`ice917@users.noreply.github.com`(不编造邮箱), 已 `setx` 写入用户级环境变量并核实。
但配完**仍是 429**, 于是做了三分身探针:

| 探针 | 请求身份 | 结果 |
|---|---|---|
| A | `mailto` 查询参数 + 邮箱 UA（本项目做法） | `HTTP 429` |
| B | `mailto` 查询参数 + 浏览器 UA | `HTTP 429` |
| C | 裸请求（无 mailto、无邮箱 UA） | `HTTP 429` |

三者还都带着同一组头部: **`Retry-After: 56941`（≈15.8 小时）、`X-RateLimit-Remaining: 0`**。
结论: 这是**纯 IP 级配额耗尽**, 与身份无关 —— 配邮箱进"礼貌池"解决不了它,
当天只能等重置。（此前 09-16 手测过密, 40 术语的批量复跑 + 多次单测把配额打光了。）

### 真缺陷: 服务器说等 15.8 小时, 我们在 14 秒内连撞 4 次
`_http_json` 原逻辑对 429 一律走 2s/4s/8s 退避, 这在"短时限流"下正确, 但对
**带 `Retry-After` 的配额型 429** 纯属空撞 —— 单次最坏白等 ≈14s, 批量场景下
每个术语都要付一遍。

### 改动(全在 `tools/term_verify.py`)
1. 新增常量 `RETRY_AFTER_GIVEUP = 60`: 阈值取得比整条退避阶梯(合计 14s)宽,
   **短时限流仍按原样重试**, 只有"服务器要求的等待远超阶梯"才放弃;
2. 新增 `_retry_after_seconds(e)`: 解析 `Retry-After` 的两种合法形式
   —— 秒数(`56941`)与 HTTP-date; 无该头/解析不出返回 `None`(退回原重试策略);
3. 新增 `_human_wait(sec)`: 把秒数说成人话(`约 15.8 小时` / `约 5 分钟` / `30 秒`);
4. `_http_json` 的 429 分支: 先读 `Retry-After`, 超过阈值则**直接返回**并给出
   `HTTP 429: Too Many Requests（服务器要求 约 15.8 小时 后再试，配额已耗尽，已跳过重试）`
   —— 让调用方、预检警告与报告都能如实说明"要等多久", 而不是一句干巴巴的 429。

### 验证
单测(临时脚本, 测完已删)六组全过:
- A `_retry_after_seconds`: 秒数 `56941` ✓ / 无头 `None` ✓ / 乱码 `None` ✓ /
  过去日期 → `0`(钳位) ✓ / 未来日期 → 2303696816 ✓
- B `_human_wait`: 56941→`约 15.8 小时` ✓ / 300→`约 5 分钟` ✓ / 30→`30 秒` ✓
- C 429 + `Retry-After: 56941` → **只调用 1 次**, 错误含"15.8 小时"与"已跳过重试" ✓
- D 429 **无** `Retry-After` → 仍重试满 4 次(未误伤短时限流) ✓
- E 429 + `Retry-After: 5`(低于阈值) → 仍重试满 4 次 ✓
- F 503 → 仍重试 3 次(`max_retries` 生效, 5xx 逻辑未受影响) ✓
  (重试计数用 monkeypatch 的 `urlopen` 调用次数判定, `time.sleep` 被替换成空操作)

线上实测(真实 429 环境):
- 只用 `--source openalex` → **0.9s** 退出, 不再 16.6s;
- `--source zotero,openalex`(豆包那条路的真实形态) → 预检 WARN 直接给出
  `服务器要求 约 15.8 小时 后再试`, openalex 被剔除, **zotero 照常 0.6s 成功 1/1**。

### 挂账变化
- ~~`OPENALEX_EMAIL` 未配置~~ → 已 `setx`(用户级), 但已证明它救不了配额耗尽;
- ~~批量模式 429 按 4 次重试硬扛~~ → **已缓解**: 带 `Retry-After` 的配额型 429 不再重试,
  仅"无 `Retry-After` 的短时限流"才走退避(这正是期望行为);
- 新增: ~~**OpenAlex 配额当天已耗尽, 本日不宜再依赖该源**~~ → **09-18 复测已恢复**(见下节)。

### 附: README / PIPELINE_STORY
- README「已知限制」加一条: OpenAlex 按 IP 限流, 配额耗尽会被自动剔除且报告头写明等待时间,
  配 `OPENALEX_EMAIL` 只影响礼貌池、解决不了配额耗尽;
- PIPELINE_STORY 实战教训加第 7 条(六条 → 七条): "限流要听服务器的, 别自己拍退避阶梯",
  并记下"限流可能与你无关"这个易误判点。

### 补充(09-18): 配额恢复后复测 —— 补跑配额耗尽时未能真跑的路径
OpenAlex 配额重置后(服务器当时明说要等 `Retry-After: 56941` ≈ 15.8 小时)复测两项:

1. **单源恢复确认**: `--term herkogamy --source openalex` → **1/1 成功、0.7s**,
   首条正是该术语的源头文献 Webb & Lloyd(1986)《The avoidance of interference between the
   presentation of pollen and stigmas in angiosperms II. Herkogamy》(被引 902)
   —— 与 v26.17 写进 README 对比表的"被引数是结构化字段、可作证据强度"对上了;
2. **全源模式(4 源)**: `--source all` 这条路径此前因配额耗尽只跑过两源组合, 这次真跑了全源。
   结果: zotero(Zotero 未运行) / wikipedia(连接超时) / wikidata(TLS 握手超时)
   **三者被预检剔除且理由各异**, openalex 2/2 成功、4.0s;
   报告头把三条剔除理由**逐条写明**并注明"本报告不含其证据", 正文只列 openalex 证据
   —— 即"多源 + 预检剔除"在真实混合故障下表现正确(此前只验证过单源不可达)。

至此四源可用性定格: **zotero / openalex 可用, wikipedia / wikidata 待代理**(见 v26.14 挂账)。

3. **豆包路径双源复验**(Zotero 与豆包均开启后): 直连 stdio MCP 桥发
   `tools/call search_term{term:"herkogamy"}`(即豆包实际走的那条链路),
   返回 **7 条证据 = 2 条 `[zotero]` + 5 条 `[openalex]`, 1.4s**:
   - `[zotero]` 给出本地库 PDF 原文摘录(书里那张 `H Herkogamy, D Dichogamy` 缩写表,
     以及 Desert Plants 2009 那句 herkogamy 与 dichogamy 并列的论述);
   - `[openalex]` 给出 5 条带被引数的标题(Webb & Lloyd 1986 被引 902 居首)。
   - **这是 v26.17 豆包首测时做不到的形态**: 当时 OpenAlex 429, 豆包只拿到本地库单源证据,
     并主动说明"OpenAlex 因限流未返回"; 现在双源齐备, 报告头两源并列、无剔除行。
   - 桥的 stderr 日志(3 行)确认**未混入 stdout** —— 读取端逐行 `json.loads` 全部成功。
   - 附带发现(非工具问题): 库里那篇的条目标题字面是
     `Sci-Hub. Reproductive Biology of Cactaceae / Desert Plants, 2009`,
     因标题与另一条不同, 去重按设计保留为两条(不同的父条目)。

4. **豆包侧实测闭环**(用户在其对话里真跑, 关闭 v26.17 挂账"豆包侧实际调用时机未实测"):
   用户先令其 `tool_search` 找到 `search_term` 再调用, 返回内容与我们桥测一致
   —— 2 条 `[zotero]` 原文摘录 + 5 条 `[openalex]` 带被引数标题, 并自行归纳出
   "花药与柱头在空间上分离、以避免自交/减少花粉-柱头干扰"的语境结论;
   **且未擅自改译文、未擅自写术语表, 只问"需要我把这条连同出处写进术语表吗"**
   —— 即 v26.17 定下的"只出证据"边界在模型侧真的生效(描述里那句"不改译文、不写术语表"起了作用)。
   - 回应它那个提问时的核实: `terms.csv` 与 `terms.babeldoc.csv` **第 10 行均已有**
     `herkogamy,雌雄异位`, 与证据一致, **无需新增**;
   - 但查出**一处真缺口**: **`dichogamy` 两个术语表都没有**, 而译文里"雌雄异熟"
     已出现 **187 次**(分布在 16 个存疑清单/审校报告) —— 与 herkogamy 成对出现的
     伴侣词(书里那张表就是 `H Herkogamy, D Dichogamy`)反而没被钉住,
     未来重译存在漂移风险(雌雄蕊异熟 / 异熟 等写法)。
     按项目约定"术语表属人工裁决", 已提请用户决定, **未擅自写入**。
     **用户决定: 暂不加, 攒着** —— 避免为一个词单独付一次全量重译的代价,
     留到下次本来就要重译时(如换 BabelDOC 引擎、或补别的术语)一并补入两表。
     → **新增挂账: `dichogamy,雌雄异熟` 待与下一次重译合并补入 `terms.csv` +
     `terms.babeldoc.csv`**; 期间若发生重译, 注意该词可能漂移成"雌雄蕊异熟/异熟"等写法。

## v26.19 (2026-09-18) Type3 字体崩解析: 自研字体校正补丁踩空 `basefont`

### 现象 (首篇计算机视觉/机器人学论文实战暴露)
新论文 `Kim 等 - 2026 - EgoPhys Learning Generalizable Physics Models of Deformable Objects
from Egocentric Video` 首译时, **进度 8/19 处 pdf2zh 子进程退出码 1**, 整篇失败(无任何产物)。
真实异常被 `subprocess.CalledProcessError` 包了三层, 剥开后是关键那行:
```
File "...\pdfminer\pdffont.py", line 1027, in __init__
    _apply_font_char_fixes(self.basefont, self.cid2unicode)
AttributeError: 'PDFType3Font' object has no attribute 'basefont'
```

### 根因 (自研补丁的缺陷, 不是 pdfminer 原生 bug)
崩点那行**是我们自己的补丁**(`_apply_font_char_fixes` 读 `server/config/font_char_fixes.json`,
用于修老式期刊 PDF 的自定义编码错解), 它在 `PDFSimpleFont.__init__` 里被调用。
而 pdfminer 的 `PDFFont.__init__` **只设 `fontname`, 从不设 `basefont`** ——
`self.basefont` 是 `PDFType1Font`(1044 行)与 `PDFCIDFont`(1112 行)**各自**赋的值。
`PDFType3Font` 继承 `PDFSimpleFont` 但从不赋 `basefont`, 于是走到补丁那行就 AttributeError。

两处加重这个缺陷的点:
1. 函数体里明明有 `if pat in (basefont or "")` 和整段 `try/except`, 看着很稳 ——
   但 `self.basefont` 是**调用点求值**的实参, 在函数体执行前就抛了, 守卫根本够不着;
2. Type3 是**嵌入字形过程字体**(图形/矢量插图里常见), 前 ~50 篇论文没遇到过一个,
   所以这条路径长期没被踩到 —— 属于"补丁打给人看的那部分很稳, 没考虑到的路径直接炸"。

### 复现 (先复现再改)
stock pdfminer 路径**不复现**(`python -m pdfminer.high_level <pdf>` 正常退出 0;
未深究差异, 推测与解释器遍历 XObject 资源的方式有关)。用 pdf2zh 自带解释器复现成功:
`PDFPageInterpreterEx` + 普通 device, 逐页 `process_page`(需按 pdf2zh 的做法
给 page 挂一个 `page_xref` 桩), 结果 **page 0~6 全过、第 8 页(0 基 7)崩在同一行** ——
与服务器 `8/19` 的进度完全对齐。

### 修法 (一行)
```python
_apply_font_char_fixes(getattr(self, "basefont", ""), self.cid2unicode)
```
取不到就给空串: 规则按字体名**子串**匹配, 空串不匹配任何规则 ——
对 Type3 等价于"未打补丁", 对有名字的字体行为一丝不变。
未改函数签名、未给 `basefont` 造假名, 也不去碰 Type1/CID 那两处(它们的 `basefont` 必然存在)。

### 验证
- **复现转绿**: 同一脚本同一 PDF → `PAGES PARSED = 19`(19 页全通);
- **镜像**: `tools/sync_patches.ps1` 8 PASS / 0 FAIL(`patches/pdfminer_pdffont.py` E71D8D56);
- **回归**: `tools/tests/run_all.py` **91 项全 PASS / 0 FAIL**
  (镜像 8 + 缓存 10 + 集成 5 + 前瞻 10 + 文档摘要 9 + 军师 49)。

### 影响面与后续
- 这是**解析层**修复, 不动任何缓存键: 失败那轮已翻的 8 页仍在缓存里, 重新提交直接命中,
  只补剩下 11 页 —— 不必重付已翻部分的代价;
- 崩在解析层意味着**产物一个都没写**, 重投不会撞"同名已完成任务"去重(去重只认进行中的任务);
- 新论文(BabelDOC 路线)尚未迁, 该补丁在 BabelDOC 侧另有一份 pdfminer, 迁移时需同步带上此修。

---

## v26.20 (2026-09-18) 通法: 「四道闸门」—— 把未知崩溃降级为已知/可复现/可读

### 缘起 (从"修一个 Type3"升维到"覆盖同类未知道路")
v26.19 修好 Type3 后, 用户的追问是: **"如果后面有与 Type3 一样的问题呢?"**
点破了要害 —— 本机 pdfminer 是 20250416, 上游已到 20260107, **落后约 9 个月**,
同类坑(某字体子类走非预期路径)永远修不完。所以目标不是再修第 N 个字体, 而是:
**让"未知崩溃"在它发生时自动变成"有类别、有位置、可复现、可读"的已知情况**。
据此确立四道闸门, 分工是"便宜的在前、昂贵的在后、崩了不静默"。

### 闸门1 (便宜筛子): `tools/pre_check.py` 扩出"字体/结构画像"
- 用 PyMuPDF 逐页读**风险特征**, 不解释内容流, 秒级出结果:
  - **Type3 字体**(`get_fonts(full=True)` 里 ftype 含 Type3) → 直接判 🔴高
    —— v26.19 实测: 单个 Type3 就能让字体构造抛错、整篇退出码 1 且产物一个不写;
  - **复合字体缺 `/ToUnicode`**(`doc.xref_get_key(xref, "ToUnicode")` 返回 kind=='null')
    → 🟡中, 解析不崩但只能得到不可搜索的伪字符;
  - **生产者指纹** `PRODUCER_RISK`: `dvips`(Type3 位图字体高发源, /Encoding 语义无意义)、
    `iText`(Type3 常被用来画符号/防文本提取) → 附加风险等级。
- 报告新增「字体/结构画像」表; 新增 `build_batch_report()` 出批量总表+等级分布+逐篇全文;
  新增 `--dir/--limit` 与 `check_one()/worst_level()`。
- **口径写死在报告里**: MuPDF 能读出结构 **≠** pdfminer 读得下来。筛子只报线索,
  标红的一律附一句"提交前必须跑 `tools/parse_smoke.py`"。

### 闸门2 (真判据): `tools/parse_smoke.py` 走产线同一条解析路径
- `PDFPageInterpreterEx` + pdfminer 字体构造逐页 `process_page`, **零 API 成本**;
  某页抛错后**重建解释器继续跑**, 目的是把雷列全, 而不是复刻产线"首崩即整篇退出"。
- 与闸门1 的分工是硬边界: 闸门1 快而粗(另一个解析器), 闸门2 慢而准(同一条代码路径)。
- 本版新增 **降级归页**: 按"处理第 N 页前后新增了几条字体降级台账"把降级点定位到具体页,
  报告加"字体降级"列 + 判读列, 合计行 `❌崩 N · ⚠降级 N · ✅干净 N`;
  新增 `--list <清单.txt>`(支持 `<TAB>#` 注释)批量入口。

### 闸门3 (兜底降级+留痕): `pdfminer/pdfinterp.py` 的 `get_font` 加 try/except
- 原 `get_font` 没有任何兜底, 字体构造函数一抛就**整篇退出**。改为:
  构造失败 → 记台账 → 换成 `PDFNullFont` → 继续。`_create_font()` 装原逻辑, 行为不变。
- `PDFNullFont`(新增于 `pdffont.py`)的两条设计:
  - `to_unichr` **刻意抛既有的 `PDFUnicodeNotDefined`** —— 这是"缺 ToUnicode 字体"
    天天在走的稳路径, pdf2zh 侧已能优雅处理, 不引入新异常类型;
  - `default_width=1000` 保住版面坐标, 不让版面塌掉。
- **留痕**(`record_font_fallback` → JSONL, 默认
  `server/translated/review/字体降级台账.jsonl`, env `PDF2ZH_FONT_LEDGER` 可覆盖):
  记录 ts/doc/objid/subtype/basefont/err/where。写盘失败一律吞 ——
  诊断绝不能改变失败本身的语义。**不留痕的降级等于没修**(静默的质量损失)。
- `settings.STRICT` 下保持原语义(严格模式就是要炸)。
- 性质必须说清: 闸门3 把"整篇退出码 1"换成"那一段无译文、版面仍在"。

### 闸门4 (摘根因): `server/server.py` 失败时从控制台尾部捞回子进程 traceback
- 问题: Windows 上 `execute.py:_execute_with_inherit` 让子进程 **stdout/stderr 直接继承控制台**
  (为了保留 tqdm 原生多进度条 UI), 于是 pdf2zh 的 traceback **只落在屏幕上**,
  没有任何一处被捕获; server 侧最后只剩
  `Command '[...]' returned non-zero exit status 1.` —— 任务记录与 HTTP 响应里就是这句废话。
- 处置: 失败那一刻用 `ReadConsoleOutputCharacterW` 读回控制台屏幕缓冲区尾部,
  抽**最后一个** Traceback, 归纳成 `异常类: 消息 @ 文件:行 (函数名)`。
  - `failure_brief()` → 写进任务记录 `error=`(5 处异常路径全部接线), `/api/history` 从此是真根因;
  - `_derive_error_info()` → 写进 HTTP 响应 `errorType` + `message`;
  - **顺序不可颠倒**: `_exception_payload` 必须**先抽根因再 `traceback.print_exception`**,
    否则会把 server 自己的 `CalledProcessError` 栈写进控制台, 而闸门4 正是取"最后一个 Traceback"。
  - 取不到输出流(服务无控制台/输出被重定向)就静默降级为原异常字符串 —— 绝不因诊断失败改变失败语义。
  - 技术要点: `GetStdHandle.restype` 必须设为 `wintypes.HANDLE`, 否则 64 位句柄被截断。
- **落地时发现的致命落差(差点让闸门4 空转)**: `logs/launch.ps1` 是
  `-RedirectStandardOutput/Error + -WindowStyle Hidden` 启动服务的 —— **服务没有控制台**,
  子进程继承的是那两个**日志文件句柄**。也就是说闸门4 初版写的 `console_tail()` 在
  **实际部署形态下必然返回空**, 等于没做。证据: `logs/server_err.log` 里躺着子进程
  tqdm 进度条原样输出(`0%|... 34/34 [00:07<00:00, 4.43it/s]`)。
  → 补 `failure_output_tail()`: 控制台能读就读屏幕, 读不到就**改读日志文件尾部**
  (`logs/server_err.log` / `server_out.log`, 只读末 256KB; env `PDF2ZH_FAILURE_LOG` 可覆盖),
  两条来源对 `extract_root_cause` 完全等价(都是"子进程输出流的尾部")。
  **加了一道防张冠李戴的闸**: 只认 120 秒内仍在写的日志 —— 否则本次失败若压根没有
  traceback(如被 kill), 就会把上一次失败留在日志里的 traceback 报成本次根因, 那比没有根因更坏。

### 语料普查 (验证这四道闸门不是空转)
按"**格式谱系 × 出版年代**"选样, 对 `D:\Zotero-Git-Backup` 全库(185 篇, 1960–2026,
20+ 生产者谱系: Distiller 3.01/4.05/7.0/8.1、iText 1.4.6/4.2.0/5.3.5、pdfTeX、MiKTeX、
XyEnterprise XPP、3B2、Arbortext、QuarkXPress、Ghostscript、PDFlib、Antenna House、
macOS Quartz、MS Word、pypdf、pikepdf …)跑闸门1:

- **全库体检** `翻译前体检_20260918_184004_batch185.md`:
  **🔴高 59 篇 · 🟡中 117 篇 · 🔵提示 0 篇 · 🟢无风险 9 篇**;
- **样本沙盘** `解析沙盘_20260918_184241_batch46.md`(46 篇, 覆盖 1998 Distiller 3.01 → 2026 pikepdf):
  **❌崩 0 · ⚠降级 0 · ✅干净 46** —— 结论: 现有语料**解析层无雷**,
  闸门3 是**保险而非救急**(但正是它把 v26.19 那种"整篇退出"变成"少一段译文")。
- Type3 命中 **7/185, 且全部是 2026 新论文**(pikepdf 产物) ——
  印证"新工具链 = 新字体路径 = 新雷区", 闸门1 对新论文尤其必要。

### 验证
- **闸门1**: 对 EgoPhys 跑 → 精确命中**第 8 页** `Type3:BDKWTK+AvenirNext-Bold, Type3:BMQQDV+DejaVuSans`,
  与 v26.19 的真实崩溃页完全一致; 另有 7 页缺 ToUnicode 提示。
- **闸门3**(故障注入, 把 `PDFType3Font.__init__` 打回 v26.19 崩法): 三条判据全过 ——
  ① 19 页照常解析不崩; ② 台账落 2 条(objid 597 `/BDKWTK+AvenirNext-Bold`、598 `/BMQQDV+DejaVuSans`);
  ③ 降级点归页正确落在第 8 页。
- **闸门4**(12 组/19 条判据, 用 v26.19 真实形态的 tqdm+ANSI+traceback 输出):
  抽根因 ✅ · 取最后一段 traceback ✅ · 无 traceback 返回 None ✅ · 无 stderr 的
  `CalledProcessError` 走输出流 ✅ · 输出流不可用退化不抛错 ✅ · 有信息量的异常不被污染 ✅ ·
  `errorType=AttributeError` + 带 `@ pdffont.py:1163 (__init__)` ✅ · ValueError 优先级不回归 ✅ ·
  **无控制台部署下改读日志尾部仍抽到根因** ✅ · **陈旧日志(>120s)被拒、不张冠李戴** ✅ ·
  日志路径 override 生效 ✅ · 无控制台 + 日志两条路径都给对 `errorType`/`message` ✅。
- **镜像**: `tools/sync_patches.ps1` **9 PASS / 0 FAIL**
  (新增 `patches/pdfminer_pdfinterp.py` 6CAC7EA9; `pdfminer_pdffont.py` 8597859C;
  `server_server.py` 062C809C)。`test_mirrors_sync.py` 同步扩容到 9 项。
- **回归**: `tools/tests/run_all.py` **92 项全 PASS / 0 FAIL**
  (镜像 9 + 缓存 10 + 集成 5 + 前瞻 10 + 文档摘要 9 + 军师 49)。
  注: 退出码 1 仅来自 TRAE 沙箱拦截 `site-packages/pdfminer/__pycache__/*.pyc` 写入, 非测试失败。

### 端到端真翻译验证 (EgoPhys, 四道闸门同时在线)
启动服务(`logs/launch.ps1`, 无控制台部署形态)后以**插件同等配置**直提 `/translate`
(service=silicon / targetLang=zh-CN / threadNum=4 / noWatermark+mono+dual+skipSubsetFonts)——
**这正是 v22 事故的复现姿势**, 所以同时验证了"字段带全 → 缓存键不变"。

- **任务**: `2d9d8c7e-…` → `accepted` → **完成, 成功生成 2 个文件**, `history` 无 `error` 字段;
- **闸门1 在产线自动质检里生效**: 翻译后台线程生成
  `翻译前体检_20260918_192039_…md`, 内含新增的**「字体/结构画像」表**
  (生产者 `pikepdf 8.15.1` / 生成器 `arXiv GenPDF`), 并精确标出
  **第8页 🔴高 Type3:BDKWTK+AvenirNext-Bold, Type3:BMQQDV+DejaVuSans**
  (与 v26.19 真实崩溃页一致) + 第3/4/6/7/16/18/19页缺 ToUnicode 🟡中;
- **越过了旧崩溃点**: 进度走到 **19/19**(v26.19 时是 8/19 整篇退出);
- **闸门3 无降级**: 台账 `字体降级台账.jsonl` 翻译前后均 **0 行** —— 说明 v26.19 的正修
  已让 Type3 走正常路径, 闸门3 是保险而非救火(符合 46 篇沙盘的普查结论);
- **闸门4 未触发**(任务成功, 无失败语义需改变);
- **产物抽查**(PyMuPDF 逐页汉字计数): mono 19 页 / 11535 汉字 / **零汉字页 0 个**,
  **第8页 871 汉字**(旧崩溃页有译文); dual 38 页(2×19)、同样 11535 汉字;
- **翻译后质检门禁**: `PASS`(引用完整性 原文 78 / 译文 71 在容差内; 无 `{vN}` 残留)。

### 影响面与后续
- 闸门3 改的是 `pdfminer/pdfinterp.py` 的 `get_font`, **pdfminer 原生路径与 pdf2zh 自带解释器都走它**,
  BabelDOC 迁移后同样受保护(但 BabelDOC 侧另有一份 pdfminer, 迁移时需同步带上 v26.19 + v26.20 两修)。
- 闸门4 只加"读屏幕/读日志尾部", 不改任何失败判定; 两条来源都拿不到时行为与改动前一致。

### 同源补修 (v26.20b): 任务卡片进度恒 0% —— 与闸门4 栽的是同一个坑
闸门4 落地后顺着同一条线索查下去, 发现**进度监视器**也在同一个陷阱里,
而且比闸门4 更隐蔽(闸门4 是"没做", 它是"做了但永远匹配不上")。

- **缺陷① 来源错**: `_monitor_windows_console_translate_progress` 只读控制台屏幕缓冲
  (`ReadConsoleOutputCharacterW`), 而 `launch.ps1` 无控制台部署下子进程 tqdm 落在
  `logs/server_err.log` 里 —— 与闸门4 完全同源。
- **缺陷② 句柄误判**: `GetStdHandle(STD_OUTPUT_HANDLE)` 在 stdout 被重定向时返回**文件句柄**,
  虽然"有效"却**不是控制台**。旧代码直接拿它调 `GetConsoleScreenBufferInfo` → 失败 → `break`
  → 监视器**静默退出**(连日志都不写)。
- **缺陷③ 正则错(关键)**: 旧代码只试 `MAIN_PROGRESS_RE`, 它要求行里出现 `translate` 字样;
  而 pdf2zh 1.x 的页进度条是 `tqdm.tqdm(total=total_pages)` —— **不带 desc**, 实测输出
  ` 42%|███▏| 8/19 [00:02<00:03, 2.89it/s]` 压根没有 "translate" 这个词。
  即"就算读到了也匹配不上", 在 pdf2zh 1.x 上**进度必然恒为 0**。
  (PTY 那条路径 `_parse_progress` 本来就有 `PDF2ZH_TQDM_RE` 兜底, 这里属补齐。)
- **连带后果**: `activity[1]`(是否曾解析到进度)永远为 False → `_execute_with_inherit` 的
  **空闲看门狗**(`if activity[1] and now - activity[0] > TASK_IDLE_TIMEOUT`)被一起废掉 ——
  网络停滞时任务会一直挂着, 只剩 3 小时总时长兜底。

处置 (`server/utils/execute.py`):
- 新增 `_match_progress_line(line)`: 先试 `MAIN_PROGRESS_RE`(pdf2zh_next), 再试
  `PDF2ZH_TQDM_RE`(pdf2zh 1.x), 两条都试才叫"认得出"。
- 新增 `_open_progress_log()` / `_read_progress_log(state)`: 从日志**末尾**跟读
  (env `PDF2ZH_PROGRESS_LOG`, 默认 `logs/server_err.log`), 只认本次任务新写的行,
  不会被上次任务留在日志里的旧 `x/y` 污染(否则会锁错 total)。
  tqdm 输出到**非 tty** 时仍用 `\r` 原地刷新, 所以 **`\r` 与 `\n` 都当行分隔符** ——
  不切 `\r` 的话整个进度流是一条几十 KB 的巨行, 正则只能看到最后那次刷新;
  残段留到下轮, 避免读到半行(实测"当前行"是被**下一次刷新**结尾的, 时延 ≤ 一次刷新)。
- 源选择: 实探 `GetConsoleScreenBufferInfo` 是否为真控制台; 是→屏幕缓冲(原逻辑不动),
  否→日志尾部。两条都拿不到就静默返回(= 改动前的无进度行为, 不影响翻译)。
  另加测试钩子 env `PDF2ZH_PROGRESS_SOURCE=console|log` 强制来源。
- 循环体改为双源分支: 候选位置控制台用行号、日志用递增 `seq`(越大越新),
  日志模式取同批最新一行(控制台模式保持原"离光标/离上次最近"的择取策略不变)。

### 验证 (v26.20b)
- **新增 `tools/tests/test_progress_log.py` (19 例全 PASS)**: tqdm 无 desc 行可解析 /
  子任务括号 `(1/1)` 不误当页进度 / `\r` 分行 / 残段不吐出且跨轮拼接 / 从末尾跟读不吃旧进度 /
  **端到端(强制 log 源)任务卡片进度 42% → 99% 且 `activity[1]` 置真** / 无源时不抛错且不喂狗。
- **新增 `tools/tests/test_failure_brief.py` (20 例全 PASS)**: 把闸门4 的临时探针
  (`%TEMP%\gate4_probe.py`) 提升为常驻回归, 覆盖抽根因/取最后一段/无 traceback/
  无控制台读日志尾部/陈旧日志被拒/`_derive_error_info` 优先级不回归。
- **回归**: `tools/tests/run_all.py` 从 6 套件 92 项扩到 **8 套件 131 项, 全 PASS / 0 FAIL**。
- **产线实测(修后重启服务, `/api/history` 已清空 → 真跑一遍)**: 以插件同等配置提交 EgoPhys,
  任务卡进度快照 `0% → translate 0/19 → 52% translate 10/19 → 99% translate 19/19 → 完成 100%`,
  **全程 18.2 秒**(缓存全命中)。旧代码下这里会一直停在 `0%` 直到任务结束。
  同次运行重出产物: mono 19 页 / 11535 汉字 / 第8页 871 汉字 / 零汉字页 0,
  翻译后质检 `PASS`, 字体降级台账前后均 0 行。
- **重启注意(复述一次, 踩过)**: `launch.ps1` **不会杀旧实例** —— Windows `SO_REUSEADDR`
  会让新实例静默共绑 8890 而旧实例继续服务。正确姿势: 先杀**所有** `server.py` 进程,
  确认剩 0 个再 `launch.ps1`, 然后核对 `/api/history` 为空。
  本次实测重启前确有两个 `server.py` 进程并存(20448/14548 系同一次启动的 uv shim + 真身)。

- 待办挂账: `dichogamy,雌雄异熟` 待与下次重译合并补入 `terms.csv` + `terms.babeldoc.csv`;
- 待办挂账: `server/utils/` 下 `execute2.py` / `execute3.py` 为**未被引用**的重复实现
  (`server.py` 只 import `utils.execute`), 待确认后删除; `translator.py` 里
  `_cache_key_suffix` 定义两次(574-599 被 670-701 覆盖)属死代码。

## v26.20c (2026-09-18) 共享输出流的三道闸: 修掉 v26.20/b 自己引入的跨任务串读

### 缘起 (闸门4 与进度监视器栽在同一个前提上)
v26.20 与 v26.20b 各自新增"读输出流"的能力时, 都默认了同一个**未经检验的前提**:
**我读到的输出, 是这个任务的输出**。这在单任务时成立, 在并发时不成立 ——
`launch.ps1` 是 `-RedirectStandardOutput/Error` 启动服务的, 服务自身与**所有**子进程
都往同一组 `logs/*.log` 写; 控制台屏幕缓冲同理(进程级共享, 只读一份、多方在写)。
于是会出两类**静默错误**:
- 闸门4: 任务 A 失败时, 锚点窗口里落进了任务 B 的 traceback → 报成 A 的根因;
- 进度监视器: 任务 A 的卡片吃到了任务 B 的 tqdm → 进度条显示别人的进度。
两者比"没有数据"更坏: **错的数据会被当成事实用来做判断**。
`locked_total`(页数相同才认)只挡得住"页数不同的两个任务", 挡不住同文档并发。

### 处置
统一判据放在 `task_manager.has_other_running(task_id)`: 除本任务外还有没有未结束的任务。
- **进度监视器** (`server/utils/execute.py`): 新增 `_sole_translation(task_id)`,
  两处设闸 —— ① 源选择处: 非独占时不启用日志源, 退化为"无实时进度"
  (与无控制台环境的旧行为一致, **不影响翻译本身**); ② 日志读取循环内: 消费掉新内容后、
  回报前再查一次, 期间另起了翻译就只推进读取位置、不回报, 等重新独占再继续
  (即"自己的进度不丢, 别人的进度不吃")。
- **闸门4** (`server/server.py`): 补齐三道闸, 顺序固定 ——
  1) **独占闸**: `_sole_task(task_id)` 为假直接弃用共享日志(宁可没有根因, 不要错的根因);
  2) **锚点闸**: `anchor_failure_logs(task_id)` 在任务登记处(`_start_accepted_job`)
     记下各日志当时的字节数, 失败时只认锚点**之后新增**的内容
     (`_tail_file(start=)`: 无新增返回空) —— 挡住上一次失败留在日志里的 traceback;
     锚点表 `_FAILURE_LOG_ANCHOR_KEEP=32` FIFO 淘汰, 不随任务数无限增长;
  3) **新鲜度闸**: 超 `_FAILURE_LOG_FRESH_SECONDS=120` 没写过的日志不认(无锚点时兜底, v26.20 原有)。
- `task_id` 贯穿 5 处异常路径(690/709/970/1355/1432 行)与
  `_handle_exception` / `_exception_payload` / `_derive_error_info` 三处签名;
  `/crop` 那条同步路由无 task, 保持原样。
- 三条设计约束: 取不到任务表 → 按"非独占"处理; 任何一环抛错都不改变失败语义;
  非独占只是**退化为旧行为**, 绝不阻断翻译。

### 验证 (v26.20c)
- **`test_progress_log.py` → 27 PASS / 0 FAIL**(新增 8 例):
  `[F]` 共享日志串读闸 F1–F4(非独占期间的进度未被告成自己的 / 不喂活动信号 /
  恢复独占后进度继续 / 消息同步更新); `[G]` 启动即不独占 G1–G4(监视器同步返回且不喂狗,
  不影响翻译; 对方结束后恢复独占; 只剩自己时算独占)。
- **`test_failure_brief.py` → 32 PASS / 0 FAIL**(新增 12 例):
  `[13]` 锚点闸(锚点后无新增→不认旧 traceback; 旧 traceback 仍在文件里也不再被采纳;
  新增的 traceback 被采纳) / `[14]` `_tail_file(start=)` 锚点语义 /
  `[15]` 独占闸(有别的翻译在跑时弃用共享日志, 对方结束后重新可用) /
  `[16]` 锚点表上限。
- **镜像**: `tools/sync_patches.ps1` **9 PASS / 0 FAIL**; `patches/server_server.py` 更新为 `FB46A6F6`。
- **回归**: `tools/tests/run_all.py` **8 套件 151 项全 PASS / 0 FAIL**
  (镜像 9 + 缓存单元 10 + 迁移集成 5 + 前瞻 10 + 文档摘要 9 + 军师 49 + 进度 27 + 根因 32)。
- 说明: 本次只改诊断/显示路径(读日志、报进度), **不碰翻译与缓存**;
  非独占时行为与"该能力存在之前"完全一致。
- **部署 (2026-09-18 20:04:33)**: 按重启姿势先杀干净 `server.py`(20448 uv shim + 14548 真身),
  确认 `8890 已释放` 后 `logs\launch.ps1`; 新进程树 37596 → 50388 → **39240**(监听 8890),
  无幽灵实例; `/api/history` **0 条**(干净起点); `server_err.log` 无 Traceback。
  注: 只杀 `server.py` —— 同机另有 `tools/doubao_bridge.py`(豆包 MCP 桥)与两个 uv 缓存
  python(MinerU MCP), 它们与本服务无关, 不动。
- 待办挂账: `execute.py` / `task_manager.py` 自研改动量已不小(18 / 2 处 `[自研补丁]` 标记),
  但**不在** `sync_patches.ps1` 与 `test_mirrors_sync.py` 的 9 文件镜像清单内 ——
  是否需要把镜像清单扩到这两个文件(乃至 `venv.py` / `cropper.py` / `config_migration.py` /
  `auto_update.py` / `deepseek_thinking.py` 这些同样带自研标记的文件), 待定。

## v26.21 (2026-09-19) 门禁第四断言: 文献区禁汉化

### 缘起 (三项断言全 PASS, 文献表却已被翻成中文)
EgoPhys(2026, Kim 等, arXiv)首轮 LLM 直译把**整张参考文献表中文化**: `K. Zhang, B. Li,
K. Hauser, and Y. Li` → `K. 张， B. 李， K. 豪泽， 和 Y. 李。`, 期刊名被加书名号,
人名被逐字音译, 全文 **174 处**。而当时 `post_check.py` 的**三项断言全部 PASS**。
根因是断言口径的单向性: 汉化率(断言1)问的是**"译文汉字是否太少"**, 从不问
**"该留英文的地方是否留了"** —— 文献表被判"汉字充足"从而放行。参考文献表被汉化后
整张表不可用(检索不到、无法回溯原文), 属交付级硬伤, 却恰好落在所有断言的盲区。

### 处置 (`tools/post_check.py`, 判据取"原文页行首 [n] 密度")
新增第四断言"文献区禁汉化", **单一判据**:
- **文献页识别**: 原文该页 `ref_density = 行首 [n] 条目数 × 1000 / 该页字符数`
  `>= REF_DENSITY=2.5` → 该页是文献页, 参与判定; 否则直接跳过(正文页不误伤)。
  实测 EgoPhys: 纯文献页 4.31 / 4.46 / 4.60, "正文末页+文献开头"的第 9 页只有
  1.77 —— 用密度而不是"条目数 >= 3"才能把那半页正文排除, 否则正文的中译会被
  当成"文献被汉化"。
- **事故判定**: 译文该页"被汉化的条目块"数 `>= REF_ZH_MIN=2` → 高危。
  条目块 = 从行首 `[n]` 起到下一个 `[n]` 或块首 + `REF_ZH_WINDOW=200` 字符
  截断为止; 块内含汉字即该条被汉化。截断是必需的 —— 文献区末尾那条后面若
  紧跟同页正文, 不截断会把正文的中文算进最后一条。
- **不变量**: 密度一律取**原文页**。译文页不可用 —— 条目一旦被汉化/标点换成
  全角, 行首 `[n]` 会被 pypdf 拆散(实测 CLAP 第15页原文 3.18 → 译文 0.40)。
- **断言1 联动豁免**: 文献条目按规定整条保留英文, 故文献页译文"汉字为零"是
  预期结果而非翻译缺失 —— 断言1 对该类页直接豁免(文案"文献页原文保留"),
  反向由断言4 把关。EgoPhys 第10/11/12页曾因此误报。
- 新增常量 `REF_ZH_MIN` / `REF_DENSITY` / `REF_ZH_WINDOW` 与函数
  `count_zh_ref_blocks()`; 删去 `REF_ENTRY_MIN` / `REF_PAGE_CJK` / 正则
  `REF_ZH_LINE` 及 `page_features()` 的 `ref_zh_lines` 字段, 改为
  `ref_density` / `ref_zh_blocks`; 报告"断言项说明"第 4 条同步。
- 检查通过时输出**通过**结论(含文献页数), 使"确实检查过"在报告里可见, 而非静默无输出。

### 口径返工 (首版判据在 EgoPhys 上误报, 当日改正)
首版判据为"行首 `[n]` 且含汉字的**行数** >= 2"(主) + "原文 0 汉字且译文
汉字数 >= 60"(兜底)。在 EgoPhys 交付稿上实测**误报 4 页**:
- 第 10/11/12 页原文是纯文献页、译文整条保留英文(0 汉字) → 被**断言1**当成
  "原文有实质内容但译文汉字占比<5%"判缺失(断言1 的盲区, 不是断言4 的问题);
- 第 9 页是"正文最后一节 + 文献开头", 译文 450 汉字全是**正文中译**(正确),
  被**兜底**当"整页汉化"(用"原文该页 0 汉字"当纯英文文献页的代理, 而正文页
  原文当然也是 0 汉字, 代理失效)。
两处都不是译文的问题, 是判据的问题 —— 故改为上面的"原文密度 + 条目块"口径,
并同步给断言1 加文献页豁免。**首版入口径的"28 PASS / 现场 FAIL"结论已作废。**

### 验证 (修订口径后实测)
- **`test_post_check.py` → 31 PASS / 0 FAIL**(重写): 汉化文献页判高危且门禁 FAIL /
  原样文献页判通过且 PASS / 非文献页不误伤 / **正文末页+文献开头(密度1.77)不触发** /
  **纯文献页译文无汉字不被断言1判缺失** / 汉化条目数恰为阈值-1 不误判 / 报错页跳过 /
  多页混合只报汉化页 / 与断言1串联同出两条高危 / 三个常量口径锁定 /
  `count_zh_ref_blocks()` 五条正反例(全英文 0 条 / 单条 1 条 / 两条 2 条 /
  窗口截断不吞同页正文 / 行中 `[n]` 不算条目) / 报告渲染含密度判据。
- **现场实测(EgoPhys 交付稿)**: `post_check` **PASS**(退出码 0)。第 10/11/12 页
  判"文献页原文保留(预期)", 断言4 报"3 个文献页的条目均保持原样"。
- **真伪核查(既有译文库全量 12 篇 mono)**: 6 篇被断言4 判高危, **逐页抽查确认
  全部为真阳性** —— `Attention Is All You Need` 第11/12页(`[8] 克里斯·戴尔,
  阿迪古纳·昆科罗…`、`[11] 何恺明, 张翔宇, 任少卿, 与孙剑…`, 19 条)、
  `CLAP` 第15页(原文密度 3.18 / 21 条)、`TO-SoFiT` 第6页(13 条)、
  `A 0-1 Law…` 第6页(6 条)、`Extended Kalman…` 第43/44页(17 条)。
  即"文献表被汉化"并非 EgoPhys 孤例, 是**旧直译路径的普遍缺陷**; 该断言补上了
  三项断言共同的盲区。另 `Cactaceae` 等用作者-年份式引用、无行首 `[n]` 的篇目
  **不产生任何文献区结论**(其 FAIL 来自断言1, 与本断言无关)。
- **回归**: `tools/tests/run_all.py` **9 套件 182 项全 PASS / 0 FAIL**。
- 说明: 本次只改**只读质检**路径, 不碰翻译、缓存与渲染; 未改动 `patches/` 下任何文件,
  故镜像清单与 `test_mirrors_sync.py` 不受影响。

### 同批根因挂账 (连字符丢失, 未处理)
EgoPhys 译文另有**连字符丢失 163 种 / 379 处**(`held-out`×15、`per-spring`×12、
`test-time`×8、`zero-shot`×7 …)。根因在 `server/config/config.toml` L73
`formular_char_pattern` 含 `-`、`\d`、`.`、`,` —— 连字符与数字被判为"公式",
每处都变成 `{vN}` 占位符交给 LLM 原位回填, LLM 一调语序就必然错位/丢失
(现场已见 `zero⟨公式: -⟩shot transfer⟨公式: .⟩`)。修法需重估该正则的取舍并
**全量重译验证**(会影响既有缓存键), 不在本次只读门禁范围内, 挂账待办。

## v26.22 (2026-09-19) M1 回锚匹配器: 从"单调游标"到"长 core 唯一落点优先"

### 缘起 (Wang 篇走豆包链首跑, 回锚层 99 处 FAIL)
第一篇走通"豆包定稿"链的论文(Wang 等《Differentially Private Consensus for Time-Delay
Multi-agent Systems》, 19 页 / 266 个编号段)首跑时, `seg_import` 的结构层对账**全过**
(266 段编号齐、7 处 `⋮` 齐、无多缺段), 但回锚层报 **99 处"高价值字形未回锚"**。
关键判断: **这不是数据问题** —— 换任何一篇中文译文都会撞同一堵墙, 必须动匹配器本身。

### 三个根因 (逐轮诊断, `out/_diag_reanchor.py` 临时脚本分类统计)
1. **单调游标假设译文语序与原文一致** (约 66 处)
   旧实现 `seg_zh.find(core, cursor)` 带单调递增游标。中文一调语序, 游标就越过该符号,
   该 token **及其后全部**判 FAIL。战例 p7#3: 原文 `each nonzero eigenvalue {λi} of {L}
   with {λ=λi}` → 译文"对 **L** 的每个非零特征值 **λi**，取 **λ=λi** 时" (L 被提前)。
2. **core 内部标点只认半角** (约 20 处)
   `split_value` 只剥离 core 首尾标点, 内部标点是字面字符。合格中文译者按排版规范写成
   全角/顿号 → 字面匹配失败: `2.1,(20)`→`2.1，(20)`; `(26),(27)`→`(26)、(27)`;
   `=1.(2)`→`=1。(2)`。
3. **短字形抢占长公式的唯一落点** (7 处, 第一轮修法后新暴露)
   长公式更长更独特, 落点全局唯一; 但按原文顺序贪心时, 短字形(λi / ak,s / s)先落点,
   恰好落进长公式的区间 → 长公式的候选全被"占用"而判 FAIL。
   战例: `{v49}='Pk−1s=0Γ(k,s+1)c(s)u(s)→0.'` 被 `{v48}='u(s)→0,'` 抢；`{v73}='kPk−1s=0ak,s≤Ma.'`
   被 `{v69}='ak,s'` 抢。

### 修法 (`tools/seg_import.py`)
- **标点等价类** `PUNCT_EQUIV` + `_core_pattern(core)`: core 转正则时, 标点位允许半/全角
  等价(`,`↔`，`↔`、`, `(`↔`（`, `.`↔`。` …); 数字之间的逗号额外允许整块消失
  (豆包把 `2,000` 写成 `2000` 仍能命中 —— **渲染时仍用字形原值**, 不是采用译文的写法)。
- **两阶段定位**(取代旧的两遍重排):
  ① **长 core 优先**: 按 core 长度降序, 只占"全局唯一落点"的 token(长公式先落地);
  ② **语序推进**: 剩下的按原文顺序推进单调游标, 游标之后没有候选(中文把符号提前了)
     才退到"离游标最近"。
- **占用区间防重叠** `_spans` / `_overlaps` / `_free_spans`: 每个落点独占区间,
  短字形落在长公式里就不再可能。
- **歧义锚定台账**(`notes`): 多候选落点不再静默取最近, 返回 `(vn, 值, 候选数)` 供人工复查;
  `reanchor` 由返回 3 值改为 4 值 `(译文, fails, drops, notes)`, `main()` 增"歧义锚定"报告行。
- 纯标点字形仍按设计丢弃(`drops`)—— 渲染契约要求占位符多重集 ⊆ 原文。

### 验证
- **新增 `tools/tests/test_seg_reanchor.py` 19 例全 PASS**: 语序一致 / 语序重排(L 提前战例) /
  全角逗号 / 顿号 / 全角句号 / 千分位弹性 / 同值两处各自回锚 / 真找不到仍 FAIL /
  歧义不误判且记台账 / 纯标点丢弃 / 未回锚者绝不入译文 / **长公式不被短字形抢位**。
- **`tools/tests/run_all.py`** 注册第 10 套件, **10 套件全 PASS / 0 FAIL**。
- **Wang 篇复跑**: 回锚 FAIL **99 → 21 → 12**(第一轮"标点等价+回退搜索"降到 21,
  第二轮"长 core 唯一落点优先"再降到 12)。

### 剩余 12 处: 全部是豆包改写/搬移了公式片段 (匹配器已到极限)
第三轮诊断按"逐段打印 raw + 全部字形值 + 译文"逐条核对, 结论: **没有一处是提取层字形重叠**,
全是译文侧动了公式片段。此时再放宽匹配只会误锚, 只能靠提示词约束。

| 段 | 豆包做了什么 | 性质 |
|---|---|---|
| #S46 p3#31 | `i6=j` 写成 `i≠j` | 好心修正 |
| #S47 p3#32 | `⋮` 断点漂移("若存⋮在"), 前半段丢了"系统 (1)" | 断点错位 |
| #S52 p4#5 | `∆H-adjacent` 只译"相邻"(漏符号) + `Ha∼Hb` 展开成 `Ha=(...),Hb=(...)` | 改写+漏译 |
| #S166 p11#7 | `i6=i0` 两处写成 `i≠i0` | 好心修正 |
| #S177 p11#27 | `(44).✷` 写成"(44) 得出。✷" | 拆开搬移 |
| #S201 p13#16 | `λi6=0` 写成 `λi≠0`; `L,αi` 被"的每个非零特征值 λi，"插开 | 修正+拆开 |
| #S241 p17#17 | `(A.3),Φt(k)≤` 被"对 k≥t 有"插开 | 拆开搬移 |
| #S261 p18#25 | `1−e−u≥u/(1+u),u≥0,` 写成`…（u≥0）` | 标点全角化 |

### 同批: 任务包 RULES 升级 (`inbox/wang2026.txt`, 8 条 → 12 条)
按上表逐条加规则, 不改任何 `#S` 段(266 段一字未动, manifest 仍有效):
- 第 4 条: 公式片段**逐字符原样照抄**(明写 `i6=j 不要写成 i≠j`), 不许修正/补全/展开/改标点;
- 第 5 条: 公式片段连同紧邻编号/括号/引用/标点**原地照抄**, 不得插中文词或搬到别处;
- 第 6 条: 公式片段内部标点保持**半角**;
- 第 7 条: 原文每个符号/编号/片段都要出现, **不得漏译**(`∆H-adjacent` 要写"∆H-相邻");
- 第 10 条: `⋮` 必须在与原文**完全对应**的断点处, 断点两侧词序不得调整;
- 第 12 条: 语序可随中文习惯调整, 但第 4–6 条的公式片段及其紧邻成分例外。

### 教训
- **回锚不是形式主义。** 回锚后渲染引擎画的是 **PDF 里的原始字形对象**(真正的 `≠`、`‖`、
  上标); 不回锚就用正文字体画**提取出的退化文本**(`Ekx∗k2<∞` 这种)。所以
  **豆包"好心修正"公式反而害了自己** —— 改一个字符就取不回来。
- 匹配器只能在"译文忠实照抄"时救回; 译文改了字, 机械层救不回来 —— **那是提示词的责任,
  不是匹配器的责任**。两者分工必须分清, 否则会一直试图用放宽匹配去掩盖译文改写。

### 影响面
- 只改 `tools/seg_import.py`(M1 工具层), 不动缓存、渲染、`patches/` 下任何文件
  → `sync_patches.ps1` 与 `test_mirrors_sync.py` 的 9 文件镜像清单不受影响。
- 待办: 豆包按新 RULES 整篇重贴后重跑 `seg_import` 复核, 再进
  `seg_inject → force_rerender → verify_render + post_check`。

## 2026-09-19 02:56 修复 post_check 文献页判据盲区（作者-年份制文献表）

### 触发
Wang 2026 豆包链回灌收尾: `seg_inject` 实写 + `force_rerender` 重渲染后, `post_check`
判 **第15页 FAIL**（汉化率断言：原文有实质内容但译文汉字占比 <5%）。

### 事实（不是注入把版面搞坏, 是门禁看漏了文献页）
- 第15页原文 = 上半页**英文参考文献表**（**作者-年份制**：`R. Olfati-Saber and R. M.
  Murray (2004). ...`, 整表无行首 `[n]`）+ 下半页附录中文证明。
- 行首 `[n]` 密度 **0.00** → 旧判据 `o["ref_density"] >= REF_DENSITY` 不认它是文献页
  → 断言1 不豁免 → 判"翻译缺失"。而译文里条目**按规矩保留英文**（正确）, 该页汉字
  只有附录那 107 字（占比 3.6%）→ 被判 FAIL。**内容更正确反而挂红灯。**
- 同一根因的另一面: 断言4（文献区禁汉化）在此类页面上同样形同虚设 —— 注入前硅基首轮
  把整表条目汉化（标题全译中文, 仅姓名/期刊名留拉丁）仍判 PASS。**该防的事故没防住。**

### 修法（`tools/post_check.py`, 只改这一个文件）
1. **原文侧判据**: 新增 `REF_AY`（行首姓名 + 其后 80 字符内 `(19|20)xx`）与
   `ref_ay_entries` / `ref_ay_density`; 新增 `is_ref_page()` = `max([n]密度, 作者-年份
   密度) >= REF_DENSITY` —— 两种体例共用阈值 2.5。
2. **译文侧计数**: 新增 `count_zh_ay_blocks()` —— 以译文中幸存的**姓名锚** `AY_AUTHOR`
   切块, 相邻两锚之间跨度含汉字即前一条被汉化。边界: 首锚之前 / 末锚之后**不计**
   （那是正文或附录, Wang 第15页文献区之后就是中文附录证明）; 跨度 > `REF_AY_SPAN_CAP`
   (400 字符) 不计（正文夹缝）。
3. **两处门禁统一走 `is_ref_page()`**: 断言1 的文献页豁免 + 断言4 的判据。断言4 的计数
   = 编号臂 `ref_zh_blocks` + **仅当该页确为作者-年份制时才启用** 的 AY 臂。

### 实测分离度（12 篇 PDF 逐页比选）
- 作者-年份密度: Wang 文献页 **2.93 / 3.58**, 其余全部正文页 ≤ 0.7, 编号制文献页恒 0.00
  → 同阈值 2.5 即可分离, 不误伤正文。
- AY 计数: 现译文（条目英文原样）Wang p14/p15 = **0**; 注入前硅基事故译文
  （cache id=5339） = **26** → 能判 FAIL。
- 误报防护: 若不按体例启用该臂, 编号制文献页 + 中文正文会误报 —— 实测 CLAP 第15-17页
  27/53/17 条 → 故加体例门控（这正是 `REF_DENSITY` 注释里"用密度而非条目数"的同一条纪律）。

### 验证
- `tools/tests/test_post_check.py` 31 → **49 例全 PASS**（新增 ⑬~⑰: 作者-年份页识别与
  豁免 / 条目被汉化判高危 / 编号制页不得启用 AY 臂 / 姓名锚切块口径（含首末锚边界、
  超长跨度、姓名汉字化仍可锚定）/ 常量与报告文案）。
- `tools/tests/run_all.py` **全部通过 ✅**。
- Wang 实跑 `post_check.py`: 第15页 **FAIL → 通过**; 断言4 覆盖文献页 **1 → 2**;
  门禁 **PASS**（exit 0）。

### 影响面
- 只改 `tools/post_check.py`（仓库工具, **不在** `sync_patches.ps1` 的 9 文件镜像清单内）
  与其测试 —— 不触 venv / `patches/` / 缓存 / 渲染。
- 全语料逐页回归确认: **判决变化面仅 Wang p14/p15**, 其余文献页的分类与计数逐页未变
  （编号制文献页 `ref_ay_density` 全为 0.00, AY 臂不启用）。

## 2026-09-19 03:01 关闭润色钩子（POLISH 退场，职责移交豆包链）+ 清理诊断产物

### 背景
主线已定型为"**硅基出草稿 → 豆包整篇定稿 → 机械回灌**"。润色钩子的职责（术语表、
翻译指南、反思式审校修订、人审报告）已由豆包环节整体取代；继续开着等于同一篇论文
被两套改写互相打架，还多付一次 LLM 费用。

### 改动（两份配置同步 —— 遵守"config.json 与 .example 必须同时改"的硬约束）
- `server/config/config.json`：silicon 段 `POLISH: "1"` → **`"0"`**
- `server/config/config.json.example`：同上。**这才是托管事实源** ——
  `server/utils/config.py` L358-373 在插件推送配置时，从 example 无条件回填 POLISH*
  键（`POLISH` 被插件置 null 的老事故就是这么救的）。
- 保留但已失效：`POLISH_GLOSSARY` / `POLISH_GUIDELINE` / `POLISH_REPORT_DIR` /
  `POLISH_REFLECT` 仍留在配置里，但 `_polish_enabled` 是所有润色分支的总闸
  （`patches/pdf2zh_translator.py` L474 判定 / L510 报告文件 / L517 启动自检），
  为 False 时一并不生效。
- 未动：openailiked 段的 POLISH 仍为 "1"（另一条通路，主线只用 silicon）。

### 验收（脚本复刻回填逻辑实算）
- 两份配置 JSON 均可解析；silicon 段 `POLISH = "0"`。
- 复刻 `config.py` L367 的回填过滤条件（非空值才回填）：`"0"` 非空 → 会被回填为
  `"0"`，**不会被跳过**。这点必须验 —— 若写成 `null`/`""` 会被过滤器拦下，
  反而回填不进去（旧值 "1" 存活）。
- `_polish_enabled = str("0").lower() in ("1","true","on")` → **False**。
- 服务端每次翻译任务现读配置 → **无需重启**。

### ★ 必须知道的副作用：缓存键世代切换
润色开关本身登记在缓存键里（`patches/pdf2zh_translator.py` L518-564）：
`polish` / `polish_anchor` / `polish_reflect` / `polish_model` / `polish_domain` /
`polish_formula_map` / `polish_formula_hint` / `polish_glossary_fp` /
`polish_guideline_fp`。关掉后这些键**不再写入**，于是现库 **3488 行 silicon 译文**
（键含 `polish: on` 等）在 POLISH=0 下**全部失配** → 同一篇论文再渲染会**整篇重译**
（硅基直译，且会把豆包注入过的行覆盖掉）。
- **对新论文无影响**：首次翻译本来就是全新键，流程照走（且这正是新范式的定义 ——
  草稿 = 不润色的直译）。
- **对已交付的 Wang**：只要不再用 POLISH=0 重渲染，豆包注入成果完好。将来若确要再
  渲染 Wang，须先拍板：(i) 接受整篇重译，或 (ii) 给 cache 加一条
  "polish-on → polish-off" 世代桥（照 v24-A 的 prev/legacy ladder，把 polish_* 键
  列为可迁移形态）。**尚未拍板, 挂账。**
- 注: `polish_reflect` 在 L530 是**在 `if self._polish_enabled:` 块之外**登记的,
  故 POLISH=0 + REFLECT=1 时键里仍留一项 `polish_reflect: on`（无害但无用, 已记此
  以便日后复查键形态时不困惑）。

### 清理
删除 `out/` 下全部诊断产物（临时探针/取证脚本 + 日志/截图, 共 26 个）：
`_probe_*` / `_diag_*` / `_verify_*` / `_preflight_*` / `_cmp_*` / `_peek_*` 等。
保留交付物：`wang2026.doubao3.txt`、`wang2026.imported.json`、各 payload/egophys 的
`*.doubao*.txt` 与 `*.imported.json`、`species_*.json`、`成品/`（Cactaceae 成品 PDF）。

## 2026-09-19 03:2x 退役参数投影（v27）：关 POLISH 不再让既有译文失配

### 背景
收口上一节挂账的副作用：POLISH 退场后 `polish*` 九个键不再写入，现库 **3488 行
silicon 译文**（键含 `polish: on`）在 POLISH=0 下全部失配 → 同一篇论文再渲染会整篇
重译，并覆盖豆包回灌的定稿行。Wang 是已交付件，必须零重译。

### 方案选型（联网检索 + 库内先例）
"版本化缓存键 + 惰性回写迁移"是缓存键演进的标准做法；本项目已有先例 —— v23
（`snapshot_legacy_key` + `remove_param`）与 v24-A（`snapshot_prev_key` + 带每段
`v23s` 后缀的 ladder）。但**这两条通道都覆盖不了润色世代**：
- `polish_guideline_fp` 是"**每篇论文一份**的翻译指南"文件指纹，**随文档而变**，
  无法像静态键那样把历史形态原样登记下来回查；
- 库里**同时存在** pre-v23（带 `polish_glossary_fp`）与 post-v23（该键已被
  `remove_param` 移除）两种形态，而 ladder 只有 prev / legacy 两个槽位。

故推广为**投影**：按"退役维度"把世代身份整体降维，而不是逐个登记历史形态。

### 改动（venv 真源 → 镜像 patches）
- `pdf2zh/cache.py`（MD5 `D01B74B7`）
  - 模块常量 `_RETIRED_PARAM_PREFIXES = ("polish",)` / `_RETIRED_PARAM_KEYS = ("v23s",)`
  - `_strip_retired(base_json)` → `(形态, 剔除个数)`：无退役参数时**原样返回并报 0**，
    保证当前世代的键形态逐字节不变（不引入无谓重排序）。
  - `_retired_index()`：**只登记确实带退役参数的行**，故与当前世代的行互不覆盖；
    按 `id` 升序写覆盖 —— 同一段落被翻过多次时留最新一条。
  - `get()` 新增 ③ 段：命中即 `_migrate_store()` 回写当前形态（与 v23/v24-A 同约定）。
  - **门控** `if self._strip_retired(pj)[1] == 0:`：仅当**当前世代本身不含退役参数**
    时才启用。润色若日后重开（键里又出现 `polish*`），③ 直接跳过，保持世代严格隔离
    —— 不会把"另一份翻译指南"下的旧润色译文喂给开启润色的任务。
- `pdf2zh/translator.py`（MD5 `0079B950`）：`polish_reflect` 的登记**归入
  `if self._polish_enabled:` 门闩**。它原本在门闩之外无条件写入 —— POLISH=0 时键里
  会残留一项 `polish_reflect`（既是无意义的世代维度，又让投影形态与库中行差一维）。
- **为何 `v23s` 一并退役**：它的生产者是润色术语表（`_cache_key_suffix` 在 POLISH /
  术语表未启用时**恒返回 ""**）。润色退场后该维度不再产生，而库里的行几乎都带它 ——
  不剔除则投影形态仍差一维，**一行也命中不了**。两者都只由润色子系统生产，故同批处理。

### 预演（真库副本只读，`out/_preflight_retired.py`）
```
silicon 行总数: 3488      投影形态(distinct)数: 7      live 可达形态数: 5
投影后可达行数: 2518 / 3488
可达组内校验: 命中 2183 / 未命中 0 / 取回异组译文 335
副本内 silicon 行数: 5087 (回写迁移新增 1599)     结论: PASS
```
- 335「异组译文」= 同 canon 但来自其他世代的同组行（键降维的预期代价，非错误）。
- 970 不可达行属**更早的 prompt 世代**（键里 `prompt` 是提示词模板 md5，跨代本就该
  失配）—— **改前也不可达**，非本次引入。
- 教训（测试脚本自身的坑）：v1 误用"某条老行的基形态"当全局基形态 → 3304 未命中；
  必须取**最高 id 行**（当前世代）推导 live 基形态再判可达性。

### 测试
`tools/tests/test_cache_canonical.py` 14 → **24 例全 PASS**，新增：
- ⑪ 润色世代行（`polish*`+`v23s`）在当前世代下**命中 + 回写迁移**
- ⑫ **门控**：当前世代含退役参数时投影停用 → 跨指南旧润色译文不串
- ⑬ `_strip_retired` 纯函数口径（无退役参数逐字节不变 / 前缀+键剔除计数 / 非法 JSON）
- ⑭ 只带 `v23s`（无 `polish*`）的退役行亦命中
- ⑮ 同段多世代 → 按 id 覆盖**取最新**
- ⑯ translator 侧契约：`polish_reflect` 登记须在 `_polish_enabled` 门闩内（AST 断言）
`tools/tests/run_all.py` **全部通过 ✅**（镜像同步 9 PASS、各套件 0 FAIL）。

### 实机验证（Wang，零重译）
`tools/force_rerender.py --pdf <Wang 原文>`（`force=true`，`service=silicon`，全插件
config 字段已固化，避免静默回落 bing）：

| 判据 | 结果 |
|---|---|
| 任务 | success，**99 秒**（含 19 页布局解析；整篇重译量级 4.5min+） |
| 库内 silicon 行数 | 3488 → **3759（+271）** |
| 新增行的键形态 | `polish=None / v23s=None`（**当前形态**） |
| 新增行译文 vs 润色世代行 | **逐字相同** → 是**迁移回写**，不是重译 |
| `verify_render --expect` | **2/2 落页**（"假设通信图含生成树" p5/p7/p10；"由拉普拉斯噪声的零均值性质" p7） |
| `post_check` | **PASS（可交付）**；第15页走"文献页条目原样保留"（上一节判据修复在此生效），断言4 覆盖 **2** 个文献页，无 `{vN}` 残留 |

（`+271` 即本文档被实际渲染/命中的段落数；其余文档的行仍停在退役形态，待其渲染时
按同一路径惰性迁移。）

### 影响面与挂账
- 不改渲染/注入/门禁任何逻辑，只加一条**回查通道**；未命中的行为与改前完全一致。
- 当前世代键形态**逐字节不变**（无退役参数时 `_strip_retired` 原样返回）→ 新论文的
  首次翻译不受影响。
- **挂账**：润色若日后重开，它将作为**独立世代维度**参与判定（已被门控保证不串），
  该路径**未测**。另：流程必经化（A）vs 钩子内化（B）仍未拍板。

## 2026-09-19 方案 A 拍板 + 豆包软件闭环确认（服务侧自检 + 豆包侧实调取证）

### 拍板
采纳管线走**方案 A（流程必经化）**：豆包软件站在翻译管线的**外部**，不进入 pdf2zh
的逐段翻译进程。五道工序 —— 解析建模 → `seg_export` 导出 `inbox/` 编号段落包 →
**豆包软件整篇定稿** → `seg_import`/`seg_inject` 回灌 → `force_rerender` + 门禁。
方案 B（钩子内化）的问题：pdf2zh 翻译接口按段回调，钩子内化后**整篇上下文消失**，
且中间产物与四道门禁一并失效；故 B 只保留为"每段兜底"的备用位。

前置必须是：A 里的"豆包"= **豆包软件**，而软件要能真读写 `inbox/`、`out/`。
本节即该闭环的取证。

### 一、服务侧自检（桥本身）
`out/_bridge_selftest.py`（临时脚本，已删）以**真实 MCP 客户端**身份拉子进程跑
`tools/doubao_bridge.py`，走完整 JSON-RPC 往返：

```
PASS initialize -> pdf2zh-bridge          {'name': 'pdf2zh-bridge', 'version': '0.1.0'}
PASS tools/list 四工具齐全                 [list_inbox, get_payload, submit_result, search_term]
PASS list_inbox 看得到 wang2026.txt        inbox/ 共 17 个文件
PASS get_payload 取回全文且未被截断         55070 字符 / 266 段 / 断点 ⋮ 13 处
PASS submit_result 落盘 out/ 且内容逐字一致
PASS get_payload 缺件报错不崩              (isError, 未抛异常)
PASS 未知方法回 -32601
PASS search_term 本地源可返回证据          zotero 源命中 2 条 / 0.5s
结果: 8 PASS / 0 FAIL      stdout 洁净（日志全在 stderr）
```
坑（自检脚本自身的）：`notifications/initialized` 是**通知**，若按 `send()` 带 `id`
发出，桥会当未知方法回一条 `-32601`，后续读取整体错位一格 → `tools/list` 假 FAIL。
通知必须**不带 id**。

### 二、豆包侧注册 + 实调取证（软件真的在调）
不看推测，取软件自己的落盘记录：

| 层 | 位置 | 内容 |
|---|---|---|
| 注册表 | `Doubao\User Data\Default\Local Storage\leveldb`（`samantha-web-agent-connector-list-store`） | `connector_id=364864694530`, `name=pdf2zh-bridge`, `enabled=true`, `install_status=1`, `connect_status=1`, `runtime_types=[2]` |
| 会话库 | `Default\IndexedDB\chrome_doubao-chat_0.indexeddb.leveldb` | 工具调用卡片，`"path":"mcp__pdf2zh_bridge__submit_result"` + 实参 `{"name":"wang2026.doubao3.txt","text":"#S1\n..."}` |
| 运行痕迹 | `User Data\sandbox_runtime\.cache\python\pyc-cache\...zotero-pdf2zh-venv\Lib\*.pyc` | 豆包**确实执行过该 venv 的 python.exe**（stdlib 全量 pyc 缓存） |

`saman_2026.09*.log` 里 `CONNECTOR_SERVICE` / `MCP_RUNTIME` 的实调流水
（`transport="stdio"`，`success` 均为 true，**零失败**）：

| 日期 | dispatched | 往返成功 | 耗时 |
|---|---|---|---|
| 09-12 | list_inbox 1 / get_payload 1 / submit_result 1 | 3/3 | 0 / 2 / 1 ms |
| 09-16 | search_term 1 | 1/1 | 3122 ms |
| 09-18 | search_term 1 | 1/1 | 8874 ms |
| 09-19 | get_payload 3 / submit_result 7 | 10/10 | 3 ms / 2 ms |

09-19 日志里还能看到人在豆包对话框里下的指令原文：
`用 get_payload 读 egophys2026.txt，按文件里开头的规则翻译，译完用 submit_result
存成 egophys2026.doubao.txt`。

### 三、产物侧对账（inbox → out 一一对应，段号守恒）
| inbox | out（豆包交件） | 段号 |
|---|---|---|
| `wang2026.txt` 266 段 | `wang2026.doubao.txt` / `doubao2` / `doubao3`（三轮） | 266 / 266 / 266 |
| `egophys2026.txt` 165 段 | `egophys2026.doubao.txt` | 165 |
| `payload_b4/b5/p5_p9/p16_p20` | 同名 `.doubao.txt` | 10 / 15 / 9 / 12（**与源一致**） |

段号守恒是关键判据：`#S1..#Sn` 一站不落，回锚（`seg_import`）才有对齐依据。

### 结论
「豆包软件取件 → 整篇定稿 → 交件」这条链**已实跑通**，且四工具全部经豆包 GUI
调用成功；桥的服务侧契约（命令/协议/文件/错误码/stdout 洁净）100% 可复现。
A 的**外部半环**成立，剩下的是内部半环的**顺序强制**（见挂账）。

### 挂账
- **方案 A「必经化」施工未开工**：五道工序尚未串成一个带门禁的入口，顺序靠人工记。
  待做：单一入口 + 前置检查（inbox 有未交件件则拦、out 有未回灌件则拦）+ 每步落台账。
- `out/` 残留历史测试件：`selftest.txt`、`roundtrip_test.txt`、空目录 `_pycprobe/`
  （首次桥联调与 `.pyc` 探测的遗留，非交付物）。

## 2026-09-19 v28 采纳管线「必经化」：(A) 的施工落地 —— 单一入口 + 前置门禁 + 台账

### 背景
上一节确认了 A 的**外部半环**（豆包软件读写 inbox/out）已跑通，剩下内部半环：
五道工序是散的脚本，**顺序靠人记**。实测踩过的坑全在"顺序/身份"上，而不是某个
工序内部 —— 故本节的产物不是新算法，而是把这些判据做成**前置门禁**。

### 改动（新增 `tools/adopt.py`，~470 行；不改任何既有工具）
六阶段，后一阶段的前置是前一阶段 `state=="ok"`（`--force` 可跳过，用了哪几步记在
台账 `forced_stages` 里留痕）：

| # | 命令 | 前置门禁（**拦**的判据） |
|---|---|---|
| 1 | `export --pages P` | 侧车存在；**库内查不到本次任何段落原文 → 拦**（否则人工豆包环节白跑）；探到的 `doc_fp` 写进台账 |
| 2 | `deliver --text/--clip` | 交件件**必须唯一或显式指定**（wang2026 有三轮 doubao1/2/3，拿错整轮白干）；**段号集合与 manifest 完全一致**；⋮ 分页断点逐段对账 |
| 3 | `import` | 侧车指纹与 export 时一致（`latest.jsonl` 是**全局单文件**，换论文会覆盖它）；`seg_import` 必须 PASS |
| 4 | `inject` | **先 `--dry` 演算，PASS 才实写**；`--fp` 一律用台账固定值 —— 堵住 `seg_inject` 探测失败**静默回落 Cactaceae 默认值**（对别的论文就是"UPDATE 命中 0 行"） |
| 5 | `render --pdf` | 服务可达；解析子进程输出的产物 mono 路径与耗时入账 |
| 6 | `gate --expect ...` | **必须至少一条 `--expect`**（否则无法证明"缓存改了且真落到页上"）；`verify_render` + `post_check` 双 PASS 才算交付 |

`status` 看台账 / 列全部 run。台账 `logs/adopt/<name>.json`（`logs/` 已 gitignore），
**原子写**（tmp + `os.replace`），失败只覆盖本阶段、不抹掉前序 ok 记录。

两处刻意的"口径复用"（防门禁规则与执行规则漂移）：
- 段号行识别用 `seg_import.KEY_LINE` / `parse_blocks`（容忍 markdown 加粗）
- 页级 `{vN}` → 段内 `{vk}` 用 `seg_inject.renumber`，指纹探测用 `seg_inject.detect_fp`

### 坑（本次踩到并已修）
`seg_import`/`seg_inject` 在 import 时会把 `sys.stdout` 重包一层 utf-8 wrapper。
"import 后把 `sys.stdout` 恢复成原对象"是**错的**：那些 wrapper 被 GC 时会
**关掉底层 buffer**，之后本工具自己的 `print` 全部 `I/O operation on closed file`。
正确做法是导入期间先把 `sys.stdout` 换成指向一次性 `BytesIO` 的替身，让它们的
包装打在替身上（`_load_siblings()`）。

### 测试
新增 `tools/tests/test_adopt.py`（**28 例全 PASS**），已登记进 `run_all.py`：
六阶段顺序门禁 / `--force` 留痕 / export 0 命中拦 / 指纹落账 / deliver 缺段·多段·
⋮ 数不符·多份歧义 / 侧车被改写拦 / inject 无 fp 拦 + 先 dry 后实写 + `--fp` 透传 /
render 产物与耗时解析 / gate 缺 expect 拦 + 双 PASS 与 post_check FAIL 两种落账 /
failed 不抹前序 + 台账无 `.tmp` 残留 / 非法 run 名 / status。
`tools/tests/run_all.py` **11 套件全部通过 ✅**（镜像同步 9 PASS 照旧）。

### 实机验证（真实侧车 + 真缓存库，临时沙箱 `P2Z_PROJ`/`P2Z_INBOX`，不碰真 inbox/out/台账）
```
export  --pages 5-9                    -> ok  78 段 / 库内命中 211 行
deliver (交件 = 该 payload 去掉规则头)  -> ok  78 段 / sha1=17b4e60c
import                                 -> PASS（回锚 80 段=78+2 处跨页合并）
inject --dry                           -> PASS（未写库）
guard  --dry 未写库 => render 被拦 ✓   render 未过 => gate 被拦 ✓   gate 缺 --expect 被拦 ✓
```
**本次最值钱的一条**：该 run 探到的 `doc_fp = docsummary:c80bd0f4427090bb:fdbaf00e`，
与 `seg_inject.DOC_FP_DEFAULT`（Cactaceae 的 `docsummary:3768fd6fce999176:bc4a2947`）
**根本不是一篇** —— 若按老路径走，注入会打在错误的文档作用域上。

### 顺带确诊一处既有缺陷（未改，需授权）
`seg_export.py` 的 `RULES` 抬头是**硬编码**的：
`[文档] Reproductive Biology of Cactaceae (Desert Plants, 2009) 第{pages}页`。
本次导出的是 Wang 那篇（多智能体一致性/延迟随机递推），payload 抬头却仍是仙人掌科
—— 豆包会拿到**错误的文档类别先验**。这正是挂账里"`seg_export.py` RULES 参数化"
那一项，现有实据。

### 挂账
- `render`/`gate` 两阶段**未做真机端到端**（会真提交渲染任务、写 `server/translated`
  并动真库），其分支逻辑由 28 例单测覆盖；首次真机跑建议用一小篇 payload 试。
- `seg_export.py` RULES 硬编码（同上）。
- `out/` 残留历史测试件 `selftest.txt` / `roundtrip_test.txt`（未清）。

## 2026-09-19 v28.1 导出抬头/术语表参数化：去掉硬编码的论文先验（关闭 v28 挂账第 2 项）

### 背景
v28 实机沙箱暴露：导出的是 Wang 那篇，payload 抬头却是
`[文档] Reproductive Biology of Cactaceae (Desert Plants, 2009) 第5-9页`
—— 豆包整篇定稿时被喂了一个**错误的文档类别先验**；`[规则]` 第 4 条同样写死仙人掌科术语。
抬头与术语都是"每篇不同"的东西，不该焊在代码里。

### 改动
| 文件 | 改了什么 |
|---|---|
| `tools/seg_export.py` | `RULES` 改模板（`{doc}`/`{pages}`/`{terms}`）；新增 `--doc`（文档抬头，缺省只写页码）、`--terms`（术语表 csv，缺省 `server/glossary/terms.csv`，传空串则不注入）；新增 `load_terms()`/`terms_line()`；默认术语表落空时**告警出声**（否则沙箱改 `P2Z_PROJ` 会连带它一起落空并静默退化） |
| `tools/adopt.py` | `export` 新增 `--doc`/`--terms` 透传（非空才透传，保持必经路径可用） |
| `server/glossary/terms.csv` | 补回旧硬编码抬头里有、而 csv 里没有的 3 条：`selfing,自交` / `selfer,自交个体` / `outcrossing,异交` |
| `tools/tests/test_seg_export.py` | 新增（**22 例全 PASS**） |
| `tools/tests/run_all.py` | 登记第 12 套件 |

语义要点：
- **抬头缺省只写页码**（`[文档] 第2-3页`）—— 不内置任何论文名，宁可少给也不给错的
- 术语行**格式不变**（`en=zh；en=zh`），仅数据来源外置
- 术语源缺失/为空/传空串 → 退化为 `4. 术语统一：按学科惯例统一译名，同一术语全文译法一致。`
  （第 4 条编号不塌陷，且**绝不泄出别领域的术语**）
- csv 解析：`utf-8-sig`（吃 BOM）、`split(",", 1)`（英文项含逗号不炸）、跳过空行/`#` 注释/半条
- 术语来源外置后必须确认 csv 覆盖了旧硬编码的全部条目，否则是**静默的内容回退**
  （本次就漏了 `selfing/selfer/outcrossing` 3 条，已补）

### 测试
`test_seg_export.py` 用**子进程**跑真 CLI（避开 `seg_export` 导入时重包 `sys.stdout` 的老坑），
临时沙箱 + 合成两页侧车，锁四条语义：① 抬头可参数化且缺省无残留 ② 术语可注入且格式正确
③ 术语源缺失/置空 → 退化不误导向 ④ 参数化没动到既有契约（`#S` 编号、跨页合并 `⋮`、manifest 结构）；
另加默认表落空必须告警、显式传空串不告警。
`run_all.py` **12 套件全部通过 ✅**。

### 实机验证（真侧车 + 真术语表，沙箱 inbox，不碰真 inbox）
```
$env:P2Z_PROJ=<temp>; python tools/seg_export.py --pages 2-3 --name probe \
    --doc "Wang 2026 (multi-agent consensus)" \
    --terms D:\zotero-pdf2zh\server\glossary\terms.csv --sidecar <真 latest.jsonl>
-> 34 段 / 9035 原文字符
-> [文档] Wang 2026 (multi-agent consensus) 第2-3页      (旧版此处恒为 Cactaceae)
-> 4. 术语统一：…Cactaceae=仙人掌科；…；selfing=自交；selfer=自交个体；outcrossing=异交。
```
**观察到但未改**：`terms.csv` 是跨项目累积的（既有多智能体/VLA 术语，也有仙人掌科术语），
外置后 payload 会带上整份表 —— 对单篇是噪声（多数条目不匹配即惰性）。
是否做"只注入文中真出现的术语"的按篇过滤，留待定夺。

### 仍挂账
- `render`/`gate` 未做真机端到端（有真实副作用）。
- `out/` 残留 `selftest.txt` / `roundtrip_test.txt`。
- 分支未 push。

## 2026-09-19 v28.2 文献页判据四处缺陷：翻前体检与翻后门禁的口径终于对上

### 背景
承接跨类型质检（`server/translated/` 全 13 篇 mono 产物），7 篇 FAIL 里"文献区汉化"
是一大簇。逐页取证后确诊：根因既不在渲染也不在模型，而在**同一个概念被两个工具
各自定义** —— 体检放行的页，门禁判死；门禁要保护的页，体检根本没识别。

本次把这条不变量写进代码与注释：

> **门禁判 FAIL 的文献页，翻前体检一定也认**（体检可以更敏感，不可更宽）。
> 否则流程必然出现"翻前没拦、翻后判死"。

### 缺陷与修法（每一条都有实机取证）
| # | 缺陷 | 实机证据 | 修法 |
|---|---|---|---|
| 1 | `pre_check` 只认编号制（行首 `[n]`） | Wang 2026 文献页 p14/p15 是 `R. Olfati-Saber and R. M. Murray (2004). ...` 体例，行首 `[n]` 恒为 0 → 拿不到 `skipLastPages`，文献区照常送翻 | 抄 `post_check` 的 `REF_AY` + `REF_AY_DENSITY=2.5` |
| 2 | payload 规则里**没有**"文献区原样保留"这一条 | 中段文献区（Melhani p43-44）不属文档后缀，`skipLastPages` 用不上，只剩译文侧自律这一条路 | `seg_export.RULES` 加第 6 条，两种体例口径与 `post_check` 同源 |
| 3 | `pre_check` 的 heading 是**整页子串匹配** | Wang p4（AY 密度 0.00、行首 `[n]` 0）被判文献页 → 多跳页 + 假「中」级提示 | 先收紧为"独立成行的标题行" |
| 4 | 收紧后仍有假阳性，且编号制方向反了 | 见下 | 删标题行判据；编号制补"行首占比"闸门 |

缺陷 3 的收紧实测后仍有漏网：16 篇原文对账跑出 10 篇口径不一致，逐页取证归成两类：

- **标题行判据净贡献为 0**。16 篇里它只产出假阳性 —— Brody p22（Author
  Contributions / 表格页）、Hagihara p8（Methods）、Mandujano p28（Acknowledgments）；
  真阳性 0（Wang p14 有标题，但密度 2.93 早已命中）。**有危害**：假阳性页若与真文献区
  连号，`skipLastPages` 会多跳一页正文。→ 整条判据删除（`REF_HEADING_RE` / `is_ref_heading`）。
- **编号制判据两头都错**。只数"行首条目 ≥ 3"会把正文页当文献页（Vaswani p10 =
  "Table 4 + 正文结尾"行首 4 条；CLAP p2 = teaser 页行首 3 条 —— 两页没有一条真文献条目）；
  而 `post_check` 只按密度，**长条目**文献页密度会掉到阈值下被整页漏掉
  （Jiang p7 = 15 条/6891 字 = 2.18；Kim p9 = 6 条/3392 字 = 1.77；Melhani p42 = 5 条/3491 字 = 1.43）。
  → 编号制改成两道闸门：**共享的密度判据**（原样保留，保子集关系）+ **行首 `[n]`
  占全部 `[n]` 的比例 ≥ `REF_ENTRY_RATIO`(0.5)** 且条数 ≥ `REF_ENTRY_MIN`(3)。

分离度实测（`REF_ENTRY_RATIO` 的取证基础）：

| 页 | 性质 | 行首 `[n]` | 全部 `[n]` | 占比 | 密度 |
|---|---|---|---|---|---|
| Vaswani p11 | 文献页 | 20 | 20 | 1.00 | 6.22 |
| Melhani p42 | 文献页 | 5 | 6 | 0.83 | 1.43 |
| Brody p14 | 文献页 | 16 | 18 | 0.89 | 5.30 |
| Jiang p7 | 文献页 | 15 | 15 | 1.00 | 2.18 |
| Vaswani p10 | 正文页 | 4 | 17 | **0.24** | 1.29 |
| CLAP p2 | 正文页 | 3 | 16 | **0.19** | 0.55 |
| Jiang p6 | 正文页 | 1 | 5 | 0.20 | 0.24 |

### 对账结果（16 篇原文，纯只读）
不变量违反 **0/16**。三篇真文献页被救回：**Jiang p7（`post_check` 原本整篇没有文献页）、
Kim p9、Melhani p42**；假阳性全部清零：Brody `[14..17,22]`→`[14..17]`、
Vaswani `[10,11,12]`→`[11,12]`、CLAP `[2,15,16,17]`→`[15,16,17]`、
Wang p4 消失、Exton / Hagihara / Mandujano 归零。
Jiang 现在拿到 `skipLastPages=1`（真文献页恰是末页）；Kim 正确降级为「中」级提示
（文献区 p9-12，其后还有附录，不该整段跳）。

### 改动文件
| 文件 | 改了什么 |
|---|---|
| `tools/pre_check.py` | 新增 `REF_DENSITY`/`REF_ENTRY_RATIO`/`ref_density()`/`ref_entry_ratio()`；`analyze_page()` 特征加 `ref_density`、删 `heading`；`_is_ref` 提为模块级 `is_ref_page()`（与 `post_check` 同名同位便于对账）；删 `REF_HEADING_RE`/`is_ref_heading()`；报告表格备注列改标"文献页" |
| `tools/seg_export.py` | `RULES` 加第 6 条"参考文献区不得汉化"（编号制 / 作者-年份制两种体例、整条原样保留） |
| `tools/tests/test_pre_check.py` | 新建（**30 例全 PASS**） |
| `tools/tests/test_seg_export.py` | 加 ⑧ 节 5 例，锁 payload 规则第 6 条（27 例） |
| `tools/tests/run_all.py` | 登记第 13 套件 |

### 测试
`test_pre_check.py` 直接 import（本文件不重包 `sys.stdout`），用**线上同一批函数**造页特征，
锁四条：① 正则口径 ② 密度闭区间下界 ③ `decide()` 两体例行为（编号制不回归）
④ 编号制两道闸门（正文页行首 4 条不得判文献页 / 长条目文献页密度 2.18 必须救回 /
只有构成后缀才推荐跳页）。
`run_all.py` **13 套件全部通过 ✅**。

### 仍挂账（本次**未改**，待定夺）
- **`post_check` 侧未镜像新判据**：它的编号制仍只有密度，会漏掉 Jiang p7 / Kim p9 /
  Melhani p42 三页。改它会**新增 FAIL**（Jiang、Kim 目前是 PASS）—— 属门禁语义变更，
  且会牵出新的内容矫正工作量，先不动。
- **小数编号制文献区**（Mandujano 的 `1. Bravo-Hollis ...`）两种工具都不认 →
  该篇文献区至今无保护。
- 对账时发现 `server/translated/Exton ... .pdf` 的 p10 是**中文页**（含"参考文献"标题）——
  `translated/` 下同名"原件"未必是英文原文，日后做对账要先验。

## 2026-09-19 v28.3 豆包桥的两处"换台电脑就废"：路径可移植 + 交件命名对齐

### 背景
本次的判断前提被明确了一条：**别人电脑上没有 Claude，桌面上只有豆包** —— 所以"内容矫正"
的闭环必须落在豆包软件 + MCP 桥内部。据此先回答"豆包有没有修改能力"，再把其中
**属于代码缺陷的两条**补掉（新增能力的部分另议）。

### 判断：豆包当前的"修改能力"边界（读 `tools/doubao_bridge.py` 全文）
| 工具 | 方向 | 边界 |
|---|---|---|
| `list_inbox` | 读 | 列 inbox 待译文件 |
| `get_payload` | 读 | 读 payload 原文（编号段落包） |
| `search_term` | 读 | 取术语证据 |
| `submit_result` | 写 | **只能往 `out/` 写一个新文件**（整篇重写，非增量修改） |

- **能**：把整篇 payload 重译一遍并交件。
- **不能改**：缓存库 / 侧车 / 渲染产物 / `terms.csv` —— 正式产物一个都碰不到。
- **不能看见问题**：桥里没有任何工具能读质检报告（`server/translated/review/`）、
  采纳台账（`logs/adopt/`）、缓存库、已渲染 PDF。

→ 结论：**现在的豆包只能"重抄一遍"，不能"矫正"，而且信息进不来、结论出不去。**
缺的不是模型能力，是**桥的能力**。

### 本次补的两条（都是代码缺陷，不是新能力）
| # | 缺陷 | 后果 | 修法 |
|---|---|---|---|
| 1 | `INBOX`/`OUTDIR` 焊死 `D:\zotero-pdf2zh\` | 别的用户把项目装在别的盘符 → 四个工具全部指向空目录 | 与 `adopt.py` / `seg_export.py` 共用 `P2Z_PROJ` / `P2Z_INBOX`（`OUTDIR = PROJ/out`），缺省值不变 |
| 2 | 交件命名与下游发现规则不一致 | 豆包按 inbox 原名交件 → 落成 `out/egophys2026.txt`，而 `adopt deliver` 缺省只认 `out/<name>.doubao*.txt` → **断在交件这一步** | 新增 `_result_name()`：落盘前统一规范成 `<stem>.doubao.txt`；已带 `.doubao` / `.doubao2` / `.doubao3` 的原样保留 |

缺陷 2 的取证：2026-09-19 豆包会话日志里，人给豆包的指令原文是
`用 get_payload 读 egophys2026.txt，按文件里开头的规则翻译，译完用 submit_result 存成
egophys2026.doubao.txt` —— 历史上没断，是因为**人记得叮嘱**。依赖人记得说，不是契约。

顺带重写 `submit_result` 的工具描述（告诉豆包"填原名即可，会自动规范"，并说明多轮定稿用
`.doubao2`），纯豆包环境下不再需要人额外叮嘱。

### 改动文件
| 文件 | 改了 |
|---|---|
| `tools/doubao_bridge.py` | 路径改环境变量驱动（`P2Z_PROJ`/`P2Z_INBOX`）；新增 `_result_name()`；`tool_submit_result` 落盘前规范化；工具描述与模块 docstring 同步 |
| `tools/tests/test_doubao_bridge.py` | 新建（**23 例全 PASS**） |
| `tools/tests/run_all.py` | 登记第 14 套件 |

### 测试
真拉子进程跑完整 JSON-RPC 往返：`initialize` / `tools/list` 四工具齐全 / `get_payload` 取原文 /
`submit_result` 落成 `payload_x.doubao.txt`（并断言**没有**裸落成 `payload_x.txt`）/
缺件走 `isError` 不崩 / 未知方法回 `-32601` / stdout 每行都是合法 JSON（日志不许混进协议通道）。
全程 `P2Z_PROJ=<临时沙箱>`，真项目 `inbox/`、`out/` 一个字节不碰。
（"通知必须不带 `id`"那条坑写在测试注释里 —— 带 id 会回 `-32601` 让后续读取整体错位一格。）
`run_all.py` **14 套件全部通过 ✅**。

### 仍挂账（"豆包矫正能力"的真正缺口，待定夺）
要让"只有桌面豆包"的用户完成内容矫正，桥还缺三个动作：
1. 读问题清单（哪篇 / 哪页 / 哪段 / 什么问题 / 原文片段 + 现有译文片段）；
2. 读单段的原文 + 译文 + 占位符图例；
3. **局部改写单段** → 写进待注入区（而不是整篇重交）。

对照：仓库里已有这套能力的是 `tools/strategist.py`（军师层，能写回缓存）—— 但它走的是
**API**（DeepSeek/方舟），不是豆包软件。也就是说"矫正能力"目前落在 API 侧，
不在桌面豆包侧；这正是要补的距离。

## 2026-09-19 v28.4 让豆包"看得见问题"：桥加两个只读报告工具（缺口 1/3 关闭）

### 背景
v28.3 判定的三缺口中，第 1 条「**读问题清单**」是"矫正是伪命题"的直接原因：
豆包面前只有 `inbox/` 里的一篇原文，它不知道上一轮哪一段被门禁判死。
本次只补这一条 —— 纯新增**只读**工具，不碰任何写路径。

### 取证：问题清单长什么样（先确认"清单真的存在且可机读"）
`server/translated/review/` 实测 **272 份**报告，六类：

| 类型 | 份数 | 判定行 |
|---|---|---|
| 翻译后质检 | 111 | `- **门禁判定: FAIL**（存在高危问题，需处理后交付）` |
| 翻译前体检 | 78 | `- 推荐 skipLastPages: **0**` |
| 审校报告 / 存疑清单 | 33 / 33 | — |
| 术语查证报告 | 14 | — |
| 解析沙盘 | 3 | — |

质检报告的正文就是问题清单本体，形如
`🔴 **[高]** 第11,12页：文献区汉化断言失败：原文的参考文献条目在译文中被翻译成中文`。
—— 之前 13 篇跨类型质检里那 7 篇 FAIL 的"哪页错、错在哪"，全在这个目录里。

### 改动
| 文件 | 改了 |
|---|---|
| `tools/doubao_bridge.py` | 加 `REVIEW` 常量（`P2Z_REVIEW`，缺省 `PROJ/server/translated/review`）；加 `_report_names()` / `_headline()` / `tool_list_reports()` / `tool_get_report()`；`TOOLS` 与 `_DISPATCH` 登记；启动日志带上 review 路径 |
| `tools/tests/test_doubao_bridge.py` | 23 → **40 例**（新增 ④ 节 17 例） |
| `tools/tests/run_all.py` | 无需改（第 14 套件已在册） |

两个工具：
- `list_reports(kind, limit)` —— 默认只看『翻译后质检』，**每条附一行判定**（FAIL/PASS 或推荐跳页数），
  按时间倒序，默认 20 条、上限 100，被截断时回一句"另有 N 份更早的"。
- `get_report(name)` —— 读全文（即问题清单）。`name` 从 `list_reports` 结果原样复制。

### 三个设计取舍
1. **默认只列门禁报告**：272 份全列会撑爆豆包的上下文；而"哪篇没过"在门禁报告里。
   一次调用就能定位，再用 `get_report` 读细节。
2. **判定行随列表返回（`_headline`）**：只读头部 6KB 抽 `门禁判定` / `推荐 skipLastPages`。
   否则豆包得逐份读 111 个文件才能知道该修哪篇 —— 这个工具就等于没用。
3. **`get_report` 不套 `_safe_name`**：报告名含中文与空格，会被它的
   `[A-Za-z0-9_\-. ]` 白名单整体挡掉。改成"**只认目录里真实存在的名字**"
   （精确命中，否则唯一子串）—— 路径由 `os.listdir` 的目录项拼出，
   所以 `..\..\x.md` 这类名字天然匹配不上，不构成越权。已加用例锁死。

### 测试
`test_doubao_bridge.py` ④ 节（真沙箱 `review/` 夹具）：判定行抽取（FAIL/PASS/推荐跳页）、
默认只列门禁报告、`kind` 过滤、`limit=1` 截断提示、子串定位、空 name / 歧义 / 不存在 /
路径穿越四类错误不崩、**报告目录缺失时只报空不崩**（首跑场景）。
往返段新增一条：**不设 `P2Z_REVIEW`，报告必须仍能从 `<PROJ>/server/translated/review`
找到** —— 这条锁的是与 `post_check.review_dir()` 的路径同源。
实机取证（真目录 111 份质检报告）：最近 8 份的判定行全部正确抽出。
`run_all.py` **14 套件全部通过 ✅**。

### 边界（本次刻意没做）
- 仍然**只读**：豆包的写能力没有扩大，还是只能往 `out/` 交件。
- 缺口的第 2、3 条未动：
  2. 读单段的原文 + 译文 + 占位符图例；
  3. **局部改写单段** → 写进待注入区（而非整篇重交）。
- 因此现在的闭环是"**看得见问题 + 整篇重交**"，还不是"**局部矫正**"：
  豆包能照着清单把整篇再交一版，但没法只动第 11 页那 19 条文献。


## 2026-09-19 v28.5 渲染前预检：把「文献区禁汉化」这道闸门从 PDF 之后挪到 PDF 之前

### 背景（用户提的问题）
> 「你这个用户判断是在出 PDF 之后还是？PDF 之前。如果是出 PDF 之后，那就会造成一种现象，
> 就是结果不一定好，然后用户要删掉再重新给我生成，这样还浪费时间。就是在这个过程中，是否有预览这一些。」

顺着这个问题把 PDF 线两边的门禁数了一遍，答案是**内容质量判定确实在 PDF 之后**：

| 侧 | 闸门 | 查什么 |
|---|---|---|
| 产出前 | `pre_check` 翻前体检 | 文献页/扫描页/字体风险（**结构**） |
| 产出前 | `deliver` 段号集合 + `⋮` 断点对账 | 结构 |
| 产出前 | `import` 编号/`⋮`/字形回锚对账（须 PASS 才注入） | 结构 |
| 产出前 | `inject --dry` 写库前演算 | 结构 |
| **产出后** | `verify_render`（13 项版面断言） | **版面** |
| **产出后** | `post_check`（汉化率/引用完整性/占位符/文献区） | **内容** |

13 篇跨类型质检里那 **7 篇 FAIL 全部来自出 PDF 之后的第二道** —— 也就是说：
用户要的"结果不好就别出 PDF"，在 v28.5 之前是做不到的。

### 四条断言能不能前移到 PDF 之前（逐条判）

| 断言 | 能否前移 | 理由 |
|---|---|---|
| 文献区禁汉化 | ✅ **能** | 纯内容判据，跑在 `imported.json` 上即可 |
| 汉化率 | ⚠️ 能但不做 | 按页聚合，而 `imported.json` 的页码是**原文页码**，跳页/合页会错位 |
| 占位符残留 | ❌ 不能 | `{vN}` 是**渲染期**才被替换的，回锚后必然还在 |
| 引用完整性 | ❌ 不能 | 它查的正是"渲染后 `[n]` 有没有被版面吃掉/拆散"（Brody 220→88） |

**所以本次只前移一条** —— 恰好就是 5/7 篇 FAIL 的那条（文献区汉化）。

### 判据不是新写的：同源重构
前移最大的风险是"翻前放行、翻后判死"（两边口径漂移）。避免办法是**不写第二份判据**，
把 `post_check.py` 里的实现抽成可复用函数，让两条路跑同一段代码。

| 文件 | 改了 |
|---|---|
| `tools/post_check.py` | 抽出 `text_features(text, page_no)`（原 `page_features` 的内联逻辑）与 `check_ref_zh(orig_feats, trans_feats)`（第 4 断言整体搬出，`findings.append` 改 `return`）；`page_features` / `run_checks` 改为调用它们。**行为零变化** |
| `tools/pre_render_check.py` | **新增**：`load_trans_pages()` 读 `imported.json`、`trans_features()` / `build_features()` 拼译文页、`check_pages()` 调 `PC.check_ref_zh`、CLI `--original/--imported/--quiet` |
| `tools/adopt.py` | `stage_render` 在 `force_rerender` 之前插预检；FAIL 就 `mark(render, failed, pre_render="FAIL")` 并退出，**不渲染**；成功路径记 `pre_render="PASS"` |
| `tools/tests/test_pre_render_check.py` | **新增**，24 例（含④同源对账 4 例） |
| `tools/tests/run_all.py` | 登记第 15 套件 |

关键数据形态（实测 `out\wang2026.imported.json`，273 键）：
键 = `"<页码>#<段序>"`（页码 1 基、段序 0 基），合成 `"14#9"` / `"1#1"` 这类；
回锚后的译文**自带段→页映射**，这就是能按页算特征的原因。

### 证据（实机）
- **渲染前**：`pre_render_check.py --original <Wang原文> --imported out\wang2026.imported.json`
  → `原文 19 页 / 译文覆盖 19 页 / 判据 1 条` → `✅ PASS —— 2 个文献页的条目均保持原样`
- **渲染后**：同篇的 `翻译后质检_20260919_112554_Wang....md` 第 15 行
  → `✅ 文献区禁汉化检查通过：2 个文献页的条目均保持原样`
- **两边"2 个文献页"完全一致** —— 同源成立。
- 另：对 Vaswani 真机复检重构后的 `post_check`，输出与重构前基线**逐字吻合**
  （第 11 页 19 条、第 12 页 16 条，`门禁判定: FAIL`）。

### 设计取舍
1. **宁可漏，不可误杀**：`imported.json` 是按段拼回的，行首 `[n]` 判据按行锚定，
   段内第二条起的条目可能匹配不到 → 本工具比 `post_check` **略宽**。
   方向是安全的：漏掉的由阶段 6 的 `gate` 兜住，不会把好的判死。
2. **缺页一律当空串**：被 `skipLastPages` 跳掉的文献页本来就没有译文（那页保持原文英文），
   空文本 `chars=0`、`ref_zh_blocks=0`，在 `check_ref_zh` 里既不构成汉化也不影响结论 ——
   与 `post_check` 口径一致。
3. **没有 `imported.json` 时拒绝静默跳过**：`adopt render` 找不到回锚产物会直接报错并提示
   先跑 `adopt import`；只有显式 `--force` 才跳过（运维单步重跑场景）。

### 边界
- 前移的**只有**文献区禁汉化这一条；汉化率/占位符/引用完整性仍留在 `gate`（原因见上表）。
- 预检**依赖方案 A 的产物**（`imported.json`）。老链（硅基）的译文没有这个文件，
  所以那 5 篇历史 FAIL **跑不了预检** —— 它们属于缓存手术范畴，见下节挂账。
- 预检省掉的是"判 FAIL 那一轮服务端渲染"，不是"所有渲染"。

### 测试
`test_pre_render_check.py` 24 例：① `imported.json` 解析 5 例（键格式/段序乱序/非法键/单段页/空 json）；
② `trans_features` 对齐 3 例（等长/页码 1 基/缺页补空）；③ 编号制 3 例、③b 作者-年份制 4 例、
③c 非文献页不参与 2 例、③d 跳页不误判 1 例；**④ 同源对账 4 例**（两种体例 × PASS/FAIL，
同一份内容走 `pre_render_check` 与走 `post_check.run_checks` 必须同判）；⑤ CLI 兜底 2 例。
`run_all.py` **15 套件全部通过 ✅**（另修正 docstring 里两处过时例数：`test_post_check` 31→49、
`test_doubao_bridge` 23→40）。

### 挂账（本次没做，已在待办里）
- ② 5 篇文献区汉化的**缓存手术**（用户已拍板"机械还原"）：侧车 `latest.jsonl` 已被覆盖，
  只能按**内容**匹配缓存行（源文含行首 `[n]` 条目 + 译文是中文），做完逐篇 render/gate 验收。
- 「豆包矫正能力」剩余缺口 2/3（读单段三件套 / 局部改写写回）未获授权开工。


## 2026-09-19 v28.6 豆包桥补缺口：读回自己上一版交件（+ 交件时的改动量自检）

### 背景（用户拍板"按方案 C 先补桥的缺口"）

触发是一次真跑。wang2026 走方案 A 到 `import` 阶段被门禁整篇退回：

```
FAIL p11#9  高价值字形未回锚: {v32}='λ=κi0,(43)'
FAIL p11#27 高价值字形未回锚: {v78}='(44).✷'
FAIL p13#16 高价值字形未回锚: {v73}='L,αi'
FAIL p17#17 高价值字形未回锚: {v51}='.3),Φt(k)≤'
```

四处是**同一类错**：中文被插进了"公式片段 + 紧邻标点/编号"的一整块里。对照上一轮通过
`import` 的 doubao3，通过写法有共同点——**整块原样搬运、中文一律放块外**，宁可别扭甚至
冗余（多一个 `L,`）也不动块内一个字符。

但真正要记的不是这四处，而是**它为什么会发生**：

| 事实 | 实测数据 |
|---|---|
| doubao4 不是"修正版"，是整篇重译 | 与 doubao3 逐段比对：**266 段里 206 段不同**（连 `#S3` 的作者单位都从英文变成了中文） |
| 豆包并非不听话 | 指令里写了"报告没提的段落保持原样"；它无从执行 |
| 因为桥只给了它半个视野 | 能读 `inbox/`（原文）、能读 `review/`（报告），**读不到 `out/`（自己上一版）** |

结论：**"最小改动"在纯豆包环境下根本不成立** —— 看不到上一版，每一次"按报告改"都只能
退化成从英文原文重译一遍，而重译每次都重新掷一次字形块/编号的骰子。这就是 v28.4 那节
记的"缺口 2/3"的真实代价，现在有实测数据了。

### 顺带查明：v28.4 的两个工具为什么"没生效"

豆包当时回报"只有四个工具"。**不是缺陷，是没重启**：

| 项 | 时间 |
|---|---|
| 桥进程 PID 11900 启动 | 2026-09-19 **00:15:28** |
| `tools/doubao_bridge.py` 最后一次改（v28.4） | 2026-09-19 **11:22:43** |
| 提交 `4e8ec1a` | 2026-09-19 11:24:29 |

STDIO MCP 的桥是豆包在连接建立时拉起的**常驻子进程**，`tools/list` 只在握手里问一次 ——
进程不重启，工具清单就永远停在上线那一刻；改代码不会热更，新开一个对话也可能复用同一个
进程。**处置：注册表里改完桥必须完全退出豆包再开**（含托盘/后台），并用
"列出你当前可用的工具"验一次。

### 改了什么

| 文件 | 改了 |
|---|---|
| `tools/doubao_bridge.py` | 新增两个**只读**工具 `list_results(name, limit)` / `get_result(name)`（指向 `out/`）；`submit_result` 回执追加"与上一版的改动段数"；模块抬头六个工具 → 八个 |
| `README.md` | 桥的工具清单与说明同步（并写清 `list_reports` 给"哪儿错了"、`list_results` 给"上一版长什么样"） |
| `tools/tests/test_doubao_bridge.py` | 新增 ⑤ 节；往返段加两条请求（二轮交件 + `list_results`） |
| `tools/tests/run_all.py` | 无需改（第 14 套件已在册） |

两个工具：
- `list_results(name, limit)` —— 列 `out/` 下已交译文，按时间倒序，**每份附段数与改于何时**。
  `name` 传论文名只看该篇（大小写不敏感），留空列全部；默认 20 条、上限 100，截断时回一句"另有 N 份"。
- `get_result(name)` —— 读回全文（通常就是豆包自己上一轮的稿子）。`name` 从 `list_results` 原样复制。

### 四个设计取舍

1. **只认 `*.doubao*.txt`（`_RESULT_FILE`）**：`out/` 里还堆着 `seg_import` / `seg_inject` 的
   中间产物（`.imported.json` / `.merged.txt`）。把它们当"上一版交件"甩给豆包，比不给更糟。
2. **列交件必须带段数（`_seg_count`）**：同一篇在 `out/` 下有多轮（`.doubao` / `.doubao2` /
   `.doubao3` / `.doubao4`），光看字节数分不清哪份是全的、哪份是残件 —— 而"该在上一版基础上
   改"的前提，正是先认出哪份是全量上一版。
3. **`get_result` 与 `get_report` 同一套取名规则**：不套 `_safe_name`（论文名可能带空格），
   改成"**只认目录里真实存在的名字**"（精确命中，否则唯一子串）—— 名字必须命中
   `os.listdir` 的某一项，所以 `..\..\x` 这类天然匹配不上，不构成越权。已加用例锁死。
4. **交件回执附改动量（`_diff_line`，阈值 30%）**：这是本次最省力也最关键的一处 ——
   "矫正"和"重译"从交付文本上**看不出区别**，直到门禁把整篇退回。给一行
   `与上一版 X 相比: N/M 段有改动`，豆包交完立刻能自己发现"改动面过大 = 我在重译"。
   只做提示：任何异常都不影响交件本身。阈值取 30% 是因为"只修被点名的几处"正常远低于此，
   而像 206/266 那种重译必然远超。

### 测试与验证

`test_doubao_bridge.py` **61 例全过**（原 40 例）：

- 往返段（真子进程 STDIO）：八工具齐全、九条请求都有应答；首轮交件**不带**差异行（还没有上一版）、
  次轮带 `与上一版 payload_x.doubao.txt 相比: 1/2 段有改动` 并正确触发"在重译"提示。
- ⑤ 节（沙箱 `out/`，显式 `os.utime` 定死 mtime，不靠写入顺序）：中间产物不混入、段数随列表返回、
  时间倒序、篇名筛选（含大小写）、`limit` 截断提示、子串定位、空 name / 歧义 / 不存在 /
  路径穿越 / 中间产物五类错误不崩、`out/` 缺失时只报空不崩；
  另锁**小改动不报警**（1/10 段）—— 否则提示会被当噪音忽略。
- `run_all.py` **15 套件全部通过 ✅**。

### 边界（本次刻意没做）

- 仍然**只读**：豆包的写能力没有扩大，还是只能往 `out/` 交件。
- 缺口 2、3 仍未动：
  2. 读单段的原文 + 译文 + 占位符图例；
  3. **局部改写单段** → 写进待注入区（而非整篇重交）。
- 所以现在的闭环是"**看得见问题 + 看得见上一版 + 整篇重交**"，比 v28.4 的
  "看得见问题 + 整篇重抄"进了一步，但**仍不是"局部矫正"** —— 只是这一轮的重译有了
  可对照的基线，且越界会被当场点破。

### 挂账

- wang2026 那四处公式字形块：等豆包重启（拿到八工具）后按新指令交 `doubao5` 重跑
  `deliver → import`；`inbox/` 前的旧载荷已留备份（`*.bak-preexport`）。
- **建议补的一条通用规则**（等拍板）：`seg_export.py` 的通用 6 条模板在 v28.1
  （`d21a7b7`「去掉硬编码的论文先验」）里删掉了"公式片段逐字符照抄"整段，而新规则 5
  "段内语序可按中文习惯调整"正好鼓励了这次的错法。建议以**通用表述**补回（不硬编码
  具体例子，避免重新引入论文先验）："公式片段连同紧邻标点/编号是**一整块**，块内逐字符
  照抄，中文一律放块外"。

## 2026-09-19 v28.7 采纳管线 render 的产物解析：文件名带空格时台账被截断（+ wang2026 doubao5 交付完成）

### 背景（doubao5 一路绿灯，卡在最后一步）

豆包按 v28.6 的新规矩只改 4 段（266 段里其余 262 段一字未动），`deliver` → `import`
（回锚 273 段，原 4 处 FAIL 消失）→ `inject`（更新 552 行）→ `render` 全过，却在 `gate` 上
直接拒绝执行：

```
[adopt] 拒绝执行: 找不到 mono 译文 PDF; 用 --mono 指定
```

### 根因：`\S+` 遇到带空格的文件名

`stage_render` 从 `force_rerender.py` 的**人读日志**里回收产物路径，用的是 `\S+`：

```python
prods = re.findall(r"产物: (\S+\.pdf)", out)
mono = re.findall(r"(\S+-mono\.pdf)", out)
```

而这篇的原文文件名带空格（`Wang 等 - 2026 - Differentially Private Consensus for Time-Delay
Multi-agent Systems.pdf`），`\S+` 只能吃到空格后的最后一段 —— 台账把 mono 记成了
`Systems-mono.pdf`：

```json
"render": { "pdf": "D:\\...\\Wang 等 - 2026 - ... Multi-agent Systems.pdf",
            "mono": "Systems-mono.pdf",   <-- 不存在的路径, 却是非空值
            "pre_render": "PASS", "state": "ok" }
```

`stage_gate` 取路径的顺序是 `args.mono or 台账 render.mono or latest_mono()` —— 那个**截断的
非空假路径正好绕过了 `latest_mono()` 兜底**，于是拿着不存在的文件报"找不到"。（真实产物一直在
`server/translated/` 下躺着，渲染日志也写着"成功"。）

这个 bug 对**不带空格的文件名**永远隐形：能过 gate、能交付，只是台账里躺着一串假路径，等哪天
有人拿台账去核对产物就露馅。

### 改了什么

| 文件 | 改了 |
|---|---|
| `tools/adopt.py` | `stage_render` 产物解析改为**按整行取值**（`(?m)^\s*产物:\s*(.+\.pdf)\s*$`），并在多产物里优先取 `-mono.pdf` |
| `tools/tests/test_adopt.py` | ⑭ 用例改成**文件名含空格 + 两个产物**（dual 在前、mono 在后），把这个 bug 钉死；抬头第 ⑧ 条同步 |
| `tools/tests/test_failure_brief.py` | 顺带修一处与真实日志未隔离导致的**假红**（见下"附带"节）——只改测试 |

两处一起改而不是只放宽正则，是因为第二处同样会出事：`force_rerender` 回传的是**两个**产物
（实测 mono 在前、dual 在后），原来"一律取第一个"语义上只是**碰巧**对 —— 顺序由服务端的
`filePaths` 决定，gate 要的是 mono，不是"第一个"。

### 测试与验证

- `test_adopt.py` **28 例全过**（⑭ 两例：含空格路径解析 + 耗时）。
- 实跑 `adopt.py render --name wang2026`：渲染前预检 PASS（2 个文献页）→ 服务端 48 秒 →
  台账 `render.mono` 记下**完整路径**（修复在真实日志上生效，不再需要 `--mono` 兜）。
- `adopt.py gate --name wang2026 --expect "中国地质大学未来技术学院" "文中引入了一种新的邻接定义"`
  → **双 PASS**，六阶段全绿，`wang2026 doubao5` 交付完成。
- **落页取证**（pypdf 直读 mono PDF，复用 `verify_render.load_pages`）：4 处修复串全部命中
  —— `由引理 2.2 取` @p11、`(44).` @p11、`L,α` @p13、`(A.3),` @p16/p17；3 处旧错写法
  （`(44) 得出` / `λi，αi :=2 Re` / `对 k≥t 有`）全部消失。
  （`应用引理 2.2` 在 p6 另有一处命中，属另一句话，非本次目标段 `#S167`。）

### 附带修掉一处"测试假红"（`test_failure_brief.py`）

上面那次 `run_all` 先报了一次红：`test_failure_brief.py` 的 [12] 期望 `errorType` 是夹具里的
`AttributeError`，实得 `CalledProcessError`。**不是本轮的改动引起的，是这套测试与真实部署没隔离**：

- `server._failure_log_paths()` = `PDF2ZH_FAILURE_LOG`(测试钩子) **+ `<项目>/logs/{server_err,server_out}.log`**；
- 后两者带**新鲜度闸**（120 秒内写过才认）。而 12:47 刚跑完 `render`，真日志正在新鲜窗口里
  —— 于是夹具的 `CONSOLE` 之后又拼上了真日志的尾部，"最后一段 traceback"成了那次真翻译的输出
  （`[前瞻] 段30 … @ pdffont.py:1163`），抽到的 `errorType` 自然不是 `AttributeError`。

任何"刚跑过翻译/渲染就立刻跑 run_all"的人都会撞上，与本次修复无关。处置（**只改测试**）：
测试期间把 `server._FAILURE_LOG_NAMES` 临时清空，只认夹具日志，退出前还原；抬头写明隔离原因。
**验证**：先把两份真日志的 mtime 触成"现在"（复现原假红条件），再跑 —— **32 PASS / 0 FAIL**；
随后 `run_all.py` **15 套件全部通过 ✅**。

### 边界

- 只动 `stage_render` 的**回收解析**，不改渲染本身、不改门禁判据。
- 台账里那份截断的旧记录（`Systems-mono.pdf`）已被重跑覆盖为完整路径。

## 2026-09-19 v28.8 两处收尾（用户拍板）：台账失败字段不残留 + 规则 7 补回"公式片段整块照抄"

### 一、台账里"ok 却带着失败原因"

v28.7 交付 wang2026 时看到台账自相矛盾：

```json
"import": { "state": "ok", "imported": "...wang2026.imported.json",
            "source": "...wang2026.doubao5.txt",
            "reason": "seg_import 退出码 1"   <-- 上一轮失败的遗物
            "at": "2026-09-19 12:43:05" }
```

根因：`mark()` 是 `rec.update()` —— 失败时写的字段，本阶段重跑成功后不会被清掉。产物解析
那类 bug 修好之后，这类"台账说谎"就是下一个会咬人的：交付台账是给人核对产物用的。

**改法**（`tools/adopt.py`）：新增模块常量 `_FAIL_ONLY = ("reason", "missing", "extra", "detail")`
（全部只在 `mark(..., "failed")` 时写），`mark()` 在 `state == "ok"` 时把它们剔除。
作用范围仅限"失败专属"字段，成功态自己的字段（`sha1`/`n_seg`/`rows`/`mono`/`verify`…）原样保留。

**测试**：`test_adopt.py` 新增 ⑤b —— `deliver` 先缺段失败（`missing`/`reason` 落账），补全后
重跑成功，断言这两个键**消失**。**29 例全过**。
**实跑**：对已交付的 wang2026 重跑一次 `import`（同样回锚 273 段 PASS，`imported.json` 内容不变），
台账里那条 `reason` 随 `mark(..., "ok")` 消失 —— 交付台账现在自洽（六阶段全 ok，无失败遗物）。

### 二、规则 7：公式片段整块照抄（v28.6 挂账项关闭）

wang2026 那 4 处字形块 FAIL 的直接成因，是 payload 规则里没有一条约束"公式片段 + 紧邻标点/编号
是一整块"。v28.1（`d21a7b7`）删掉硬编码论文先验时，把原来逐字举例子那段一起删了，而留下的
规则 5"段内语序可按中文习惯调整"恰好**鼓励**了这种错法。

**改法**（`tools/seg_export.py` 的 `RULES`）新增第 7 条，**纯通用表述、不举任何论文的例子**
（避免重新引入论文先验）：

```
7. 公式片段（变量、符号、运算符、上下标及其紧邻的标点或编号连成的一串）是一整块：
   整块逐字符照抄，块内不得插入中文、不得增删或改动任何一个字符；要调整语序就把中文放在块外。
   宁可读起来别扭、甚至略有冗余，也不要拆开这块去"理顺"。
```

放在第 6 条之后（= 末条），不为它重排既有编号 —— 编号被 `test_seg_export` 与术语行共用。

**测试**：`test_seg_export.py` 新增 ⑨ 三条 —— 规则文本在位 / 语序让位于整块 / **不含别篇例子**
（断言 `(A.3)`、`λ=κ` 不出现在 payload 里）。**30 例全过**；`run_all.py` **15 套件全部通过 ✅**。

## 2026-09-19 v28.9 侧车按文档归档：换论文不再覆盖，`export --pdf` 自动认领

### 背景：一个"必须先做"的缺口，而不是可选优化
侧车 `~/.cache/pdf2zh/segflow/latest.jsonl` 是**全局单文件**，`receive_layout` 在每篇的
首页截断它 —— 换论文就覆盖。今天做 Vaswani 样板时正中这一枪：`latest.jsonl` 里已是
wang2026，**Vaswani 的侧车已经没了**，为了拿它只能对整个文档白烧一次渲染（398 秒），
再人工把 `latest.jsonl` 另存为 `segflow/vaswani2023.jsonl`。全项目目前只有 3 份人工归档，
漏一份、记错一份，"导出别人的载荷"这件事就会静默发生——

而它**不会在导出时暴露**（段号照样连续、库内照样命中），要等 `import` 回锚或落页才现形。
剩 4 篇文献区汉化都要走这条链，所以先把它修掉。

### 改动（三个文件，同一件事的三段）

| 文件 | 改了什么 |
|---|---|
| `patches/pdf2zh_converter.py`（+ venv 镜像） | 新增 `import hashlib` 与 `_segflow_paths()`：除 `latest.jsonl` 外，再按 **PDF 内容散列**落一份 `<segflow>/pdf-<md5[:16]>.jsonl`；写段改双写 |
| `server/server.py` | `translate_pdf` 起 pdf2zh 子进程前 `os.environ["P2Z_DOC_PDF"] = os.path.abspath(input_path)` |
| `tools/adopt.py` | 新增 `pdf_md5_16` / `archived_sidecar` / `resolve_sidecar`；`export` 新增 `--pdf`；`--force` 移入 `common()`；台账加 `sidecar_source` |

三条设计取舍（都不是随手选的）：

1. **身份取 PDF 内容散列，不取文件名。** 同一篇改名、重下，仍是同一份归档件；
   不同篇内容不同，天然不会互相覆盖。
2. **路径由 `P2Z_DOC_PDF` 环境变量传入，不由转换器自己找。** 上游 `TranslateConverter`
   的签名里**没有输入路径**，转换器拿不到它；子进程默认继承父进程环境，故在
   `server.py` 起子进程前设一次即可。唯一假设是"同时只翻一篇"，与既有的
   **共享日志独占闸**是同一假设。取不到就**只写 `latest.jsonl`**（行为退回 v28.8 前，
   不阻断翻译）——归档是增强，不该成为翻译的新失败点。
3. **`--pdf` 给了却找不到归档件时"拒绝执行"，不回落 `latest.jsonl`。**
   `latest.jsonl` 里躺的是"最近翻过的那一篇"，拿错它会安静地导出一份**别人的载荷**，
   回锚还会 PASS —— 这是最难发现的一类静默错。宁可停机要求显式交代
   （报错里给两条路：a) 先渲染一次补上归档件；b) `--sidecar` 显式指一份并自己确认）。

散列只算一次（`_segflow_doc_path` 缓存）：同进程内环境变量再怎么变，结果不漂移。

### 测试
- 新增 `tools/tests/test_segflow_archive.py`（**12 例全 PASS**，已登记进 `run_all.py` 第 16 套件）：
  无 `P2Z_DOC_PDF` → 只有 latest / 归档件路径形态 / **与 `adopt.pdf_md5_16` 同一口径**（两端口径
  一旦漂移，`export --pdf` 会"找不到归档件"而拒绝执行 = 整条管线卡死）/ 同内容异名同一份 /
  异内容不互相覆盖 / 源不存在或源是目录 → 退化 None 且不抛 / 结果缓存不漂移 / 目录自动创建 /
  **往返：converter 认领的路径 `adopt.archived_sidecar` 认得出同一份**。
- `tools/tests/test_adopt.py` 新增 ㉑ 节 8 条：没给 `--pdf` 仍用 latest / 给了且归档件在 →
  认领归档件 / 显式 `--sidecar` 优先于 `--pdf` / **无归档件 → 拒绝（不回落）** / PDF 不存在 →
  拒绝并点名 / 无归档件时导出被拒（不静默换侧车）/ 导出确实用了归档件且来源入账。**36 例全过**。
- `run_all.py` **16 套件全部通过 ✅**（镜像同步 9 PASS：`pdf2zh_converter.py` 新哈希 `4B6D139C`）。

### 实机验证（真渲染 + 真缓存库）
```
① 重启服务端(旧实例无 v28.9 注入)   kill 两个 server.py -> launch.ps1 -> /api/history == 0 ✓
② adopt render --name vaswani2023 --pdf "<Vaswani>.pdf"
     预检 PASS(2 个文献页) -> 耗时 48 秒 -> mono 产物 ok
③ 归档件落盘: segflow/pdf-18e1b007a1dab45b.jsonl       ← 与 md5(PDF)[:16] 一致(预测值)
     与 latest.jsonl SHA1 完全相同 (BDAD6453…)          ← 双写逐字节一致
④ 仅用 --pdf 导出(不再人工归档):
     adopt export --name v28_9_probe --pages 11-12 --pdf "<Vaswani>.pdf"
     -> [adopt] 侧车: …\pdf-18e1b007a1dab45b.jsonl (按文档归档件)
     -> 35 段 / 库内命中 35 行 / doc_fp=docsummary:e970641d691330dd:bdacb9c5
        (与 13:05 那次 run 的 doc_fp 完全相同 = 文档身份未漂移)
     -> 台账 export.sidecar_source == "按文档归档件"
⑤ 重渲染使 13:09 的 gate 时序失效, 复跑 gate: 3/3 期望串 + 引用 91/89 + 无 {vN}
     + 文献区禁汉化 PASS -> 双 PASS（台账时序恢复一致: render 13:17 -> gate 13:19）
```
探针产物（`inbox/v28_9_probe.*`、`logs/adopt/v28_9_probe.json`）验证后已清理。

### 挂账
- **v28.9 之前翻的文档没有归档件**。剩 4 篇若已在旧管线下渲染过，`export --pdf` 会拒绝执行
  并给出两条路 —— 走 a) 对该篇跑一次渲染即可自动补上（缓存已热，成本≈一次重渲染）。
- `segflow/` 下的 `pdf-*.jsonl` 会随篇数累积，**暂无清理策略**（单份 ~100KB 量级，先不处理）。
- 归档件与 `latest.jsonl` 是同一份内容的两个副本，**没有一致性校验**：若只渲染到一半，
  两者可能不等长（此时以归档件为准，因为 `export` 只认它）。


## 2026-09-19 v28.10 侧车选页口径：`--pages` 改为真实 PDF 页码

### 背景：一次"看起来完全正常"的取错页
做剩 4 篇文献区汉化的第一篇 Melhani（50 页 PDF）时，按质检报告写的「第 43,44 页」执行
`adopt export --pages 43-44`，导出 **10 段**，跑完 `import` 回锚还 PASS —— 但它取回来的
是**第 31,32 页的正文**，不是文献条目。没有任何一处报警：

- 段号照样从 #S1 连续编号；
- 库内照样命中（命中 12 行，比导出段数还多，也没人会觉得不对）；
- `doc_fp` 照样正常生成。

只有把载荷点开逐段读，才会发现"这根本不是文献区"。**静默错，最贵的一种。**

### 根因：`page` 是回调计数，不是页码
侧车每行 = 一次 `receive_layout` 回调，而**图形对象也会各占一行**
（`begin_figure` / `end_figure` → `receive_layout(fig)`）。所以：

| 字段 | 真实语义 |
|---|---|
| `page` | `receive_layout` 的**回调计数**（图形回调也占号） |
| `pageid` | `LTPage.pageid`（0 基）——**这才是真实 PDF 页码**（图形继承所在页的 pageid） |

Melhani 图多，50 页的文档侧车有 62 行，整体漂 12：真实第 43,44 页对应侧车第 55,56 条。
v28.10 之前 `load_pages` 按 `o["page"]` 匹配，于是取回侧车第 43,44 条 = 真实第 31,32 页。

### 改动（`tools/seg_export.py`）
- 新增 `true_page(o)`：`pageid + 1`；记录里没有 `pageid` 时回落 `None`。
- `load_pages(pages_want, sidecar)` 重写：按**真实页码**选页，返回 `(记录表, 缺页列表)`；
  **老侧车（无 `pageid`）退化为按 `page` 匹配**，与旧行为一致，不误伤已归档件。
- 缺页不再静默跳过：`main()` 打印 `侧车缺页: [...]` 并返回 1。
- 跨页续接的**相邻性判据**改按真实页码（用回调计数判，会把"同页的图形记录"当成隔页，
  也会把"隔着一页"当成相邻）。
- 控制台位置标注改印真实页（`p43#1`），`--pages` help 与文件抬头同步说明口径。

**一条刻意不动的地方**：返回的记录表**仍以侧车原 `page` 为键**。那是**载荷内部坐标** ——
`manifest.json` / `imported.json` / `seg_inject.py`（`pages[p["page"]]["segs"][seg]`）
全链路沿用它。v28.10 只改"用户看得见的页码口径"，不动"载荷内部坐标"，
因此 `seg_import` / `seg_inject` / `imported.json` 一处都不用改。
附带好处：`--pages` 现在与翻前体检、翻后门禁报告（都写真实页码）**同一口径**，
用户照抄报告里的页码即可。

### 测试
- `tools/tests/test_seg_export.py` 抬头「锁五条语义」→「锁六条语义」，新增 ⑩ 节 10 条断言。
  合成侧车 `pid_side`（8 行，含图形回调与真实页重复）：`--pages 2` 取回第 2 页的**两条**记录
  （图形回调那条也在内）、不越界取第 3 页、`--pages 3` 取回"计数坐标 4"的记录、
  控制台印 `p3#0` 而非 `p4#0`、**manifest 仍记侧车坐标 `page==4`**（锁住内部坐标不动）、
  `--pages 4` → `侧车缺页: [4]`、`--pages 5-7` 同真实页不合并 / 真实页相邻仍合并。
  **39 PASS / 0 FAIL**。
- `run_all.py` **16 套件全部通过 ✅**。

### 实机验证（Melhani，真侧车 + 真缓存库）
```
修复前: adopt export --name melhani2026 --pages 43-44 --pdf "<Melhani>.pdf"
        ->  10 段(正文!) / 库内命中 12 行 / doc_fp=docsummary:a98f55968d146ed7:34c7d7a6
        （侧车 page 43/44 实为真实第 31/32 页正文; 真实第 43/44 页 = 侧车第 55/56 条, 漂 12）
修复后: adopt export --name melhani2026 --pages 43-44 --pdf "<Melhani>.pdf" --force
        ->   5 段 / 8180 原文字符 / 库内命中 5 行
            #S1 p43#1  4330ch  尾: …cs 25(15)(2009)1923–1929.
            #S2 p44#1  3229ch  尾: …Working Paper Series (2020).
            #S3 p44#2    47ch  尾: …emma 1(Determinant of O9)
            #S4 p44#3   367ch  尾: …hich we track at the end.
            #S5 p44#4   207ch  尾: …ows 1,2,3(the rows of O0)
```
（归档件 `pdf-9ea2413e206fbf7e.jsonl` 由本会话对该篇重渲染铸出，`force_rerender.py` 耗时 265 秒。）

### 挂账
- `tools/backfill_pages.py` 的 `zero_pages_from_sidecar` **仍用 `o["page"]` 当输出页码** ——
  同类漂移的潜在点位。当前对外表现为"零可译段页"（整页表格），图多的论文上同样会点错页；
  **本次未改**（改它要连带核对回填的页数 1:1 校验，另起一次改动）。
- 老侧车（v28.9 之前的归档副本）没有 `pageid`，只能按旧口径匹配 —— 页码标注会与报告不一致，
  但**不误伤**（退化路径已在测试里锁住）。


## 2026-09-19 v28.11 `deliver` 自动认领交件漏读正文（Melhani 交付时当场炸出）

### 现象
豆包交件后跑 `adopt deliver --name melhani2026`（不给 `--text`，让它自动认领
`out/melhani2026.doubao.txt`）：

```
[adopt] 交件件(唯一候选): D:\zotero-pdf2zh\out\melhani2026.doubao.txt
UnboundLocalError: cannot access local variable 'text' where it is not associated with a value
  File "tools/adopt.py", line 372, in stage_deliver
    got = SI.parse_blocks(text)
```

### 根因
`stage_deliver` 有三条取件路：`--clip`（读剪贴板）、`--text`（读指定文件）、
**自动认领**（`out/<name>.doubao*.txt`）。前两条各自赋了 `text`，
**第三条只赋了 `path`，忘了读文件** —— 而紧随其后的段号守恒、`⋮` 断点对账两道门禁
全部吃 `text`，于是这条路一进门就崩。

之所以一直没暴露：此前每一轮交付都走 `--clip`（豆包软件把交件放剪贴板），
自动认领这条路是新加的、且只在"豆包软件自己落盘"的场景才走到。

### 改动（`tools/adopt.py`）
自动认领分支补上读取：
```python
path = cand[0]
print("[adopt] 交件件(唯一候选): %s" % path)
# 自动认领这条路也要把正文读进来: 下面所有门禁(段号守恒/断点对账)都吃 text
with open(path, encoding="utf-8-sig") as f:
    text = f.read()
```
`utf-8-sig` 与 `--text` 分支一致（豆包的 `.txt` 可能带 BOM）。

### 测试
`tools/tests/test_adopt.py` 新增 ⑦b（1 例）：造单份交件 + 不给 `--text`，
断言 `rc == 0` 且台账 `deliver.state == "ok"`、`n_seg == 2`。
（若正文没读进来，`parse_blocks("")` 会得到空集 → 判"缺段"而 rc=1；崩则是 UnboundLocalError
—— 断言能同时挡住这两种。）**37 PASS / 0 FAIL**；`run_all.py` 16 套件全过 ✅。

### 同批：Melhani 2026 交付完成（第 1/4 篇）
```
export --pages 43-44 --pdf "<Melhani>.pdf" --force   -> 5 段 / 8180 字符 / 库内命中 5 行
deliver  -> 5 段 / 7808 字符 / sha1=93ab0185
import   -> 回锚 5 段, 结论 PASS（p55/p56 若干"-19""[19]"歧义锚定取最近）
inject   -> 更新 5 行 (备份 cache.v1.db.bak-20260919-133535), fp 用 export 探到的 doc_fp
render   -> 渲染前预检 PASS -> 重渲染 57 秒 (缓存已热)
gate     -> verify PASS 3/3 ('A new approach to linear filtering and prediction problems' p43,
            'Global identifiability of nonlinear models of biological systems' p43, '附录 A. 引理 1' p44)
            post_check PASS ── 13:26 那条 FAIL「第43,44页文献区汉化(10+7条)」已转通过:
            "第43,44页 文献页条目原样保留" + "文献区禁汉化检查通过: 2 个文献页的条目均保持原样"
            => 本 run 交付完成
```
豆包交件内容符合指令：`#S1`/`#S2`（[7]–[51] 文献条目）整段原样英文，
`#S3`–`#S5`（附录 A · Lemma 1 证明）按常规译成中文、公式片段整块照抄。

### 同批：Padmaprabhan 2026 交付完成（第 2/4 篇）
该篇无归档件（v28.9 之前翻的），先 `force_rerender.py` 铸出 `pdf-c27d295376703b17.jsonl`
（96 秒），之后全程走 `--pdf` 认领。**中间多了一轮返工**：

```
export --pages 6 --pdf "<Padmaprabhan>.pdf"  -> 6 段 / 7095 字符 / 库内命中 4 行
      （命中 4 而非 6 是正常的: 探库只统计 raw >= 40 字符的段, #S2(27字)/#S4(10字) 是短标题不计入）
deliver 第 1 版 -> 6 段 / 5813 字符 / sha1=7d179774
import  第 1 版 -> FAIL: p9#4 高价值字形未回锚 ["{v29}='3'"]     ← 卡在这里
```

**返工原因（译文内容问题，按角色边界交豆包）**：原文是

```
Exciting future avenues include {v29}D fabrication of the optimized fishtail …
```

`{v29}='3'` —— 版面里数字 `3` 是**独立字形对象**（`D` 是普通文本），豆包把 `3D` 译成了
「三维制造」，数字 `3` 没了落点，回锚无从对齐。这不是代码缺陷（数字原样保留是规则 2 的既有
要求，`seg_import` 判 FAIL 是正确行为），所以按三个开发侧杠杆里的"**写给豆包的指令**"退回：
只改 `#S3` 末句，其余 5 段逐字符照旧。

```
deliver 第 2 版 (doubao2.txt) -> 6 段 / 5815 字符 / sha1=d6c99ed9
import  第 2 版 -> 回锚 6 段, 结论 PASS（高价值字形 FAIL 消失）
inject  -> 更新 6 行 (备份 cache.v1.db.bak-20260919-134607)
          9#7 一行提示"现行译文与侧车不一致(可能被重译过)" —— 仅提示, dry 已 PASS
render  -> 渲染前预检 PASS -> 重渲染 42 秒
gate    -> verify PASS 4/4 ('3D 制造' p6 / '结论与未来工作' p6 / '参考文献' p6;
            禁止串 '三维制造' 0 命中) + post_check PASS
            "文献区禁汉化检查通过：1 个文献页的条目均保持原样"
            => 13:26 那条 FAIL「第 6 页文献区汉化(13 条)」转通过
```

**挂账**：`3D→三维` 属"数字与字母连写的记号被当词翻译"，很可能在剩 2 篇上重演。
候选修法是往 `seg_export.py` 通用规则 2 补一句（`3D`/`2D`/`4×4`/`P1`/`SIR` 这类整体原样保留，
不得改写成「三维/二维/四乘四」）。RULES 只是载荷抬头、不进缓存键，加它对已入库译文无影响。
**是否要做待用户拍板**（本批未改）。


## 2026-09-19 v28.12 通用规则 2 补"数字与字母连写记号整体照抄"（关闭上节挂账）

### 缘起
用户拍板"该加的就加"。触发是 Padmaprabhan 那一轮返工（见 v28.11 同批）：原文
`Exciting future avenues include {v29}D fabrication`（`{v29}='3'`，数字 `3` 是**独立字形
对象**，`D` 是正文字符），豆包把 `3D` 译成「三维制造」→ 数字字形没有落点 → `import`
判 `FAIL p9#4 高价值字形未回锚 ["{v29}='3'"]`。

**定性**：这不是代码缺陷。规则 2 早就写了"数字…原样保留"，`seg_import` 判 FAIL 是正确行为。
缺的是**规则写得不够具体** —— 中文习惯把 `3D` 说成"三维"，译者会本能地把它当一个词翻译。
所以修法是补通用规则，而不是给这一篇打补丁。

### 改了什么
`tools/seg_export.py` 的 `RULES` 规则 2 下加三个缩进行（**不新增规则编号**，1–7 仍是 1–7，
故"规则编号连续"的既有断言不受影响）：

```
2. 数字、拉丁学名、人名、单位、[n] 引用标号、化学式：原样保留，不译不改不移动位置。
   数字与字母连写的记号（如 3D、2D、4×4、P1、COVID-19）是一整块，整体照抄，
   不得改写成「三维」「二维」「四乘四」这类中文说法 —— 原文里的每个数字字符
   都必须在译文中原样出现，一个都不能少。
```

写法的两个取舍：
- **点形态不点单词**：写"数字与字母连写的记号"而不是枚举某几个词，`3D`/`4×4`/`P1` 只是例子，
  换任何一篇论文的记号形态都覆盖得到。
- **给出机理而非只给禁令**：末句"原文里的每个数字字符都必须在译文中原样出现"解释的是
  **为什么**（字形要有落点），译者照做不吃力，也不容易在别的形式上犯同类错。

### 边界（为什么这次改动是安全的）
`RULES` 只是**载荷抬头**，不是段文本的一部分：
- **不进缓存键** —— 缓存键取的是 `#S` 段原文 + 指纹参数，抬头改了不会让任何已入库译文失效；
- **不改段号与 manifest** —— 只影响 `inbox/<name>.txt` 的抬头块，`deliver`/`import` 只认 `#S` 行；
- **不在同步清单里** —— `sync_patches.ps1` 的 9 文件清单本就不含 `seg_export.py`，无需镜像。

### 测试
`tools/tests/test_seg_export.py` 新增 ⑪ 节 4 例（抬头"锁六条语义"→"锁七条语义"，补 ⑦ 条）：
规则 2 点到"数字与字母连写的记号" / 点明不得改写成「三维」「四乘四」/ 点明"一个都不能少" /
例子是通用记号（`3D`、`4×4` 在，且不混入别篇的具体例子）。

```
test_seg_export.py  ->  43 passed, 0 failed
run_all.py          ->  16 套件全部通过 ✅
```

### 挂账
- Padmaprabhan 那一篇已按旧规则交付完毕（返工一轮后双 PASS），**不回溯**：规则是向前生效的。
- 新增规则要等**下一篇导出**才进入载荷；已发出的载荷（`out/` 里那些）不带这条。


## 2026-09-19 v28.13 import 失败自动落「给豆包的返工单」（用户侧简便化）

### 缘起
用户拍板："用户后期是要给用户使用的，如何让用户更方便地做到这个翻译，它非常简便；
用户遇到问题时，也很容易解决。" 收敛范围 = 返工单（改报错口径 + 落盘）。

盘出来的三个**用户卡点**（都在 `import` 判 FAIL 这一步）：

| | 卡点 | 实测证据 |
|---|---|---|
| A | 报错用**工具内部坐标** | `FAIL p9#4 高价值字形未回锚 ["{v29}='3'"]` —— Padmaprabhan 全文 6 页，p9 根本翻不到 |
| B | 报错只打控制台、**不落盘** | 豆包（桥只读 `inbox/`）看不见；用户读完也不知道该改哪一段、缺哪个字符 |
| C | 写有"用户看不见的坐标" | 同一批里 `p9#4` 与 `#S3` 是同一个段，用户要在两个编号体系间自己换算 |

**A 的根因**（属代码缺陷，v28.10 只改了导出选页、没改报错）：侧车的 `page` 是
`receive_layout` 的**回调计数**，图形对象也各占一号，图多的论文整体漂移。报错沿用了这个
坐标，于是"页码"指向不存在的页。真实页码只有 `pageid+1`（`LTPage.pageid`，0 基）说得准。

### 改了什么
**① `tools/seg_export.py`** —— manifest 的每条 part 增 `true_page`（真实 PDF 页码），
与原有的 `page`（内部坐标）并列存放：

```json
"parts": [{"page": 9, "seg": 0, "true_page": 6}]
```

`page` 一个字符都没动 —— `imported.json` / `seg_inject` / 台账沿用它，本改动不动载荷契约。
加 `true_page` 是为了让**下游不必各自重算一遍 pageid 口径**（双口径写在一处才不会漂）。

**② `tools/seg_import.py`** —— 四个新函数 + 报错口径 + 落盘：

- `part_true_page(part, o)`：三级退化 —— `true_page` → 老清单落 `pageid+1` → 老侧车才落内部坐标；
- `restore_with_spans(raw, vv)` / `glyph_context(raw, vv, vn)`：给出"这个字形在原文哪儿"。
  按**还原后**文本切片（直接切 `raw` 会把 `{v29}` 切成 `{v2` 这种半截货），两端超宽打 `…`；
- `write_rework_note(...)`：写 `inbox/<name>.rework.md`，返回路径；
- 报错与提示从 `p9#4` 改为 `#S3（第6页）`（控制台与单子同一口径）；
- `json.dump` 之后：**FAIL 写单子 / PASS 删旧单子**。删是必须的 —— 留着过期单子比没有更糟：
  豆包会照它改已经改好的段。

### 单子长什么样（Padmaprabhan 真数据跑出来的，未手改）

把已交付的 `padmaprabhan2026.doubao2.txt` 里两处 `3D` 换成「三维」后重跑（沙箱 `P2Z_PROJ`，
不碰真项目 `out/`）：

```
# 返工单 —— padmaprabhan2026
...
**只改下面点到的段**; 没点到的段请逐字符照抄你上一版, 不要顺手改动 ——
整篇重译会重新掷一次公式块与编号的骰子(v28.6 实测: 266 段里 206 段被动过)。

门禁判定: FAIL —— 需返工 2 段 / 2 处字符没有落点

## 一、必须改
**共同原因**：版面里这些字符是独立的字形对象，译文里不出现它就**没有落点**，...
**共同修法**：把它**原样写回**（数字/字母照抄，不要改写成「三维」「二维」这类中文说法）；...

### #S1（第 6 页）
- 缺的字符: `3`
- 原文里它在哪: …mputational cost. The actuator is discretized using ABAQUS C3D10HS quadratic tetrahedral hybrid elements to accurately ca…

### #S3（第 6 页）
- 缺的字符: `3`
- 原文里它在哪: …iles are also demonstrated. Exciting future avenues include 3D fabrication of the optimized fishtail to illustrate the ap…

## 二、不必改（工具已自动处理，别动）
- 提示 #S1（第6页） 纯标点字形丢弃 9 个: [...]

## 交件
- 交件名: `padmaprabhan2026.doubao*.txt`（递增，别覆盖上一版）
- 段号必须 #S1–#S6 连续无缺。
- 没点到的段逐字符照抄上一版。
```

控制台同步变成：`FAIL #S1（第6页） 高价值字形未回锚: ["{v7}='3'"]` /
`返工单: D:\zotero-pdf2zh\inbox\padmaprabhan2026.rework.md`。

两处细节是按"读的人好懂"定的：
- **计数行只在真有回锚失败时报数**：缺段类 FAIL 没有"处数"可数，硬写"需返工 0 段"
  会让人以为单子发错了；
- **小节编号随实际出现的小节走**：只写「一、必须改」和「三、不必改」、中间空着「二」，
  读的人会以为单子缺了一节。

### 边界（为什么这次改动是安全的）
- **桥不用改**：`list_inbox` 本就列 `inbox/` 下全部文件，`get_payload` 按名读任意文件 ——
  单子放在 `inbox/` 里，豆包 `list_inbox → get_payload` 直接拿得到（零新增契约）。
- **不动管线契约**：`manifest` 的 `page` / `imported.json` / 台账 / 门禁判据全不变；
  单子只是**报告的另一种渲染**，不参与任何判定（PASS/FAIL 仍由 `import` 原报告决定）。
- **不动缓存**：`out/*.imported.json` 内容与改动前逐字节一致（同输入同输出）。
- **不在同步清单里**：`sync_patches.ps1` 的 9 文件清单不含 `seg_export.py` / `seg_import.py`，无需镜像。

### 测试
新增 `tools/tests/test_seg_rework.py`（32 例，已注册进 `run_all.py` 第 17 套）：
`part_true_page` 三级口径 / 上下文按还原后切片（不切出半截占位符）/
FAIL 落单（真实页 + `#S` + 缺的字符 + 原文线索 + 交件名）/ PASS 删旧单 /
小节分节与编号连续 / 缺段类 FAIL 不报数 / 取不到论文名不写文件。

`test_seg_export.py` 补 2 例：manifest 记 `true_page`、合并段逐条记真实页。

```
test_seg_rework.py  ->  32 passed, 0 failed
test_seg_export.py  ->  45 passed, 0 failed
run_all.py          ->  17 套件全部通过 ✅
```

真数据冒烟（沙箱）：用真实 `padmaprabhan2026.manifest.json`（该清单导出于本改动之前，
**没有** `true_page`）重跑，报错口径正确落在「第6页」（走了 `pageid+1` 那级退化）；
单子写进沙箱 `P2Z_INBOX`，真项目 `inbox/` 未被写入。

### 挂账
- `tools/backfill_pages.py` 的 `zero_pages_from_sidecar` 仍用 `o["page"]` 当输出页码（同类漂移），
  本次未动；它只在补页小工具里用，等真的踩到再改。
- 单子只覆盖"回锚失败"这一类；若将来出现**新增**的 FAIL 类别，`write_rework_note` 的
  「其他门禁失败」一节会自动收纳（不认得的 FAIL 行原样转述，不会静默丢）。


## 2026-09-19 Petkevičius 2026 交付完成（第 3/4 篇）

篇名：*A 0-1 Law for Multifractal Spectra via the HGDS Scale Derivative*（6 页）。
门禁 FAIL 在「第 6 页文献区汉化（6 条）」——第 6 页是**正文尾 + 参考文献**混排页。

该篇无归档件（v28.9 之前翻的），先 `force_rerender.py` 铸出
`pdf-2d5f3ff66c1061a9.jsonl`（187 秒），之后全程 `--pdf` 认领归档件：

```
export --pages 6 --pdf "<Petkevičius>.pdf" -> 13 段 / 1479 字符 / 库内命中 11 行
      （命中 11 而非 13：探库只统计 raw >= 40 字符的段，#S1「Sharpness and examples」(6字)
        与 #S7「References」(4字) 是短标题不计入）
deliver  -> 13 段 / 1121 字符 / sha1=473e2afd
import   -> 回锚 13 段, 结论 PASS
            （提示若干"纯标点字形丢弃"，其中 #S11–#S13 丢的是人名里的 `ˇ` 组合符；
              #S3 一处 "{v17}='n' x2" 歧义锚定取最近，属既有台账项）
inject   -> 更新 13 行 (备份 cache.v1.db.bak-20260919-140723)
render   -> 渲染前预检 PASS（"1 个文献页的条目均保持原样" -> 42 秒，全缓存命中）
gate     -> verify PASS 4/4 ('参考文献' p6 / '尖锐性与例子' p6 / '注记 6.2' p6 /
            'arXiv:2209.14612,2022.' p6) + post_check PASS
            "文献区禁汉化检查通过：1 个文献页的条目均保持原样"
            => 11:25 那条 FAIL「第 6 页文献区汉化(6 条)」转通过
```

**一条通过，无返工**：豆包按指令把 `#S1`–`#S6` 正文译出（公式块整块照抄、中文放块外）、
`#S7` 标题译「参考文献」、`#S8`–`#S13` 六条文献条目逐字符照抄（含 `Petkeviˇcius` 的组合符）。
这是 v28.12 规则 2 加固后发出的第一批载荷之一，也是 v28.13 上线后第一次真跑 ——
本次没触发返工单（`inbox/` 里无 `.rework.md`），说明"一次过"时这条链路不增任何开销。

**顺带观察**：本篇 6 条文献是**作者-年份/预印本**体例（不是行首 `[n]`）；与 Melhani 的
编号制条目（`[7]`–`[51]`）合起来，两种体例现在都是"条目保持原样"通过。


## 2026-09-19 v28.14 采纳管线补「末 N 页保留原文」（Zhang 篇 gate FAIL 的根因）

### 缘起
Zhang 2026（*CLAP* 系列，17 页）交付链走完后 `gate` 双闸只过了一半：

```
verify_render = PASS (4/4)
post_check    = FAIL
🔴 全文:     引用完整性断言失败: 原文引用标号 205 个, 译文仅 163 个（差 42）。
             偏差最大: 第3页(差38)、第12页(差2)、第5页(差1)
🔴 第16,17页: 文献区汉化断言失败: 第16页(29条)、第17页(14条)
```

### 定层：这是**管线参数缺表达能力**，不是译文缺陷
判据用「换译文会否撞同墙」——把豆包的译文换一版重译，这两条一样过不去，因为：

| 证据 | 说明 |
|---|---|
| `pre_check.py` 对本篇的结论 | `17 页 \| skipLastPages=3 \| 第15-17页: 检测到参考文献区（编号制）…建议"最后几页跳过翻译"=3` |
| 11:25 那轮历史质检 | 同篇「引用完整性对账通过：原文 205 / 译文 201」，FAIL 只有「第15页(2条)」 |
| 逐页标号计数 | `p3 42:4`（差38全部落在第 3 页）/ `p5 3:2` / `p12 15:13` —— 都是**正文页**，不是文献页 |
| `force_rerender.py` 的 `PLUGIN_CONFIG` | `"skipLastPages": 0` **写死**；`adopt render` 也**没有任何参数**能表达跳页 |

也就是说：**产品那份的真实设置是「末尾 2 页保留原文」**（第 16–17 页保持英文，第 15 页照译），
而 `adopt render` 缺省 `skipLastPages=0`，把第 16–17 页也翻了 —— 我这一轮渲染的产物与用户
实际拿到的那份**不是同一份**，门禁拿"整篇翻"的产物去对"末尾 2 页保留"的基线，第 16/17 页
当然被判汉化。

「2」这个数是**从 11:25 那份基线反推**出来的，不是 `pre_check` 推荐的值：11:25 的质检报告
里只有「第15页(2条)」汉化 FAIL —— 第 15 页被翻译过（否则不会有这条），第 16/17 页没被
翻译（否则会像 skip=0 那样各报 29/14 条）。`pre_check` 推荐的是 **3**（它只按文本体例数
"第15-17页是参考文献区"），把"正文尾 + 表题 + 文献开头"的**混排页 15** 也算了进去。
可见推荐值只是提示，**权威是产品那份实际用的值** —— 这正是本次要把 `render` 的参数记进
台账、并让 `gate` 缺省沿用的原因。
至于第 3 页那 38 个引用标号差 —— **与跳页无关**（skip=0/2/3 三种渲染下都是 205:163），
是翻译期的占位符丢失，见下节「遗留」。

结论：`skipLastPages` 是**渲染参数**，门禁判 FAIL 的却是"参数没传"，属代码缺陷。

### 改了什么
**① `tools/force_rerender.py`** —— 新增 `--skip-last`，覆盖写死的 `skipLastPages`：

```python
ap.add_argument("--skip-last", type=int, default=0,
                help="末尾保留页数(原样不译); 与 pre_check 推荐的 skipLastPages 同口径, "
                     "也要跟 Zotero 插件里该任务设的「最后几页跳过翻译」一致 —— "
                     "否则产出的 PDF 与用户实际拿到的那份不是同一份")
...
cfg["skipLastPages"] = int(args.skip_last)
```

提交行同时打印该值（`提交: x.pdf (force=True, service=silicon, skipLastPages=3, …)`），
让"这一份到底是按几页跳渲染的"在日志里可验证。

**② `tools/adopt.py` / `render`** —— 新增 `--skip-last`，透传给 `force_rerender` 并落台账
`skip_last`（缺省 0 = 整篇翻）。**不做隐式推断**：adopt 不去猜 `pre_check` 的建议值，
`skipLastPages` 是用户/插件侧的口径，必须显式给。

**③ `tools/adopt.py` / `gate`** —— `--skip-last` 缺省由 `0` 改为 `None`，缺省时
**沿用 render 阶段实际用的值**：

```python
skip_last = args.skip_last
if skip_last is None:
    skip_last = int((led["stages"].get("render") or {}).get("skip_last") or 0)
    print("[adopt] skip_last 缺省沿用 render 台账值: %d" % skip_last)
```

原先 gate 缺省 0，于是**末 N 页保留原文的 run 必然自相矛盾**：render 按跳页出图，
gate 却按"整篇都该有译文"去查，把本该保持英文的文献页报成「文献区汉化」。
现在 gate 与 render 同口径；要查整篇翻的产物，显式 `--skip-last 0` 覆盖即可。

**④ 渲染前预检（`pre_render_check.py`）故意不透传**：它只比对 `imported.json` 里
**有译文的那几页**（缺页一律当空串），末 N 页保留原文的页本就不在里面 —— 无须豁免。
已在 `stage_render` 里写明注释，免得后人以为是漏传。

### 边界（为什么这次改动是安全的）
- 不改 `PLUGIN_CONFIG` 的默认值（仍是 0）：只有当调用方显式给 `--skip-last` 时才变，
  既有脚本行为逐字节不变。
- 不改门禁判据本身：`post_check.py --skip-last N` 早已存在（`pre_check` 就按它推荐），
  本改动只是把 adopt 两侧**接上这根线**。
- **不在同步清单里**：`sync_patches.ps1` 的 9 文件清单不含 `adopt.py` / `force_rerender.py`，无需镜像。

### 测试
`tools/tests/test_adopt.py` 新增 ㉒ 节 5 例：render 透传 `--skip-last` 给 force_rerender /
render 落台账 `skip_last` / gate 缺省沿用 render 的值并带上 `/ gate 台账同记 /
显式 `--skip-last 0` 覆盖台账值。

```
test_adopt.py -> 42 PASS / 0 FAIL
run_all.py    -> 17 套件全部通过 ✅
```

### 实测（Zhang 2026，17 页，豆包已交第 15 页译文）

| 渲染 | verify_render | 文献区禁汉化 | 引用完整性 |
|---|---|---|---|
| `--skip-last 3` | **FAIL 1/4**（p15 被跳过，豆包译文没落页） | PASS（15-17 全豁免） | FAIL 205:163 |
| `--skip-last 2` | **PASS 4/4**（p15 命中全部 4 个期望串） | **PASS**（16-17 豁免；15 页条目照豆包译文保持英文） | FAIL 205:163 |

`--skip-last 3` 那一行反过来印证了「2 才是产品那份的值」：跳页跳到第 15 页，用户花力气
让豆包修的第 15 页译文就根本不会落页 —— 而 11:25 的 FAIL 恰恰发生在第 15 页。

`--skip-last 2` 的结果 = **用户那两条断言的目标达成**：11:25 的「第15页文献区汉化(2条)」
转通过（第 15 页文献条目按豆包译文保持英文、第 16/17 页按产品口径保留原文）。

### 遗留（本次未修，与跳页无关）：第 3 页丢了 38 个引用标号

`gate` 仍 FAIL 在引用完整性（原文 205 / 译文 163，差 42，其中第 3 页差 38）。查证过程：

- 换 extractor（pypdf 与 pymupdf）结果一致 → **不是提取假象**，页面上确实没有这些标号；
- 侧车（`~/.cache/pdf2zh/segflow/pdf-c85f1954b970c0f9.jsonl`）第 3 页 21 段里，4 段
  `raw{v}=31/44/10/8` 而 **`trans{v}=0`** —— 初译把这些段的 `{vN}` 全部丢了；
  另有 1 段译成了字面 `v*`（`A. Problem Formulation` → `Av* 问题表述`）。
- 那些 `{vN}` 就是引用标号（`vars` 里 `"1": "[31],"` `"2": "[32]"` `"3": "[33],[34]."`），
  第 3 页标号密（42 个/页），是全文最密的一页。

**根因**：服务端初译路径**没有占位符完整性校验** —— LLM（DeepSeek-V3.2）在 upstream 那句
prompt（`Keep the formula notation {v*} unchanged.`，上游原文，非本仓库所加）下，既可能把
`{v0}` 写成字面 `v*`，也可能整段丢掉，而**没有任何闸门在入库前拦它**；`post_check` 的
「占位符残留」只查**残留**（多出来的 `{vN}`），不查**丢失**。

**为什么现在才发现**：本篇第 3 页的缓存行是 14:19 那次「铸归档件」渲染新建的（该文档在本机
缓存里此前只有 2 行，属**正确地重译**，不是查找失败），这批新译文就带了这个缺陷。

**路由（按角色边界）**：这属于**内容**缺陷 —— 修法是标准豆包回路，不需要新代码：
规则 2 已写明「数字、拉丁学名、人名、单位、**[n] 引用标号**、化学式：原样保留」，
`adopt export --pages 3` 出的载荷里标号是还原成人可读的 `[31]` 形态，
`import` 的锚定门禁会**逐字形校验**（丢一个就 FAIL 并落返工单）——
正是服务端初译路径缺的那道校验。开发侧不手改这一篇的缓存行。

**挂账（代码侧，未动）**：初译路径的占位符多重集校验。改动要碰 `patches/pdf2zh_translator.py`
且 prompt 属缓存键的一部分，动它=全体用户缓存键变更，风险与收益需要单独评估，
本次只记观察项。


## 2026-09-19 v28.15 收口一轮：代码侧遗留清完 + 术语表判据 + 第二轮保护

### 缘起（用户原话）
「先将**我们的所有的现在所做的东西问题全部解决完**之后。然后进行一场保护。」
（紧接其后才是容器里隔离评估官方引擎、以及"我们保留我们的线、不换产品"。）

### 一、`backfill_pages.py` 零可译段页判定：页码口径 + 按页聚合（真缺陷，会毁正文页）
`zero_pages_from_sidecar` 原先把侧车的 `o["page"]` 当输出页码，而 `page` 是
`receive_layout` 的**回调计数**（图形回调各占一号，同页可占多行）；且判据是**逐条记录**，
而同页的图形回调那行天生没有可译段。两个口径叠加的后果不是"点错一页"：

- 该篇（Zhang 15 页 / 26 条记录）回放实测：旧口径报 **`[1, 11, 13, 15, 17, 18, 23, 24]`**
  八个零页，其中 **11–15 全是正文页**，会被原版英文页顶掉；新口径报 **`[]`**（本篇无整页表格，正确）；
- 另 4 篇归档件回放：Melhani 旧报 12 页假零页、`c27d2953` 旧报 3 页假零页，新口径均为 `[]`。
- 模块 docstring 本来就写着"**该页**所有段落 raw 均为纯 {vN} 占位符"= 按页，实现没照做。

修法：加 `true_page(o)`（`pageid + 1`，与 `seg_export.py` v28.10 同一口径；老侧车无
`pageid` 时回落 `page`），判据改为**按页聚合**（该页所有记录都不可译才算零页）。
新增 `tools/tests/test_backfill_pages.py`（9 例，走 CLI + 合成侧车 + 合成 PDF，
用**每页 mediabox 宽度不同**当"这一页取自哪一侧"的指纹），已挂进 `run_all.py` 第 18 套件。

### 二、死代码清理（关闭 v26.18 挂账）
- 删 `server/utils/execute2.py`(547 行) / `execute3.py`(215 行) —— 全仓零引用
  (`git grep` 无 `from utils.execute2|execute3`)，`server.py` 只 import `utils.execute`。
  **排雷**：`代码检查报告_20260903.md:86` 曾写它们的增量解码器"应回移"，核实**早已回移**
  (`execute.py:175-187` 带 `[自研补丁 2026-09-03]` 标记)，故这两个文件是真·已被取代。
- 删 `server/utils/record.py`(41 行) —— 上游自己废弃的 `RecordTracker`
  (文件头 `## server.py v4.0.0`，注释"TODO: 目前进度同步逻辑还没有完成")，已被 `task_manager.py` 取代。
- 删 `translator.py` 里**被覆盖**的那份 `_cache_key_suffix`（原 578-603，v23 版、缺前瞻）。
  三处同名：98 = 基类钩子（活，`translate()` 调它）、578 = 死、674 = 活（v23.4 含 `_look_ahead`）。
  **venv 运行侧同步删了同一段**（镜像方向是 venv → patches，只改镜像会让门禁 FAIL）：
  两侧 MD5 均 `D05C29C1`，`ast.parse` 通过。

### 三、镜像清单 9 → 11（关闭 v26.18b 挂账）
`server/utils/execute.py`（PTY 增量解码 + 看门狗 + 独占闸，18 处 `[自研补丁]`）与
`server/utils/task_manager.py`（任务状态机，2 处）**此前不在门禁内** —— 改崩了没有任何提示。
`sync_patches.ps1` 与 `test_mirrors_sync.py` 同步扩到 11 文件，实跑 **11 PASS / 0 FAIL**。

> ⚠️ 坑：`tools/sync_patches.ps1` 必须带 **UTF-8 BOM**。Edit 工具写回会吃掉 BOM，
> 而 Windows PowerShell 5.1 按 GBK 读无 BOM 的脚本 → 中文注释吞掉引号 → `TerminatorExpectedAtEndOfString`
> 解析失败（本机无 `pwsh`，不能靠 7.x 的默认 UTF-8）。改这个文件后请确认前三字节是 `EF BB BF`。

### 四、仓库卫生
删掉两个带空格的残留：`server/config/config.json.bak- 20260919-004623`、
`config.toml.bak- 20260919-004631`（`untracked`，且 `config.json/config.toml` 本身在
`.gitignore` 内 —— 明文密钥不进库）。

### 五、术语表：全局累积 + 记判据（用户选 A）
按篇隔离会引入"这篇的术语表从哪来"的新状态，而**加词只作废含该词的段**（v23 按段指纹）
已经把"全局表的副作用"压到最小 —— 故继续全局，但把**判据与来源**记进表内：

- `server/glossary/terms.csv` 抬头写清判据，按 `# 来源` 分段：早期累积（篇名未记，
  `in-silico` 系 2026-09-06 修"硅基准"误译时所加）/ 《Reproductive Biology of Cactaceae》(Desert Plants 2009)。
  **无据可查的就不编**（宁可写"篇名未记"）。
- 补齐挂账术语 `dichogamy,雌雄异熟`（`terms.csv` + `terms.babeldoc.csv` 两文件同步，防启动覆写）。
- 注释行对三处读取端一律不可见，逐一实测：`seg_export.load_terms` 跳过 `#` 行；
  `translator._load_polish_glossary` 按 csv 字段数 ≥2 过滤（故注释里**不写逗号**，
  否则会被当字段收进润色术语表）；`term_verify.load_glossary` **本次补上 `#` 跳过**
  （它原先把注释行当普通行计数，`--propose` 报告会说"术语表共 N 行"多算几行）。
  实测三端各读到 **15 条**、无 `#` 开头的键。

### 六、密钥/配置可环境变量化（v26.18 挂账 #29 的落地部分）
`tools/strategist.py`：`SILICON_API_KEY` / `SILICON_MODEL` **环境变量优先**于 server config
（容器/换机时不必拷带明文密钥的 `config.json`），两处都没有才报错且报错文案点明两条来源；
`SRV_CFG` 支持 `P2Z_SRV_CFG` 覆盖（原先焊死 `D:\zotero-pdf2zh\...`）。实测：config 路径取到
`deepseek-ai/DeepSeek-V3.2`，设 `SILICON_API_KEY` 后走环境变量。

### 七、内容侧（走豆包回路，开发侧不碰文本）
Zhang 篇第 3 页丢 38 个引用标号的返工载荷已导出：
`python tools\adopt.py export --name zhang2026-p3 --pages 3 --pdf <原文> --doc "CLAP: ... (Zhang et al., 2026)"`
→ `inbox/zhang2026-p3.txt`（**16 段 / 5831 原文字符**，库内命中 13 行，`doc_fp` 与 zhang2026 同）；
台账 `logs/adopt/zhang2026-p3.json`。载荷里标号已是人可读形态（`[35]–[40]`、`[3],[4],[6]` …），
豆包按规则 2「[n] 引用标号原样保留」定稿后走 deliver/import/inject/render/gate，
`import` 的逐字形锚定门禁正是初译路径缺的那道校验。质检报告：
`server/translated/review/翻译后质检_20260919_150430_Zhang_等_-_2026_-_CLAP_...md`。

### 挂账关闭清单
| 原挂账 | 位置 | 处置 |
|---|---|---|
| `execute2/3.py` + 重复 `_cache_key_suffix` | 2147-2149 | ✅ 本次删除 |
| `execute.py` / `task_manager.py` 是否入镜像 | 2204-2207 | ✅ 本次扩到 11 文件 |
| `dichogamy,雌雄异熟` 待重译合并补入 | 1912 / 2146 | ✅ 本次与判据一起补入（不等重译） |
| 5 篇历史 FAIL 的"机械还原文献区 / 缓存手术" | 3015-3018 | ✅ **否决**：按角色边界改走标准豆包回路（桥 + 规则 + 报告已足够，v28.4/6/8 已具备） |

### 仍挂账（本次**没做**，附理由）
- ~~**连字符丢失**（EgoPhys 163 种 / 379 处，根因 `config.toml:73 formular_char_pattern` 含
  `-` `\d` `.` `,`；2273-2279）~~ ✅ **本次关闭（v28.18）**：用户拍板「只移除 `-`」+「改配置 +
  重译 EgoPhys」，零成本实证先行（侧车差分 487 丢失 / 缓存天然对照 0 丢失）→ 改配置 →
  EgoPhys force 重译取证（`-` 占位符 0 / 连词 328 处完整 / 渲染件 0 残留）。详见下方 v28.18。
  剩余同源项（`.` `,` 退保护、行末断字合词）仍挂账，见 v28.18 节。
- **wikipedia / wikidata 术语源**（v26.14 挂账）：本机实测仍不可达 ——
  `HTTPS_PROXY` 未设、`127.0.0.1:7897/7890/10809` 全部无监听、`zh.wikipedia.org` 与
  `www.wikidata.org` 直连均超时（`urllib` 会自动读 `HTTPS_PROXY`，故启用代理后无需改码）。
  → 阻塞在"先起代理"，代码侧无待办。
- **豆包桥缺口 2/3**（读单段三件套 / 局部改写写回，3018）：**未获授权开工**，见下轮。
- **初译路径占位符多重集校验**（3824）：用户已拍板"先不做，只记账"。

### 测试
`tools/tests/run_all.py` → **18 套件全部通过**（新增 `test_backfill_pages.py` 9 例；
`test_mirrors_sync.py` 11 文件全绿）。

---

## 2026-09-19 v28.16 第二轮保护 + 官方引擎容器隔离评估（BabelDOC 2.9.0 同题对照）

### 缘起（用户原话）
「先将**我们的所有的现在所做的东西问题全部解决完**之后。然后进行一场保护。然后呢，就是官方新出的这个
插件我们是**可以装取的，但是要隔离，跟我们现在那个隔离起来**。你要不考虑一下用容器？……我们在容器里
面跑完之后。再看看他是不是我们想要的那个效果。……我希望我的这篇论文它首先它翻译质量要比较高，然后它是
**可以维护化**。……我们**的价值点**。以及就是**他的上限在什么地方**？……就是我们**保留我们的线就行
了。至于换产品的话就不换**。」

### 一、保护②（第二道里程碑快照）
前提是"当前状态可复现"，故先复跑回归再打保护：
- `tools/tests/run_all.py` → **18 套件全绿**；
- `git tag -a v28.15-baseline`（附注：backfill 页码口径修复 + 死代码清理 + 镜像 9→11 + 术语表判据 + 密钥环境变量化; 18 套件全绿）；
- 快照 `D:\zotero-pdf2zh-snapshots\v28.15-20260919-151011\`：
  `v28.15-baseline.bundle`（`--all`，**125,020,188 B**）+ `tree\`（robocopy `/E`，排除
  `.git`、`server\translated`、`server\fonts`、`*.zip`）+ `translated-review\` + `logs-adopt\`
  + `MANIFEST.txt`（3,037 B：HEAD sha + 逐文件 SHA256 / 字节数，含 `logs\adopt\zhang2026-p3.json`、
  `server\glossary\terms.csv`、`terms.babeldoc.csv`）；
- 校验：`git -C D:\zotero-pdf2zh bundle verify <bundle>` → "records a complete history / is okay"。

> ⚠️ 坑：`git bundle verify` 必须在**仓库内**执行；在快照目录里跑会报
> `error: need a repository to verify a bundle`（那一层没有 `.git`）。

### 二、官方引擎容器隔离评估（零接触，不碰现有产线）
用户要求"官方新插件可以装，但**要隔离**，用容器跑一遍看是不是想要的效果"。实测：

| 隔离项 | 做法 |
|---|---|
| 运行环境 | 官方镜像 `awwaawwa/pdfmathtranslate-next`（pdf2zh-next **2.9.0**，3.34 GB），容器内运行，**不进我们的 conda 环境** |
| 文件 | 只挂载 `D:\zotero-pdf2zh-eval\official-next\{in,out}`；**不挂本仓库、不挂 `~/.cache`** |
| 端口 / 缓存 | 不发布端口（与 8890 无冲突）；用容器自身可写层，`--rm` 即弃，与本机 pdf2zh 缓存互不可见 |
| 引擎 | `--siliconflow --siliconflow-model deepseek-ai/DeepSeek-V3.2`（**与我们同模型同 key**，译文可直接对比） |
| 样本 | Cactaceae 专著（Desert Plants 2009）`--pages 1-3`；mono；无水印 |
| 产物 | `...zh-CN.mono.pdf`（568,147 B）+ 官方**自动抽取术语表** `...zh-CN.glossary.csv`（148 条） |

镜像获取路径踩坑记录：Docker Hub 直连被墙 / daocloud 白名单不放行 / ghcr 需鉴权 →
最终走 `docker.1ms.run` 镜像源成功；`~/.cache/babeldoc` 资产**随镜像打包**，容器内不联网下模型。
耗时 3 页 ≈ **108 秒**（含 ~30 s 预热，ONNX 走 CPU）。容器 `--rm` 已自动清理（`docker ps -a` 无残留）。

复现命令（可整段重放）：
```powershell
$cfg = Get-Content 'D:\zotero-pdf2zh\server\config\config.json' -Raw | ConvertFrom-Json
$key = ($cfg.translators | Where-Object { $_.name -eq 'silicon' }).envs.SILICON_API_KEY
docker run --rm --name p2z-next-eval `
  -v D:\zotero-pdf2zh-eval\official-next\in:/work/in:ro `
  -v D:\zotero-pdf2zh-eval\official-next\out:/work/out `
  -w /work/out docker.1ms.run/awwaawwa/pdfmathtranslate-next:latest `
  pdf2zh_next --siliconflow --siliconflow-model deepseek-ai/DeepSeek-V3.2 `
  --siliconflow-api-key $key --lang-in en --lang-out zh-CN `
  --pages 1-3 --no-dual --watermark-output-mode no_watermark `
  /work/in/cactaceae2009-orig.pdf
```

### 三、同题对照结论
**官方初译质量已与我们同一档。** 逐页汉字数（`post_check` 同口径 + pypdf 逐页文本落
`cmp\{orig,official,ours}.txt`）：

| 页 | 原文 | 官方 mono | 我方 mono |
|---|---|---|---|
| p1 | zh 0 / lat 1802 | **zh 508** / lat 215 | zh 387 / lat 524 |
| p2 | zh 0 / lat 2612 | **zh 723** / lat 164 | zh 688 / lat 238 |
| p3（整页大表格） | zh 0 / lat 1332 | zh 9 / lat 1302 | zh 0 / lat 1332 |

- 核心术语（herkogamy 雌雄异位 / dichogamy 雌雄异熟 / 近交衰退 / 交配系统 / 自交不亲和 / 传粉生态学）**双方一致**；
- 表页**双方都保留英文**（官方 `table_mode` 已退役；我们 `translate_table_text=false`）——口径一致；
- 用**我们的** `verify_render` 跑官方产物：**6/6 PASS**；用**我们的** `post_check` 跑双方产物：
  **p3 都判 FAIL**（同为"整页表格页零汉化"这一口径误报，见下"建议 3"）。

### 四、官方三处硬伤（实测可复现，非推测）
1. **自动术语抽取会吞引用人名与出版信息**：148 条自动条目里 **14 条**是引用人名/机构/出版行
   （`Anderson→安德森`、`Guzmán et al.→古兹曼等人`、`Darwin 1876→达尔文 1876`、
   `Springer-Verlag Berlin Heidelberg→施普林格出版社柏林海德堡分社`），而**同一批里**
   `Brown 1990` / `Waser 1983` 却原样保留 —— **同类字符串两种处理，说明该层不可控**；
   这正是我们第 4 断言（文献区禁汉化）要防的事故类（EgoPhys 174 处）。
2. **渲染残渣泄漏**：p2 页面上真印出 `</style\x01id='25'>`（16 个码点，含控制符 U+0001；
   用 `repr()` + `ord()` 取证）。我们产品 34 页扫描 **0 处**。**但我们的四断言目前也抓不到这一形态**
   （现有断言无"渲染残渣"项）——记为门禁下一个候选断言。
3. **引用串空格丢失**：`Waser1983`、`Nobel1986`、`Arias等人2005a`（同段 `Brown 1990` 有空格，故非纯提取噪声）；
   我们产物为 `Waser 1983` / `Arias et al. 2005a`。

### 五、我们自己的缺陷（同题照出，属内容侧）
- p1 署名块被**音译**（`玛丽娅・德尔・卡门・曼杜哈诺…`），官方保留拉丁原文；机构/出版行我们保留英文
  （官方翻成中文）——**两种取向都不是 bug，是需要定策**；
- p2 括号错位与半角残留：`特有种占 （84%；Arias et al. 2005a）。`、`（Rzedowski 1991).`、`Table 10.1).`；
- 术语取向差异：我们"繁殖生物学 / 繁殖系统 / 雌雄同株"，官方"生殖生物学 / 繁育系统 / 雌雄同体"
  （hermaphroditic 译"雌雄同株"在植物学上与 monoecious 易混，**待裁决**）。

> 以上三条按角色边界走**内容侧杠杆**（通用规则 / 质检报告 / 给豆包的指令），开发侧不手改单篇文本。

### 六、`PIPELINE_STORY.md` 新增第九章（本次唯一入库的文档改动）
写成《官方引擎隔离评估：BabelDOC 2.9.0 同题对照》，含隔离方式表 / 同题对照 / 官方三处硬伤 /
我方可见缺陷 / **项目价值定位与上限** / 给官方四条可实行建议（成本视角）。
**价值定位一句话**：我们不造更好的翻译器，我们造**论文交付的第二公里**——第一公里（把英文变成好中文）
交给引擎（官方 BabelDOC / 豆包），第二公里是"这篇论文在任何一天被再次修改时，能用 45 秒、零 LLM、
不破坏版面地改对，且每改必验、每验留痕"。
**上限诚实三条**：① 版面解析上限不在我们手里（换引擎＝换掉一批旧伤、继承一批新伤）；
② 内容终审仍须人；③ "通读全文"这类全局能力本地架构模拟不了，故定稿层继续外包。
**给官方四条建议**：抽取与强制注入解耦 / 补"渲染残渣"断言 / 表页零汉化视为预期 / 术语抽取按文档指纹复用。

### 挂账与待拍板
- **p4 连字符全量重译验证**：仍挂账，修正需全量重译（要用户掏钱），待拍板；
- **引擎是否择机迁移**：本轮结论是"**保留我们的线，换产品不换**；引擎可择机换（换引擎＝换一批解析伤）"，
  是否列入下一轮议题待用户拍板；
- **门禁"渲染残渣"断言**：官方实测暴露该形态，我方四断言无此项 → 候选新增（未开工）。

---

## 2026-09-19 v28.17 门禁第五断言「渲染残渣」（官方评估暴露的形态，四项断言全抓不到）

### 缘起
官方 BabelDOC 2.9.0 隔离评估（v28.16）暴露出**渲染残渣**：官方产物第 2 页真印出
`</style\x01id='25'>`（16 个码点，中间夹控制符 U+0001）——IL（行间注）标签没被回填，
就被当成页面文字画了出来。这是 `PIPELINE_STORY.md` 第九章「给官方四条建议」的第 2 条，
也是**我方四项断言全都抓不到**的形态（汉化率只看汉字够不够，引用/占位符/文献区都不看标记）。
用户拍板：先补这条断言（纯代码、零成本）。

### 判据设计：从「绝对计数」退到「差分」
第一版想法是"控制符出现在译文即 FAIL"。**实测否决**——控制符是字体子集/编码的**抽取产物**，
原文自己就有，会把我方合格产物大面积误判：

| 篇目 | 原文控制符 | 我方译文控制符 |
|---|---|---|
| Proximity Modeling Rainfall | 576 | 11 |
| Zhang CLAP | 73 | 73 |
| Melhani | 65 | 69 |
| Wang DP-Consensus | 0 | 18 |
| Kim EgoPhys | 9 | 9 |

改判"标签形态"，且只认三类（**刻意排除** `<EOS>` / `<pad>` 这类裸 token 名 ——
后者在 NLP 论文正文里合法出现，Vaswani 原文 30 处）：

- 闭合标签 `</name …>`；带属性的开标签 `<name … = …>`；自闭合 `<name … />`

再退一步成**差分**：只报"译文有、而原文整篇没有"的形态。原因同上——讲标记语言的论文
正文里就有 `<div class=…>`，绝对计数会把正文内容判成事故。
形态比对前先归一化（去空白与控制符），防渲染改写属性后绕过同形判断。

实测分离度：Vaswani 原文 30 处 / 我方译文 2 处（差分**负**）→ 不触发；
官方该篇原文 0 处 / 官方译文 1 处（差分**正**）→ 触发。

### 实现（只改 `tools/post_check.py`，一行未动别处）
- 常量 `RESIDUE_TAG` / `RESIDUE_BLANK`（含上面两条反面教训的注释）；
- `page_residues(text)`：抽一页的残渣形态（归一化后）；
- `text_features()` 增 `residues` 字段；`check_residue(orig_feats, trans_feats)`
  做差分并产出结论；`run_checks()` 挂成第 5 项；
- 模块 docstring「四项断言」→「五项」；`build_report()` 断言项说明补齐 1–5。

> 未动 `pre_render_check.py`：残渣是**渲染时**产生的，渲染前不可能有（它只复用
> `check_ref_zh`，不调 `run_checks`），前移无意义。
> `adopt.py` 的 gate 只看子进程退出码、不解析报告文本，故无需改动。

### 真样本回归（13 篇我方产物 + 官方产物）
| 样本 | 第五断言 |
|---|---|
| 我方 13 篇既有产物（Brody / Exton / Hagihara / Jiang / Kim / Mandujano / Melhani / Padmaprabhan / Petkevičius / Proximity / Vaswani / Wang / Zhang） | **全部「通过」，0 误报** |
| 官方 BabelDOC 2.9.0 产物 | **第 2 页判「高」**：`译文出现 1 处原文中不存在的标签/内部标记残渣（</styleid='25'>）` |

CLI 实跑（官方产物）：`post_check.py <官方 mono> --original <原文> --skip-last 31`
→ 第 2 页（残渣）+ 第 3 页（整页表格页零汉化，旧有口径问题）两条高危 → **exit 1**，
报告落 `server/translated/review/翻译后质检_20260919_153842_*.md`。

### 测试
`tools/tests/test_post_check.py` **49 → 69 例**（新增 ⑱ 组 19 例）：官方正样本判高危 /
门禁 FAIL / 结论定位到第 2 页 / 回显样本；我方负样本（正文合法 `<EOS>`/`<pad>`）不误判；
译文新增**新形态**仍要报且不牵连同形；报错页跳过；多页只报命中页；形态口径 8 例
（认三类标签、不认裸 token 名、不认换行 `<EO\nS\n>`、不认数学比较符、归一化等价）；
报告断言项编号齐 1–5。
`tools/tests/run_all.py` → **18 套件全部通过**。

> `tools/tests/` 在 `.gitignore` 内（开源红线：含本机路径的工具/文档本地保留），
> 故本次入库只有 `tools/post_check.py`，测试与运行器留在本地。**不要 `git add -f`**。

### 挂账（本判据的边界，记账不修）
- 只堵"**在页面上画出来**"的残渣。若残渣被渲染成图像、或被压成空白/不可见字形，
  文本层抓不到 —— 需要图像层手段，不在本断言范围；
- 判据是差分，故"原文自己就带、且被原样搬到译文"的标记不算事故（这是有意的：
  那属于内容侧，应走通用规则/质检报告给豆包）。

---

## 2026-09-19 v28.18 连字符丢失的根因在**配置层**（本仓库引入的回归）+ EgoPhys 重译取证

### 缘起（用户拍板两项）
上一轮把「连字符丢失」挂账时留了口子："属要用户掏钱的动作，待拍板"。
本轮两问：**「修哪里」→「只移除 `-`」**；**「重译谁」→「改配置 + 重译 EgoPhys」**。
开工前先把判据做成**零成本实证**（复用既有侧车 + 缓存库，一次 LLM 都不调），
确认风险面之后才动配置 —— 即"先证后改"，避免拿付费翻译去试错。

### 一、根因链（**配置引入的回归，不是上游行为**）
1. `server/config/config.toml:73` 的 `formular_char_pattern` 把 `-` 列进了字符类；
2. `server/server.py:1603-1616` 读 `[pdf]` 段 的 `_vchar`，非 `'null'` 就
   `cmd.extend(['-c', str(_vchar)])` 传给子进程；
3. `pdf2zh/converter.py` 的 `vflag()`（248-282）**逐字符独立** `re.match`
   → 每个连字符被判为"公式字符"，单独包成 `{vN}`；
4. 占位符是**位置锚**：LLM 只要调整语序，`end{v59}to{v60}end` 就必须精确回填回原位置，
   回填不上即**丢失**；`converter.py:553-559` 的"纯标点占位符消除"（v20 自研补丁）会把
   孤立的 `-` 占位符**直接删掉** —— 这是第二层伤口。

上游对照（证明是**我们**引入的）：
- `pdf2zh/pdf2zh.py:67-73`，`--vchar/-c` 默认是**空串**（不保护任何字符）；
- 仓库自带的 `server/config/config.toml.example:72-77`，官方模板两项都是 `"null"`。

### 二、零成本实证（判据先于动手）
**① 侧车差分**（5 篇既有侧车，改配置前的快照）：1626 处连字符占位符 / 1139 幸存 / **487 丢失**。

| 类别 | 数量 | 改配置后 |
|---|---|---|
| 纯字母复合词（`self-attention` / `CLAP-RF` / `state-of-the-art`） | 1224 | 整词不再被切，**必然保住** |
| 行末断字（`cou-\|pling` / `multi-\|modal`） | 325 | 连字符保留，但词仍被空格分开（上游抽取口径） |
| 字母数字混（`2D-optimized`） | 2 | 中性 |
| **数字区间 / 数学减号（`1-3` / `p<0.05`）** | **0** | **风险面为零**（删 `-` 不会撞到数学符号） |

**② 缓存天然对照实验（决定性）**：`_translationcache` 表里存在 **366 行"连字符是普通文本"**
的历史译文（同引擎 silicon / 同提示词），等于一份免费的对照答卷：
- 去重 **290 种 / 699 处**，**专名 111 处 100% 保住**：`CLAP-RF` 28/28、`CLAP-NTP` 21/21、
  `Act-VAE` 14/14、`VD-VAE` 9/9、`TO-SoFiT` 5/5、`Bole-Feysot` 2/2；
- 普通复合词被**当成一个词**正确处理：`sound-attenuated`→隔音室、`t-tests`→t检验、
  `post-orgasm`→性高潮后、`three-way`→三通；
- 同一行里其它占位符仍在撕裂文字（`e{v2}ect`→「效{v2}应」、`U{v0}ckert`）——反证症结在
  **连字符被误判为公式**，而非"占位符机制本身"。

**③ 顺带账面（同一张表的更大面）**：5 篇共 10126 个占位符，其中 `,` 1839 + `.` 1680 +
`-` 1626 = **正文标点占 51%**；真数学符号合计约 **150 处（1.5%）**。
`converter.py:553` 的纯标点消除补丁正是在给这个兜底。本轮**只移除 `-`**，其余记账待议。

### 三、改动（本轮唯一生产改动；**git-ignored，不入库**）
`server/config/config.toml:73`，只从字符类里删去 `-|`：

```toml
# 改前
formular_char_pattern = "(\\(|\\||\\)|\\+|-|=|\\d|\\.|\\$|\\[|\\]|\\{|\\}|%|,|/|√|×|÷|≥|≤|≈|[\\u0080-\\ufaff])"
# 改后
formular_char_pattern = "(\\(|\\||\\)|\\+|=|\\d|\\.|\\$|\\[|\\]|\\{|\\}|%|,|/|√|×|÷|≥|≤|≈|[\\u0080-\\ufaff])"
```

- `formular_font_pattern`（72 行）**未动**；
- 子进程每个任务**现读**该文件（`server.py:349-353` → `config_path[pdf2zh_next]`），**无需重启**；
- 跑完后回查文件未被回写（配置不被污染）。
- `server/config/config.toml` 在 `.gitignore:40` 内 → 本次仓库内无配置可提交。

### 四、EgoPhys 重译与取证
- **改动前快照**：`D:\zotero-pdf2zh-snapshots\egophys-prefix-20260919-154843\`
  （mono / dual / 原文 共 3 件，38,597,878 B）；
- **重译**：`tools/force_rerender.py --pdf "<翻译目录>\Kim 等 - 2026 - EgoPhys …(原上传件).pdf" --skip-last 0`
  （force 直通道，全插件等价 config 字段）→ taskId `1726e59d-ba3e-4307-b911-53f3873e2c86`，
  **19/19 页 / 461 秒 / success**，产物已覆盖 `server/translated/`；
- **after 取证**（侧车 + 渲染件）：
  - 269 段 / 1887 占位符，**字形为 `-` 的占位符 = 0**（改前每段都在做 LLM 往返）；
  - raw 里完整连词 **158 种 / 328 处**（`held-out`×15、`CMA-ES`×13、`per-spring`×12、
    `per-object`×10、`test-time`×8、`spring-mass`×8、`RGB-only`×7、`zero-shot`×7 …），
    与载荷侧 159 种 / 329 处一致；
  - **只删了 `-`，没伤其它**：残留保护仍正常（`.` 732 / `,` 623 / 数字括号）；
  - 渲染件抽查：`CMA-ES`×13、`xArm6`×4、`EgoPhys`×41、`RGB`×25、**`{vN}` 残留 0**；
  - 中途（5/19 页）已是 76 段 / **0 个 `-` 占位符** → 改动**立即生效**（无需重启）。
- **代价（诚实口径）**：含连词的 **117 / 269 段**缓存键变更（`cache.py:_canon_seq()` 把
  `{vN}` 规范化为段内出现序，键仍依赖段落**结构**，故 `end{v0}to{v1}end` ≠ `end-to-end`），
  这部分**必须重译**；本次按拍板对 EgoPhys 全篇跑了一次（461 秒）。
  **已交付 4 篇保持现状**（不动，零成本）；对**新论文/新用户**零额外成本。

### 五、门禁质检（改后 EgoPhys）
`tools/post_check.py <mono> --original <原文> --skip-last 0` → 5 项断言 **4 过 1 红**：

| 断言 | 结果 |
|---|---|
| 引用完整性 | ✅ 原文 78 / 译文 84（容差内） |
| 占位符残留 | ✅ 无 `{vN}` 残留 |
| 渲染残渣（v28.17 新增） | ✅ 通过 |
| **文献区禁汉化** | 🔴 第10页(16条) / 第11页(14条) / 第12页(3条) |
| 门禁判定 | 🔴 **FAIL**（exit 1） |

报告：`server/translated/review/翻译后质检_20260919_155721_Kim_等_-_2026_-_EgoPhys_Learning_Generali.md`

> 文献区汉化属**论文内容**（译文措辞/版面），按角色边界走**豆包回路**：报告已落在 `review/`
> 目录，桥的 `list_reports` / `get_report` 可直接读；开发侧**不手改**该篇译文或缓存行。

### 挂账关闭
| 原挂账 | 位置 | 处置 |
|---|---|---|
| 连字符丢失（EgoPhys 163 种 / 379 处） | 3913-3916 | ✅ **本次关闭**：配置移除 `-` + 重译取证（`-` 占位符 0 / 连词 328 处完整 / 渲染件 0 残留） |

### 仍挂账（本次**没做**，附理由）
- **公式保护表的其它字符**（`,` 1839 / `.` 1680，占正文标点 51%）：本轮用户只选移除 `-`；
  若要一并退保护，同样会换掉含这些标点段落的缓存键 —— **仍需用户拍板**。
  判据已知：危险面同样是数学符号（约 1.5%），但 `,` `.` 在自然语言中的语义权重远高于 `-`，
  不能照搬本次结论。
- **行末断字（325 处）**：`obser- vation` 是**上游 PDF 抽取口径**问题（连字符+空格），
  本轮只保证"连字符不丢"；要合词需渲染层做 `tools/seg_export.py:136-145` 那套
  `restore()` 同款处理，而该处理**目前只在载荷侧（给豆包）存在**，渲染侧没有 ——
  **开发/渲染两侧不对称**，记账待议（属代码侧，可低成本做，但会再动一次渲染文本）。
- **EgoPhys 文献区汉化**（第10/11/12页）→ 豆包回路。
- **官方插件容器化**（v28.16 评估结论）—— 用户本轮未选，未动。

### 六、连带影响（**必须记账**）：EgoPhys 的 `inbox/` 载荷已过期
重译换掉了 117 段的缓存键，而 `inbox/egophys2026.{txt,manifest.json}` 是**改配置前**导出的
（时间戳早于本次，且 `logs/adopt/` 下**无** `egophys2026.json` → 当时是直接调 `seg_export.py`，
没走 adopt 管线）。载荷里的段文本与现在的侧车/缓存**已不是同一形态**，
若拿它去给豆包做文献区返工，回锚会失败（fail-closed，不会静默错位，但白跑一轮豆包）。

→ **已重新导出（用户拍板当场执行，零 API 成本）**：
`python tools\adopt.py export --name egophys2026 --pages 1-19 --pdf <原文> --doc "EgoPhys: … (arXiv 2026, Kim et al.)" --force`
→ **export OK：165 段 / 库内命中 167 行 / doc_fp=docsummary:35460f7178018d8a:fac0448e**，
台账 `logs/adopt/egophys2026.json`（本次才建，此前无）。

> 两个坑记下来，免得下次再踩：
> 1. **页码口径换了**：旧载荷的 `"pages": "1-28"` 是 **v28.10 之前**的"回调计数"口径；
>    现在 `--pages` 是**真实 PDF 页码**（v28.10 起），本篇 19 页 → `--pages 1-19`。
>    侧车里 `page` 字段仍是 1–28（回调计数，**载荷内部坐标**），`pageid` 才是真页（0–18）——
>    别再拿 `page` 当页码用。
> 2. `--pages` 超出真实页数时 seg_export 会 `侧车缺页: [...]` 退出 1（fail-closed），
>    重跑需 `--force` 跳过顺序门禁。
> 本项属**开发侧契约**（不是论文内容），交豆包前必须先做。

### 测试
本次**无 `.py` 改动**（唯一改动是 git-ignored 配置），回归套件无需重跑；
v28.17 的 `tools/tests/run_all.py` → **18 套件全绿**仍代表当前代码状态。

> `tools/tests/` 在 `.gitignore` 内，**不要 `git add -f`**。本轮探针脚本全在
> `%TEMP%\p4_*.py`（p4_probe_a/a2/b/c/d/e/e2/f、p4_verify_cfg、p4_check_after、p4_count_segs），
> **未入库**。

---

## 2026-09-19 v28.19 保护表残留字符（`.` `,` `\d`）与行末断字的零成本结案：**配置不动**，两项挂账关闭

### 缘起
用户：「继续解决残留问题。然后告诉我对比结果是什么样的。」——对象是 v28.18 仍挂账的
两项：① 公式保护表其余字符（`.` 1680 / `,` 1839 / digit 351，占 5 篇占位符总量 38%）
要不要也退保护；② 行末断字（325 处）要不要在渲染侧补 `seg_export.restore()` 同款合词。
继续 v28.18 的方法论：**先零成本实证，后动配置**。

### 实证（探针全在 `%TEMP%\p5_*.py`，未入库；零 API 成本）
| 探针 | 内容 |
|---|---|
| `p5_glyph_audit.py` | 6 份侧车全字形审计：raw/丢失/丢失率/多余 |
| `p5_glyph_audit2.py` | 按文档拆 + 丢失占位符的前后邻分类（占位符先替换回字形再判） |
| `p5_final.py` | 「数字邻接丢失」全量验证 + `()`/`$%/` 丢失样本 + POST 断词合词检查 |
| `p5_close.py` | 53 个「无补偿段」定性（文献/公式/其它）+ md5→论文名映射 |

### 一、核心对比表（PRE = 5 篇既有侧车；POST = EgoPhys 重译后）

| 字形 | PRE raw | PRE 丢失 | POST raw | POST 丢失 | 定性 |
|---|---|---|---|---|---|
| `,` | 1839 | 718 (**39.0%**) | 623 | 23 (3.7%) | **句读**，数字邻接 **0 例** |
| `.` | 1680 | 675 (**40.2%**) | 732 | 13 (1.8%) | **句读**，数字邻接 **0 例** |
| `-` | 1626 | 487 (30.0%) | 0 | 0 | 复合词结构 → v28.18 已修 |
| digit | 351 | 6 (**1.7%**) | 86 | 2 (2.3%) | 数字本体，**保护有效** |
| `()` | 580 | 75 (12.9%) | 37 | 0 | 文献/公式段为主 |
| `$%/` | 30 | 5 (16.7%) | 17 | 0 | URL/文献，量极小 |

### 二、关键定性（"39-40% 丢失"是虚惊）
1. **丢失的 `.`/`,` 全部是句读**（前邻=字母、后邻=空格/段尾），**无一例小数点/千分位**
   ——`3.14`、`1,000` 这类"数字|数字"占位符 **0 丢失**（全 6 文档验证）。
2. LLM 丢掉句读占位符后**自写中文标点**（`。` `，`）——这是翻译的**正确行为**，
   不是损伤。抽 8 段人看：译文全通顺。
3. PRE(39-40%) 与 POST(1.8-3.7%) 的差异主要来自**文献条目段**：译文按规则保留英文，
   占位符标点被**字面标点补偿**（`Chollet{v12} Xception` → `Chollet, Xception`），
   渲染结果完好。53 个"无中文标点补偿"段经定性**全部是文献/引用段**，无一正文。
4. `()`/`$%/` 的少量丢失同样集中在文献/公式段——内容侧旧伤，量小，与配置无关，
   走豆包回路的地盘，不在本轮处理。

### 三、行末断字（POST EgoPhys 实测，字面连字符进 LLM）
raw 里字面断词 **28 处**（`over- comes` / `sup- port` / `physi- cal` / `deformable- object` /
`RGB- only` / `parame- ters` / `graph- and` …），译文**疑似残词 0 例**——LLM 全部正确
合词（"克服了…限制"、"物理参数"、"可变形物体"）。**渲染侧不需要移植 `restore()` 合词**。

### 四、判定（两项挂账均以"否决"关闭）
| v28.18 挂账 | 判定 | 理由 |
|---|---|---|
| `.` `,` 退保护 | **否决，配置不动** | "丢失"是正确行为（换写中文标点），零质量收益；而退保护会让**几乎全部段落**缓存键换血（每段都有句读）→ 全库重译的成本纯白花 |
| 行末断字渲染侧 `restore()` | **否决，不需要** | POST 实证 28/28 正确合词，LLM 直译路径无此缺陷；载荷侧已有 restore()，两侧不对称不构成缺陷 |
| `\d`（附带确认） | **留守保护** | 丢失率 1.7%/2.3%，保护是数字完好率 98%+ 的直接原因 |

### 挂账关闭
| 原挂账 | 位置 | 处置 |
|---|---|---|
| 公式保护表其它字符（`,` `.`） | v28.18 仍挂账第 1 条 | ✅ **否决关闭**：零收益 + 全库键换血，配置不动 |
| 行末断字渲染侧合词 | v28.18 仍挂账第 2 条 | ✅ **否决关闭**：POST 28/28 正确合词，无需移植 |

### 仍挂账（未变）
- **EgoPhys 文献区汉化**（第10/11/12页）→ 豆包回路（报告 + 新鲜载荷 `inbox/egophys2026.*`
  已就位，等用户发起）；
- **官方插件容器化**（v28.16 结论）—— 用户未拍板，未动。

### 测试
本轮**零代码/零配置改动**（纯只读实证），回归套件无需重跑；
`tools/tests/run_all.py` 18 套件全绿的状态不变。

---

## 2026-09-19 v28.20 EgoPhys 文献区返工交付完成（豆包回路全流程收官）

### 缘起
v28.18 连字符配置修复后的 force 重译（15:57）把 p10-12 文献区**重新汉化**了（全新 LLM 输出
覆盖了此前豆包采纳过的文献行）→ 门禁第 4 断言 FAIL。按角色边界走豆包回路返工。

### 过程（全程零 API 成本除渲染前置的缓存命中渲染）
1. **载荷**：`adopt.py export --name egophys2026-refs --pages 10-12`（32 段 / 8,061 字符，
   只含 FAIL 页，doc_fp=docsummary:35460f7178018d8a:fac0448e）；
   **交付方式升级**：载荷拷入豆包沙箱 `C:\Users\97638\Doubao\inbox\`（哈希校验一致），
   豆包直接读文件、回稿写 `out\egophys2026-refs.doubao.txt`——不再靠贴聊天窗口；
2. **deliver**：32 段守恒 / 8,244 字符 / sha1=6d71e32c；
3. **import**：PASS（32/32 锚定；提示项全部为句读占位符被豆包人读标点替代——v28.19 已证
   良性口径；3 处歧义锚定取最近，复查级非阻塞）；
4. **inject**：dry PASS → 实写 32 行（seg_inject 自动备份 `cache.v1.db.bak-20260919-165159`，
   备份里留有 15:57 时代 LLM 汉化行的证据）；
5. **render**：渲染前预检 PASS → force 重渲染 **19/19 页 / 48 秒**（全缓存命中，
   "45 秒零 LLM 改对"承诺兑现）；
6. **gate**：双 PASS——
   - verify_render **8/8**：expect `PAC-neRF`/`Difftaichi`/`Chainqueen`/`Physgs`(p10)、
     `arXiv:1509.01149`(p12) 全落页；forbid 三条 15:57 汉化串
     （「用于几何无关系统识别的物理增强连续神经辐射场」「第十一届国际学习表征会议」
     「用于物理模拟的可微分编程」）**0 命中**；
   - post_check **5 断言全绿**，文献区禁汉化由 🔴 转 ✅；
   - 门禁判定 🟢 **PASS（可交付）**，报告
     `server/translated/review/翻译后质检_20260919_165509_Kim_等_-_2026_-_EgoPhys_Learning_Generali.md`。

### 事故复盘（已交付论文 × 配置重译的完整代价，首次全部记账）
15:57 连字符重译的代价不止缓存键换血：**豆包此前采纳过的文献行也被 LLM 全新输出覆盖**，
文献区随之重新汉化，需要一轮豆包返工（本轮，闭环）。今后凡对已交付论文动配置重译，
**文献区返工要预算进流程**。

### 顺带完成（系统侧，不入库）
- 豆包沙箱通道建立：`C:\Users\97638\Doubao\{inbox,out}\`；
- 官方 pdf2zh-next 镜像（3.34 GB）已删 + `docker_data.vhdx` diskpart 压缩
  （6.05→2.40 GB），C 盘可用 98.6→104.4 GB；Docker 内 searxng/crw 完好；
  官方复评可按 v28.16 命令几分钟重拉。

### 状态
EgoPhys **完整收官**（正文连字符 + 文献区均闭环）。挂账仅剩：官方插件容器化（未拍板）、
本地 27+ 提交未推送。


## 2026-09-19 v28.21 文档画像键漂移治本 + 探测/回查口径对齐（fp 世代错配通用修正）

### 缘起
两处缺陷**同源**：配置层仍是最大的缓存键变量。
1. **摘要键漂移**：`doc_summary_fp` 由"首页拼接文本"算出，而那段文本里含 `{vN}` 公式占位符，
   占位符集合由 `config.toml` 的 `formular_char_pattern`（`vchar`/`vfont`）决定 —— 一次
   配置改动就让摘要落盘键与 fp 一起漂移，**全篇换键重译**（CLAP 即此受害者）。
2. **探测与回查口径不一致**：`adopt export` 探到的 fp 属**旧世代**（38bc），渲染当前世代却是
   f6 迁移新建的 `6c71`。按缺省值注入会把译文写进**被遮蔽的旧行** —— `inject` 报 OK，
   PDF 毫无变化（**静默失效**，最坏的一类失败）。

### 过程
**治本（摘要键不再随配置漂移）**
1. `converter._maybe_summarize_document`：拼接前先把 `{vN}` **还原成真实字形**（取 `var[i]`
   的字符流），40 字过滤 / 4000 字截断均在还原后的文本上做 —— 摘要输入从此与占位符配置解耦；
2. `translator._summary_text_key`：落盘键侧再剔一次残留 `{vN}`，两侧口径一致；
3. `cache.py` 增**第 ④ 档「文档画像投影」**（剔 `doc_summary_fp` 后回查 + 命中回写迁移），
   与 ③ 档（剔 polish*/v23s）同约定的门控：仅看 `polish*` 维度、当前世代不含退役参数；
4. 镜像同步（`sync_patches.ps1`，cache.py MD5 `4A91231B`）+ 测试扩展
   （`test_cache_canonical` 用例 17-21、`test_docsummary` 用例 ⑦⑧⑨）；
5. **f6 实测**：CLAP 重渲染 **491s → 72s**（第二次 45s），新摘要键
   `6c71c61ab046e9b9` 稳定；新世代 192 行与上一代 **192/192 逐字节相同、0 新增**
   —— 证实纯回写迁移、**零重译**。

**口径对齐（注入必须落在渲染器会读到的那一行）**
6. 复现投票：`38bc` 5 票（id 6693/6774/6873/6874/6875）对 `6c71` 5 票
   （id 6879/6964/7063/7064/7065）**平票**，旧 `max(votes, key=votes.get)` 取先插入者 → 返回旧世代；
7. `seg_inject.detect_fp` 通用修正：投票单元改为 `(票数, 该 fp 命中行的最大 id)`，`max` 按元组
   字典序比较 —— **平票取最后写入的世代**，与 cache.py ③④ 档"按 id 升序后写覆盖"同一约定；
   依据是 `cache.set()` 恒为 INSERT / `ON CONFLICT REPLACE`（从不原地 UPDATE），id 单调即写入时序；
8. 新增 `newest_row()` 助手 + **注入自检**：dry 阶段查"该段最新行是否在作用域内"，
   实写阶段复查"最新行译文是否真的变了" —— 不符即 FAIL。"报 OK 但渲染器读不到"当场暴露；
9. 新增 `tools/tests/test_seg_inject.py`（9 例：平票取最新 / 票数优先 / 判据是写入序而非名字新旧 /
   自检正反例 / 无行 / 无票与短段），注册进 `run_all.py`；
10. **真实数据回放**（只读 CLAP 侧车 + `zhang2026-cite` 载荷）：OLD → `38bc`（旧），
    NEW → `6c71`（渲染当代）—— 事故复现与修复同时成立；
11. **回归**：19 套件 / **541 条断言 / 0 FAIL**（新增套件含在内）。

**f7 CLAP 第 3 页收口**
12. 重导 p3 载荷 → 与旧载荷**逐字节相同**（sha1 `6AACA3F0`，豆包回稿可直接复用）；
13. adopt 后四阶段：deliver → import（PASS）→ inject（16 行，dry PASS）→ render（45s）
    → gate：`verify_render` **PASS**（豆包措辞「重定向（retargeting）策略 [31], [32]」落 p3）、
    引用完整性 **PASS**（205 vs 191，diff 14，容差 41）、**第 3 页差 38 → 0**。

### 结论
- `doc_summary_fp` 从"配置敏感量"变成"配置无关量"，配置改动不再导致全篇重译；
- **探测口径必须等于回查口径**：判据不是"哪个世代的名字新"，而是"渲染器会读到哪一行"
  （即该段 id 最大的那一行）；
- 缺省注入路径（正常用户跑标准管线的那条）从此不再踩静默失效。

### 状态
**f8 CLAP 全篇已收口（五阶段全 PASS）。**
14. 豆包回稿两版：`doubao1.txt`（用户粘贴免费对话框，15,799 B）→ `import` **FAIL**
    （2 段 / 3 处高价值字形未回锚：`#S36` 的 `88.8%,π0` / `86.0%,π0.5`、`#S48` 的 `A10080G`）；
    `doubao2.txt`（豆包**直接写文件**到沙箱 `out\`，16,493 B）→ 两处改对；
15. `deliver` OK（49 段 / 9460 字符 / sha1 `2b4fa304`）→ `import` **PASS**（49 段回锚）；
16. `inject` **自检立功**：无 `--fp` 时被拦下，报 `[FAIL 自检] 该段最新行(id 7024)不在
    作用域 docsummary:38bc257f82804e11:9fd924bc 内 —— 实写落在被遮蔽世代`，dry 未写库。
    根因是**台账在修复前（17:50）冻结了旧世代 fp**；探针确认当前世代为 `6c71`（最大 id 7067）
    → 显式 `--fp docsummary:6c71c61ab046e9b9:f9ac3711` → 更新 49 行。
    这正是本次修正要挡的静默失效（旧逻辑会报 OK、PDF 无变化）；
17. `render --skip-last 2`：渲染前预检「文献区禁汉化检查通过：3 个文献页的条目均保持原样」
    → force 重渲染 **45 秒**（13% → 73% → 100%，全缓存命中）；
18. `gate`：引用完整性 **205/205 PASS**、占位符无残留、文献区禁汉化 PASS、渲染残渣 PASS
    → **🟢 门禁判定：PASS（可交付）**，报告 `翻译后质检_20260919_200329_...CLAP_Contrastive_Latent.md`；
19. 补验 `verify_render --expect`：`A10080G→p15×1`、`优于 SmolVLA→p12×1`、`[1],[2]→p1×1 p3×1`、
    `[10],[69]→p8×1`，**4/4 PASS**。上一轮 p15 文献区汉化硬 FAIL 已消失。

挂账三项：
- **`export` 探测的 fp 在修复前生成的台账里偏旧**（本次靠 inject 自检 + 显式 `--fp` 解决）；
  重跑 export 即用新逻辑。建议后续让 export 探测口径直接复用 `seg_inject.detect_fp`；
- **`adopt gate` 的 `--expect` 多值传递只保留了最后一条**（实测 `通过 1 / 共 1`），
  否则多断言场景会**静默漏验** —— **v28.22 已修**（`action="extend"`，含 `verify_render`）；
- 官方插件容器化（未拍板）、本地提交未推送。


## 2026-09-19 v28.22 两处「静默失守」的收口（门禁漏验 + 负载规则漏项）

### 缘起
v28.21 收口 CLAP 时留下了**两条挂账**，性质相同：**都属"报 OK 但实际没守住"**。
1. **门禁漏验**：`adopt gate --expect A --expect B --expect C --expect D` 的输出是
   `通过 1 / 共 1` —— 门禁判 **PASS**，实际只验了 **1/4** 条断言。比 FAIL 更危险：
   FAIL 会被看见，漏验不会。
2. **负载规则漏项**：同一篇豆包两个交件版本只在两处不同，恰恰都被 `seg_import` 判
   「高价值字形未回锚」→ `import` FAIL：
   - `#S36` 的 `88.8%,π0` → 写成「88.8%、π0」（块内**半角逗号**被换成中文顿号）；
   - `#S48` 的 `A10080G` → 写成「A 10080G」（块内**被插入空格**）。
   规则 2 只写了"数字字符一个都不能少"，没写"块内的标点与空格也不能动"。

### 过程
**① 门禁漏验（`adopt.py` / `verify_render.py`）**
1. 复现（隔离 argparse，不涉业务）：
   `add_argument('--expect', nargs='+')` + `parse_args(['--expect','A','--expect','B','--expect','C'])`
   → `['C']`。**根因**：`nargs="+"` 对**重复出现**的选项是"后者覆盖前者"，不是追加；
   adopt 内部 `["--expect"] + list(args.expect)` 与 `verify_render` 的 argparse 同病。
2. 修法：`action="extend"`（`_ExtendAction` 先 `_copy_items` 再 extend，不污染缺省值）
   → 实测 `['A','B','C']`；`--expect A B`（一次多个）与 `--expect A --expect B`
   （重复给）**两种写法都累加**，可混用；`--forbid` 同改。
3. 两处同改：`tools/adopt.py` 的 gate 子命令、`tools/verify_render.py`（独立使用也踩同一坑）。
   修后 `adopt` 仍把完整列表透传给 `verify_render`，台账也记全量断言（可复查验了几条）。

**② 负载规则漏项（`tools/seg_export.py` RULES 第 2 条）**
4. 第 2 条在"数字与字母连写是一整块"之后补两条通用约束（不引任何具体论文的记号）：
   - (a) **空格不加不减**：字母与数字直接相邻处不得插入空格（如 `A100` 不得写成「A 100」）；
   - (b) **标点不换形状**：块内原有半角标点（`. , / - _ % ~ = : *`）照抄，不得换成全角/
     中文标点（如 `0.5`、`3,000`、`a/b` 里的 `. , /` 不得写成 `。 ， ／`）。
   并给一条**逐字符自查**办法："把这一小段从原文逐字符抄下，只要有一个字符被顺手改通顺
   了，就算错"。
5. 边界：这是**给翻译方的通用规则**（开发者只提供规则/报告/指令三根杠杆），
   不含任何单篇论文的文本改写 —— 符合"论文内容走豆包回路"的角色边界。

**③ 回归**
6. `test_adopt.py` 新增 ㉓ / ㉓b（4 条断言）：重复 `--expect` 累加、重复 `--forbid` 累加、
   台账记全量、`--expect A B --expect C` 混用累加。→ **46 PASS / 0 FAIL**。
7. `test_seg_export.py` 新增 ⑫（7 条断言）：点明块内空格/标点约束、逐字符自查、
   例子是通用数值（`A100`/`0.5`/`3,000`，**不含** `A10080G`/`π0` 这类本篇记号）、
   规则编号未被打乱。→ **52 passed / 0 failed**。

### 结论
- **凡是"多值参数"都要确认是累加还是覆盖**：`nargs="+"` 单写 ≠ 可重复给。
  实测口径必须是"给了 N 条就验 N 条"，否则门禁的 PASS 不可信；
- **连写块的边界不只是"数字和字母"**：块内的空格与标点同属该块。
  规则写"数字不能少"时，译者仍可能按中文习惯把 `,` 换成 `、`、或为可读性补空格；
- 两条都是**通用修正 + 回归测试**，对任意论文生效，不针对某一篇打补丁。

### 状态
- 已提交（本地 `doubao-pipeline` 分支，未推送）。
- 挂账剩两项：`export` 探测 fp 复用 `seg_inject.detect_fp` 口径（重跑 export 即新逻辑）；
  官方插件容器化（未拍板）。


## 2026-09-19 v28.23 豆包参与「不走 LLM 的那条路」：一个任务内的两趟采纳回路

### 缘起
此前豆包只能事后"修一篇"（缓存手术），参与不进**翻译管线本身**：用户点一次翻译，
机器翻译就把钱花掉了，豆包稿只能是事后补丁。诉求是让豆包进入**不走 LLM 的翻译
产物**这条路径，且钩子必须落在**服务端能停下来的位置** —— 不是引擎子进程内部、
也不是 HTTP 请求内部（那两处都没法"等一个人交稿"）。

设计前提（先想清楚才动手）：
- 引擎是**逐页交替**（`high_level.translate_patch`：一页进去就在 `converter.receive_layout`
  里提段→翻译→排版回写），**切片（页）是引擎唯一的空间参照系**（版面模型、段落聚合、
  回填定位、缓存键全挂在页上）。所以"整篇提字再交豆包"只能在**页这一层**切，不能换别的粒度。
- 新插件是**异步协议**（`_client_wants_async_job` → worker 进后台线程，POST 立刻返回
  `accepted`，进度走 `/tasks` `/events`），所以 worker 可以中途等人，界面不卡。
- MCP 反向语义里**没有"唤醒 agent 干活"**的标准通知，故豆包触发只能是**我们轮询交件**
  （`out/<name>.doubao*.txt` 落盘），不是服务端推送。这条边界值得写死，免得再设计一次。

### 过程
**① 回路本体（`server.py` `_two_pass_adopt` / `_two_pass_run`）**
1. 一个任务四个状态：`提字中 → 待译 → 回灌重渲染 → 完成`。第一趟 `PAUSE_TRANSLATE=1`
   只提字（引擎返回原文、不调翻译 LLM）；侧车→`adopt export` 成 inbox 载荷；任务停在
   「待译」等交件；交件后 `deliver/import/inject` 回灌；第二趟关掉开关重跑，全命中缓存。
2. 交件判据 `since_ts`：`out/` 里常堆同一篇的历史交件（`*.doubao1..5`），自动回路无人
   可问，只能认"**本次任务开始之后**落盘的最新一份"，并显式 `--text` 点名交给 deliver
   （不让它去 glob）。`--force` 只跳**顺序**门禁，段号守恒 / ⋮ 断点对账 / dry 演算一个不减。

**② 真机连撞三个洞（都是"看着正常、实际没走回路"）**
3. **export 必然 0 命中**：`adopt export` 原先靠"库内命中行"反探文档指纹，而暂停档按
   死规矩一列不写 → 反探 0 命中 → 拒导 → 服务端**静默回落成付费机器翻译**。
   修法：指纹**随侧车带出**（`converter.py` 侧车每行加 `doc_fp`，来自引擎缓存参数；
   库行是"翻过的产物"，骨架档恰恰没有产物，从库行反推本就脆弱）→ `adopt.sidecar_doc_fp()` 优先取它。
4. **运营开关被当一次性令牌**：第一趟 finally 里 `os.environ.pop("PAUSE_TRANSLATE")` 顺手把
   **运营开关也 pop 了**，于是**第一个任务跑完，后续任务读不到开关，静默退回直接机器翻译**
   （第二个任务照样烧钱，日志看起来一切正常）。修法：`_two_pass_adopt` 拆成"开关还原壳"
   —— 进门先记 `_op_switch`，出门原样还回去（`None` 才 pop）。
5. **`inject` dry 演算 FAIL**：`seg_inject` 是 **UPDATE-only**（按"原文 + 文档作用域"找行，
   找不到就 FAIL），这是它防"注入进别的文档"的**核心安全设计，不能削弱**；而暂停档不写行
   → 豆包稿无处可写。修法：暂停档改落 **raw→raw 骨架行**（`translator.translate()`），
   且位置必须移到 `cache.get()` **之前** —— 命中旧世代缓存的段落同样要留下"本次键形态"的
   骨架行，否则该段最新行不在本篇作用域内，世代自检照样 FAIL。

**③ 骨架行的止损坏（"敢落骨架行"的前提）**
6. 新增止损阶段 `adopt.py rollback` + `seg_inject.py --rollback`：删"`translation ==
   original_text`"的库行（骨架行的定义就是这个，故不可能误删真译文）；作用域两级 ——
   优先**本篇载荷段原文**，其次文档指纹；两级都没有则**拒绝执行**（宁可漏撤，不可误删全库）。
   `server.py` 三条失败路径（导出失败 / 交件阶段失败 / 等待超时）统一走 `abort()`：
   **先撤骨架行，再回落**成正常翻译 —— 不撤就重跑，第二趟全命中骨架行 → 整篇英文 PDF。
7. 补一处作用域漏洞：若**导出阶段就失败**，台账里没有 export 条目 → rollback 只能退回
   全局 `latest.jsonl`（= "最近渲染过的那一篇"），并发渲染别的论文时会被顶掉 → 撤不掉。
   故 `rollback` 增 `--pdf`，用它认领**按文档归档件**（v28.9 起 converter 每篇必落
   `pdf-<md5[:16]>.jsonl`），作用域变成确定性的。

### 结论
- **暂停档不能"一列不写"**：`seg_inject` 的 UPDATE-only 是安全设计，只能让引擎落
  "待改写"的骨架行，再用 rollback 兜住半途失败 —— 不能为了暂停档去削弱注入端的安全。
- **指纹不该从库行反推**：库行是翻过的产物；指纹本就在引擎缓存参数里，侧车随行带出最省事也最权威。
- **运营开关不是一次性令牌**：一进一出必须原样还回，否则"第二个任务静默退回机器翻译"这类
  问题最难查（日志全绿）。
- **等待上限必须有兜底**：等不到交稿不能让用户拿不到东西，`abort()` 撤骨架行后回落成正常翻译。

### 测试（真机端到端，非离线）
8. **正常回路**：`提字中(0-6s) → 待译(8s) → 交件 → 回灌重渲染 → 完成(22s)`。
   第二趟日志**无任何 siliconflow 请求**（零翻译调用）；`verify_render --expect "豆包译"`
   → **`p1×4 p2×1`**（豆包稿特征串实测落到页上，证明走的是豆包稿不是机器翻译）；
   缓存 5 行由 UPDATE 就地改写（`id 7488-7492`，`translation` = 豆包稿），未新增原文。
9. **超时兜底**：2 分钟到点 → `abort()` → 台账 `rollback: ok` → 骨架行 **5 → 0**（dry 复查 0）
   → 回落成机器翻译，任务 `success` 且产物为**中文**正常 PDF（未出现整篇英文的损坏形态）。
10. 回归：`tools/tests/run_all.py` **全部通过**；`sync_patches.ps1` **11/11 PASS**。
11. 环境侧踩坑（已复现并处置，值得记账）：`server.py` 报"端口 8890 已被占用"却照常起来 ——
    Windows `SO_REUSEADDR` 让新实例与**上一轮遗留的旧实例**同时 LISTEN，请求落到谁身上不确定
    （旧实例还带着旧代码/旧环境）。正确重启顺序：**先杀干净 server.py 进程，再启动，再看
    `/api/history` 为空**。本次即先杀掉遗留 PID 5820 后重来。

### 状态
- 已落地并真机验证（本地 `doubao-pipeline` 分支，未推送）。
- 开关默认**关**（`PAUSE_TRANSLATE` 未设时行为与过去完全一致），别的用户路径不受影响。
- 挂账未变：Yoshioka 第 48 页文献区汉化归属（走豆包回路）、`guideline.txt` 接 `--prompt`、
  `skipLastPages` 判据前移、官方插件容器化（未拍板）。

## 2026-09-19 v28.24 硬编码路径收口：引擎补丁也认 `P2Z_PROJ`，README 解除「必须装 D:\zotero-pdf2zh」

### 缘起
"项目是否落地"分两层：**自用**已落地（真机跑通、有产物），**给别人装**还差一步。扫全仓
（`*.py` / 配置模板 / 脚本）找"换台电脑就废"的硬编码，结论：工具层早已环境变量化
（`adopt` / `seg_export` / `seg_import` / `doubao_bridge` / `server.py` 全走 `P2Z_PROJ`），
**唯一的功能性硬编码残在引擎补丁** `patches/pdfminer_pdffont.py` 两处 —— 字形校正表与字体
降级台账焊死 `D:\zotero-pdf2zh`。危害不是报错而是**静默**：读取处是 `except Exception: pass`，
换盘符后校正表为空、**字形校正无声消失且零报错**（正是它当初要修的那类"某字解码错"会回归）。
配套的 README 第 3 步也只能写"**路径必须是 `D:\zotero-pdf2zh`**——配置文件里写死了这个路径"，
即让用户迁就代码。

### 过程
1. 新增 `_proj_root()`：`P2Z_PROJ` 优先，未设才退回本机默认 `D:\zotero-pdf2zh`
   —— 与 tools/ 及 `server.py` 同一套约定，不引入新的探测方式（不猜路径、不爬目录）。
2. 台账路径改 `os.path.join(_proj_root(), ...)`（保留 `PDF2ZH_FONT_LEDGER` 覆盖，供测试隔离）。
3. 校正表加 `PDF2ZH_FONT_FIXES` 覆盖 + 同源回落；并把 `except Exception: pass` 拆成
   **`FileNotFoundError` 静默**（允许不部署该表，行为与历史一致）+ **其他异常出声**
   （JSON 坏 / 权限不足 → stderr 警告），不再让人拿到空表还以为在生效。
4. README 第 3 步重写为两条：① 配置模板里的绝对路径替换（正斜杠）② `$env:P2Z_PROJ`；
   删掉"路径必须是 `D:\zotero-pdf2zh`"的硬要求。

### 为什么引擎侧不用额外接线
`server.py:85` 本就把 `P2Z_PROJ` 写进它拉起工具的子进程环境；引擎侧
（`server/utils/execute.py`）用 `os.environ.copy()` 继承 → **在启动服务端的窗口里设一次，
服务端 → 引擎一路到底**，无需改动镜像契约之外的任何东西。

### 测试
5. 探针（venv python，同一脚本跑改前/改后）：默认 → 校正表 `D:\zotero-pdf2zh\server\config\font_char_fixes.json`、**3 条**规则；`P2Z_PROJ=D:\__probe__` → 校正表与台账
   **两条路径同时跟随**；把 `PDF2ZH_FONT_FIXES` 指向坏 JSON → stderr 出现加载失败告警、表 0 条。
6. `tools/sync_patches.ps1` **11/11 PASS**（含 `pdfminer_pdffont.py`）；
   `tools/tests/run_all.py` **全部通过**。
7. `git status` 只有两个文件：`README.md`、`patches/pdfminer_pdffont.py`（venv 真源在仓库外，
   靠镜像契约保一致）。

### 结论
- **功能性硬编码已清零**：剩下的同形命中只有两类 —— ①docstring 里的示例路径
  （`adopt.py:51` / `seg_export.py:18-19` / `force_rerender.py:16`，无害）②**本机未开源工具**
  （`tools/strategist.py`、`tools/dedouble_sweep.py`，不随仓库分发）。
- **能让别人装 ≠ 已经验证别人装**：真正的验证是"换个目录从第 1 步走到最后一步"
  （本次未做，挂账）；本步只消除了**已知**的阻塞点。

### 状态
- 已落地，未提交（与"无用代码清理"一并提交）。
- 挂账：干净机/换目录安装验证；`tools/dedouble_sweep.py` 归属（本机一次性脚本，待定去留）。

## 2026-09-19 v28.25 无用代码二轮清理（悬空测试 / 死变量 / 过时上游文档）

### 缘起
v28.24 那轮（38 个文件）删的是"一看就是垃圾"的（macOS 解压残留、一次性实验脚本）。这一轮换
**判据**：不再看文件名，而是按"**是否入库**"分三层查 —— ① `git ls-files --others
--exclude-standard` 为空（仓库里没有"既未跟踪也未忽略"的真缺口）② **已跟踪的 81 个文件**逐个
过三类检查（ruff pyflakes 规则 / AST 全仓引用扫描 / 资产引用核对）③ 忽略区只做记录、不擅自删。

### 过程与结论
1. **死变量（我们的代码）**：`tools/relink_pages.py:56` 的 `kept` —— 计数器废弃后残留，文末
   摘要只打印 `skipped/moved/unresolved` → 删。
2. **死变量（引擎侧）**：`pdf2zh/converter.py:761` `lo, hi = 0, len(chars) - 1` ——
   `_extract_body_chars()` 里**双指针方案的残留**（实现早已改成"从左逐个消费 head + 回退尾部"）
   → 删；顺带把上方那句已不符实现的"用双指针从两端向内收缩"注释改成与实际一致。
3. **函数级孤儿扫描**：AST 遍历 460 个 `py/md/ps1`，对 `tools/*.py` 的每个顶层函数/类统计
   "本文件外的引用数" → **0 命中**：我们的工具没有"定义了没人调用"的函数。
4. **悬空测试**：`tools/tests/test_strategist.py` 被跟踪，但它 `import strategist` —— 而
   `tools/strategist.py` 按开源红线**本地保留、未开源**（`tools/tests/` 整个目录本就在
   `.gitignore` 里）→ 别人 clone 下来这个测试**必然不可运行**。处置：`git rm --cached`
   取消跟踪（**本地文件保留**，军师 49 项单测照跑）。
5. **上游噪声明确不动**：`patches/*` + `server/*` 共 18 处 F401/F841（`io`、
   `perform_update_optimized`、`datetime`、`needUpdate`、`source_mode`、`cropper.h_offset`…），
   特征是在**上游底座与我们的镜像里成对出现** = 上游自身的死代码。动它等于往"最小可审计
   补丁集"里掺入与自研无关的改动 → 不碰（这是判断，不是漏查）。
6. **过时上游文档**：`server/README-v4.0.1.pdf`（5.2 MB，2026-03-31）与
   `README-v4.1.7.pdf`（7.8 MB，2026-08-29）并存、全仓无任何引用 → 删旧留新（可从 git 历史取回）。
7. **复核后保留**：`server.zip`（本地忽略、不入库）、`md-translate/test_input.md`（0.2 KB 样例）、
   `tools/table_zh.py`（README 已声明"研究留存"）、`terms.babeldoc.csv`（此前已定保留）、
   `server/config/venv.json.example`（`environment_lifecycle.DEFAULT_VENV_CONFIG` 在用）。

### 测试
8. `sync_patches.ps1` **11/11 PASS**；`tools/tests/run_all.py` **全部通过**（含军师 49 项单测，
   证明取消跟踪没动本地能力）。

### 状态
- 已落地，未提交。
- 挂账：干净机/换目录安装验证。


## 2026-09-19 v28.26 提字过程可见 + 载荷就绪响铃（进度源换成「文件即真相」）

### 缘起（用户原话）
「它提取文字，我怎么知道它有没有提取好呢？」→「我们在准备里面显示它的提取过程。它文件
做好之后，它最好就是有一个**响铃**，告诉我们已经将文件生成好了。」

### 一、先说发现：看不到进度**不是提字档的问题**，是进度源本来就空
服务端的进度解析有两条路：①抓 Windows 控制台**屏幕缓冲区**（`server/utils/execute.py`
的 monitor，`console_usable` 为真时）；②读 `logs/server_err.log` 的**日志尾部**（只有用
`logs/launch.ps1` 无窗口启动、输出被重定向时才有内容）。

在 IDE 终端里手工 `python server.py` 启动时**两条都断**：IDE 终端不是真控制台（抓屏拿不到
任何行），日志文件也没人写。实测 2026-09-19 23:47：引擎已经 `16/81` 页（控制台 tqdm 可见），
而 `/api/tasks` 仍是 `progress: 0 / message: 正在初始化...`。→ 卡片会全程停在 0%，机器翻译
那趟同样中招。

### 二、修法：进度源换成**侧车**（文件即真相）
引擎逐页自己往侧车追加记录，与"服务端跑在哪种终端里"无关，所以拿它当源最稳：

- `_segflow_doc_sidecar()`：按**PDF 内容 md5 前 16 位**定位 `~/.cache/pdf2zh/segflow/
  pdf-<md5[:16]>.jsonl`，散列口径与 `converter._segflow_paths()` 逐字节一致（实测：那篇
  SILAGE 定位到 `pdf-42c668f646f0fb33.jsonl`，命中）。用归档件而不是 `latest.jsonl`：后者是
  全局单文件，并发/重跑时会读到别人的进度。
- `_segflow_progress()`：读得 `(段数, 页数)`，页数取 `pageid+1`（`page` 是回调计数会漂）。
- `_two_pass_run()` 里起一个 3 秒轮询的**后台线程**（daemon），把卡片刷成
  `提字中：第 22/81 页 · 已提取 350 段（不翻译、不写缓存）`，`progress` 同步给到 99% 上限。

**口径坑（自己踩到并当场修）**：侧车是**一行 = 一页**，记录形如
`{page, pageid, doc_fp, segs:[{raw,trans}…]}` —— 段数必须数 `len(segs)`。拿行数当段数会
**少报一个数量级**（实测那篇 22 行 = 350 段；第一版就是这么写错的，用真实侧车核出来才改）。

### 三、「待译」搬到 export **成功之后**（卡片不再撒谎）
原顺序是"引擎那趟跑完 → 立刻置待译 → 再跑 export"，于是那句"待译：全篇原文已提取，等待豆包
交稿"可能对应一次**失败的导出**（载荷还没落盘就叫人来交稿）。改为 export `rc==0` 之后，
由 `_notify_payload_ready()` 置状态。

### 四、就绪信号：响铃 + 横幅 + ready 标记
`_notify_payload_ready()` 做四件事（提字这趟不走 LLM、比机器翻译快一个量级，人一转头的
功夫就过去了，而"文件已生成、该叫豆包了"是这条链上唯一需要人**立刻**接手的时刻）：
1. **响铃**：`winsound.Beep(880,160)` ×3（3 行、`try/except` 静默失败：无声卡/被禁用不影响任务）；
2. **终端醒目横幅**：`🔔 载荷已就绪：inbox/<名>.txt（N 段 / 覆盖 1-81 页）+ 下一步`；
3. **ready 标记**：`inbox/<名>.ready.json`（段数 / 页覆盖 / `ready_at`）—— "响过了但当时没人"
   时仍可追溯是哪一轮的载荷，且 `list_inbox` 里一眼可见；
4. **卡片**：`待译：载荷已就绪（N 段 / 覆盖 1-81 页），等待豆包交稿`。

另加 `_inbox_dir()`：认 `P2Z_INBOX`，缺省回落**本文件所在项目**的 `inbox/` —— 延续 v28.24
的收口原则，不新焊盘符。

### 测试
1. `py_compile` 通过；镜像 `sync_patches.ps1` **11/11 PASS**（`server_server.py` 新哈希 `76653514`）；
   `tools/tests/run_all.py` **19 套件全部通过**。
2. 侧车口径用**真实数据**核：md5 定位命中 `pdf-42c668f646f0fb33.jsonl`（那篇 SILAGE），
   实读 22 行 = 350 段 / 到第 20 页 —— 正是"行数是页数、段数在 segs 里"的实证。
3. 未做的验证：实机响铃与逐页刷新（下次真跑提字档时看）。

### 状态
- 已提交 `fa45426`（配 PIPELINE_STORY 的 `b6d7c6a` 一起推送到远端）。
- 挂账：机器翻译那趟的进度仍依赖控制台抓屏/日志尾 —— 想稳就按 `launch.ps1` 无窗口启动
  （输出重定向到 `logs/server_err.log`，进度解析走日志尾）。本次未改那套解析。

---

## 2026-09-19 v28.27 就绪铃换成自带铃音（可选换音，且不在前端）

### 缘起（用户原话）
「你找的提示音是什么样的?」→「你可以去网上找一些公开的提示音，听起来比较舒服，铃铛样子
也可以，有叮咚。」→「不要太古板了」→ 试听后选定 **`dding-dong · musical`**（钢琴和弦）。

### 一、先说清：前端早就有提示音，但它救不了这一刻
`server/index.html` 一直带提示音：`DEFAULT_DING_SOUND = "/bo.mp3"`，界面上还能换自定义音
（存 localStorage 的 data URL）、有静音开关。可它的触发条件是
`!task.active && (st==="success"||st==="failed")` —— **任务结束才响**。而两趟流程在「待译」
这一刻任务仍算 active（worker 正每 5 秒等 `out/<名>.doubao*.txt`），于是整段都是静的：
**恰恰是需要人立刻接手的时刻没有声音**。→ 就绪铃只能由服务端自己发。

### 二、选音：三个公开 CC0 站点都被环境挡了，最后走 GitHub
| 来源 | 许可 | 结果 |
|---|---|---|
| BigSoundBank（有 `Notification`/`Doorbell`） | CC0 | 直链有热链接保护，`UPLOAD/wav/<id>.wav` 回的是 HTML 页（36KB，`<!DO` 开头） |
| Freesound | CC0 | 下载要登录 / API key |
| Wikimedia Commons | 自由许可 | TCP 443 通，但 TLS 握手被重置（本机连不上） |
| **GitHub `CaesiumY/dding-dong`** | **MIT** | 通。三套音色（default / musical / retro）× 5 事件，全是现成 16-bit WAV |

实测确认 26 个候选（上述 15 + `opencode-notifier` 5 + `Windows\Media` 6）**全是 16-bit PCM**，
`winsound` 可直接放 —— 这点必须先验，24-bit 会被拒（`not a valid wave file`）。

### 三、修法（四件事）
1. **音频随服务端走**：`server/notify-complete.wav`（34KB，44.1kHz 单声道，0.4 秒）——
   与既有 `bo.mp3` 同一套定位方式（`os.path.dirname(__file__)`），不新焊盘符。
2. **可换音**：`PDF2ZH_NOTIFY_SOUND` 指向任意 WAV 即覆盖自带音（仍是 16-bit 要求）。
3. **响两下**：0.4 秒一声太容易错过，`NOTIFY_SOUND_REPEAT = 2`（间隔 0.25 秒）可调；
   文件缺失（换机/被删）或非 Windows → **退回原来的三声蜂鸣**，全程静默失败。
4. **README 记来源与许可**（`dding-dong` 的 musical 包 `complete.wav`；该仓库未单独标注
   音频素材出处，写明"介意可自行替换"）。

### 测试
1. `py_compile` 通过；镜像 `sync_patches.ps1` **11/11 PASS**（`server_server.py` 新哈希 `D46B1F22`）；
   `tools/tests/run_all.py` 结论「全部通过」（末尾 `TRAE Sandbox Error` 是沙箱拒写 venv
   `__pycache__`，非测试失败）。
2. 用 `_play_notify_sound()` 同一段调用实放两遍，出声、无异常。
3. 未做的验证：真机跑到「待译」时听一次（需重提一篇走完第一趟）。

### 状态
- 已落地，未提交。
- **需重启服务端**才吃到新音（当前进程仍是 v28.26 的三声蜂鸣）。
- 挂账：`dding-dong` 音频素材出处未标注 → 若日后要更严格，可换成本项目自己合成的 WAV。

---

## 2026-09-20 v28.28 交付门禁升到「内容级」+ 两趟回路止损分家（SILAGE 事故）

### 缘起
SILAGE 那篇走完两趟回路，用户拿到的 PDF 出现**翻译重复、排版乱序**。排查到交付稿时发现：
老 `deliver` 门禁只查两件事 —— **段号集合守恒** + **⋮ 断点条数** —— 而这份问题稿**两样全过**。
真正的问题在它下面一层：交付稿**整段错位**（载荷 #S2 的译文被放到了交付的 #S3 上，成片平移），
加上大量 `[n]` 引用号、数字被译者顺手改了。老门禁查的是"段落个数对不对"，没查"每段装的是不是
那一段的东西"。这类错**渲染出来照样有字、照样排得下**，但整篇对不上号。

### 一、交付门禁升到内容级（`tools/adopt.py`）
新增「逐段不变量」整节，与既有 ⋮ 对账合并成同一道门禁、同一份拒收报告。

**判据只取"译者不该动的东西"**：数字多重集 / `[n]` 引用多重集 / `𝒪(` 记号计数（⋮ 那条已有，并入同一报告）。

**定标（先量判别力，再决定是否硬拦）**：
| 样本 | 数字多重集不符 | `[n]` 引用 | `𝒪(` | ⋮ |
|---|---|---|---|---|
| 对照组：10 篇**历史上已被采纳**的交件（311 个含数字段） | 1 例（0.3%） | 0/25 | 0/0 | 0/7 |
| 问题稿：SILAGE（790 段） | **131 段不符** | — | — | 4 条不符 |

那唯一 1 例误报已查明（`wang2026 #S25`：译者把加粗的 `1` 展开写成"元素全为 **1** 的 N 维列向量"，
属合理改写）。误报 ~0.3% vs 问题稿 24.6%，**判别力足够** → 做**硬门禁**：不符即拒收。

**为什么必须硬拦、不能静默回落机翻**：静默回落看着"总比没有强"，实际是最坏的一档 ——
用户拿到一份**机器翻译**的 PDF，却以为是自己的稿渲染出来的，对着"重复、乱序"排查一整晚，
而那份 PDF 里一个豆包的字都没有。

**错位带（shift band）单列**：源某段的签名出现在交付的**相邻段号**上（先试 ±1 再试 ±2，
只留连续 ≥2 段的，单段命中当噪声）→ 说明内容没丢、只是号错位。单列的意义在于**告诉豆包
改什么**：错位带要改的是**编号**，不是重译 —— 而重译正是重新引入错位的做法。不分离出来，
豆包面对的是 131 条"数字不符"，会以为要重译那 131 段。

**拒收报告落 `server/translated/review/门禁拒收_<时间>_<名>.md`**（与 `post_check.review_dir()`
/ `doubao_bridge.REVIEW` 同一目录）。落文件而不是只印终端，是因为"豆包能矫正"的前提是它
**看得见**自己哪一段被拒；头部第一行固定写 `- 门禁判定: FAIL（…）`，因为桥的 `_headline()`
只读前 6KB 抓这一行，抓不到的话 `list_reports` 里这条就是空白。

**`--waive` 显式放行**：少数合理改写（实测 311 段里 1 例）不该把整篇卡死。放行**必须留痕**：
台账记 `waive=True` + `waived={"cut":…, "inv":…}`，报告照落，事后查得出"谁放过了什么"。
（`--force` 跳的是**顺序**门禁，不跳内容门禁 —— 两者用途不同，没有合并。）

**一个刻意的降级**：载荷（源文）读不到时**打印警告 + 台账记 `inv_ok=False`**，而**不据此拒收**。
理由：缺的是我们自己产物的路径，拿它挡住豆包的稿子是本末倒置；且段号守恒与 ⋮ 对账本就不依赖
载荷，那两道已经跑过了。同时明确**不能拿空基准去逐段比** —— 那会把每一段都判成"数字多出来"，全线误杀。

### 二、两趟回路止损分家（`server/server.py`）
原来只有一条止损路：任何中断 → `rollback` 撤骨架行 → 回落成正常机器翻译。SILAGE 这一晚证明
这条路对"交件被拒收"是**双向有害**的：

1. **撤了就白跑**：骨架行是 `seg_inject`（UPDATE-only）唯一的落脚点，撤掉之后修复回路得从整趟
   提字（81 页）重来；
2. **不撤又跑机翻更糟**：骨架行按最终键形态躺在库里，机翻会整篇命中它们，直接出一份**整页英文**的 PDF。

故拆成两个函数，按"**有没有交件可留**"分工：

| 函数 | 触发条件 | 动作 |
|---|---|---|
| `abort_fallback` | 载荷导出失败 / 等稿超时（**没有交件**） | 撤骨架行 → 回落成正常机翻（用户至少拿到东西） |
| `abort_keep_delivery` | deliver / import / inject 任一阶段失败（**有交件**） | **保留现场 · 判失败 · 不跑机翻**；骨架行与交件都留着，不必重跑提字 |

`abort_keep_delivery` 新增异常类型 `TwoPassGateRejected(RuntimeError)` 往上抛 —— 服务端"任务失败"
本来就是异常驱动的（引擎侧也是 `raise ValueError(...)`），抛出去就落进既有的 `complete_task(failed)`
+ 失败根因提取，不必在 `_execute_translate_job` 里另开分支。它同时在高亮框里点名两件事：
修法是"让豆包按 `review/` 下最新的「门禁拒收」报告改完重交"，以及**警告**"切勿改回正常档重跑本篇：
库里的骨架行会被机翻整篇命中，产物会是全英文 PDF"。

### 三、桥认得出新报告（`tools/doubao_bridge.py`）
`REPORT_KINDS` 加「门禁拒收」（排在「翻译后质检」之后）—— 它比质检更靠前：那份是**出 PDF 之后**
的质检，而这份交件**根本没进渲染**。豆包改稿照的就是它。

### 四、载荷规则加严（`tools/seg_export.py` 的 `RULES`）
两条通用约束（**不举该篇的例子**，否则就是论文先验）：
- **规则 3（⋮）补正反例**：原来只有一句"除此之外不得出现该符号"，既没给正例（合并段该有几个）
  也没给反例（普通段一个都不许有），译者把它当成了可自由使用的分隔符。现在写清：载荷有 n 个就写
  n 个；(b) 载荷没有 ⋮ 的普通段**一个都不许有**；(c) 标点/换行/空行/栏末都不算断点。附自查办法：
  **数一数载荷的 ⋮ 个数，再数一数译文里的，两个数必须相等**。
- **新增规则 8「残句碎片照译不补全」**：跨页/跨栏切断产生的半词段（以半个词开头/结尾、或句子被
  拦腰截断）**只译它现有的字面** —— (a) 不补全成完整词句；(b) 不臆测被切掉的部分；(c) 不与相邻段
  合并。补全 = 译文凭空多出内容，拼接处会重复、与原文对不上号。

### 五、术语查证子项（第 4 条末尾，用户当场追加）
用户：「加上专业术语查询，保持准确性」。缘起是豆包侧反馈——**术语拿不准时凭直觉生造译名**。
项目本就有查证能力（`doubao_bridge` 的 `search_term`：Zotero 库 PDF 原文上下文 + OpenAlex 学术文献），
但规则里一个字都没写，译者不知道要查。

做法（`tools/seg_export.py` 新增 `TERMS_LOOKUP`，由 `terms_line()` 拼在第 4 条后）：

- **挂在第 4 条下、不动编号**：第 4 条本就是"术语"条，查证是它的下一个动作；单列成第 9 条会离
  术语表太远（译者读到第 9 条时早已开始译了）。编号 1–8 保持不变，既有断言全不受影响。
- **两条分支都要带**：无论走真表还是退化句，子项都跟着出现 —— 退化只退术语**内容**，不退要求。
- **给"没有工具"留退化路径**：(a) 明写"手边没有该工具就直接进入 (b)"。理由是这条规则会随载荷
  发给**任何人的**豆包，不能假设桥一定注册了 —— 不能让别的用户被一条用不上的工具卡住。
- 四条实义：**(a) 先查证**（中文学术文献用过的译名优先照它）→ **(b) 再定名**（查不到证据才按学科
  惯例定，首次出现处括注原文、之后不再括注）→ **(c) 不得生造**（不硬凑像术语的中文词，也不原样
  留英文蒙混）→ **自查**（把译文里像术语的词列一遍，逐个答出依据，答不出的回原句重译）。
- 例子用**通用词**（「热带气旋(tropical cyclone)」），不举本项目任何一篇的术语 —— 与规则 2/7 同一条纪律。
- **组名必须点名**（用户当场追问「(b)是什么」）：规则 2/3/4 各有一组 (a)(b)(c)，子项里裸写一句
  "就直接进入 (b)" 会串到上一条去。故开头写死「按下述 (a)(b)(c) 三步定」，引用处写「直接做**下面**
  (b) 的定名」。原文"就直接进入 (b)"已改。

### 六、仓库卫生
`.gitignore` 加 `state/`：根目录下那个 `state/_xpi_probe`（zotero 插件 xpi 解包试验，400KB+）
代码不引用、也未被忽略 —— 按"开源红线"判据它是**真缺口**（既没入库也不在 `.gitignore` 里），
防日后 `git add -A` 漏提交。

### 测试
| 套件 | 结果 |
|---|---|
| `tools/tests/test_adopt.py` | **54 PASS / 0 FAIL**（新增 ㉔a–㉔d 共 8 例：数字不符被拦 / 报告落 review/ 且头部可供桥抓 / 错位带诊断 / `--waive` 留痕 / 缺载荷不误杀） |
| `tools/tests/test_seg_export.py` | **72 PASS / 0 FAIL**（新增 ⑬ 9 例：规则 3 正反例与自查办法 / 规则 8 三条子项 / 未混入该篇例子；新增 ⑭ 11 例：查证子项两条分支都在 / 点名工具与两种证据源 / 退化路径 / 不自造编号 / 组名点名） |
| `tools/tests/run_all.py` | **19 套件全部通过** |
| `tools/sync_patches.ps1` | **11/11 PASS**（`patches/server_server.py` 新哈希 `D8892DF5`） |

实跑验证：拿 SILAGE 那份交件跑新版 `deliver`，精准抓出 **⋮ 4 条 + 逐段不变量 131 条 + 错位带 13 条**；
报告落盘后经桥 `list_reports` / `get_report` 可列可读（headline 正常返回）。
另渲染了一份规则块逐行肉眼核对（全角标点与既有条目一致）。

### 状态
- 已提交 `6558fc4`（术语查证子项与台账本节的追加随后续提交）。
- 服务端已**重启为正常档**（`PAUSE_TRANSLATE` 未设）并自检：8890 单监听 / `/health` 返回
  `version 4.1.7` / `/api/tasks` 与 `/api/history` 均空。
- 挂账（内容侧，走豆包回路）：SILAGE 交件照 `review/门禁拒收_20260920_101602_Sokolov….md`
  改好重交，只走 deliver/import/inject + 重渲染，**不重跑提字、不花 LLM 钱**。
- 挂账（代码侧，未做）：跨页半词碎片是上游 1.x 的解析限制，`look-ahead`（v23.4）够不着跨页对；
  治本要等 BabelDOC 的 DocLayout-YOLO 解析迁过来，本轮只把"不许补全"写进规则。
- 挂账（低优先）：`inbox/` 下现有载荷都是 v28.28 之前导出的，规则文本已冻在文件里 —— 想吃到
  规则 3 正反例 / 规则 8 / 术语查证子项，得**重新 export**。SILAGE 那份正在修复回路里，**不要重导**。

---

## 2026-09-20 v28.28b 门禁补两类判据「留空 / 只译半截」+ 错位带假阳性修复（SILAGE doubao2 取证）

### 缘起（用户原话）
「豆包翻译完了，我也把原 pdf 文件交给了她，可以有个参照」。即：豆包**带着原 PDF 参照**改出了第二版
交件 `out/…doubao2.txt`。交付门禁（v28.28）当场拒收 —— 但拒收报告把 **89 条逐段不变量不符** 全列成
"数字按载荷原样，不改不减不增"。译者照那条改会去**抠数字**，而真相不是数字。

### 一、零成本取证（只读侧车/载荷/交件，一次 LLM 都不调）

| 探针 | 问题 | 结论 |
|---|---|---|
| `_probe_shift.py` | 我们的载荷顺序是不是错了？ | `(page, seg)` **严格单调不减，0 处逆序** → 载荷没问题，错位出在**交付稿** |
| `_probe_shift3.py` | 89 条不变量不符怎么归因？ | **38 条是错位连带**（号错位导致整片对不上）+ **51 条真"改错"** |
| `_probe_shift2.py` | 那 51 条真改错长什么样？ | 绝大多数是**段尾截断**：`#S774` 停在"执行在"、`#S596` 停在"成立。"、`#S370` 停在"计算 𝑔𝑡。" |
| `_probe_cut.py` | 空段有多少 | doubao2 有 **8 个空段**（`#S270/#S308/#S309/#S336/#S337/#S364/#S580/#S697`），比 doubao.txt **多 5 个**，**全在错位带上** |
| `_probe_len.py` | "只译半截"能不能做硬判据？ | 覆盖率 < 15%：对照组 **4 篇已被采纳**的交件（324 个 ≥40 字符长段）**0 例**；SILAGE **29 例** |
| `_probe_frag.py` | 是不是碎片密度异常导致？ | SILAGE 17.1% vs wang2026 15.4% —— **不异常**，问题在豆包**处理碎片的方式**（规则 8 就是治这个） |

**机制闭环**：豆包把相邻的半句碎片**并成一句**译出 → 顶掉一个段号 → 后面整片上移 → 末尾留空。
所以"留空 + 只译半截 + 错位带"是**同一个病因的三种表现**，而"数字不符"只是它的**结果**。

### 二、改动一：新增「留空 / 只译半截」判据（`tools/adopt.py`）

```python
# [自研补丁 2026-09-20b] 留空/只译半截判据 (SILAGE doubao2 事故)。定标: SILAGE 790 段
# vs 4 篇**已被采纳**的交件(324 个 >=40 字符的长段) —— 空段 8 例 / 0 例,
# 覆盖率 < 0.15 共 33 例 / 0 例。干净到可以当硬门禁。
_GAP_MIN_SRC = 40       # 源段短于这个字符数不判覆盖率: 短段比值噪声大(中文天然短)
_GAP_RATIO = 0.15
```

`gap_defects(src, dst)` 返回 `(留空段号, [(截断段号, 源长, 交付长)...])`：
- **留空** = 交付该段一个字符都没有（源段本身为空的"空对空"跳过，不算缺陷）；
- **截断** = 交付长度 < 源长的 15%（**且源段 ≥ 40 字符** —— 短段比值噪声大）。

**为什么必须单列一类**（而不是并进"逐段不变量"）：这类段的"数字不符"是**结果**不是原因。上一版报告
把它们全列成"数字按载荷原样"，译者照那条改会去抠数字，而真相是"这段没译完" —— **改法指错地方，白跑
一轮**。分出来之后改法才对得上病因（把丢掉的尾巴补上）。

### 三、改动二：`shift_bands()` 修假阳性（**这是本轮真正的 bug**）

签名不具判别力的段会**成对误报"互换"**。SILAGE 的 `#S58-59` / `#S60-61` 两段都是全列举句
（`(1,2)` 等同一串数字），四段**各自在家**（offset 0 就对），却因 ±2 处签名相同被报成"错位带"。
修法：进 `for off in (-1,1,-2,2)` 循环**之前**先判 offset 0：

```python
        if inv_sig(dst.get(k)) == sig:
            continue                      # 在家的(offset 0 就对), 不是错位
```

**实测效果**：修前 5 条错位带（含 2 条假阳性），修后 **2 条**（`#S266-#S269` / `#S332-#S337`），
与探针复核定性的"真错位带"完全一致。

> 连带：`test_adopt.py` ㉔b 的末段必须从"引理 44"改成"引理 55" —— 否则交付 #S4 与载荷 #S4
> 同签名，会被这条新前置检查跳过，测不到"纯错位"的完整错位带。该场景由新增的 ㉕d 单独覆盖。

### 四、改动三：拒收报告按「好改、影响大」重排（`write_gate_report()`）

节序改成 **留空/半截 → 错位带 → 逐段不变量 → ⋮ → 改法**：
- 新增 `## 一、留空 / 只译半截（先看这里，多半是这批）`：留空段列号；半截段给表
  `| 段号 | 原文长度 | 交付长度 | 原文结尾（照它补完） |`；
- 不变量那节加一句 **"若某段同时出现在第一/二节，它的数字不符是后果：先修上面，不要单独调这里的数字。"**
- 「改法」补两条可执行项：**相邻碎片段各译各的，不要并成一句**（并段 = 顶掉一个段号，后面整片上移、
  末尾留空 —— 直接对应本次事故机制）；**段尾公式残留照抄**，不要因为"看着不完整"就省略。
- 台账同步记 `n_empty` / `n_short`，`--waive` 的 `waived` 也带上这两项。

### 五、实测（拿 doubao2 那份交件跑新版 deliver）

```
#S270 #S308 #S309 #S336 #S337 #S364 #S580 #S697          <- 留空 8 段
#S280(59->3) #S375(589->81) … #S786(491->36)             <- 只译半截 29 段
⚠️ 错位带: 载荷 #S266-#S269 (4 段) 的译文被放到交付的 #S265-#S268
⚠️ 错位带: 载荷 #S332-#S337 (6 段) 的译文被放到交付的 #S330-#S335
→ 拒收: 留空 8 段 / 只译半截 29 段 / 错位带 2 条 / 逐段不变量 89 条 / ⋮ 4 条  (exit 1)
```
报告：`server/translated/review/门禁拒收_20260920_104737_Sokolov _ - 2026 - SILAGE Memory-Efficient_ Full-Gradient-Fr.md`
（桥 `list_reports` / `get_report` 可列可读，headline 正常）

### 测试
| 套件 | 结果 |
|---|---|
| `tools/tests/test_adopt.py` | **62 PASS / 0 FAIL**（新增 ㉕a–㉕d 4 例：留空被拦且单列一类 / 只译半截报 源长→交付长 且表里给原文结尾 / 空对空不误杀 / 签名碰撞不再误报错位带；㉔b 末段改「引理 55」） |
| `tools/tests/run_all.py` | **19 套件全部通过** |
| `tools/sync_patches.ps1` | 本轮只动 `tools/`，**无需重跑** |

### 挂账
- 内容侧（走豆包回路）：SILAGE 照 `review/门禁拒收_20260920_104737_Sokolov….md` 改完重交
  （**先修留空/半截，再看错位带；不要重译整篇，不要并段**），只走 deliver/import/inject + 重渲染。
- 代码侧（未做）：跨页半词碎片治本仍要等 BabelDOC 的 DocLayout-YOLO 解析迁过来。


## 2026-09-20 v28.29 报告改成「并排证据」+ 上标数字归一化 + 桥不再藏「门禁拒收」（SILAGE 第三轮）

### 缘起：第三轮交件「数字上去了、尾巴还是没补」

豆包第二版（54746 字符）把 28 段半截补回来了（`#S375 589→252`、`#S778 1101→375`、
`#S594 550→189`、`#S377 526→236`），留空清零、错位带从 2 条降 0 条 —— 但 deliver 仍拒收。
逐条读报告第三节发现：**改法仍然指错地方**。`#S774 295→69` 停在"执行在"、`#S772 164→42` 停在"为"、
`#S775 174→46` 停在"小批量大小。" —— 都是**长段译到一半就停**（数字落在没译的尾巴里），
可它们不在第一节（覆盖率 0.19~0.30 > 0.15 阈值），于是被第三节一句"数字按载荷原样"推去抠数字。

### 一、先证「阈值不该动」（`logs/_probe_sweep.py`）

| 阈值 | 0.45 | 0.40 | 0.35 | 0.30 | 0.25 | 0.20 | **0.15** |
|---|---|---|---|---|---|---|---|
| gate=ok 干净语料 344 长段 误报 | 153 | 126 | 88 | 53 | 16 | 1 | **0** |
| Sokolov（阳性对照） | 427 | 368 | 280 | 177 | 78 | 24 | 2 |

`_GAP_RATIO=0.15` 已是"0 误报"下的最高值（英中字符比天然 0.3~0.4，靠长度比值区分不出截断）。
**结论：阈值保持不动**，改用"给证据"而不是"加判据"—— 把载荷原文与交付译文**并排**摆给译者看。

### 二、改动一：`inv_sig()` 上标/下标数字归一化（`tools/adopt.py`）

载荷里的脚注/指数经 PDF 提取常落成行内 ASCII（原文 `complexity4`），译者按语义写成上标
（译文 `复杂度⁴`）—— 同一个数字两种写法。不归一化就是 3 条伪影（`#S22`/`#S90`/`#S557`），
报告会让译者去改**本来是对的**数字。

```python
_SCRIPT_DIGITS = {ord(c): str(i) for i, c in enumerate("⁰¹²³⁴⁵⁶⁷⁸⁹")}
_SCRIPT_DIGITS.update({ord(c): str(i) for i, c in enumerate("₀₁₂₃₄₅₆₇₈₉")})
_SCRIPT_DIGITS.update({0x207B: "-", 0x208B: "-"})   # ⁻ / ₋ 上标·下标负号
```

两侧同做（`str.translate` 是纯函数，归一只可能**减少**不符、不可能新增不符）。实测 52 → **49 条**。

### 三、改动二：报告第三节改成「载荷 ↔ 交付」并排 + 三种病三种改法（`write_gate_report()`）

```
**#S772**
- 不符: 数字 载荷 `0, 0, 20` → 交付 `无`
- 载荷: The resulting empirical constants … are reported in Table 4 of Section 4.
- 交付: 合成实验使用的实证常数 …（保留两位小数）为
```

节首给三种病的对号入座：**① 译文其实是相邻段载荷的译文**（内容整体挪位）→ 按第二节的法子对齐、
别动数字；**② 只译了前半句** → 补完后半句；**③ 公式被改写成文字** → 按 rule 7 整块照抄。
片段口径 `_REPORT_SNIP_*`（≤180 全出，否则头 110 + 尾 60）—— 留尾是因为"数字常落在没译的尾部"。

### 四、改动三：「改法」订正三处

1. **交件口径反了**：原文写"按同一命名重交（如 `xxx.doubao2.txt`）"—— 而 `deliver` 见到
   两份候选会判"交件件不唯一"直接拒收。改成 **用同一个文件名覆盖**。
2. 补一句 **"并段/重切正是内容漂移的头号成因"**（碎片段并句 → 顶掉段号 → 整片上移）。
3. 补一句 **错位判据会漏报**：`shift_bands` 只认数字 / [n] / 𝒪(，载荷里数学记号多、ASCII
   数字少的段落它看不见；第三节并排若读出"译文是相邻段的译文"，即便第二节没列也当错位带处理。

### 五、改动四：桥缺省不再替豆包选一类（`tools/doubao_bridge.py`）

`list_reports` 缺省只列 `REPORT_KINDS[0]`=「翻译后质检」，把 deliver 落的「门禁拒收」
（逐段清单）藏在缺省视野之外 —— 豆包"知道失败了、不知道哪 30 段"。改成缺省**列全部类型概览**
（每类最近 3 份，一类都不藏），TOOLS 的 kind 枚举也补上「门禁拒收」。

### 六、取证：这一版还有一条「门禁漏报的错位带」（挂账，**未改判据**）

`#S298-#S310` 窗口并排读出：交付 `#S302` = 载荷 `#S304` 的译文、交付 `#S306` = 载荷 `#S308` 的译文，
`#S308/#S309` 重复 `#S306/#S307` —— **整片内容前移 2 格**，而 deliver 报"错位带 0 条"。
原因：这一带的数学全是 Unicode 斜体（`𝑝=𝑛/𝑚`、`𝑔0𝑖=∇𝑓𝑖(𝑥0)`），`NUM_TOK` 看不见，
签名空 → 被 `shift_bands` 跳过，凑不出"连续 ≥2 段"。
试过两版加强（只让"有判别力的段"连续 / 邻域放到 ±2 外），干净语料上都开始出**假带**，
故**判据不动**，只在报告里把这条局限写明、把证据交给译者（`logs/_probe_bands.py`、`_probe_hits.py` 留证）。

### 实测（第三版交件跑新版 deliver）

```
只译半截 2 段: #S472(50->6) #S698(50->7)        <- 均为已知判据误报(英中比天然 0.12)
逐段不变量 49 条 (上标归一化前 52)  /  留空 0 段  /  错位带 0 条  /  ⋮ 0 条  -> 拒收
报告: server/translated/review/门禁拒收_20260920_120954_Sokolov….md  (25610 字节)
```

### 测试
| 套件 | 结果 |
|---|---|
| `tools/tests/test_adopt.py` | **67 PASS / 0 FAIL**（新增 ㉕e 上标数字不误报 / ㉕f 第三节并排 + 覆盖同名重交 + 漂移成因 + 漏报交待） |
| `tools/tests/test_doubao_bridge.py` | **64 PASS / 0 FAIL**（④ 组改为"缺省列全部类型、一类都不藏"） |
| `tools/tests/run_all.py` | **19 套件全部通过** |
| `tools/sync_patches.ps1` | 本轮只动 `tools/`，**无需重跑** |

### 挂账
- 内容侧（走豆包回路）：SILAGE 照 `review/门禁拒收_20260920_120954_Sokolov….md` 逐条按并排对照改
  （补全尾巴 / 对齐 `#S300` 一带的漂移 / 公式整块照抄），**覆盖同名**重交。
- 代码侧（未做）：`shift_bands` 的"弱签名漏报"要治，得给不变量加一套**逐字保留记号**
  （数学斜体、拉丁术语）当锚，且必须在干净语料上验证 0 假带 —— 与"跨页半词碎片治本
  （BabelDOC 解析）"一同排期。


## 2026-09-20 v28.29 续 「只译了开头」写进报告与载荷规则（SILAGE 第四轮）

### 第四轮交件：并排证据确实起作用，但只修掉了 6 条

豆包按新版报告（120954）在 3 分半内改回 6 条 —— 恰好是并排最扎眼的那几条：
漂移带的 `#S301/#S302/#S306/#S307` + 两段截断 `#S772/#S774`。
**49 → 43 条**，且没有新增（改法不再指错地方，这点已验证有效）。

### 剩下的 43 条是**同一个病**：长段只译了开头，尾巴整段丢

`logs/_probe_tails.py` 逐条列出"交付停在哪、载荷→交付多少字"：

| 段号 | 载荷→交付 | 交付停在哪 |
|---|---|---|
| #S386 | 398→65 | 停在"…首先是锚点索引 𝑖𝑡，然后是集合 Ω𝑡。"（丢掉后两句） |
| #S349 | 331→77 | 停在"…和相同的集合 Ω𝑡。" |
| #S720 | 320→58 | 停在"…设计矩阵。(67) 中的正则项 Hessian 不定但"（半句） |
| #S518 | 247→63 | 停在"…每个 𝑗𝑡𝑖 在 [m] 上均匀。" |
| #S442 | 188→55 | 停在"…我们得到"（半句） |

13 条停在半个短语上、30 条停在第一个句号上，**43 条的交付长度全是载荷的 15%~52%**。

### 排除"是不是我们自己载荷的锅"（三条独立取证）

1. **载荷无重复**：`#S386` 的尾巴文本（`uniform over the size`/`independent across`）
   全文只出现一次 —— grep 载荷确认，不存在"这段文字在别处也有一份"。
2. **交付也没把它搬到别段**：交 #S387 是它自己那段的译文（同样只有头一句），
   不存在"尾巴被挪到相邻段"。
3. **总量比值不撒谎也不可用**：`logs/_probe_ratio.py` 显示 Sokolov 总比 0.40（段级中位 0.36），
   与**已验收**的 wang2026（0.49 / 0.43）、zhang2026-cite（0.51 / 0.36）同量级 ——
   这条**没有判别力**（43 段只占 790 段，摊薄了），但足以排除"整篇只译了一半"。
   结论：是**交付侧**的截断，不是载荷侧的问题。

### 改动五：第三节每条加「长度」（`write_gate_report()`）

```
**#S386**
- 不符: 数字 1, 1, 1 → 1, 1
- 长度: 载荷 398 字 → 交付 65 字
- 载荷: is deterministic. On the other hand, … Recall that 𝑑𝑡 is defined by
- 交付: 是确定性的。另一方面，表 … 首先是锚点索引 𝑖𝑡，然后是集合 Ω𝑡。
```

43 条一列出来全是"载荷大、交付小"，比任何措辞都直白；改法新增第 2 条
「**每段都要整段译完**」（点明 M 明显偏小 = 只译了开头，并教它按三到六成比长度自查）。

### 改动六：载荷规则新增第 9 条「整段译完，不许只译开头」（`tools/seg_export.py`）

规则 8 只说"碎片不许补全"，没说"完整句不许漏译" —— 译者把"这段是碎片"当成了
"译到哪儿算哪儿"的许可。第 9 条明写两者的界限：碎片译到那儿为止，**只要还有完整句没译
就必须补上**，并给比长度的自查办法。**通用约束，不举该篇例子**（否则就是论文先验）。
从下一份载荷起生效；**SILAGE 这份不重导**（避免打扰豆包手上的载荷），本轮靠报告改法第 2 条传达。

### 测试
| 套件 | 结果 |
|---|---|
| `tools/tests/test_adopt.py` | **69 PASS / 0 FAIL**（㉕f 增「长度行」+「整段译完」两条） |
| `tools/tests/test_seg_export.py` | **77 PASS / 0 FAIL**（新增 ⑭ 组锁规则 9；并订正旧守卫"第 9 条必须空着"→"术语查证不得单列成条"） |
| `tools/tests/run_all.py` | **19 套件全部通过** |

### 实测（第四版交件）
```
留空 0 段 / 只译半截 2 段(#S472 #S698, 已知误报) / 错位带 0 条 / 逐段不变量 43 条 / ⋮ 0 条 -> 拒收
报告: server/translated/review/门禁拒收_20260920_121911_Sokolov….md  (26071 字节)
```

### 挂账
- 内容侧（走豆包回路）：照 `门禁拒收_20260920_121911` 把 43 段的**后半句补译完**（不是抠数字），覆盖同名重交。
- 代码侧（未做）：同上一条 —— `shift_bands` 弱签名漏报、"只译开头"若还有下一轮，
  就得考虑把"段内覆盖率"做成**带证据的独立章节**（而不是只给数字）。


## 2026-09-20 v28.30 「只改被点名的段」与「静默失败」两处病根（A+C）

第四轮的复盘（豆包自述 + 本机取证）把病根摊成了四条：①**初始翻译没译完**（790 段一次吐，
为控输出长度只译前半句）；②**前几轮头痛医头**（报告只列它检出的段，它就只改那些段）；
③**第一次补全时代码 bug**（`fills` 字典替换逻辑写错，28 段没生效就提交）；
④**没自查**（靠门禁替它验收）。
本轮只做两件（用户选定 **A+C**）—— ①是结构债（deliver 强制段号守恒，无增量通道），
④是能力缺口（桥九个工具里没有一个是"交件前自检"），均**不在本轮**。

### 一、A：报告加「全篇长度比」（`tools/adopt.py`）

病不在判据松，而在**报告只列判死的段**：硬线 `_GAP_RATIO=0.15` 是 0 误报下限
（定标：4 篇已采纳交件 324 个长段命中 0 例，SILAGE 命中 33 例），**不能抬** ——
抬到 30% 会把中文天然精简的正常段全判死。但 0.15 与"人一眼看出没译完"之间有片灰区：
第四轮那 43 条只译开头的段，交付/载荷比落在 0.15~0.52，硬门禁**一条都没拦住**。

故硬门禁一格不动，报告另加一层**候补清单**（明写「**不判 FAIL**」，只供自查）：

```python
_ADVISORY_RATIO = 0.30                     # 候补线: 门禁放行、但短到值得自己再看一眼
_ADVISORY_BUCKETS = (0.15, 0.30, 0.50)     # 分布分档: <15% / 15~30% / 30~50% / >=50%
```

- 新增 `ratio_audit(src, dst)`：全篇长段（载荷 ≥ `_GAP_MIN_SRC=40` 字）长度比 →
  `(长段数, 比值中位数, 四档计数, 候补清单)`。候补只收 **15%~30%**（硬线以下的归
  `gap_defects` 的 short，**不重复列**）。
- `write_gate_report()` 在「## 改法」之前插入「## 附：全篇长度比（自查用，**不判 FAIL**）」：
  全篇分布（长段数 + 中位 + 四档） + 候补段表（段号/载荷/交付/比值）。
- 改法第 1/2 条加「**别只改门禁点名的那些**」：照「附：全篇长度比」一次扫完，
  否则下一轮又冒出一批同病的新段（这正是第②条病根的直接对策）。
- 与硬门禁**同一份实现**（`ratio_audit`），两处不会漂。

### 二、C：新增 `tools/seg_merge.py` 按段号合稿（治「静默失败」+ 给增量通道）

第③条（28 段没生效就提交）之所以全程无人察觉，是因为**唯一哨兵缺失**：
按段号替换之后没有任何东西回读比对。本工具把它变成硬约束：

| 函数 | 职责 |
|---|---|
| `scan_spans(text)` | 按出现顺序切出每段的**原始字节区间**；重复段号直接报错（`parse_blocks` 只认最后一条，重复会让回读校验与实际提交对不上） |
| `merge_text(text, patch)` | 只换点到段的**正文**，尾部空白（段间空行/换行风格/末尾无换行）照旧；返回（新文本, 原样重发段, 交件里不存在的段） |
| `verify_applied(text, patch)` | **回读哨兵**：补丁每段在写回的文本里必须真的等于新文本，否则返回未生效段号 |
| `parse_patch(text)` | 唯一一处补丁解析（CLI 与桥都走它，免得"什么算合法补丁"两处判得不一样） |
| `merge_file(path, patch)` | 临时文件 → 回读逐段校验 → `os.replace` 原子替换；`ok=False` 时**正式交件一定未被改动**（要么全生效、要么完全没动） |
| `audit_text(name, text)` | 合稿后长度比自查（与 `adopt.ratio_audit` **同一份实现**），报出仍偏低的长段 |

- **字节保真**：按正文字节区间切片，未点到的段一个字节都不动（不重排全文，
  不顶掉 ⋮ 断点/段间空行）；BOM、末尾无换行单独记账。
- CLI：`--name`（必填）、`--patch`（必填）、`--delivery`（可选，缺省取
  `out/<name>.doubao*.txt` 且须唯一）。通不过校验则**非零退出**。

### 三、桥第九个工具 `merge_result(name, patch)`（`tools/doubao_bridge.py`）

合稿工具只有豆包能调到才有意义 —— 桥是它唯一的入口。新增 `_resolve_result(raw)`
（与 `get_result` 同口径，额外允许直接给 payload 原名）与 `tool_merge_result`；
回执报「已按段号合入 N 段 / 未点到的段一个字节都没动」+ 长度比自查；
**被拒时明写"交件 X 未被改动"**。TOOLS 与 `_DISPATCH` 同步登记，模块 docstring
「暴露八个工具」→「九个」。改法第 6 条（覆盖同名重交）也据此补上指引：
**只改了几段就别整篇重吐**（输出一长就撞上限被截断 → 下一轮又冒出一批没译完的，自锁回路）。

### 测试
| 套件 | 结果 |
|---|---|
| `tools/tests/test_adopt.py` | **82 PASS / 0 FAIL**（新增 ㉖ 组 11 条：灰区不判 FAIL / 只有硬线以下被拒收 / 报告含新节与全篇分布 / 候补只收 15~30% / 长段短段边界 / 改法一次扫完 + merge_result） |
| `tools/tests/test_seg_merge.py` | **18 PASS / 0 FAIL**（新建：正常合稿未点到段字节不变 / 补丁无 #S 块拒绝 / 段号不存在拒绝并点名 / 交件段号重复拒绝 / `verify_applied` 哨兵三态 / BOM 与末尾无换行保真 / 原样重发⚠️点名 / 交件候选不唯一拒绝） |
| `tools/tests/test_doubao_bridge.py` | **70 PASS / 0 FAIL**（② 组 8→9 工具 + 三条 `merge_result` STDIO 往返：合入段数回执 / 只换点到段 / 名字歧义报错 / 段号不存在报错且交件原封不动） |
| `tools/tests/run_all.py` | **20 套件全部通过**（新增 `test_seg_merge.py`） |
| `tools/sync_patches.ps1` | 本轮只动 `tools/`，**无需重跑** |

### 顺带订正
- 本文件 2026-09-03 P1 轮「至此代码检查报告处置情况」处，🔴4 的口径偏大 —— 已订正为
  「**只覆盖 `/translate`**，`/crop`/`crop_compare`/`compare` 三个端点至今无同名在途去重」，
  并补记 2026-09-07 的 `_completed_task_id_for_file`（第二层去重，报告未覆盖）。

### 挂账
- 内容侧（走豆包回路）：SILAGE 那 43 段的**后半句**用 `merge_result` 增量合稿（**不要整篇重吐**），
  合稿后 `audit_text` 会当场报出仍偏低的长段 —— 一次扫完，别再等下一轮门禁点名。
- 代码侧（未做）：**B** 桥加第 10 个工具 `selfcheck`（交件前自检：比值 + 段号集合 + 不变量），
  **D** 分批载荷 / 增量交件通道（治第①条结构债）。
- 对外分发：`tools/tests/`、`tools/sync_patches.ps1` 是否开源（`PIPELINE_STORY.md:428`
  已承认"别人 clone 下来这个测试必然跑不动"），或给「无这些工具时的等价校验」说明 —— 待用户决策。


## 2026-09-20 v28.31 对外分发：测试工具链入库 + 只装插件时的等价校验

上一轮挂账「对外分发」的收口。同时给出给豆包的 `merge_result` 用法话术（不在仓库改动内）。

### 一、工具链脱敏入库（解除「开源红线二期」对 tools/tests/ 的排除）

**先取证，再改**（不靠印象）：

- `tools/tests/` 21 个文件**全部是 .py + 合成夹具**，没有人名、没有密钥、没有论文原文；
- 本机路径只有 **10 处「一行式」默认值**，形态完全一致：
  `_VENV_SITE = os.environ.get("PDF2ZH_VENV_SITE", r"D:\Users\<user>\...\Lib\site-packages")`
  （`run_all.py` 的解释器默认值、`sync_patches.ps1` 的 `$venvSite` 同类）；
- **反证**：已入库的 `adopt.py` / `doubao_bridge.py` / `post_check.py` 里本来就公开写着
  SILAGE / wang2026 / EgoPhys 等论文名与事故背景 → **入库不引入任何新的泄露类别**，
  本机路径才是唯一真障碍。

| 位置 | 改法 |
|---|---|
| 10 处 `_VENV_SITE` 默认值（9 个套件 + `test_mirrors_sync` 的 `VENV_SITE`） | `os.path.join(sys.prefix, "Lib", "site-packages")` |
| `run_all.py` 解释器默认值 | `sys.executable`（用哪个 python 跑 runner，就用哪个跑套件；仍可 `PDF2ZH_PYTHON` 覆盖） |
| `sync_patches.ps1` 的 `$venvSite` | `$env:PDF2ZH_VENV_SITE` → 否则由 `$env:PDF2ZH_PYTHON`(或 PATH 上的 python)推出同级 `Lib\site-packages`；都拿不到则显式报错退出 1 |
| docstring 里的 `<用户名>\...\python.exe`（`run_all.py` / `test_cache_canonical.py`） | `<venv>/python.exe` |

本机行为**逐字不变**（用 venv python 跑时 `sys.prefix` 就是那个 venv），换台电脑自动跟随。

### 二、SKIP 契约（不让别人 clone 下来一片红）

`tools/tests/test_strategist.py` **仍不入库** —— 它的被测对象 `tools/strategist.py` 属"含本机路径、本地保留"。
但套件清单是共享的，于是 `run_all.py` 新增 `OPTIONAL_SUBJECTS`：
**套件文件缺、或缺被测对象 → SKIP 并单列，退出码不受影响**。
（这正是 `PIPELINE_STORY.md` v28.25 那条教训的正面做法：入库一个必然跑不动的测试，就是坏的。）

### 三、.gitignore

放行 `tools/tests/` 与 `tools/sync_patches.ps1`，单独排除 `tools/tests/test_strategist.py`，
并写入契约：入库脚本不得出现 `C:\Users\<某人>`（用户身份）或焊死到某个 venv 的绝对路径；
项目默认根 `D:\zotero-pdf2zh` 作兜底缺省是既有约定（有 `P2Z_PROJ` 覆盖），不算违规。

### 四、踩坑：PS 5.1 读无 BOM 的 .ps1 会按 ANSI 解码

改完 `sync_patches.ps1` 后直接跑报 `The string is missing the terminator` ——
根因不是语法，是**文件无 UTF-8 BOM**：PS 5.1 按 ANSI(GBK) 解码，中文字节错位吞掉了后面的引号。
补 BOM（`UTF8Encoding($true)` 重写）后 **11/11 PASS**。附带效果：接收方 PowerShell 也能正确显示中文。
（注：`Edit` 工具保存时会丢 BOM —— 以后改这个文件若又报同类错，先查 BOM。）

### 五、验证
| 项 | 结果 |
|---|---|
| 本机全量回归 | **20 套件全绿、0 SKIP** |
| 模拟公开 clone（临时移走 `tools/strategist.py`） | `SKIP 本机未部署: tools/strategist.py`，退出码 **0**；文件已还原 |
| `tools/sync_patches.ps1` | **11/11 PASS**，`patches/` 无改动（仍与 venv 一致） |
| 残留本机路径扫描 | 待入库集合 **0 命中**（唯一剩的 `seg_export.py:5` 是 `C:\Users\<user>` 占位符，且为既有已入库文件） |

### 六、说明文档

`PIPELINE_STORY.md` 新增「v28.30 对外分发」章，并订正 `:428` 的过时说法（"只有 test_strategist 未跟踪"
→ 实际是 `tools/tests/` 整体排除，本轮才收口）。章内含**「只装插件时的等价校验」对照表**：
开发侧套件 ↔ 运行期做法（`/health` + `/api/history`、`server/translated/review/` 自动报告、
`adopt.py` 六阶段台账与 `--dry`、`sync_patches.ps1` 的 11 项 MD5）——
结论一句话：**开发侧门禁是"改代码前后必须跑"，用户侧门禁是"每次翻译自动跑"**，后者不依赖 `tools/tests/`。

### 挂账
- **公开分支未推送**：本轮改动全在本地 `doubao-pipeline`，推不推、何时推由用户定
  （`tools/tests/` 一旦推上去即公开，之后再收回要改写公开历史）。
- 未做（上一轮已挂）：**B** 桥加 `selfcheck`（交件前自检）/ **D** 分批载荷 · 增量交件通道。



## 2026-09-20 v28.32 治自锁回路：分批交件通道（D）+ 交件前自检（B）

收口 v28.30 挂账的 B、D。两件都只补"回路本身"，不改任何判据 —— 门禁一格未动。

**要治的病**（豆包自述 1、4 两条）：① 790 段一次吐 → 撞输出上限被截断 → 只译前半句 →
下一轮又冒出一批同病段（自锁回路）；② "改完没抽查就直接提交，靠门禁替我验收" ——
桥九个工具里没有一个是"交件前自检"，门禁成了唯一的质控，退回一轮就跑一轮。

### 一、D：分批交件通道（3 个改点，一条动线）

| 位置 | 改动 |
|---|---|
| `tools/seg_merge.py` | 新增 `skeleton_text()` / `write_skeleton()` —— 由载荷生成**交件骨架** |
| `tools/doubao_bridge.py` `get_payload` | 新增 `from_seg`/`to_seg`（闭区间），**只读那一段号区间** |
| `tools/doubao_bridge.py` | 新增第 10 个工具 `start_delivery(name, force)` —— 开一版骨架底稿 |

**动线**：`start_delivery`（段号全立齐、正文全空）→ `get_payload(from_seg,to_seg)` 读一批 →
翻这一批 → `merge_result` 只填自己那几百段 → 下一批。**豆包永远不用吐整篇。**

为什么骨架是必需的：`merge_result` 只认交件里**已存在**的段号 —— 没有骨架，第二批开始就
无处可填，只能回头整篇重吐，也就是自锁回路的入口。骨架的段号与 ⋮ 直接取自载荷，
所以填满之后交付侧的段号守恒与断点对账一定能过（除非填的人自己把 ⋮ 弄丢了）。

`start_delivery` **拒绝覆盖已有交件**（除非显式 `force=true`）：骨架是底稿，
把已改好的稿子冲掉是最坏的一种失败。开底稿时若 `out/` 里还有同篇的其他交件，
回执一并点名（deliver 要求交件唯一）。

### 二、B：第 11 个工具 `selfcheck(name, text)`

把 deliver 的判据**前移**到交件之前，且**不另写一套**：直接 `import tools/adopt.py` 的同一批
函数（`gap_defects` / `diff_invariants` / `shift_bands` / `ratio_audit`），期望段号取
`inbox/<篇名>.manifest.json`（与 deliver 读的是同一份文件；无 manifest 时退回"载荷自身 ⋮ 计数"口径）。
逐项报 PASS/FAIL：段号守恒 / ⋮ 断点对账 / 留空 / 只译半截 / 逐段不变量 / 错位带 / 长度比分布，
末行给结论（`✅ 与门禁预期一致, 可以交` 或 `❌ 门禁会拒收`），FAIL 项一律点名段号。

**只读不写** —— 落报告、拒收、记账仍全部归 deliver。差别只有一个：豆包能在交件前
自己看见问题，而不用等门禁退回来。

### 三、测试逮到的两处真问题（都不是"补个断言"级别的）

1. **骨架的 ⋮ 占位与"正文必须为空"互相打架**（`test_seg_merge` 首次运行即 FAIL）：
   `write_skeleton` 的回读校验用裸的 `if v` 判"段正文为空"，而合并段的骨架里
   **故意留着占位 ⋮** → `parse_blocks` 读出的是 `{2: "⋮"}` → 骨架把自己拒掉了。
   修法不是删掉 ⋮（那是交付侧断点对账要吃的东西），而是把不变量说准：
   **"没有正文"= 去掉 ⋮ 与空白后为空**。骨架要拦的是"混进译文"，
   不是"存在占位符"。
2. **`_load_payload` 重写时丢了既有报错契约**：新写的 loader 报
   `inbox 里没有 'no_such.txt'`，而 `test_doubao_bridge` 的既有断言盯的是「不存在」——
   改回 `inbox 里不存在 %r`。**这正是"改工具实现顺手换掉对外措辞"这类静默回归**，
   靠既有套件当场拦下。

### 四、验证
| 项 | 结果 |
|---|---|
| `test_seg_merge.py` | **33 PASS / 0 FAIL**（原 18 + ⑨⑩ 骨架 15 条） |
| `test_doubao_bridge.py` | **94 PASS / 0 FAIL**（原 70 + 工具数 9→11 / schema / 分批读 / 开底稿 / 自检 24 条） |
| 全量回归 `run_all.py` | **20 套件全绿、0 SKIP** |
| 新增 STDIO 往返 | 请求 12 → 18 条：`get_payload` 分批 / `start_delivery` 开·拒·force / `selfcheck` FAIL·PASS 两态 |

### 挂账
- ~~**未推远端**：本轮全在本地 `doubao-pipeline`。~~ → **已推**：`6a873bd..83d0047`（2026-09-20，远端 ref 与本地 HEAD、4 个改动文件 blob SHA 逐字符一致）。
- 历史挂账不动：`logs\launch.ps1` 启动前杀旧实例；`shift_bands` 弱签名漏报的治本；跨页半词碎片治本。
- SILAGE 那 43 段的后半句：等豆包用 `merge_result` 增量补完 → deliver → import → inject → 第二趟重渲染。


## 2026-09-20 v28.33 不变量判据改为「不漏 + 不幻觉」：复述不算改数字（交接前核现场，43 段→1 段）

用户说"现在从豆包开始"。**交接前先核现场，发现要交接的东西已经不是账上那个了。**

### 一、核现场：43 段那件事，豆包已经做完了

| 判据来源 | 时点 | 逐段不变量 |
|---|---|---|
| `门禁拒收_20260920_121911`（最后一份报告，交件 sha1=`2fb935a7`） | 12:19 | **43 条** |
| 现交件本体（`out/…Full-Gradient-Fr.doubao.txt`，mtime 12:23:23） | — | **2 条** |

交件比最后一份报告**新**，也就是报告判的那一版早被换掉了。若照旧报告把 43 段甩给豆包，
它会去重做 41 段**已经改好**的 —— 正是"整篇重吐"那条自锁回路的入口。
**所以交接前必须先重跑门禁**（`deliver` 落 `门禁拒收_20260920_130049`），否则交出去的是过期判据。

重跑后剩 2 条，且**性质不同**（全篇逐段扫描，按 token 归因）：

| 段 | 载荷有、交付缺 | 交付多出、载荷已有 | 交付多出、载荷没有 | 定性 |
|---|---|---|---|---|
| `#S11` | `['2']` | — | — | **真漏**（数字丢了） |
| `#S699` | — | `['1', '2']` | — | **门禁误报**（复述） |
| 全篇 | 1 段 | 1 段 | **0 段** | 零幻觉 |

### 二、误报的机理：判据比它自己声明的意图更严

第三节写着「按载荷**原样** —— 不改、不减、不增、不换位」，意图是**别改动信息**；
实现却是 `inv_sig` 的多重集**逐个计数相等**。于是中文要把英文省略的主语补出来时 ——
英文 `𝛿1 is small when… and large when…` → 中文「…时 𝛿1 小，…时 𝛿1 大」——
数字跟着复述一遍就被判成"增了数字"。

- `#S699`：载荷 `1, 2`，交付 `1, 1, 2, 2` —— 零真漏、零幻觉，纯复述。
- `#S348`（更早）：`算法 2 而实现算法 3` → 下文再提「算法 2 和 3」，白改过一轮。

这是**实现严于意图**的 bug，不是"要不要放宽门禁"的政策问题：报告会把译者指去改
**本来是对的**段落，正是病根 ② 本身。

### 三、改动（`tools/adopt.py`）

| 位置 | 改动 |
|---|---|
| 新增 `_inv_missing()` | 载荷有、交付缺的 token（多重集差）→ **FAIL** |
| 新增 `_inv_hallucinated()` | 交付出现、载荷**根本没有**的 token → **FAIL** |
| `diff_invariants()` | 数字 / `[n]` 引用 改用上面两条判"不漏 + 不幻觉"；**复述放行**。`𝒪(` 仍按个数相等 |
| 报告第三节判据行 | 同步写明「多出现几次也算过；本节只抓『载荷有的数字不见了』和『冒出载荷里没有的数字』」 |

判别力没有丢：**漏**照抓、**幻觉**照抓，只有"同一 token 多出现几次"这一种被放行。

### 四、验证

| 项 | 结果 |
|---|---|
| `tools/tests/test_adopt.py` | **90 PASS / 0 FAIL**（原 82 + 新增 ㉗ 组 8 条） |
| 新增 ㉗ 组 | ㉗a 复述放行 / ㉗b 真漏仍拦 / ㉗c 幻觉仍拦 / ㉗d 复述掩盖不了真漏 / ㉗e `[n]` 引用两侧同判 / ㉗f 端到端复述段不再拒收 / ㉗g 端到端真漏仍拒收 |
| 全量回归 `run_all.py` | **20 套件全绿、0 FAIL、0 SKIP** |
| 既有断言是否冲突 | 改前先单跑 82 PASS —— **没有一条断言依赖"复述必须判不符"**，说明改的是实现与意图的偏差，不是既有约定 |
| 真实交件重跑 | 逐段不变量 **43 → 2 →（改判据后）1**，只剩 `#S11` |

### 五、实测（重跑 deliver，`门禁拒收_20260920_130416`）

```
留空 0 段 / 只译半截 2 段 / 错位带 0 条 / 逐段不变量 1 段(#S11) / ⋮ 0 条 -> 仍拒收
```

`#S11` 是**同一份交件内部不自洽**：载荷 `complexity2`（`complexity` 后一个上标数字），
而豆包在结构完全相同的 `#S90`/`#S557` 里写的是「迭代复杂度**4**为」「迭代复杂度**5**为」
（数字留着、行内 ASCII），唯独 `#S11` 写成了「梯度评估复杂度**为**」—— 那个 `2` 被丢了。

### 挂账
- **内容侧（走豆包回路）**：只剩 `#S11` 一段 —— 用 `merge_result` 只补那一个数字，别整篇重吐。
- **`--waive` 的用法边界**：`#S472`/`#S698` 两条「只译半截」是已知误报（载荷 50 字 → 交付 6/7 字，
  「Substituting this into the previous display yields」→「代入前一式得」是完整译文，中英字符比
  天然够不到 15%）。硬线 `_GAP_RATIO=0.15` **按既有证据不动**（0 误报下限）；豆包改完 `#S11` 后
  用 `--waive` 放行，台账会留痕 `waived={"cut":0,"inv":0,"empty":0,"short":2}` —— 只放过这 2 条已知误报。
- 历史挂账不动：`logs\launch.ps1` 启动前杀旧实例；`shift_bands` 弱签名漏报的治本；跨页半词碎片治本。
- **未推远端**：本轮（v28.33）在本地 `doubao-pipeline`。


## 2026-09-20 v28.34 回锚匹配器三档放宽 + 合并段 ⋮ 挪位判定（SILAGE 21 段 FAIL 逐条定性）

### 缘起
v28.33 收口交付门禁后重跑 `import`：**21 段「高价值字形未回锚」**。逐条按**码点**比对定性后
发现其中 12 段根本不是"译文缺字"，是匹配器认不出**同一个字的另一种写法**：

| 档 | 段 | 现象 |
|---|---|---|
| 上标数字 3 段 | `#S22`(v4 `3—`) `#S90`(`4`) `#S557`(`5`) | 版面里数字是独立字形，中文排版写成 `⁴`(U+2074) / `⁵` / `³`(U+00B3) |
| 空白弹性 5 段 | `#S79` `#S93` `#S283` `#S560` `#S343` | 译文按可读性补了空格（字形值里没有） |
| OHM SIGN 4 段 | `#S389` `#S471` `#S485` `#S518` | 字形值 `Ω`(U+2126)，译文写同义的希腊大写 `Ω`(U+03A9) |

剩 9 段是真漏译，1 段（`#S22`）是**合并段 ⋮ 被挪**。定性靠三份一次性诊断脚本
（逐条列 `exact_free`/`norm_free`、分档试命中、逐字符列码点），跑完即删。

### 一、改动 `tools/seg_import.py`

| 位置 | 改动 |
|---|---|
| 新增 `_EQ_RANGES` / `_eq_index()` / `_eq_class()` / `_char_pattern()` | 兼容等价类。只并入「NFKC 折成同一个**单字符**、且折出来是字母/数字」的写法（`³/³/₃/３` 同类）；多字符折叠（`Ⅲ -> III`、`℃ -> °C`）一律不收 —— 否则会把无关字符拉进等价类、制造错锚。**正体是 ASCII 时不并入**：否则数学斜体 `𝑏`(U+1D44F) 会被折成 ASCII，去匹配译文里任意一个 `b`，把短 core 的全局唯一性打散 |
| `_core_pattern()` | 三条放宽**都只是定位口径**，命中后写回的仍是字形原值：① 标点等价类 ② 兼容等价类 ③ 空白弹性 —— `\s*` 只在字与字**之间**放宽，两端不放（免得匹配区间外溢） |
| 新增 `sibling_hits()` | 合并段里本侧没落点的字形出现在**别侧**译文 → 判「⋮ 位置被挪」，不是「漏字」 |
| `main()` 回锚循环 | 带索引 + 分流报错：挪位报「合并段的 ⋮ 位置被挪」并说明是哪一侧；其余仍报「高价值字形未回锚」 |
| `write_rework_note()` | 挪位类附「**这一处的修法不同**」段：把 ⋮ 摆回原文那一处，同侧**内部**才可调语序 |

### 二、一个自己踩的坑（被新增测试 ⑭ 逮到）
`_eq_class()` 初版只返回 `{ch} | idx[NFKC(ch)]`，而索引是按「**正体 -> {异体}**」建的 ——
当 ch 自己**就是异体**时（OHM SIGN U+2126，正体是希腊 `Ω` U+03A9）查出来**只有它自己**，
恰好漏掉译文实际会写的那一个字。修法：正体本身也并进来（**仅当正体非 ASCII**）。
测试首跑 `25 passed / 1 failed`，改后 `26 passed / 0 failed`。

### 三、改动 `tools/seg_export.py`（载荷规则 3）
补 (d)「**⋮ 的位置也不能挪**」：左边只写左边原文的译文、右边只写右边原文的译文；
自查办法改为「数一数载荷该段的 ⋮ 个数与译文里的 ⋮ 个数，两个数必须相等，再确认左右内容一一对应」。
依据 `#S22` 实况：原文断点落在 "…the running aggregate⋮that supplies the descent direction in
Algorithms 1 and 2—…"，译文把「即为算法 1 和算法 2 提供下降方向的运行聚合」提到了 ⋮ **左**侧，
右页那侧的 `1`/`2—` 两个字形于是没有落点。**这类 FAIL 让译者"把它原样写回"是无解的** ——
他那侧的译文本来就不该有这段内容，照着改只会来回打转。

### 四、验证

| 项 | 结果 |
|---|---|
| `test_seg_reanchor.py` | **26 PASS / 0 FAIL**（原 19 条 + 新增 ⑬–⑱ 七条：上标命中 / OHM 命中 / 空白弹性命中 / 真缺字仍 FAIL / 挪位判定正反例）|
| `test_seg_export.py` | **77 PASS / 0 FAIL**（规则 3 新文案未打破既有断言）|
| 全量回归 `run_all.py` | **21 套件全绿 / 0 FAIL** |
| 真实数据重跑 `import` | **21 段 FAIL → 10 段**；消失的 11 段正是上面三档放宽型 |

注：⑬–⑱ 的用例**一律写码点转义**（`"\u2126"` / `"\u03a9"` / `"\U0001d44f"` / `"\u2212"`）——
这组测的就是"码点不同、字是同一个字"，直接敲字符看不出自己敲的是哪个
（`Ω` 的两种写法肉眼完全一样，首跑 3 条红全因手敲的码点不对）。

### 五、剩余 10 段（**内容侧，走豆包回路**）
- 9 段真漏译（`#S380` `#S405` `#S406` `#S555` `#S635` `#S643` `#S664` `#S669` `#S727`）：
  **共同点在段尾断掉** —— 最后那个从句/公式整块没译（`𝑏grp.` / `|𝑆|=𝑏grp.` / `𝑏grp∈[𝑛],` /
  `𝐿max.` / `𝜀dir` / `R𝑑→(R𝑑)𝑛` 等）；
- 1 段 `#S22` 是 ⋮ 挪位。
返工单已落 `inbox/…rework.md`（含「缺的字符 + 原文线索 + 修法」），交豆包：
`get_payload` 读单 → `merge_result` 只补点到的段 → `selfcheck` 自查 → 交 `…doubao2.txt`。

### 挂账
- **v28.33 与 v28.34 都未推远端**（本地 `doubao-pipeline`）。
- 返工单的「二、不必改」一节体量远大于「一、必须改」（本次 640 行 vs 60 行）——
  豆包读单要整个吞下。是否给它加"只列计数不列明细"的开关，待拍板（既有测试锁了现文案）。
- 历史挂账不动：`logs\launch.ps1` 启动前杀旧实例；`shift_bands` 弱签名漏报的治本；
  跨页半词碎片治本；官方插件容器化（未拍板）。


## 2026-09-20 v28.35 SILAGE 收口：回锚标点残留 + 引用判据提取空隙 + 自动回路两处自锁

### 缘起
v28.34 把 21 段 FAIL 定性成 10 段内容侧 + 1 段 ⋮ 挪位。豆包改完 10 段（含 ⋮ 挪位那一段）
交回 `doubao2.txt`，跑完整动线后**两道门禁同时亮红**，而页面上看不见任何对应缺陷：

| 症状 | 表面读数 | 真相 |
|---|---|---|
| `post_check` 引用完整性 | 原文 69 / 译文 88（差 19）判 FAIL | 不是引用多出来，是**原文侧提取带空格**（`[ 22]`），旧判据只认 `[22]` |
| `import` 回锚残留 | 20 段译文里出现 `{vN}]` | 吸收口径与定位口径不一致 → `]` 漏吞，渲染写回字形原值即并排出现 `],]` |

另有两处**只在不驻守的自动回路里才暴露**的自锁：内容门禁拒收把两趟回路整条卡死、
render 超时 900 秒硬编码把还在跑的渲染记成 failed，且出门整份覆盖台账会抹掉并发的
deliver/import 记录。

### 一、回锚残留：吸收口径统一 + 「能吃多少吃多少」（`tools/seg_import.py`）

根因是**定位口径与吸收口径不一致**：定位走 `_char_pattern`（认 `PUNCT_EQUIV` 整类），
吸收只认 `_fw(a) == _fw(b)`（`_FW` 只折 `（）：；，` 六个）。两者不重合的位置就**漏吸** ——
字形值 `[16,20],` 的 core 是 `16,20`、pre `[`、post `],`；译文 `SVRG [16,20]、SCSG` 里
`]` 被吞、`、`（≡ `,`）留在占位符外，渲染写回原值后页面成为 `[16,20],]、`。

| 位置 | 改动 |
|---|---|
| 新增 `_punct_eq(a, b)` | 吸收专用判据，口径与 `_char_pattern` 同源：先 `PUNCT_EQUIV` 等价类，再退回 `_fw` |
| `reanchor()` 吸收段重写 | 不再要求"整段对上才吸收"（旧实现有两道整段匹配门），改为**按字形值原序逐位对账、能吃多少吃多少**（pre 从右往左、post 从左往右，首位不等价即停） |
| `reanchor()` docstring | 写清「占位符写回的是字形**原值**，译文里已写出的那套标点必须全部删掉，漏一个就多一个字」 |

「整段对上才吸收」是**第二个 bug**：实况 `SCSG [22] 和` 只对得上 `]`、对不上 `,`
（中文语序改用空格分隔），整段匹配会一个都不吃，残留的 `]` 照样把计数顶出容差。
逐位对账保证首位不等价即停，不会误吞译文里字形并不覆盖的标点。

### 二、引用完整性：判据要容忍提取空隙（`tools/post_check.py`）

同一份 PDF 的两侧提取形态可以不一样：原文里 `[22]` 是公式字体里的字形对象，pypdf 提取
出来带空格（实测 SILAGE 第 2 页 8 处全是 `[ ` 开头），译文侧写的是字形原值 `[22],`（无空格）。
旧判据 `\[\d{1,3}\]` 只认后者 → 逐页对账虚高 19，**差的不是引用，是空格**。

`CITE_ANY` 改为 `\[\s*\d{1,3}\s*\]`。要点：放宽的是**两侧同样放宽**（同一正则对原文与
译文各跑一遍），不是只放过译文；两侧形态一致时行为与旧判据完全相同。带逗号的多引
（`[16, 20]`）与超三位数两侧都不认，译文**真丢**引用仍在容差外判 FAIL。

### 三、定标修订 2026-09-20e：半截面下限 40 → 60（`tools/adopt.py`）

v28.28 的 `_GAP_MIN_SRC=40` 定标只对照了「4 篇采纳稿里 >= 40 字符的长段」，**没覆盖
40~59 这一档**（对照稿里没这么短的段）。SILAGE 第五轮实测该档 2 例双双落在硬线以下，
却都是**完整译文**（`#S472` 50 字 → 「代入前一式得」6 字，比值 0.120）——英文 50 字
本来就是一句短话，中文短句的自然下限 6~8 字与「只译了开头」重叠，**整档没有判别力**。

代价不止误报：误报走「拒绝交件 → 保留现场判失败」那条路，**自动回路直接卡死**。
下限提到 60（0.15 × 60 = 9 字，刚好高过 6~8 字的自然下限）。改后 SILAGE 790 段中
512 段参与判定，`< 0.15` 的 0 例（最低 0.163）。

### 四、自动回路两处自锁

| 位置 | 改动 |
|---|---|
| `server/server.py` `_two_pass_adopt` | deliver 串追加 `--waive`（内容门禁**留痕放行**）+ 放行时打印告警点名。v28.28 的「拒收 → 判失败」是**人在场**的正确收场；自动回路无人值守，豆包读不到任务状态（桥只读 inbox/out 与 review/），拒收会让整条动线停住而稿子还躺在 out/。覆盖面仅限内容门禁不符，结构错误（段号守恒/载荷不可读/交件不唯一）仍走 `die`，waive 到不了；要去掉 `--waive` 即恢复严格口径 |
| `tools/adopt.py` 新增 `render_timeout()` | 重渲染等待上限与 server 的「等豆包交稿」上限**同源**（`PAUSE_WAIT_MINUTES`，默认 30 分），`max(900, N*60)`。由来：81 页稿子重渲染一轮超过 15 分钟，被 900 秒硬编码记成 render failed，而任务其实还在跑。900 留作下限，避免上限比一轮渲染还短时「超时」不再是「卡住」的信号 |
| `tools/adopt.py` 新增 `save_ledger_merge()` | 长阶段（render 跑分钟级）出门只把**自己那一条**并回磁盘最新台账，不再拿进门快照整份覆盖 —— 否则抹掉这期间并发的 deliver/import/inject 记录，台账自相矛盾。`stage_render` 三处（预检 FAIL / force_rerender 非 0 / ok）改用它 |
| `tools/seg_export.py` | 无改动（本轮） |

### 五、验证

| 项 | 结果 |
|---|---|
| `test_seg_reanchor.py` | **29 PASS / 0 FAIL**（新增 ⑲：三处引用全回锚 / 无字形标点残留在占位符外 / 顿号与右括号被吞净；⑫ 断言按「`。` 被吸收是正确行为」同步）|
| `test_post_check.py` | **77 PASS / 0 FAIL**（新增 ⑲ 八条：认无空隙/前导/尾随/两侧空隙、带逗号多引不计、超三位数不计、两侧形态不同计数一致、译文真丢引用仍 FAIL）|
| `test_adopt.py` | **97 PASS / 0 FAIL**（新增 ⑭b render 超时同源四例 + ⑭c 台账并写两例）|
| 全量回归 `run_all.py` | **22 套件全绿 / 0 FAIL** |
| 真实数据 `import` | 回锚残留 `{]}` **30 处 → 0**（`imported.json` 实测），800 段 PASS |
| 重渲染 | inject 824 行 → render 第二趟 524 秒 success；`--timeout 1800` 已生效 |
| `post_check` | 引用完整性 **88 / 88** 转 PASS；占位符残留 / 文献区禁汉化 / 渲染残渣 三项通过 |
| `adopt gate` | **双 PASS**（verify_render 4/4 命中「任意批大小/自然选择/最大光滑常数/定义线性算子」+ 质检 PASS）→ run 交付完成 |

### 挂账
- `selfcheck` 与交付门禁仍共用 `adopt.py` 同一份 `gap_defects`，下限改 60 后两边自动同步 ——
  豆包侧「交付 < 载荷 15% 标半截」的旧口径**已一并消失**（同源实现的好处）。
- **本轮提交（v28.35）与 v28.33 / v28.34 都未推远端**（本地 `doubao-pipeline`）。
- 历史挂账不动：返工单瘦身（60.7 KB）；`logs\launch.ps1` 启动前杀旧实例；
  `shift_bands` 弱签名漏报的治本；跨页半词碎片治本；官方插件容器化（未拍板）；
  2026-09-19 两个内容口径待拍板（公式保护管不管 `,` 和 `.`；行末连字符）。


## 2026-09-20 v28.36 挂账四项逐条推进：返工单瘦身 + 启动清场 + 弱签名定标 + 内容口径试点方案

### 缘起
v28.35 收口后剩余挂账四项经拍板各有去向：返工单瘦身＝**只列前 20 条 + 其余计数**；
跨页半词碎片＝**记为上游限制**，等 BabelDOC 迁移一并解决；官方插件容器化＝**暂缓**；
两个内容口径（公式保护管不管 `,` 和 `.`；行末连字符）＝**先做对照试点再拍板**。

### 一、返工单瘦身：提示只列前 20 条 + 报计数（`tools/seg_import.py`）

「不必改」一节原先把 `report` 里每一条 `提示` 原样列出。一篇几千段的稿子里
「纯标点字形丢弃」一类提示能有几百条（SILAGE 实测单子 60.7 KB / 640 行），
而这些段**一条都不用改**（工具已自动丢弃并继续渲染），逐条列出来只是把
「必须改」淹没掉 —— 豆包本来也不需要为了不去改它而逐条读它。

| 位置 | 改动 |
|---|---|
| 新增 `HINT_MAX = 20` | 阈值与测试同源（测试读 `SI.HINT_MAX`，改阈值不会留下断言旧数字） |
| `write_rework_note()` hints 分支 | 只渲染 `hints[:HINT_MAX]`；超出时补一行 `- …（同上提示共 N 条，其余 M 条不再逐条列出 —— 这些段**不用改**，逐字符照抄你上一版即可）` |
| 未动 | 「必须改」（回锚失败明细）与「其他门禁失败」**一条不少** —— 这两节才是要动笔的地方；判据仍是 `adopt.py` 同一份实现，瘦身只影响**呈现** |

### 二、`logs\launch.ps1` 启动前清场（本机脚本，不入库）

Windows 的 `SO_REUSEADDR` 允许新实例**静默共绑** 8890：旧实例继续服务，新实例的代码/
缓存改动一概不生效（症状＝重启后 `/api/history` 仍是旧任务、去重命中旧 taskId、修好的
缓存渲染不出来）。脚本开头补三段：

1. `Get-StaleRoots` 定"根"：命令行含 `server\.py` 或 `zotero-pdf2zh[\\/]server` 的
   python/uv **＋** 8890 监听者（`Get-CimInstance Win32_Process` 的字段是 `ProcessId`，
   不是 `.Id`）；
2. `Resolve-Tree` 按 `ParentProcessId` **递归收子孙** —— server.py 的 multiprocessing
   子进程命令行里没有 `server.py` 字样（spawn worker），只按命令行过滤会漏；
3. 杀完等端口真正释放（20 × 500 ms 轮询，失败 `exit 1`），**再**清 out/err 日志 ——
   旧实例还握着这两个文件，先清会 `being used by another process`。

实跑验证：`KILL 4820/41004/49904/44700` → 端口释放 → 新 PID 26732；`/health` OK、
8890 单一监听者、`/api/history` 0 条。**注意**：`doubao_bridge`、`uv tool uvx
mineru-open-mcp` 不是本工程实例，脚本正确地没动它们。`logs/` 按 `.gitignore` 不入库，
此条只作本机档案。

### 三、`shift_bands` 弱签名：定标后判**不改**（`tools/adopt.py` 无改动）

`inv_sig`（数字多重集 + `[n]` 引用多重集 + `𝒪` 计数）为空或同篇内非唯一的段＝弱签名，
是错位带漏报/误报的来源。探针（一次性脚本，跑完已删）量化：把"字形 token"加成第 4 项
后弱签名从 **61.9% 降到 8.6%**（判别力大涨），但**跨语言稳定性只有 ~30%** —— token 在
译文里被合法地删除/复述/公式拆散，载荷独有 token 实测全是英文虚词（`is/the/where/of`）。

结论：**不改**。加了会制造**假错位带**（把译得没问题的段判成错位），比漏报更坏；
已定性为"不改"而非"待办"，挂账撤销。

### 四、两个内容口径：对照试点方案（待启动）

**口径 A**：公式保护字符类（`-c/--vchar`）要不要连 `,` 和 `.` 一起保护 —— 保护＝这些
标点不参与翻译被原样搬过去，译文里中文语序下的顿号/句号就写不出来；不保护＝它们会被
当普通字符翻掉，公式块内部（如 `SVRG[16,20]` 的逗号、`i.e.` 的点）可能被改动。

**口径 B**：行末连字符（英文换行处 `experi-\nment`）—— 现状是两个半词各自成段，译文
把 `experi-` 当独立词处理，接缝处语义断裂。

试点方案（**只看不改**，两版同源对照，避免重译掷骰子）：

| 步骤 | 做法 |
|---|---|
| 选篇 | 挑一篇已有基线产出的稿子（SILAGE 已有 `-mono.pdf` + 台账，可直接当对照 A） |
| 出 B 版 | 只改那一处口径（vchar 去掉 `,.` / 连字符预处理），**沿用同一份缓存与术语表**，其余参数不动 |
| 逐页比对 | 抽公式密集页与跨行连字符页各若干，逐页看：公式块是否被改动、接缝语义是否连上、汉字标点是否正常 |
| 判定 | 若 B 版在这两类页面无回归且 A 版确有可指出的缺陷 → 拍板换口径；否则维持 A。**结论要落台账**，不能停在"看过" |

### 五、跨页半词碎片：记为上游限制

长段跨页时被切成两段、LLM 收到的是截断片段（如 `three successful` → 猜成「三倍于」），
这是 pdf2zh 1.x 的**分段架构**限制：page N 渲染时 page N+1 还没解析（`sstk` 每页重建），
跨页对子在实现上够不到，只有同页内的中句切分能靠 v23.4 的 look-ahead 兜住。
**去向**：不再逐例做缓存手术，等 BabelDOC 迁移（DocLayout-YOLO 版面解析能把跨页段并成
一段）一并解决。已在 `project_memory.md` 留有同口径记录。

### 六、验证

| 项 | 结果 |
|---|---|
| `test_seg_rework.py` | **36 PASS / 0 FAIL**（新增 ⑧ 四条：提示超限只列前 20 条、其余报计数、截断不影响「必须改」分节、缺段仍判 FAIL）|
| 全量回归 `run_all.py` | **22 套件全绿 / 0 FAIL** |
| `sync_patches.ps1` | 无需跑：本轮改动文件（`seg_import.py` / `test_seg_rework.py` / `改动记录.md`）都不在镜像清单里 |

### 挂账
- 两个内容口径的**对照试点尚未启动**（方案见第四节，等选定稿件）。
- 官方插件容器化＝暂缓，无动作。
- 其余历史挂账已在本轮逐条了结（返工单瘦身 ✅ / 启动清场 ✅ / 弱签名定标 ✅ /
  跨页半词碎片 ✅ 记为上游限制）。


## 2026-09-20 v28.37 内容口径对照试点（SILAGE 1–6 + 12 页）：两个口径都判**维持现状**

### 缘起与做法
v28.36 第四节留下的试点方案，本轮按拍板执行：选 SILAGE（已有基线 `-mono.pdf` 当对照 A），
**只抽若干页**做局部对照，避免整篇重译。

| 项 | 值 |
|---|---|
| 稿件 | `Sokolov 等 - 2026 - SILAGE ...pdf`（81 页） |
| 抽页 | 正文 1–6 + 12（断词最多的 1/2/6 页与逗号最密的 5/12 页都在内） |
| A（现状） | 已交付的 `-mono.pdf`：vchar **含** `\.` 与 `,` |
| B（试点） | 直接跑 pdf2zh 1.x CLI，vchar **去掉** `\.` 与 `,`，其余（vfont / service=silicon / lang / thread=4 / `--skip-subset-fonts`）与 server 完全一致 |
| B 命令 | `pdf2zh <src> -p '1-6,12' --t 4 --output <out_B> --service silicon --lang-in en --lang-out zh --config <config.json 的副本> -f '<vfont>' -c '<vchar>' --skip-subset-fonts` |
| 产物 | `logs\pilot\out_B\...-mono.pdf`（**81 页**：`-p` 只限制"翻哪几页"，不裁剪输出）、读数 `logs\pilot\{diff,cite,tok}.txt` |

两个坑先记下，下次直接照做：**(1)** `--output` 指向的目录**必须先存在**，否则 pdf2zh 把 7 页
全翻完（两趟 100%）才在最后写 mono 文件时 `FileNotFoundError`（缓存已落，重跑 2 秒完事）；
**(2)** 直接跑 CLI 要用 `config.json` 的**副本**当 `--config`（子进程的 ConfigManager 会把
配置"规范化"写回），与 `server.py` 传 `config.json.task` 同一个理由。

### 一、口径 A（公式保护管不管 `,` 和 `.`）→ **维持现状，vchar 不动**

读数是两面的，"不去掉"的代价与"去掉"的代价都能量出来：

**去掉的好处（真实存在）**：正文标点终于能按中文写出来 —— ASCII 逗号大幅下降、全角逗号与
中文句号上升；同时 `{vN}` 字形对象骤减（`vchar +1 字符 → 全篇编号平移、缓存整批失配` 的
那套脆性也随之变小）：

| 页 | ASCII `,` A→B | 全角 `，` A→B | ASCII `.` A→B | `。` A→B |
|---|---|---|---|---|
| 1 | 18 → 12 | 17 → 21 | 18 → 15 | 13 → 16 |
| 2 | 24 → **2** | 26 → 32 | 3 → 2 | 25 → 26 |
| 3 | 18 → 9 | 24 → 35 | 9 → 3 | 19 → 21 |
| 5 | 41 → 25 | 25 → 28 | 10 → 4 | 11 → 13 |
| 6 | 18 → 9 | 22 → 32 | 5 → 2 | 24 → 24 |

**去掉的代价（致命）**：`vchar` 是**纯字符正则、没有上下文**，逗号一旦不保护，模型就把
**方括号引用里的分隔符当"列表标点"本地化**：

| 页 | 原文 / A | B |
|---|---|---|
| 2 | `[38, 3, 14]`、`[32, 4]`、`[25, 44]`、`[37, 9, 2, 10]` | `[38、3、14]`、`[32、4]`、`[25、44]`、`[37、9、2、10]` |
| 3 | `[29, 25, 26, 44]` | `[29，25，26，44]` |
| 5 | `[45, 48]`、`[17, 31]` | `[45、48]`、`[17、31]`，另有原文没有的 `[25、44、26、43、49]` |

伴随**引用丢失**：第 2 页 A 的第 14 个引用 `[22]` 在 B 消失（14 → 13 个方括号 token）；
第 3 页数字总数 47 → 40（A 侧 47）。小数本身没坏（`1.1/1.2/3.3` 两侧一致，`\d+。\d+` 0 例），
**坏的是引用语法** —— 而引用完整性是 `post_check` 与 `import` 的硬门禁。

结论：**维持 vchar 现状**。prose 标点的收益要靠"**方括号引用照抄、不得改写其内标点**"这类
**载荷规则/prompt**（`seg_export.py` RULES 那条杠杆）去拿，不能用 `vchar` 拿 ——
`vchar` 分不清"句中的逗号"和"引用里的逗号"，一改就是全局的。

### 二、口径 B（行末连字符）→ **维持现状，不需要新口径**

实测行末断词残留（原文 → A → B）：

| 页 | 原文 | A | B |
|---|---|---|---|
| 1 | 4 | 0 | 0 |
| 2 | 3 | 0 | 0 |
| 3 | 1 | 0 | 0 |
| 4 | 2 | **2** | **2** |
| 6 | 3 | 0 | 0 |

pdf2zh 的版面解析**本来就把断词并回整词**（A 侧 6 页里 5 页残留 0），第 4 页那 2 处在
A / B 里一模一样 —— 与 vchar 无关，也不是口径能治的。**结论：维持现状。**

### 三、验证与产物

| 项 | 结果 |
|---|---|
| B 版翻译 | 7 页 × 2 趟跑完；B 产物 `-mono.pdf` 81 页（第 12 页确认已是中文） |
| 小数完整性 | 两侧 `\d+.\d+` 一致（1.1 / 1.2 / 3.3），`\d+。\d+` **0 例** |
| 引用完整性 | B **打坏**（`、`/`，` 本地化 + 丢引用）→ 判据见上表 |
| `run_all.py` | 无需跑：本轮**不动任何代码**，只跑配置对照 |

### 挂账
- 顺带观察（**未深究，另账**）：A 侧第 1 页 `[𝑛]`/`[𝑚]` 被 `pypdf` 提取成 `[\x00]`，B 侧正常
  —— 可能是 A 的回锚/字形回填把公式斜体字形写成了 NUL，也可能是提取侧的怪癖，待单独定性。
- 两个内容口径（`vchar` 的 `,.` / 行末连字符）**均已判"维持现状"**，挂账撤销。
- 遗留：官方插件容器化（暂缓）。


## 2026-09-20 v28.38 挂账收口：A 侧 NUL 定性 = **渲染真伤**，根因 ASCII-only 判据

v28.37 顺带留下的观察（A 侧第 1 页 `[𝑛]`/`[𝑚]` 被 `pypdf` 提取成 `[\x00]`、B 侧正常）
本轮定性完毕：**不是提取怪癖，是页面上真的写了没有 Unicode 映射的字形**，而且根因在
我们自己的工具里（判据写成了 ASCII-only）。读数留档 `logs\pilot\nul_*.txt`。

### 一、定性：豆腐块，不是提取怪癖

| 证据 | 读数 |
|---|---|
| A 侧第 1 页 `(cid:0)` | **32 处，全部**落在 `LXGW Neo ZhiSong Regular` |
| 该字体 | Type0 / Identity-H / CIDFontType2；`--skip-subset-fonts` 嵌的是**整字体**（FontFile2 10.4 MB / 28132 字形） |
| 它的 cmap | `U+1D441`(𝑁) / `U+1D45B`(𝑛) / `U+1D45A`(𝑚) **一个都没有**；`CIDToGIDMap` 缺省 = Identity → **CID 0 → GID 0 = `.notdef`**（5 条轮廓） |
| B 侧（同引擎对照版） | 第 1 页 `(cid:0)` **0 处**；同一批字符由**原文字体** `CMMI10` 画（B 侧 CMMI10 56 字 vs A 侧 28 字），提取出来是正常的 `𝑁`/`𝑛`/`𝑚` |
| 全篇 | A 侧 NUL **1518** / 原文 **0** / B 侧 **0** |

即：这批字符在 A 侧被当**普通文本排进了中文字体**，而中文字体里没有这些码位 → 落到
`.notdef`：视觉是豆腐块、提取是 NUL。B 侧走 pdf2zh 原生公式保护，同一批字符由原文字体
回填，所以两个毛病都没有。

### 二、根因：`reanchor` 的"含字母/数字"判据是 ASCII-only

`tools/seg_import.py` 里高价值字形的判据原为 `re.search(r"[A-Za-z0-9]", core)`：

- 侧车把数学斜体存成**字面文本**（实测 SILAGE `p1#4 vars[4]='𝑁=𝑛𝑚'`），而 `𝑁`/`𝑛`/`𝑚`
  是 **U+1D400–U+1D7FF 的数学字母** —— `[A-Za-z0-9]` 认不出；
- 于是它们被归入"纯标点/纯符号字形"，**按设计丢弃**：不回锚成 `{vN}`，渲染侧
  `var[i]` 的**原字体回填**就更不会发生；
- 同文件 `_eq_class()` 用的却是 `isalnum()`（NFKC 后 `𝑁`→`N`）—— **两处判据不同源**，
  这正是漏网的缝。

对账读数（同一段）：载荷 `#S5` `{vN}=0` / 字面数学 16；`imported 1#4` `{vN}=2` /
字面数学 14 —— 只有 2 个回锚成功，其余 14 个字面数学直接进了缓存与页面。

### 三、修法

```python
def has_alnum(text):
    return any(ch.isalnum() for ch in text)      # Unicode 口径, 与 _eq_class 同源
```

`reanchor()` 里 `if not core or not re.search(r"[A-Za-z0-9]", core)` → `not has_alnum(core)`。
改动只有这一处；`seg_import.py` 不在 `sync_patches.ps1` 镜像清单，**无需跑 sync_patches**。

**故意不动**的两处同类 ASCII 判据：`seg_export.py`（判"整段有没有东西可译"）与
`backfill_pages.py`（判"整页是否零可译段"）—— 它们看的是 raw / 整段文本，里面本来就有
ASCII 与 `{vN}`；纯数学斜体的段被排除恰好是对的（那类段 pdf2zh 按公式保护原样保留）。

### 四、验证

| 项 | 结果 |
|---|---|
| 新增回归 `test_seg_reanchor.py ⑳` | 修前 **3 FAIL**（四个字形全被子句丢弃、译文里 0 个占位符）→ 修后 **33 PASS / 0 FAIL** |
| `run_all.py` | 22 套件**全绿**（含 `test_mirrors_sync`：本轮无镜像改动） |
| 离线复验（真实存档：真 manifest + 真交件 `out\...doubao2.txt` + 归档侧车；**只写临时目录**） | `{vN}` **1451 → 2229**；字面数学残留 **1470 → 7** |
| 同上, `1#4` | `…其中总样本数 {v4} 在逻辑上或物理上被划分为 {v5} 个大小为 {v6} 的块…`（原为字面 `𝑁=𝑛𝑚`/`𝑛`/`𝑚`） |
| 保守性（④ 断言） | 纯标点/纯符号**仍按设计丢弃**：同一份交件里 `','` `'='` `'.'` 照样进「不必改」一节 |

### 五、后果（要不要重出一篇由豆包环路决定）

- **这类"页面豆腐块"从此会被门禁点名**：同一份交件现在判 **FAIL —— 需返工 16 段 / 22 处**
  （`𝑓𝑖,𝑗`、`ℱ𝑡.`、`𝑠∈𝑆,`、`(𝑛>𝑚)` …）。它们正是原先被静默丢成豆腐块的字符；返工单已
  逐条给出"缺的字符 + 原文上下文"，豆包照抄即可。
- 已交付的旧篇目**不主动返工**：等各自重出时自然受益（`--waive` 回路不变，内容门禁仍可留痕放行）。
- 判据放宽只改了**分类**，没有动定位口径（`_char_pattern` / `_punct_eq` / 吸收），
  v28.35 的两阶段定位与标点吸收语义不受影响。

---

## 2026-09-20 v28.39 迁移第一步（方案 B）：引擎接缝层落地 —— 一条产线两个引擎，未接线阶段当场拒答

**范围声明**：本轮只搬**引擎无关层**（A 类），不碰任何引擎细节。B 类（`{vN}` 生产与整形、
缓存键代数、侧车、docsummary、lookahead、pdfminer 三补丁）仍留在 1.x，next 画像上**显式
未接线**；C 类（服务端已接线、产线未接）不动。产物：`adopt` 六阶段机、四类只读质检、
doubao_bridge、术语资产、出成品链全部成为**引擎无关**的骨架 —— "换个引擎我们照样交付"
的骨架先立住，引擎侧细节后补。

### 一、先保护再动（可回滚）

| 保护件 | 内容 | 校验 |
|---|---|---|
| tag | `v28.38-baseline` → `5a302c6` | 与推送过的 fork/doubao-pipeline 同点 |
| bundle 快照 | `D:\zotero-pdf2zh-snapshots\v28.38-20260920-154531\v28.38-baseline.bundle`（125,427,419 B） | `git bundle verify` 通过 |
| 工作树副本 | 同目录 `tree\`（352 文件）+ `MANIFEST`（28 个 SHA256） | 逐文件核对 |
| 迁移分支 | `babeldoc-migration`（同点 checkout） | — |

### 二、花钱前先实证：隔离试验台（0 花费，走 bing 通道）

试验台 `D:\zotero-pdf2zh-eval\`（`home\` 覆写 `USERPROFILE` 做隔离、复用 C 盘 babeldoc 资产、
`in\test.pdf` 4 页、`eval.toml`）。读数留档 `logs\` 与 `out\`。

| 跑次 | 做了什么 | 读数 |
|---|---|---|
| run1 | 冷启动 | translate **18** / cache **0**；产出 236 KB mono PDF；`~/.cache/pdf2zh_next/cache.v1.db` 落 **16** 行 |
| run3 | **手改两条缓存行**（`id=1` 摘要段、`id=17` 标题）后重跑 | translate 18 / **cache hit 17**；产物里出现 `【第二公里】`（P1 摘要段、P2 `10.1 引言`） |

- **唯一未命中 = 引擎启动探针**（`pdf2zh_next/translator/utils.py:72-73` 每次运行打一次
  `translate("Hello", ignore_cache=True)`），正文 **17 段 17/17 全命中** → **第二公里在
  BabelDOC 上成立**（改缓存行 → 零 LLM 重渲染 → 产物体现改动）。
- 生效栈定案：走 pdf2zh_next CLI 时是 `pdf2zh_next.translator` + `~/.cache/pdf2zh_next/cache.v1.db`；
  `~/.cache/babeldoc/cache.v1.db` **全程 0 行**。缓存表与 1.x **四列同名同约束**。
- 三个已定性的坑（写进 `engine.py` docstring）：
  1. **上游出错仍退出码 0**（`ignore_error=True`）→ 判成败只能看产物，不能看返回码；
  2. **纯离线重渲染做不到**：启动探针必打一次网络；
  3. **缓存键粒度两档**：非 LLM 通道（bing）键 = 段落原文；LLM 通道键 = **整条 prompt**
     → 术语表/模板一动全篇 miss（这决定 next 的 inject 定位口径必须重写，属 B 类）。
- 段级台账：`translate_tracking.json`（每段 `input`/`output`/`pdf_unicode`/`placeholders`/
  `multi_paragraph_id`），**上游无 `working_dir` 参数**，靠 `--debug` 落盘。

### 三、引擎接缝层 `tools/engine.py`（新，~340 行）

把所有"与引擎有关"的假设收拢成**显式画像**，禁止"未接线阶段静默按 1.x 口径跑"。

| 项 | pdf2zh (1.x, 现产线) | next (pdf2zh_next / BabelDOC) |
|---|---|---|
| 缓存库 | `~/.cache/pdf2zh/cache.v1.db` | `~/.cache/pdf2zh_next/cache.v1.db` |
| 段表来源 | `sidecar`（`segflow/latest.jsonl`，另有按 md5 归档件） | `tracking_json`（`working/<stem>/translate_tracking.json`） |
| 归档侧车 | `pdf-<md5:16>.jsonl` | 无（`sidecar_of` 返回 None） |
| 已接线 | 八项全接线 | **deliver / gate / status** 三项 |
| 未接线 | — | export / import / inject / render / rollback（各带**够写进 die 文案**的理由） |

- 缺省画像写死 `pdf2zh`：**缺省行为必须与历史逐字节一致**，换引擎只能显式
  `--engine` / `P2Z_ENGINE`；未知名 **报错不回落**（回落 = 把 next 的活干到 1.x 库上）。
- 段表契约 `read_tracking_segments()`（**只读**）：归一化成 `seq`(全篇连续 1-based)、`page`、
  `idx`、`src`(`input`)、`raw`(`pdf_unicode`)、`dst`(`output`)、`cache_key`(有 llm prompt 取
  prompt 否则取 src)、`batch`、`n_ph`、`err`；文件不存在抛 `IOError`、结构不对抛 `ValueError`，
  **不空手返回空表**。
- `read_segments()` 在 pdf2zh 画像上**故意抛 `NotImplementedError`**：侧车分块 / `⋮` 口径在
  `seg_export` / `seg_import` 里，不在本层复刻（复刻 = 两处口径迟早分叉）。
- CLI：`--list` 打画像（含未接线理由）、`--segments` 读段表、`--json`。

### 四、接线改动

**`tools/adopt.py`（8 处）**：`use_engine()` 重绑 `ENGINE/SIDECAR/CACHE`；台账盖 `engine` 戳；
`guard()` 两条（**台账引擎与当前引擎不符 → 拒执行**；**未接线的前置阶段不构成前置**，打印提示
后 `continue` 而不是锁死）；`archived_sidecar` / `resolve_sidecar` 在 next 上明确拒答；
`stage_gate` 的"最新 mono"兜底**只在 1.x 生效**（next 上要求 `--mono` 显式指定，否则等于拿别人
的成品当自己的验收）；`common()` 加 `--engine` 并写进 `P2Z_ENGINE` 供子进程继承；`main()` 在
**任何阶段动手之前**定引擎并检查接线清单。

**四个子工具单跑时自带接线门禁**（`P2Z_ENGINE` 取画像，`stage_ok` 不过则 `rc=2`）：
`seg_export.py`（export）、`seg_import.py`（import）、`seg_inject.py`（inject / rollback）、
`force_rerender.py`（render）。**为什么必须由子工具自己拦**：adopt 拦的是它派发的调用，
拦不住手工 `python seg_inject.py` —— 而 `seg_inject` 的 `CACHE` **没有 CLI 覆盖口**，
写死等于"父进程按 next 算、子进程往 1.x 库写"，更新报成功而渲染器读的是另一张库。

**`--sidecar` 缺省改哨兵**（`NOT_GIVEN`）：parser 是**解析参数之前**建的，那时画像还是 import
期那一个，`default=SIDECAR` 会把上一个引擎的路径当缺省带进子进程；现在解析完、引擎定了再补。

### 五、踩到的真事故（已收口并被测试锁住）

第一版 `use_engine()` 每次调用都重绑三个全局名 → **每次 `main()` 都把 `test_adopt.py` 的
沙箱覆盖踩掉**（它靠改 `AD.SIDECAR` / `AD.CACHE` 隔离），于是"假数据跑真库"：④ 文档指纹
登记的是真实侧车的指纹、⑩ import 失败、⑫ `calls[1]` 直接 IndexError。**症状全部离现场很远**。
修法：**同一引擎重复调用不重绑**（只在真的换引擎时重绑），并由 `test_engine.py ⑳` 锁死
"同引擎不踩覆盖 + 真换引擎仍重绑"两个方向。

### 六、验证

| 项 | 结果 |
|---|---|
| 新增 `tools/tests/test_engine.py` | **42 PASS / 0 FAIL**（画像 / 接线清单 / 段表契约 / 台账引擎戳 / 同引擎不重绑 / 四子工具单跑门禁） |
| `tools/tests/run_all.py` | **21 套件全绿**（`test_engine` 为新增第 21 套；`adopt.py` 8 处改动对历史行为零影响） |
| CLI 实测 | `adopt status` 缺省走 1.x（11 份台账照常列出）；`--engine next export/render` 被接线门禁拒并打印理由；`P2Z_ENGINE=next gate` 提示"前置 render 未接线，不构成前置"后仍独立校验 `--expect` |
| `engine.py --list` | 两画像路径 / 段表来源 / 已接线 / 未接线逐条可读 |

### 七、遗留（下一阶段，都是 B 类）

1. **next 的 inject 定位口径**：LLM 通道键 = 整条 prompt（含术语表/标题），要按新引擎重写
   "按段 UPDATE"；缓存表结构相同是可复用的最大便宜。
2. **next 的 export/import 整形**：`translate_tracking.json` 免编号重映射（段号全篇连续）是
   天然优势，但 `{vN}` 回锚 / 变量字形是 1.x IL 口径，下一轮定。
3. **render 入口**：改用 pdf2zh_next CLI，且**判成败只能看产物**（上游退 0）。
4. **pdfminer 三补丁 / docsummary / lookahead / `PAUSE_TRANSLATE`**：与 1.x 渲染层绑定，
   迁移时机随渲染层一起定。

---

## 2026-09-20 v28.40 迁移 B 类第一件：next 的 export（段表 → `#S` 载荷）

**为什么先做这件**：没有载荷就进不了豆包回路 —— `inject` 的"段身份"、`import` 的回写、返工单的
段号，全都挂在载荷的 `#S` 上。它是 B 类四件事里唯一"不做就什么都动不了"的一件。

真样本（试验台）：`~\.cache\babeldoc\working\Mandujano 等 - Sci-Hub …\translate_tracking.json`
（34 页，`page[]` 97 段 + `cross_page` 32 段 = **129 段**，16 条跨页配对）。

### 一、三处"照搬 1.x 口径必错"的差别（写进 `engine.py` 节头）

| | pdf2zh 1.x | next / BabelDOC | 为什么 |
|---|---|---|---|
| 正文形态 | 侧车字形 → **回锚 `{vN}` 再还原成真字形**发给译者 | 直接发 `input` **原样**（带 `{vN}`/`<style>`） | `{vN}`/`<style>` 是**引擎原生协议**：上游 `PROMPT_TEMPLATE` 明写"不许动"，渲染器自己替换。发显示形态（`pdf_unicode`）＝ 显示字形进译文 ＝ 中文字体没那个码位 ＝ **豆腐块 + 提取 NUL**（v28.38 事故成因） |
| `⋮` | 跨页续接**合并成一条**，断点插 `⋮` | **不注入** | 上游把跨页段落整段并成一次调用，每个段本身就是完整段，没有断点 |
| 跨页池 | 侧车本就逐段连续 | **必须把 `cross_page` 池并进来** | 上游按 `translated_ids` 把"上页末段 + 下页首段"从各自 `page[]` 里**摘走**单独翻（`il_translator_llm_only.process_cross_page_paragraph`），漏收 = 每页页尾/页首正文**永远译不到也永远改不了**（实测该篇 32 段，含摘要整段） |

### 二、跨页配对的页码得自己锚（`lib` 里没有）

`cross_page` 只有段落，没有页码。上游语义给两条**硬约束**：段 0 = 第 i 页最后一个正文段、
段 1 = 第 i+1 页第一个正文段 → `p1 == p0 + 1`，且条目按 i 递增故 `p1` **严格递增**。
这两条既是校验也是补全依据：

1. 锚串取**连续字面片段**（`literal_runs` 按 `{vN}`/`<style>` 切开后最长的几段）—— 整段中间
   有占位符"空洞"的地方在 PDF 抽出的文本里对不上，只有连续片段能整段照抄下来；
2. 三轮匹配：**原样 → 去空白 → 再去断词连字符**（`obser-\nvation` 会被 pypdf 抽成 `obser- vation`），
   两侧用**同一个**变换，所以 `Lovett-Doust` 这类本来就有连字符的也照样对得上；命中必须**唯一**；
3. 只锚到一侧 → 用另一侧推；两侧都锚不到（或锚出来违背约束）→ 按递增猜 **并报警** ——
   猜出来的页只进报告与 `--pages` 筛选，**不影响正文**。

首版只用句级字面 + 两轮匹配，真样本上 **9 条"页码未定"**；改成连续片段 + 三轮后降到 **1 条**
（`cross_page[9]`：两段都没能唯一命中 → 按递增猜第 25 页，不影响正文）。

### 三、落点

- **`tools/engine.py`**：`_NEXT_NO` 删 `export` 键（`wired` 改为 `export/deliver/gate/status`）；
  新增 `load_tracking()` / `_norm_paragraph()`（从 `read_tracking_segments` 抽出，段表读取与载荷
  出口共用一份校验）；新增"段表 → 载荷条目"整节：`literal_text/literal_runs/anchor_runs/anchor_page
  /pdf_page_texts/_skip_para/_cross_pages/tracking_payload_items/payload_report/parse_pages`。
  CLI 加 `--payload` / `--pdf` / `--pages`。
- **`tools/seg_export.py`**：按 `Profile.seg_source` 分**两条路线**，但共用同一个 `_emit()`（编号 /
  manifest / 报告一份写法，免得两边慢慢漂移）。`source` 非空 = next：载荷末尾附**第 10 条**
  占位符规则（`_PH_RULE`，**不动 1-9 的编号** —— 那些编号被术语子项与返工单引用过），manifest
  增记 `engine` / `source` / 每条的 `pool` / `batch`。1.x 路线 `source=None` → **载荷字节不变**。
  新增 `--tracking` / `--pdf`。
- **`tools/adopt.py`**：`stage_export` 开头按画像分流 → `stage_export_next()`：不解析侧车、**不做
  `probe_db`**（next 的 LLM 通道键是整条 prompt，拿段落原文去数必然 0 命中）、`doc_fp` 留空；
  argv 只带 `--pages --name --tracking`（+ 可选 `--pdf/--doc/--terms`）。

### 四、验证

| 项 | 读数 |
|---|---|
| `engine.py --engine next --payload … --pdf … --pages 1-34` | **129 段（34 页；跨页池 32 段）**，1 条未锚警告 |
| `P2Z_ENGINE=next seg_export.py`（真样本） | 载荷 **129 段 / 82,095 原文字符**，跨页池 32 段；含 `{vN}` 段 59、含 `<style>` 段 15、**字面 `⋮` 行 0**（符合"不注入 ⋮"） |
| `tools/tests/test_engine.py` | **68 PASS / 0 FAIL**（㉑ 拆出 seg_export、新增 ㉒-㉗：阅读序 / 原生形态+无 ⋮ / 单侧锚定推断 / 双侧失败报警 / 载荷出门字段+1.x 字节不变 / 段表不存在报业务错） |
| `tools/tests/run_all.py` | **21 套件全绿** |

冒烟产物留档 `D:\zotero-pdf2zh-eval\smoke\`（`next_smoke.txt` / `.manifest.json`）。

### 五、本轮踩到的坑

1. **载荷里查 `"⋮" not in 全文` 必假阳性**：规则 3 通篇在讲 ⋮。测试必须**只看正文行**
   （`#S` 行的下一行）→ 写成 `payload_bodies()` 一处口径。
2. **"段表不存在"报的是 `rc=1`（业务错）而不是 `rc=2`（接线错）**：原来是光秃秃一句
   "段表不存在"，拿到 `rc=1` 只会去怀疑引擎没接好。改成同时指路 `--tracking`
   （换篇论文后工作根下的旧段表会被覆盖，这是最常见的"段表不见了"）→ 由 ㉗ 锁死。
3. `_cross_pages` 返回的是 **`(p0, p1)` 两页页码**（不是 0-based 下标）；manifest 的 `batch`
   只写在**真并过**的段上（`multi_paragraph_id` 非空）—— 这两处是我写测试时的期望错，不是
   实现错，记在这里免得下次再判反。

### 六、遗留

- next 的 `import` / `inject` / `render` / `rollback` 仍**未接线**（`_NEXT_NO` 四条理由未动）；
  `deliver` / `gate` / `status` 纯读产物，两引擎共用。
- 已交付/已导出的 1.x 载荷**不受本轮影响**（㉖ 锁住 1.x 路线的载荷与 manifest 形状）。


## 2026-09-20 v28.41 迁移 B 类第二件：next 的 import / inject（回写通道）

**为什么这两件一起做**：它们挂在同一个"段身份"上 —— 载荷的 `#S` 编号，靠段表把编号对回那一段。

### 一、next 的 import = **只做守恒，不回锚**

1.x 的 `import` 核心动作是**回锚**（译文里的显示字形 → `{vN}`），因为 1.x 是把字形还原成真字形
发出去的。next 发出去的就是 `{vN}`/`<style>` **原生形态**（v28.40 节一），收回来直接能写回 ——
"回锚"在这里没有对应物，也不需要。

换成的判据是**守恒**（与上游同源：上游对这批段用的判据本来就是 full match）：
逐段 `{vN}` 多重集、`<style>` 标签多重集**逐项相等**；译文里**不许出现 `⋮`**（next 不注入它，
出现即译者自造）。判据 `_ph_ms` 连花括号一起比（返工单里要显示 `{v1}` 而不是 `1`）。
源正文从**载荷本体**取（`inbox/<name>.txt`）—— manifest 只落坐标不落正文，正文本来就是从段表
生成的那一份，再存一份只是重复。产出 `out/<name>.imported.json` 按 `#S` 编号。

### 二、next 的 inject = 精确匹配整条 prompt + 改批次 JSON 里那一条

| 1.x 的三道安全设计 | next 的对应物 |
|---|---|
| 段内 `{vk}` **重编号** | **不需要**：载荷正文就是原生形态 |
| `translate_engine_params LIKE '%<doc_summary_fp>%'` **文档作用域** | **不需要**：LLM 通道键 = **整条 prompt**（含术语表/抬头上下文），天然只属于本篇 |
| 前置断言"缓存现译文 == 侧车 trans 重编号" | 换成"该行 `translation`（上游原始回复 = 一个 JSON 数组）里 `id == multi_paragraph_index` 那一条的 `output` == 段表记下的 `output`" —— 一条断言同时证明**行找对了**与**id 映射对上了**；写完再复查该行 |

`--shift`（1.x 合并段断点移位）在 next 上**当场拒答**：next 载荷按 `#S` 编号，没有可移的段坐标。
批次的 JSON 数组只动 `id == multi_paragraph_index` 那一条 —— 同批其余段**逐字节不动**（㉙ 锁死）。

### 三、真样本验收当场逮到一个**假阳性**（本轮的主要 bug）

原对账口径：拿当前段表**重推一遍** items，再与 manifest 逐段比 `(页, 段序)` 与池。真样本 129 段
**全部**被判"段表与载荷对不上"，而段数相等（129 vs 129）→ 逐项 diff，发现**从 `#S11` 起整列错位**。

根因不在段表，在**推导参数**：载荷里的页号是**导出时**锚定的结果（给了 `--pdf` 才按页面文字锚，
否则按序猜），而 `#S` 编号是**按页号排序**的。导出时给了 `--pdf`、回写时没给 → 跨页段的页号全变
→ 排序整体错位。同一份段表、同一份载荷，只因为"重推"少了一个参数，就被判成"不是当前段表导出的"。
（给上 `--pdf` 重推，差异 0 处，确认是真阳性误报。）

**修法**：manifest 每条落

- **定位键 `src`** = `(池, 下标, 子下标)`：`page` 池 =（段在 `page[]` 里的页下标, 段在页内的下标）；
  `cross_page` 池 =（配对数下标, 0=上页末段 / 1=下页首段）。**不含任何推导参数**。
- **正文指纹 `fp`** = 该段正文 sha1 前 16 位。

回写侧改成"按 `src` 落到那一条 + 用 `fp` 确认还是那一份"（`engine.tracking_segment()`），
**不重推、不重放参数**。判据没有变弱：段表被换篇/被下一次渲染覆盖 → `src` 越界或 `fp` 不等 →
照样拒收；旧版（无 `src`/`fp`）的 manifest → 拒收并说清"重新导出一次载荷即可"。

### 四、落点

- **`tools/engine.py`**：`tracking_payload_items` 每条带 `src`；新增 `text_fp()` / `tracking_segment()`；
  `_NEXT_NO` 只剩 `render` / `rollback`；节头【两个画像】改为"已接线 export/import/inject"。
- **`tools/seg_import.py`**：新增 `import_next()`，`main()` 在**读侧车之前**按 `seg_source` 分流
  （next 没有侧车，那段按 `页码#段号` 取址的代码在 next 载荷上会当场 `KeyError`）；新增 `_fails_of()`，
  返工单的 `detail` 结构对齐 `write_rework_note` 期望的字段。
- **`tools/seg_inject.py`**：新增 `inject_next()`（上表那套），`main()` 同处分流；新增 `--tracking`。
- **`tools/seg_export.py`**：`_emit()` 的 next 分支增记 `src` / `fp`（1.x 路线不带，载荷字节不变）；
  `export_next` 那句"seg_import/seg_inject 在 next 上未接线"的旧警告改掉。
- **`tools/adopt.py`**：新增 `stage_import_next()` / `stage_inject_next()`，`stage_import` / `stage_inject`
  开头按画像分流；next 上 `--shift` / `--fp` 拒答。

### 五、验证

| 项 | 读数 |
|---|---|
| `tools/tests/test_engine.py` | **89 PASS / 0 FAIL**（新增 ㉛ 三例：载荷顺序不必等于重推顺序 / 跨页段照样落位 / 旧版 manifest 拒收；㉙㉚ 夹具补 `src`+`fp`；`seed_cache` 支持重复调用） |
| `tools/tests/run_all.py` | **21 套件全部通过 ✅** |
| 真样本 `seg_export`（重导） | 129 段 / 82,095 原文字符；跨页池 32 段；1 条未锚警告（`cross_page[9]`）—— 与 v28.40 读数**完全一致** |
| 真样本 `seg_import` | 解析 129 段，**结论 PASS**，`imported.json` 129 段，`{v1}` 原样带回 |
| 真样本 `seg_inject --dry` | **对账通过**（不再有假阳性），随后 129 段全部报"缓存里找不到该批次的 prompt 行" |

### 六、真样本暴露的**新事实**（不是本轮代码问题，但改变验收前提）

该篇（9/12 渲染）在 next 的两张缓存库 —— `~\.cache\pdf2zh_next\cache.v1.db` 与
`~\.cache\babeldoc\cache.v1.db` —— **都是 0 行**，库文件 mtime 停在 8/16，即**那次渲染一行缓存都没落**。
链路本身是会写的（`pdf2zh_next/translator/base_translator.py:110` 的 `self.cache.set(text, translation)`；
非 LLM 通道在试验台已实测落行 16 条）。最可能的原因：那次是**官方对照试点**（`official-next/` 里
就留着官方与我们的对照产物），对照跑多半带了 `--ignore-cache`。

**结论**：`inject` 的第二公里以"**渲染那次缓存开着**"为前提。这篇的存量段表无从回写。
要端到端验收 `inject`，得拿一篇缓存开着跑的论文 —— 试验台的 **bing 通道 0 花费**就够覆盖
"段表 → 载荷 → import → inject → 重渲染 → 产物体现改动"整条。`render` 接线时要一并确认
next 入口**不要**带 `--ignore-cache`（默认 false，别动）。

### 七、遗留

- next 的 `render` / `rollback` 仍**未接线**（`_NEXT_NO` 只剩这两条）。
- 一次性探针 `D:\zotero-pdf2zh-eval\probe_cache.py`、`_diag.py` 已删。
- 历史挂账不变：SILAGE 返工单 / vchar 配方 / `shift_bands` 弱签名 / 跨页半词碎片（等迁移一并解决）。


## 2026-09-20 v28.42 迁移 B 类第三件：next 的 render（重渲染落产物）

B 类的最后一件。`_NEXT_NO` 现在只剩 `rollback`（= inject 的逆操作）。

### 一、next 没有服务端 → render 不是 POST + 轮询，而是 **CLI 子进程**

| | pdf2zh 1.x | next |
|---|---|---|
| 入口 | POST `127.0.0.1:8890/translate`（异步任务，轮询 `taskId`） | `pdf2zh_next` **CLI 子进程**，没有服务端、没有 taskId |
| 成败 | 服务端返回的 `status` + `filePaths` | 上游 `ignore_error=True`：**内部出错退出码仍是 0** → 只能看产物 |
| 产物 | 服务端报回来 | 在 `--output` 目录里找 **mtime 新鲜**（`>= t0-5`）的 mono/dual |

产物行（`产物: <path>`）与耗时行（`耗时 N 秒 -> 成功`）与 1.x **同格式** —— `adopt.stage_render`
的解析不必分两套（那个解析本来就允许行首空白，见 v28.35 附近的整行解析补丁）。

可执行文件**不写死绝对路径**（入库脚本不许带用户名/venv 硬编码）：按"**与本解释器同级的
`zotero-pdf2zh-next-venv`**"约定找（与 `server.py` 的 `venv_name` 同一套约定，换 conda 根跟着走），
可用 `--exe` / `P2Z_NEXT_EXE` 指定；`--exe` 以 `.py` 结尾时用**本解释器**起它 —— 那是专为回归留的
假引擎口子（㉜）。

### 二、两道**静默**失效必须自己钉住（都不报错、不留痕）

1. **`--debug` 必带**：上游 `babeldoc/format/pdf/translation_config.py:287-296` 只在 debug 时把
   `working_dir` 落 `<CACHE_FOLDER>/working/<input_stem>`，否则是 `tempfile.mkdtemp()` ——
   **段表 `translate_tracking.json` 根本不落盘**，第二公里当场失去段身份，但渲染本身照样"成功"。
2. **config 的 `[translation] ignore_cache` 必须 false**：上游 `BaseTranslator.translate()` 头一行是
   `if not (self.ignore_cache or ignore_cache): return`（既不查缓存也不写缓存）→ 改缓存行 = **白改**，
   产物照旧。本机生产 `server/config/config.toml` 第 58 行的现值就是 `true`（官方对照跑留下的地雷）。
   做法：把 config 读进来、只把这一行置 `false`、写进**临时副本**再 `--config-file` 交给子进程
   （子进程的 ConfigManager 会规范化写回，直接给生产那份会把它改脏 —— 1.x 试点同一个坑，v28.37），
   **生产 config 一个字节不动**（㉜ 逐字断言）。
3. **渲染前零成本自检**：段表里的 LLM prompt → 只读缓存探针 `engine.probe_prompts()` → 打印
   `自检: 缓存命中 k/n 段`；**0 命中要说出来**（这次会整篇重译：既花钱又慢，多半是引擎/模型/语言
   跟上次不一致）。这件事退出码和产物都不会告诉你。

探针分块查（`chunk=400`，SQLite 变量上限），两引擎**共用一份写法**（定位口径本来就一样：按
`original_text` 精确匹配），差别只在"传什么"：1.x 的键是段落原文，next 的键是**整条 prompt**。

### 三、回归当场逮到两个真 bug（都是"照 1.x 写必错"）

1. **`TypeError: not all arguments converted during string formatting`**
   —— `"...引擎(%s)..." % _PROF.cache_engines`：`cache_engines` 是**元组**，`%` 右边不带括号的元组
   会被当成多参数。它只在 **0 命中**那条分支上触发 —— 也就是**最该打印警告**的时候，异常把整条
   渲染出口崩掉（rc=1，stdout 只剩自检那一行，看起来像"探针跑完就没了"）。
2. **产物命名两引擎不一样**：1.x = `<stem>-mono.pdf`；next = `<stem>.no_watermark.zh-CN.mono.pdf`
   （中段随 `watermark_output_mode` 与 lang 变，生产 config 第 81 行就是 `no_watermark`）。
   原判据只认 `.mono.pdf` 后缀 ⇒ 真跑起来 next 的产物**一个都认不出** = 渲染明明成功却报
   "没等到新产物"。同一个缝还在 `adopt.stage_render`：`endswith("-mono.pdf")` 挑不出 next 的 mono →
   退回 `prods[0]`，而按名排序 **dual 在前** → **gate 拿一份中英对照去做比例 / 文献区 / 引用完整性
   验收**，结论全错。修法：`force_rerender` 两种后缀都认；`adopt` 把 mono 判定提升为模块常量
   `MONO_RE = [-.]mono\.pdf$`（render 台账与 gate 验收对象**共用这一处口径**，测试直接断言该常量）。

### 四、落点

- **`tools/engine.py`**：`_NEXT_NO` 删 `render`（只剩 `rollback`）；next `wired` 加 `render`；节头与
  notes 写明"CLI 子进程 / 判成败只看产物 / `--debug` 与 `ignore_cache` 两条"；新增只读探针
  `probe_prompts()`。
- **`tools/force_rerender.py`**：新增整个 next 节（`next_exe` / `_off_in_config` / `_pdf_pages` /
  `_counters` / `_tail` / `_probe_before_render` / `render_next`）；`main()` 在接线门禁之后按
  `seg_source` 分流；新增 `--exe / --config / --output / --tracking / --lang-in / --lang-out`。
  `--service` 缺省从 `PLUGIN_CONFIG["service"]` 改为空、在 **1.x 分支**里回落 —— 免得 next 分支
  被 1.x 的 `silicon` 缺省误导（next 那边开关名是 `siliconflow`）。
- **`tools/adopt.py`**：`stage_render` 在 next 上把**导出时那份段表** `--tracking` 传下去（供渲染前
  自检；那段表路径正是本次渲染会覆盖的那一份）；`MONO_RE` 提升为模块常量。
- **`tools/tests/test_engine.py`**：⑥/⑦/㉑ 随"render 已接线"更新（㉑ 的 `force_rerender` 从"拒答
  rc=2"移到"报业务错"的正向用例）；⑱ 拆两条（next 上 gate 的前置 render **现在真的锁死** +
  临时把 `render` 标成未接线，验"未接线前置不构成前置"这条规则本身 —— 六阶段全接线后它没有活例
  了，但接新引擎时正是靠它）；⑲ 改用 `--force` 跳过顺序门禁、直达"缺 `--mono`"那条判据；新增 ㉜。

### 五、验证

| 项 | 读数 |
|---|---|
| `tools/tests/test_engine.py` | **100 PASS / 0 FAIL**（新增 ㉜ 十例 + ⑱ 拆出的 1 例） |
| `tools/tests/run_all.py` | **21 套件全部通过 ✅** |
| `tools/tests/test_adopt.py` | 97 PASS / 0 FAIL（含 `render --skip-last 透传` 4 例 —— `MONO_RE` 改动未伤 1.x 路线） |

㉜ 的手法：**`.py` 假引擎** + **沙箱 `USERPROFILE`**（画像的缓存库/工作根都从 `~` 派生，否则探针会去
读真实 home 下的库，那就不叫离线用例）。假引擎按 next 真实命名写 mono **和** dual 两个产物，并回吐
上游那两行读数 —— 这样"认不认得出 next 命名""mono 有没有挑对"都在离线用例里锁住了。

### 六、遗留

- next 的 `rollback` 仍**未接线**（inject 的逆操作）。
- **端到端验收**（真 `pdf2zh_next` 跑通 export → … → render → gate）需要一篇"渲染那次缓存开着"的
  论文：试验台 **bing 通道 0 花费**就够；本机现有唯一 working 段表那篇两张缓存库都是 0 行
  （v28.41 节六），产物能出但 inject 回写没有对象。`render` 这边另有一条要一并确认：next 入口
  **不要**带 `--ignore-cache`（本工具只传 config 副本，没碰这个开关，默认 false）。
- 历史挂账不变：SILAGE 返工单 / vchar 配方 / `shift_bands` 弱签名 / 跨页半词碎片（等迁移一并解决）。


## 2026-09-20 v28.43 迁移 B 类最后一件：next 的 rollback（行级撤销）

B 类收口。`_NEXT_NO` **清空** —— next 画像八项全接线，与 pdf2zh 对等。这一件不是"把 1.x 的
撤销搬过来"，因为**两边的损坏形态本来就不一样**：

| | pdf2zh 1.x | next |
|---|---|---|
| 撤什么 | **骨架行**（暂停档第一趟故意落的 `translation == original_text` 占位行；不撤则整篇出英文） | **我们注入进批次 JSON 的那一条**（next 没有骨架行这一说） |
| 作用域 | 有 manifest 按本篇载荷段原文逐段限；否则退回侧车 / 文档指纹 | manifest 的定位键 + 段表**逐段对账**（与 inject 同一份实现） |
| 判据 | `translation == original_text` | **provenance**：现行值 == `imported.json` 里我们注入的那一版 |

### 一、**不做** `.bak` 整库还原（这是本件最要紧的一条）

`.bak` 是"inject 前某一时刻的**整库**快照"。缓存库是**全机共用**的：快照之后**别的论文**
合法落下的译文（也包括同一篇别的批次的合法改动）会一并被退掉，而且**不报错** —— 一次撤销
伤到无关的篇，事后无从察觉。所以 next 做**行级**还原，范围 = 本篇载荷那几段。

### 二、判据用 provenance，不用"把引擎当时的译文强塞回去"

只改「现行值 **==** `imported.json` 里我们注入的那一版」的那一条，还原成段表记下的
`output`（引擎当时写下的译文）。三种情形分开处置：

| 现行值 | 处置 |
|---|---|
| == 我们注入的那版 | 还原成段表那版 |
| == 段表那版（未注入 / 已还原） | 不动（**幂等**：再撤一次报"还原行数: 0"） |
| 既不是新也不是旧（别人改过） | **不碰，只报告** |

少了第三条，撤销就成了"把引擎译文塞回去"，会**覆盖别人的合法改动**；这与 1.x 那条"只认
骨架行"是同一个精神。缓存里那行 prompt 整个不在了 -> 报"**无残留**"并 PASS（撤销是止损坏
步骤，"没东西可撤"本身就是它要的结果；判 FAIL 会把自动回路卡死）。

### 三、inject / rollback **共用一份对账口径**（`align_next()`）

撤销若用另一套定位口径，撤的就是**别的段**，而两边都"看起来对上了"。故把 inject 里那段
"manifest 校验 + 载荷 ↔ 段表逐段对账"抽成 `seg_inject.align_next(args)`，两条路线都调它：
`manifest.engine` 必须是 `next` → 段表按 `source.path` 或 `--tracking` 取 → 逐段按定位键
`src` 落位 + 正文指纹 `fp` 确认 → 任一条不成立**当场拒收且一个字节都不写**。

### 四、落点

- **`tools/engine.py`**：`_NEXT_NO = {}`（表本身留着 —— 它是留给**下一个**引擎的：加画像时
  在上头写清"为什么现在跑不了"，`stage_ok` 会在入口当场拒答 `rc=2`）；next `wired` 加
  `rollback`；节头与 notes 写明"**行级**还原，不整库回滚（库全机共用）"。
- **`tools/seg_inject.py`**：新增 `align_next()`（inject 与 rollback 共用）、新增
  `rollback_next()`；`inject_next` 改用 `align_next`；`main()` 的 `--rollback` 分支**在**读侧车
  之前按 `seg_source` 分流（1.x 那段按 `页码#段号` 从侧车取原文当作用域，在 next 上没有侧车，
  会当场 `KeyError`）；next 上 `--fp` 拒答、缺 `--imported/--manifest` 给业务错并说清这两个
  参数各管什么。
- **`tools/adopt.py`**：新增 `stage_rollback_next()`；`stage_rollback` 在算 `fp`/找归档侧车
  **之前**分流（那套兜底在 next 上没有对应物）；rollback 子命令补 `--tracking` / `--dry`。
  `rollback` **不进门禁**（1.x 如此，next 也不新引入顺序门禁 —— 它是止损坏步骤，前置阶段
  本来就是坏的）。
- **`tools/tests/test_engine.py`**：⑥/⑦ 随"八项全接线"更新（⑦ 改成"接线表仍是唯一判据：
  未登记的阶段照样当场拒答"，不再有"未接线理由非空"的活例）；㉑ 的 `seg_inject --rollback`
  从"next 上拒答 rc=2"移到正向用例；新增 **㉝**（15 例）。

### 五、验证

| 项 | 读数 |
|---|---|
| `tools/tests/test_engine.py` | **115 PASS / 0 FAIL**（新增 ㉝ 十五例） |
| `tools/tests/run_all.py` | **21 套件全部通过 ✅** |

㉝ 的现场直接**复用 ㉛ 注入后的那一行**（四条 `id==mpi` 槽位已全是"我们那一版"），于是"四条
**各**还原到自己那段"这条判据是真的在锁**串段** —— 定位口径错了，症状就是某段的译文变成别段
的意思。另含：`--dry` 不写库、再撤一次幂等、别人改过 -> 不碰只报告、prompt 行不在 -> 无残留、
三个输入缺一不可、`--fp` 拒答、段表对不上不写库；adopt 侧用**打桩 `run_tool`** 只锁"派发对不对"
（子进程本身已单独验过）。

### 六、遗留

- ~~**端到端验收**（真 `pdf2zh_next` 跑通 export → deliver → import → inject → render → gate，含
  一次 rollback 演练）需一篇"渲染那次缓存开着"的论文：试验台 bing 通道 0 花费。~~
  **已完成 → 见 v28.43.1**。
- **新增挂账**：next 默认不翻表格文本（`--translate-table-text` 上游 experimental 默认关），
  带大表的页会整页漏译并被 post_check 判 FAIL —— 先挂账，等对照试点（见 v28.43.1 第四节）。
- ~~v28.41 / v28.42 / v28.43 **三笔一起**提交到 `babeldoc-migration`。~~
  **已提交（含 v28.43.1）**。
- 历史挂账不变：SILAGE 返工单 / vchar 配方 / `shift_bands` 弱签名 / 跨页半词碎片（等迁移一并解决）。



## 2026-09-20 v28.43.1 next 全链端到端验收（试验台，真 `pdf2zh_next`）

把 v28.41/42/43 接好的六个阶段**串起来真跑一遍**：不是分件单测，而是 adopt 一条龙
`export → deliver → import → inject → render → gate`，再补一次 rollback 演练。

### 一、通道与沙箱（0 花费）

| 项 | 值 |
|---|---|
| 沙箱根 | `D:\zotero-pdf2zh-eval`（`USERPROFILE`/`HOME` → `…\home`，`P2Z_PROJ` → `…`，`P2Z_INBOX` → `…\in`） |
| 引擎 | `P2Z_ENGINE=next`（`pdf2zh_next 2.9.0 / BabelDOC`） |
| 服务 | `--service siliconflowfree` —— 社区免费端点，不花 API 额度 |
| 素材 | `in\test.pdf`（4 页，cactaceae2009 节选）+ 第一公里产物归档 `smoke\r3.*` |
| 读数留档 | `logs\r3_{first_mile,render,gate,rollback,rollback_dry,render_after_rollback}.log` |

### 二、六阶段真跑读数

| 阶段 | 结论 | 关键读数 |
|---|---|---|
| export | ✅ | 17 段 / 10137 字符 / 跨页池 2 段（口径见 v28.40） |
| deliver | ⚠ `--waive` | 内容门禁拒收 1 次：`#S5` 千分位 `2,000 → 2000`；按文档路径留痕放行 |
| import | ✅ | 四条守恒全过（段号 / `{vN}` 多重集 / `<style>` 多重集 / 禁 `⋮`） |
| inject | ✅ | dry `17/17 PASS` → 实写 17 行（先自动备份 `cache.v1.db`） |
| render | ✅ | **调用 11 / 缓存命中 10（全命中, 0 LLM）**，34 秒，"第二公里"成立 |
| gate | 半 | `verify_render` **PASS**（`【第二公里】` 落第 2 页 ×1）/ `post_check` FAIL（唯一一条，见第四节） |
| rollback | ✅ | dry 17 行 → 实写 17 行还原 → 再渲染 → `verify_render --forbid '【第二公里】'` **0 命中** |

`deliver` 那一拒**不是 bug**：夹具是"引擎自己的译文"合成出来的，引擎自己把千分位吞了，门禁
按设计拦下 —— 恰好证明内容门禁在线。

### 三、真跑才暴露的两个 bug（本件修掉）

1. **`adopt.py inject` 缺 `--tracking`** —— `stage_inject_next` 直接读 `args.tracking`，
   而 inject 子命令没注册该参数 → `AttributeError`。属于"分件单测时走打桩、一条龙才现形"。
2. **`adopt.py render` 缺 `--service`** —— next 的**缓存键含引擎名**，`force_rerender` 缺省
   按 `siliconflow` 兜底，而第一公里走的是 `siliconflowfree` → 直接渲染 = 整篇 miss（花钱）。
   补 `--service` 透传；缺省空时仍走 force_rerender 自己的兜底，与本参数引入前逐字节一致。

### 四、gate 那一条 FAIL 的定性：**不是第二公里的问题，是 next 的覆盖率回退**

- 现象：第 3 页译文汉字 `9 / 3559 = 0.3%`，原文该页 1786 字符。
- 现场：原文第 3 页整页就是 `Table 10.1`（大表格）；**段表里该页只有 1 段** —— 表格单元
  压根没进 LLM。译文该页正文与原文逐字相同。
- 根因：BabelDOC 默认**不翻表格文本**，上游 CLI 有 `--translate-table-text`
  （*Translate table text (experimental)*，默认关；见 `official-next\out\cli-help.txt`）。
- 影响面：1.x 没有"表格保护"这个概念（表格文字就是普通段落，会被翻）→ **换引擎后凡带大表的
  页都会漏译**，并被 `post_check` 汉化率断言判 FAIL。这是迁移的**验收前提变化**，不是接线错。
- 决定（2026-09-20，用户拍）：**先挂账，不动代码**。理由与 v28.37 同一口径 —— 上游
  *experimental* 开关要先做对照试点再拍板，不能直接进主链路（透传 `--translate-table-text`
  或给 `post_check` 加表格页豁免都属"先改代码"，两条都留待对照试点出读数后再定）。

### 五、落点

- `tools/adopt.py` —— `inject` 补 `--tracking`；`render` 补 `--service` 并透传 `force_rerender`。
- 其余各件代码不动（v28.41/42/43 的实现本轮**未被回改**，只被"真跑"检验过）。

### 六、验证

| 项 | 读数 |
|---|---|
| 一条龙六阶段 | 全部真跑到位（见第二节表） |
| `tools/tests/run_all.py` | 全部通过 ✅（含 `test_engine.py` 115 PASS 不变） |
| rollback 反证 | 还原后重渲染，注入串 0 命中（证明改的是缓存，不是产物） |


## 2026-09-20 v28.44 引擎画像透传：`_run_tool` 把 `P2Z_ENGINE` 带进子进程

`server.py` 起的 adopt / force_rerender 子进程读 `P2Z_ENGINE` 决定画像。不透传时**不报错、
只是静默按缺省画像 `pdf2zh` 跑** —— next 任务会在 1.x 的产线上找一个不存在的段表。

### 落点

- `server/server.py` —— `_run_tool(script, argv, timeout=None, engine="")`：`engine` 非空时
  `env["P2Z_ENGINE"] = engine`；`_two_pass_adopt` / `_two_pass_run` 透传；三个 adopt 调用点
  （rollback / export / deliver·import·inject）传 `engine`。
- `patches/server_server.py` —— 镜像同步。

### 验证

| 项 | 读数 |
|---|---|
| `tools/tests/run_all.py` | 全部通过 ✅ |
| `tools/tests/test_mirrors_sync.py` | 13 PASS / 0 FAIL（本版扩到 13，见 v28.45） |


## 2026-09-20 v28.45 next 段表根变成**配置键**：`[translation].working_dir`（去掉 `--debug`）

### 一、要解决的问题

next 的**段身份**（`translate_tracking.json`）上游只在两种情况落盘：`debug=true`，或显式
给了 `working_dir`（`babeldoc/format/pdf/translation_config.py:287-305` 三岔口；缺省是
`tempfile.mkdtemp()` 且 `_is_temp_dir=True`，进程一退就 rmtree）。

而 `debug=true` 有两个**不能接受**的副作用：`AddDebugInformation` 把页码标签 / 段号标签 /
彩色方框**烘进产物本身**，产物名还被加上 `.debug`（`pdf_creater.py:1358-1366`）。于是
"第二公里"重渲染出来的东西**不是**用户拿到的那一份 —— 交付件里全是调试痕。

### 二、定性：不是 babeldoc 的缺口，是 pdf2zh_next 的**参数面**没接出来

| 层 | 事实 |
|---|---|
| babeldoc 库 | 早就支持 `working_dir`：`babeldoc/main.py:638-651` 建目录、`:710` 透传进 `TranslationConfig` |
| pdf2zh_next 2.9.0 | **转发链断了**：全包 grep `working_dir` 0 命中（阳性对照 `auto_enable_ocr_workaround` 12 命中，证明确实是 0）、`SettingsModel` 无此字段、`high_level.py` 的 `BabelDOCConfig(...)` 是固定 kwarg 列表 |
| 上游设计意图 | `cli_env_model.py` 自己写着 "If you want to use more field parameters in `pdf2zh_next/config/model.py` please add the corresponding forwarding here!" —— 手动转发只对 engine detail 那些字段，普通字段是**自动**从 `SettingsModel.model_fields` 全量转的 |

### 三、方案（⑦）：补丁打在 pdf2zh_next 的**配置层**，不碰 babeldoc 核心构造器

| 改哪 | 改什么 |
|---|---|
| `pdf2zh_next/config/model.py` | `TranslationSettings` 加 `working_dir: str | None = Field(default=None, description=…)` |
| `pdf2zh_next/high_level.py` | `create_babeldoc_config` 的 kwarg 列表加 `working_dir=settings.translation.working_dir` |
| `cli_env_model.py` | **不用改** —— 自动转发，加字段即有 config.toml 键 / `--working-dir` / `PDF2ZH_WORKING_DIR`（实测 `pdf2zh_next --help` 里出现了 `--working-dir`） |

选它而不是"改 babeldoc 核心"的理由：① 行为变成**配置**，server 侧一行不用改；② 可以
**提 PR 上游化**，被接受后补丁消失；③ 不撞 `TranslationConfig.__init__`（所有渲染入口的
公共构造器）。

**为什么不会被 server 重写抹掉**：`server/utils/config.py` 的 `pdf2zh_next` 分支是
`new_config = old_config.copy()` + 只 `setdefault`/`pop` 特定键，不碰 `working_dir`；
且 `prepare_config_files` 每次启动跑 `_merge_defaults(config.toml.example, current)` ——
只补缺失键、保用户值，所以 **example 里加了这键，老 config 会自动补上**。

### 四、段表根 = 单一权威源

`engine.next_working_dir()` 读 `P2Z_PROJ/server/config/config.toml` 的
`[translation].working_dir`；缺键或为 `"null"` 时回落上游 debug 缺省
（`~/.cache/babeldoc/working`，只对旧读数有效）。这样**第一公里（server 起 pdf2zh_next）
与第二公里（本工具定位段表）读同一份 config**，写死或各推各的必然错位，而错位的症状是
"找不到段表" = 空载荷 = 静默。

- `force_rerender.render_next`：去掉 `--debug`；段表路径改由 config 的键推出；跑完若段表
  没落盘 → **告警指路**（不静默）。
- 配置落点：`server/config/config.toml` 与 `config.toml.example` 加
  `working_dir = "D:/zotero-pdf2zh/server/nextwork"`；试验台加
  `D:/zotero-pdf2zh-eval/nextwork`；`.gitignore` 加 `server/nextwork/`
  （段表含全文原文+译文 = 版权红线，与 `server/translated/` 同理）。

### 五、试验台四看（0 花费，真 `pdf2zh_next`，`--service siliconflowfree`）

| 看 | 读数 |
|---|---|
| 段表落盘 | ✅ `D:\zotero-pdf2zh-eval\nextwork\test\translate_tracking.json`（138 KB / 17 段） |
| 产物名规范 | ✅ `test.no_watermark.zh-CN.mono.pdf`（无 `.debug`；旧的 `--debug` 产物是 `test.debug.no_watermark.zh-CN.mono.pdf`） |
| 无调试图层 | ✅ 新产物文本里 `pagenumber` 0 处 / `paragraph[` 0 处；旧 `--debug` 产物 **4 处 / 17 处**，文本长度 8866 → 4681 字符 |
| 缓存全命中 | ✅ `读数: 调用 11 / 缓存命中 10(全命中, 0 LLM)`，34 秒 |

### 六、镜像门禁扩到第二个 site 根

`pdf2zh_next`/`babeldoc` 补丁装在 **next venv**，与 1.x venv 不是一个 site。两侧
（`tools/sync_patches.ps1` / `tools/tests/test_mirrors_sync.py`）都按同一顺序解析：
`PDF2ZH_NEXT_VENV_SITE` → `PDF2ZH_NEXT_PYTHON` 同级 → 同 `envs` 目录下的
`zotero-pdf2zh-next-venv` → 都没有则该项 **SKIP（不算失败）**，公开克隆不会满屏红。
清单 11 → 13（新增 `pdf2zh_next_config_model.py` / `pdf2zh_next_high_level.py`）。

### 七、验证

| 项 | 读数 |
|---|---|
| `tools/tests/test_engine.py` | **118 PASS / 0 FAIL**（㉜ 改判"子进程参数里**不许**有 `--debug`"；新增 ㉞ 三条：键取值 / `"null"` 回落 / 缺文件回落） |
| `tools/tests/run_all.py` | 全部通过 ✅ |
| `tools/tests/test_mirrors_sync.py` | 13 PASS / 0 FAIL / 0 SKIP |

### 八、挂账（别重新起头诊断）

1. **生产 `config.toml` 的 `ignore_cache = true` → 已改 `false`（2026-09-20，用户拍板）**。
   上游 `translator.py:127/137` 是 `if not (self.ignore_cache or ignore_cache): cache.get/set`，
   为 `true` 时**既不读也不写**缓存 → 第一公里不落缓存行 ⇒ 第二公里没有可改的行。
   改动落点：`server/config/config.toml`（不入库，见 .gitignore）与 `config.toml.example`
   （`false`，入库）现在一致；只影响 next 引擎（`[translation]` 是 pdf2zh_next 的配置表）；
   1.x 走 `config.json`，不受影响。注意 `config_migration._merge_defaults` 只补缺失键、
   保用户值，所以老 config 不会自动被改成 `false`，要手改。
2. **读数行来自 babeldoc 的 `__del__`**（`translator.py:103-110`，进程退出前打日志），
   `debug=false` 走子进程时**偶发丢行**（实测 2 次里 1 次）。丢行时工具是"不打印"而不是
   打错数；确定性判据用渲染前的 `probe_prompts` 命中率（N/N 段）。
3. 上游升级会覆盖这两个补丁文件 → `test_mirrors_sync` 会 FAIL 报出来（这正是门禁的作用）。
4. 这两个补丁**可提 PR 上游化**（一个字段 + 一行透传），被接受后本节的补丁消失。

## 2026-09-20 v28.46 `post_check` 断言1 改**全篇口径**：表格页假阳性结案（建模定阈 τ=0.18）

### 一、起点：v28.43.1 第四节那条挂账

挂账原文的定性是「BabelDOC 默认不翻表格文本 → **换引擎后的覆盖率回退**」，触发场景：
Mandujano 2026 的第 3,9,10,11,12,13,14 页是整页大表格（Table 10.1 / 10.2 / +continued），
译文这 7 页汉字为零 → 断言1 逐页口径判 FAIL。

本节的结论是：**这条挂账的定性是错的**（不是回退），据此把断言1 的口径改掉。

### 二、对照试点：透传开关是死路，"覆盖率回退"不成立

| # | 读到的 | 结论 |
|---|---|---|
| ① | 上游 `--translate-table-text` 在 BabelDOC **0.6.2 已退役** —— `translation_config.py` 收到 `table_model` 即 warning 并**强制** `self.table_model = None`；`docvision/table_detection/rapidocr.py` 的 `RapidOCRModel` 是 "Compatibility no-op"（`predict` 恒返回空 boxes） | 透传该开关**不可能**让表格文本进 LLM；B 侧实跑逐页读数与 A 侧**逐字节相同** |
| ② | **1.x 同样不翻表格页**：`pdf2zh/high_level.py` 的 `vcls = ["abandon","figure","table","isolate_formula","formula_caption"]` → 构造版面索引时把 `table` 类区域**置 0**，不参与翻译 | "next 不翻表格"**不是新行为**，1.x 早就如此 |
| ③ | 2026-09-13 / 09-16 的 1.x 时期质检报告，与 09-20 的 next 时期报告**逐页相同**（Mandujano 第 3,9,10,11,12,13,14 页） | **推翻挂账前提**：这是两引擎共有的**既有假阳性**，与换引擎无关 |
| ④ | **没有"覆盖率回退"这种通道可走**：PDF 文本层无法区分「引擎按规定不翻的整页表格/公式页」与「卡死漏译页」—— 两者在译文里都表现为**与原文逐字相同**（`alnum` 逐一相等、`cjk=0`） | 原逐页判据的假阳性**无法靠加豁免修掉**（无信息可用于加判据） |

顺带读清的**全语料误报面**（15 篇候选）：真会被豁免的只有 Mandujano p3,9-14（真表格页）；
Brody p14 / Wang p15 已由 `is_ref_page` 豁免；SILAGE p44/p60、Yoshioka p45 是**公式密集正文页**，
但 `cjk_ratio` 0.255/0.152/0.154 都 ≥ 0.15，**不进候选面**。

### 三、外部依据（用户指令：先搜解决方案）

| 来源 | 读到的 |
|---|---|
| BabelDOC ACL 2026 论文（`aclanthology.org/2026.acl-demo.25.pdf`） | `Untranslated Blocks **Count**` = 「与原文相同的文本块**计数**」，LLM-as-a-judge 输出**整数**，**无 PASS/FAIL 阈值** → **计数与判定分离** |
| Xbench（CAT 行业标准 QA） | `Untranslated segments` 与 `Target same as Source`（译文=原文）是**两个独立检查项**，后者**默认关闭**；另有 `Exclude ICE Segments` 与 Project/Personal **Checklist** 白名单；输出是**列表 + 计数器**，无"整篇 FAIL" |
| BabelDOC 0.6.0 README | `--translate-table-text`（experimental, default **False**） |

→ 两条独立的外部实践都把"可疑页"当**读数**而不是**判定**。本节的改法与之同源。

### 四、数学建模定阈（用户指令：用数学建模的方式找最优解）

不拍脑袋给数值，而是把"阈值该定多少"写成**代价敏感决策**问题：

- **H0（无故障）**：15 篇实测的**全篇**读数 `whole = Σcjk/Σ(cjk+alnum)`（参与页口径），
  实测 **0.3644 ~ 0.8082**（min = Mandujano）、中位 **0.5117**、σ **0.1274**。
- **H1（有故障）两臂**：A = **页级截断**（"卡死/跳页"：前 k 页正常、后面整段没翻）；B = **乘性完成度**（每页只翻了一半）。
- **指标**：FPR / FNR；**代价** `C = θ·FNR + FPR`，`θ = c_FN/c_FP`（取 `θ ∈ {10, 100}`）。
- **规模**：蒙特卡洛 `M=4000`、`SEED=20260920`、`P_GRID=[0,0.05,…,0.90]`、`TAU_GRID=[0.05..0.35]`、
  `P_FAULT_MAX=0.6`；探针 `D:\zotero-pdf2zh-eval\logs\pilot_tbl\threshold_opt.py`（**留档不删** —— `post_check.py` 常量注释引用的就是它；同批其余一次性探针已清理）。

**关键结论（这条决定了改法的性质）**：`whole` 对"翻了多少页"**不敏感 —— 非单调**。
只翻 10% 的页（中位 **0.5829**）比翻满全篇（0.5117）**读数还高**，因为**未翻页的 `alnum` 特别大**，
分母被未翻页主导。后果：

| 量 | 读数 |
|---|---|
| 零误报上限 | τ ≤ **0.3644**（H0 最小值） |
| τ 推到该上限时「翻一半卡死」检出率 | 仅 **10.9%** |
| 要检出 50% 需 | τ ≈ **0.44**，而 FPR 达 **26.7%** |
| 严格代价最优 | τ* = **0.35**，但只比 τ=0.18 好 **17%**，且样本外余量只剩 4% |

→ **用户拍板 τ = 0.18**（= H0 最小值的一半 = **2 倍样本外余量**）。

### 五、语义代价（如实登记，别再当 bug 重新诊断）

全篇口径实等价于**「整篇零翻译闸门」**：**局部漏译的自动监控能力丧失**（检出率 ≤ 10.9%）。
保住局部监控的唯一路径是改用**引擎自述段表口径**（`translate_tracking.json` 的 `page[]`，
能直接看到"哪些块进了 LLM"）—— 但那会让 `post_check` **不再是纯 PDF 只读**（要读段表、
要有 `working_dir`），**已作为备选方案登记，本次未采纳**。

### 六、落点

| 文件 | 改哪 |
|---|---|
| `tools/post_check.py` | ① 模块 docstring 断言1 改写（全篇口径 + 外部依据）② docstring 特别处理段追加"且不参与全篇合计" ③ 常量区：`CJK_FAIL` 降级为**逐页读数**阈值 + **新增** `WHOLE_DOC_CJK_FAIL = 0.18`（含蒙特卡洛注释）④ `run_checks()` 断言1 主体重写（`fail_pages`→`zero_pages`；新增 `w_cjk/w_alnum/used` 累加器；产出 `"高"`-FAIL / `"通过"`-读数 / `"提示"`-零汉字页 / `"提示"`-无参与页）⑤ `build_report()` 断言项说明1 |
| `tools/tests/test_post_check.py` | ① ⑨ 用例改写（要出高危须"参与页合计"低于阈值；p12 文献页被豁免不参与合计，故 t 侧去掉 `cjk=900`）② ⑩ 常量回归补两条（`CJK_FAIL == 0.05` / `WHOLE_DOC_CJK_FAIL == 0.18`）③ **新增 ⑳ 四子例**：局部整页表格零汉字 → PASS + 提示；整篇零翻译 → FAIL；略高于阈值（0.20）→ PASS + 仍有提示；无参与页 → PASS + 报明跳过原因 |

**没动的**：`tools/pre_render_check.py` 只复用 `check_ref_zh`（断言4），**无断言1**，不受影响。

### 七、验证

| 项 | 读数 |
|---|---|
| `tools/tests/test_post_check.py` | **88 PASS / 0 FAIL** |
| `tools/tests/test_engine.py` | **118 PASS / 0 FAIL** |
| `tools/tests/run_all.py` | **21 套件全绿**（`回归结果: 全部通过 ✅`） |

### 八、挂账（别重新起头诊断）

1. **v28.43.1 第四节的原挂账定性（"是 next 的覆盖率回退"）已被本节推翻** —— 该节历史文字
   保留不改（记录当时的读数与判断），但**以本节为准**：不是回退，是两引擎共有的既有假阳性。
2. **段表口径的局部漏译检测**（真解，本建模已指出）未采纳，备选方案登记如上。
3. 本节改动**尚未提交**（工作树：`tools/post_check.py` / `tools/tests/test_post_check.py` 两个文件 M）。

## 2026-09-21 v28.47 整页表格翻译管线入库：`tools/table_pipe/`（TSV 往返 + 对账 + 附录排版 + 剪贴板中继）

### 一、起点：唯一"没有归属"的质量缺口

v28.46 结案了**假阳性**（表格页零汉字 ≠ 故障），但**真缺口**还在：整页表格在两引擎下
都不进 LLM（`table` 类区域置 0 / BabelDOC `--translate-table-text` 已退役），
所以整篇读数是"正常"的，**表格里的字一个都没翻** —— 而这条路径上没有任何工具负责它。

本节把该缺口用**独立 TSV 管线**补齐并入库。**不碰渲染层与清洗层**（故不触发
`patches/` 双写与缓存键变更），是**新增独立工具目录**。

### 二、管线（两段：表格正文 + 表注）

```
mk_grid.py   按坐标切网格  -> table_10_*.tsv (6x13 / 66x13)
mk_job.py    列策略+占位符 -> job_doubao.txt / job_manifest.json / job_audit.txt
   |  (人工: 整段粘给豆包, 复制回复)
check_job.py 五道门禁 G1-G5 -> PASS 才回填 table_10_*.zh.tsv + check_report.txt
mk_appendix.py  横版 A4 双语表头 DOCX -> appendix_tables_zh.docx

表注支线: mk_notes_job.py (装配 + 对账 G1/G2/G3/G5/**G6**) -> 回填进同一份 DOCX
演练:     mock_doubao.py [--violate]  (词典直替 + 注入 4 类违规, 验证门禁有牙齿)
中继:     watch_clip.py  (剪贴板监听, 见 §五)
```

### 三、三个"不许静默"的设计（各脚本 docstring 均记失败史）

| 件 | 关键判据 | 之前错在哪 |
|---|---|---|
| `mk_grid.py` **v4** | **列 = 左边缘 x0 聚簇**（同列 x0 偏差 <1.2pt，续行缩进 7pt 天然分开）；**行 = 基线 y1**（同行不同字号 y0 可差 2pt 但 y1 一致 <0.3pt） | v1 行间空档阈值无解（块内 0.9-1.5pt / 块间 3.4-3.7pt）；v2 x 投影吃掉单字符列（12 列应为 13）；v3 用"区间交集"判跨列，列边缘舍入值与 bbox 浮点差 0.04pt 就谎报重叠，**吃掉 381 个真格** |
| `mk_job.py` 列策略 | 逐列统计画像，六条规则全部**统计式/结构式、不含本书专名**（换书可复用），命中哪条+证据比率**逐列写审计**：R1 数值 / R5 引文 / R3' 专名 / R2 代码 / R4 拉丁孤词 / 否则 R6 自由文本 | 禁翻列整体**不送译**（学名/命名人/引文/代码根本不进 MT）→ 层一结构问题被**结构性消除**，而非事后纠错 |
| `mk_job.py` 占位符 | 可翻格内数字一律换 `{S###}`（全局编号，`phmap` 存原值），装配时只发可翻列 | 数字是唯一"跨语言不变"的锚点；不锁就会被 MT 改写/丢位 |

**折行连字还原**：`词尾- + 小写开头` 与 `词尾/ + 词` 就地拼接，逐条记审计
（前段实测 15 处全部是原文自带的 `-` 或 `/`，不是版面产物）。

### 四、门禁（`check_job.py` / `mk_notes_job.py`）

| 门 | 查什么 |
|---|---|
| G1 | 编号一一对应：缺 / 多 / 重复 / 无法解析 |
| G2 | 占位符序列（个数、位数、顺序、大小写） |
| G3 | 占位符之外不得出现裸数字 |
| G4 | `±` 个数保真 |
| G5 | 译文非空 |
| **G6** | （表注独有）23 个代码缩写（SHFT/LFT/R/Z/B/UM/UF…）**逐一必须在译文出现** |

**PASS 才回填**，回填同时落 `check_report.txt`。对账永远在本机跑 ——
**校验权不外放**（豆包曾自荐"我用 Python 跑一遍校验"，已明确否掉）。

**已知放宽**：网页对话渲染会把 TAB 转成空格 → `mk_notes_job.py` 与 `watch_clip.py`
分隔符放宽为 `[\t ]`，但**落盘统一还原为 TAB**，保住 `check_job.py` 的严格 TAB 契约。

### 五、`watch_clip.py`：剪贴板中继（免费版通道）

**要解决的真实问题**（用户提出）：免费版豆包只有**普通对话框**，没有连接器/工作任务，
不能读写本地文件。用户在网页里翻完、复制了回复，却卡在"存成文件、跑脚本"这两步。

**设计分界（`relay_spec.md` §6.1）**：**只自动化本地侧** —— AI 侧保持手工
（一次粘贴 + 一次复制）。浏览器自动化越深越脆弱（豆包改一次 DOM 就废），
而"手工复制"换任何 AI、任何版本都不会坏；且天然兼容免费版。
本地侧"回复→文件→对账→出稿"则 100% 自动化，用户只按一次 `Ctrl+C`。

**三重防误触发**（全部实测）：① 单元编号齐全（表格 115 个 `k` / 表注 3 个 `n`）
② **不含装配前缀 `【待译】`/`【任务】`**（关键：job 文本的编号同样齐全）
③ 过半单元含中日韩字符。

**零依赖**：剪贴板走 `ctypes` 调 Win32（`OpenClipboard`/`GetClipboardData(CF_UNICODETEXT)`/
`GlobalLock`）—— 实测本机 `pyperclip` / `win32clipboard` **均未安装**。

**踩过的两个真 bug**：
1. **stdout 缓冲导致常驻模式无输出** —— `--once` 能打印只因退出时 flush；常驻下 banner
   与结果全卡在缓冲区（后台日志为空）。修：`sys.stdout.reconfigure(..., line_buffering=True)`。
2. **人工修正改错了层** —— 三处译法修正曾改在**派生物** `notes_zh.json` 上，监听器一重跑
   就被覆盖。修：归位到**源** `job_notes_response.tsv`。已写入 `relay_spec.md` §8 教训。

### 六、工作目录参数（只进代码，不进数据）

6 个脚本原本都用 `D = 脚本所在目录` 定位数据 → **只把代码搬进仓库会全部找不到 manifest/TSV**。
改为两行 idiom（缺省行为不变）：

```python
SD = os.path.dirname(os.path.abspath(__file__))          # 脚本目录(本件所在)
D = os.environ.get("P2Z_TABLE_DIR") or SD                # 工作目录(数据所在), 缺省=脚本目录
```

`watch_clip.py` 另加 `check_workdir()` 自检：缺 `job_manifest.json` 时打印
"工作目录 / 脚本目录 / `set P2Z_TABLE_DIR=...`"并 `exit=2`，而不是让用户撞一个 `FileNotFoundError`。

**数据（TSV / manifest / PDF / DOCX）留在评测目录** `D:\zotero-pdf2zh-eval\logs\table_appendix\`
—— 工具与项目分离，仓库不增体积。

### 七、落点与验证

| 文件 | 说明 |
|---|---|
| `tools/table_pipe/mk_grid.py` (11585) | 网格切分 v4 |
| `tools/table_pipe/mk_job.py` (9533) | 待译文装配 + 列策略 |
| `tools/table_pipe/check_job.py` (4154) | 对账 G1-G5 |
| `tools/table_pipe/mk_notes_job.py` (9134) | 表注装配/对账 + G6 |
| `tools/table_pipe/mk_appendix.py` (6830) | 附录排版（横版 A4 29.7x21.0cm、双语表头、`repeat_header`、列宽按 `vlen` 加权） |
| `tools/table_pipe/watch_clip.py` | 剪贴板中继 |
| `tools/table_pipe/mock_doubao.py` (4827) | 演练回包 + `--violate` |
| `tools/table_pipe/relay_spec.md` (5676) | 中继规格（§4 权限红线 / §7 隐私不变量 / §8 已实施件） |

**实测**：端到端闭环已通（真实往返，非 mock）—— 终稿 `appendix_tables_zh.docx`
（表 10.1 = 6x13、表 10.2 = 66x13，表注已回填）。
`watch_clip.py` 四项全过：表格回包 115 单元 G1-G5 全过且落盘**逐条一致零值差异** /
表注回包 3 单元 G1/G2/G3/G5/G6 全过 / 防误触发正确忽略 job 全文 / 常驻模式正常。
工作目录参数三情形全过：默认不变 / 仓库版 + 环境变量正常（含子进程链）/ 不设变量给提示 `exit=2`。

### 八、挂账（别重新起头诊断）

1. **与仓库已有两件的关系**：`tools/table_zh.py` 是**旧路线 B**（表格页定点中文化），
   已在文件内标注"本产品未采用" —— 否因**全在渲染层**（碎片字形、中英混合、字体对撞、
   基线错位）；新管线**不落字形**，故那些否因一条都不成立。`tools/doubao_bridge.py`
   是依赖**连接器**的 MCP 桥（= 付费路径），`watch_clip.py` 是其**免费替代通道**，两者互补。
2. **未决**：是否把 `tools/table_zh.py` 标注为"已被 `tools/table_pipe/` 取代"（尚未与用户确认）。
3. **列策略已知局限**：命名人**全裸写**（无括号）的物种列可能漏过 R3' —— 审计里给两个比率
   供人核，属"不静默判"而非自动解决。
4. **层二字符保真**：占位符化只锁数字；拉丁学名/人名片段的保真靠"禁翻列不送译"结构性保证，
   可翻列内的碎片仍依赖 MT —— 无自动判据。
5. **评测目录遗留**：`mk_excerpt_upright.py`（摘录烘正脚本，仍含硬编码绝对路径）、`archive/` 内 7 个旧文件。

## 2026-09-21 v28.48 中继面板 `panel.py`：把"提交"交回给人（检查/确认两段 + 核对表）

### 一、起点：用户提出"复制不全会不会直接就跑了"

用户诉求（原话）：「如果出现复制内容错误的话。或者复制不全 那么这个监听器会不会就直接就运行了？
然后它也没有一个铃声的一个反馈。然后我想的是我们做一个前端，就是相当于做一个 弹窗 …
还有一个确认按钮。按了之后 这个消息才发出去。才 进行下一步。」

**先实测，不先答。** 把真实回包 `job_response.tsv` 的末行逐级截断，看 v28.47 的监听器怎么判：

| 末行被吃掉 | 解析结果 | v28.47 监听器的判断 |
|---|---|---|
| 3 字符 | `k115  两种`（原文「两种传粉者」） | **判为有效回包 → 直接跑完出稿** |
| 5 字符 | `k115  `（空） | 判为有效 → 下游 G5 空译文拦住 |
| 9 字符 | `k11`（行不成形） | 编号缺失 → 正确拦住 |

→ **漏洞成立且比预想严重**：吃掉 3 字符这一档，编号齐、占位符一个没丢、非空，
**G1-G5 全过**，附录里那一格就是错的。

### 二、为什么门禁补不了这个洞（性质判断，不是实现疏漏）

`check_job.py` 的五道门锚点是**编号**与**占位符**。"末行被吃掉一截但还剩内容"两样都不触发 ——
**判据够不到**。这与 v28.46 那次"全篇口径对局部漏译不敏感"是同一性质的问题：
不是阈值没调好，是**信息不在判据里**。所以"加一条断言"这条路不成立，
唯一可靠的拦截点是**把人放回回路**，且人要能**看得见**。

### 三、设计：检查 / 确认两段

```
[① 复制待译文本]  面板直接把 job 文本塞进剪贴板(省得再翻文件), 并报 单元数/字符数
[② 粘贴回包]      粘豆包的回复; 也可「从剪贴板读入」
[③ 检查]          沙箱跑门禁 -> 核对表 -> **只读预览, 真目录零污染**
[④ 确认写入并出稿] 才落盘 -> 对账 -> 排版 -> 开 DOCX -> 响铃
```

**"检查"如何在零污染的前提下跑真门禁**（不重复实现门禁代码）：
manifest 与回包都拷进 `tempfile.mkdtemp()`，子进程的 `P2Z_TABLE_DIR` 指向该临时目录 →
它回填的 `table_10_*.zh.tsv` / `check_report.txt` / `notes_zh.json` 全落在 tmp，随后整目录删掉。
用的是**同一份** `check_job.py` / `mk_notes_job.py check`，不是第二套实现。

**v2 改核对的设计（用户原话：「如果它内容比较长, 然后我对它的格式不是特别理解, 那你觉得我该怎么处理?」）**

原设计的错位是**让人去扫 115 行全表**。答案不是"教人怎么看表", 是**别让人看那么多**：

| 层 | 内容 | 给人看什么 |
|---|---|---|
| 一 | 机器已保证（编号/占位符/数字/±/非空/代码缩写） | **不看**，只在顶部给计数 |
| 二 | 机器判不了的 | **待看清单**（正常回包为**空**） |
| 三 | 兜底读数 | 一致性 / 长度比 / 末条完整性 |

界面：顶部一行大读数 → **待看清单（默认页，空时给空状态文案）** → **全表（第二页，默认折在后面）**。
照 CAT 工具 QA 面板的范式（按项标注 + 分级 + 筛选，而不是全文 PASS/FAIL）。
**不能只靠颜色**（8% 男性色盲）：每条带 `▲/●` 图标与"高/中"文字，颜色只作加强。
**读数不判定**：待看清单非空**不阻断**确认，只在按钮上如实标出"（还有 N 条待看）"。
粘贴区一改动即作废检查结果（`<<Modified>>`）→ **"确认"不可能按在过期内容上**。

### 四、长度比读数的**边界**（如实登记，别当已解决）

实测（115 单元真实回包，中位比 0.27）：

| 情形 | 末条长度比 | 是否被列出 |
|---|---|---|
| 完整回包 | 0.33 | 否（**零误报**） |
| 末行截断 3 字符 | 0.13 | **是**（红色 + 黄底 + 日志警告） |
| 末行截断 1 字符（`两种传粉者`→`两种传粉`） | 0.27 | **否 —— 漏报** |

**边界结论**：单靠长度比**分不开"被截断"与"本来就短"**（截断后 0.13 vs 全批最小 0.15，
没有能分开两者的阈值）。因此读数只加了一条**定向**规则：**末条**低于全批中位即列入
（末条是截断的高发位置，健康回包末条通常在中位附近，故不误报）。
它能抓的是**成句丢失**级别的截断；**丢一两个字的截断读数抓不到** —— 那只能靠
**末条原文/译文并排显示**让人眼抓（`Two pollinators` -> `两种` 一眼可见）。

→ 所以面板里"读数"与"并排对照"是**互补的两件**，不能只留一件。

### 五、一致性检查：回答"我不懂格式怎么审"的那一条（本节最有价值的发现）

**判据**：同一段原文出现多次 → **译文必须相同**。不同则列入待看清单。

**为什么它是答案**：用户不需要知道 `Melittophily` 该不该译成"蜂媒" ——
只需要知道**同一个英文词在两个格子里翻得不一样**。纯机械、**零阈值、无参数**、不需要专业知识。
行业里这也是 CAT 工具的标准检查项（memoQ 的 Consistency、YiCAT 的 inconsistencies）。

**实测（115 单元真实回包）**：

| 情形 | 重复原文 | 不一致 | 待看清单 |
|---|---|---|---|
| 完整回包 | 15 组 | **0** | **空** |
| 末行截断 3 字符 | 15 组 | **1** | ▲一致性（`Two pollinators` 出现 7 次却译成 2 种）+ ▲末条偏短 |
| 人为把 `Life form` 的一处改成"生活型（改）" | 15 组 | 1 | ▲一致性（精确命中 k004 k034） |
| 表注回包（3 单元） | 0 组 | 0 | 空 |

**关键发现：一致性检查把 §一 那个末行截断也抓到了。**
`Two pollinators` 在表里出现 **7 次**（k078 k084 k097 k106 k109 k110 k115），
截断后 k115 变成"两种"而其余 6 处是"两种传粉者" → 直接命中。
**这比长度比读数强得多** —— 长度比要靠"末条定向放宽"才抓得到（§四），
一致性是**纯机械、零阈值**就抓到了，而且**对中途任何位置的截断同样有效**（只要有重复原文）。

**已知边界（如实登记）**：截断若落在**原文唯一**的单元上，一致性检查抓不到 ——
那一条只能靠长度比 + 末条并排对照兜底。覆盖率取决于表格里重复值的多少
（本表 115 单元 / 54 个不同原文 / 15 组重复，覆盖 61 个单元）。

### 六、落点

| 文件 | 改哪 |
|---|---|
| `tools/table_pipe/panel.py` | **新增**。`analyse()` 为纯逻辑（不碰 Tk，便于 `--selftest`）；`sandbox_gates()` 临时目录跑真门禁；`Panel` 为界面。零依赖：GUI 用 stdlib tkinter（实测 8.6 在 venv 内可用），剪贴板读复用 watch_clip 的 ctypes 通道、写走 tkinter |
| `tools/table_pipe/watch_clip.py` | ① `JOBS` 加 `job=` 键（待译文本名，面板复用本表，不另起一份）② 抽出 `manifest_units()` ③ **`parse_units()` 契约改为"读到多少算多少"**（齐不齐由调用方判 —— 面板要拿"缺哪几条"的明细给用户看）④ `match_job/run_job/handle` 加 `log=` 参数（面板把日志收进窗口）⑤ docstring 记明本脚本的取舍与已知局限 |

**行为变化（登记）**：`parse_units()` 重复编号由"后覆盖"改为"**取首条**"，
与 `check_job.py` 同口径（它保留首条并单列 dup）。

### 七、验证

**`analyse()` 四情形（v2 重写后）**

| 情形 | 读数 |
|---|---|
| 完整回包 | `ok=True` / 115 单元 / 中位比 0.27 / 重复原文 15 组 / 不一致 0 / **待看清单为空** |
| 末行截断 3 字符 | 不一致 1 / 待看 **2**：▲一致性 `Two pollinators` 出现 7 次却译成 2 种（两种 / 两种传粉者，k078 k084 k097 k106 k109 k110 k115）；▲末条偏短 k115 比 0.13 < 中位 0.26 |
| 人为把 `Life form` 一处改成"生活型（改）" | ▲一致性精确命中 k004 k034 |
| 表注回包 | `ok=True` / 3 单元 / 中位比 0.38 / 重复 0 / 待看 0 |

**GUI 冒烟（v2，真 `Tk` 主循环，`startfile` 打桩）**

| 步 | 读数 |
|---|---|
| ① 发送 | `已复制 115 单元 / 2803 字符` |
| ③ 检查（完整回包） | 大读数 `✓ 表格正文 115/115 · 门禁全过 · 重复原文 15 组/不一致 0 · 0 条待你核`；页签 `待看清单 (0)` / `全表 (115)`；**空状态在显示** |
| ④ 确认 | DOCX 重出 **45237 字节**，确认键回 `disabled` |
| ③ 检查（截断回包） | 大读数 `…重复原文 15 组/不一致 1 · 2 条待你核`；按钮 `④ 确认写入并出稿（还有 2 条待看）`；页签 `待看清单 (2)`；全表标红 7 个单元（k115 k078 k084 k097 k106 k109 k110） |

| 项 | 读数 |
|---|---|
| `watch_clip.py --once`（重构后） | 无回归 |
| `tools/tests/run_all.py` | **21 套件全绿** |

### 八、界面层转向 v3：tkinter → 本机网页版（同日设计评审后）

**为什么弃 tkinter**：双主题 tkinter 版做出来被评审否掉 —— 白色原生标题栏与深色卡片不搭、
页签样式断裂、滚动条原生白、大面积平色无层次。根因不是配色，是**渲染层天花板**：tkinter
画不出圆角卡片 / 毛玻璃模糊 / 鼠标跟随光影，而这三样正是用户要的苹果"液态玻璃"。
界面层整体换成本机网页应用，后端逻辑（analyse/沙箱门禁/run_job）一行不动：

- **形态**：stdlib `http.server` 起 127.0.0.1 随机空闲端口，前端单页 HTML 内嵌在 panel.py
  （PAGE 常量，零依赖零安装），优先用 Edge/Chrome `--app=` 模式开**无浏览器框**窗口
  （看着就是原生弹窗），找不到退回默认浏览器。
- **液态玻璃配方**（取自用户自己的 ice917/weather-app 仓库 + Apple WWDC25 官方设计原则）：
  CSS 变量双主题 + `backdrop-filter: blur(20px) saturate(160%)` + 22px 圆角 +
  内顶部高光 + 背景 3 团 `blur(100px)` 漂浮光斑（玻璃模糊所依托的环境） +
  每卡片 `::after` 径向高光随指针（rAF 节流，`--mx/--my` 变量）。
- **两条网页版契约**：
  退出 —— 页面关窗前 `sendBeacon` 打 `/api/bye` 进 15s 宽限期：期内收到新心跳
  （F5 刷新 / 别的页面还活着）则告别作废，否则才退出；兜底是心跳，收过心跳后
  `IDLE_LIMIT`(600s) 没再收到才算"窗口已关"（30s 的初版在真机误杀过，见 §九；
  告别立即退会在 F5 刷新、验收浏览器先关时误杀，见 v28.50）；
  改动作废 —— `/api/check` 记下文本 sha1，`/api/commit` 发现变了直接 409 拒绝
  （服务器端强制，不只靠前端禁按钮）。
- **剪贴板改走 Win32**：服务器进程没有 tk 主循环，`watch_clip.py` 新增 `write_clip()`
  （64 位下句柄必须声明 restype，否则指针截断）；`read_clip()`/`write_clip()` 的
  `OpenClipboard` 失败从"一次即弃"改为**短重试 5×120ms**；失败原因分五种记入
  `LAST_ERR` 如实转述，不再笼统一句"没有文本或被占住"（见 §九）。
- 主题选择落工作目录 `.panel_theme`（数据侧不进仓库），前端 CSS 变量承载全部配色；
  tkinter 版双主题代码已整体删除，不留两套 UI。

**验证（v3）**：API 冒烟 12 项全过（真实回包 check → 115 单元门禁全过/待看 0；
commit 带改动文本被 409 stale 拒、不落盘；theme 持久化；404 处理）；
内置浏览器实操验收全过（液态玻璃观感/悬停高光/注入真实回包 → 检查绿横幅 →
全表 115 行末行黄底/深浅主题互切/无 JS 报错）；`--selftest` 无回归；
`run_all.py` 21 套件全绿（须用 zotero-pdf2zh-venv 的解释器跑，裸 python 缺 pdf2zh）。

### 九、真机验收暴露的两个真 bug（v28.49 / 界面 v3.1，同日）

真机（Edge `--app` 无框窗口）第一次走 ①→豆包→② 时，② 报「剪贴板里没有文本
(或被别的程序占住)」。**这个报错本身是假的**，真因分两层：

1. **服务器自己退出了。** 初版退出契约阈值 30s —— 用户点完 ① 就去豆包翻译，面板窗口
   失焦，浏览器把后台页面的 `setInterval` 节流甚至暂停，30s 收不到心跳，服务器判定
   "窗口已关"自杀。用户回来点 ②，服务器早没了。
   实测证据：写入剪贴板后 `t=0s len=3053`、`t=10s len=3053`，`t=20s` **直接连不上** ——
   服务器在中间那 10s 内退出，而剪贴板内容全程完好（`/api/clip` 与 PowerShell
   `Get-Clipboard` 读数一致）。**剪贴板从来没问题。**
2. **前端把连接失败谎报成剪贴板问题。** `api()` 的 `catch` 返回 `{error:'服务器无响应'}`，
   而 `/api/clip` 的处理只看 `!r.text` → 服务器已死也被说成"剪贴板里没有文本"，
   把真因整个盖掉（上一轮因此误判成"harness 占剪贴板"，见 §十 第 5 条）。

**修复**：
- 退出改两条腿：页面 `pagehide` 时 `navigator.sendBeacon('/api/bye')` → 立即退出不留孤儿；
  心跳兜底阈值 30s → `IDLE_LIMIT = 600s`（用户去豆包翻一轮的典型时长）。
- `api()` 失败区分 `{net:true}`，前端如实显示"连不上本机服务器 —— 面板可能已退出"。
- `read_clip()` 五种失败成因分别记入 `LAST_ERR`（被占 / 无纯文本格式 / 空句柄 /
  `GlobalLock` 失败 / 空文本），面板如实转述，不再一句话糊过去。
- 顺带：打开面板不再假装"✓ 没有需要你核对的项目"，初始态改为"尚未检查"。

**验证**：退出契约冒烟 10 项全过（关键两项 —— 「心跳静默 60s 不再误杀」
「告别后服务器已退出」；另含 why 字段、初始态文案、写入/读回 115 单元）；
`run_all.py` 21 套件全绿。

**教训**：① 心跳阈值这类"活性判定"参数，取值要按**最坏的用户行为**定（去别处翻几分钟），
不是按理想轮询间隔；② **任何"读不到/失败"的报错都必须带具体成因** —— 笼统的兜底文案
会把真因埋掉，导致后续诊断全跑偏（本轮为此白查了一轮剪贴板）。

### 十、挂账（别重新起头诊断）

1. **丢一两个字的截断读数抓不到**（§四），只能靠末条并排对照人眼抓 —— 已知边界，非待修项。
2. **截断落在"原文唯一"的单元上，一致性检查也抓不到**（§五末）—— 与上一条同源，
   别当新 bug 重新诊断；覆盖率取决于表里重复值的多少（本表覆盖 61/115 单元）。
3. **`watch_clip.py` 仍无确认环节**（用户选择保留为"无交互通道"）。它拦不住 §一 那一档，
   已在文件 docstring 里写明取舍并指向 panel.py。**别把它当成"面板的旧版本"删掉**。
4. 面板未做"重新装配"入口（`mk_job.py` 仍需手工跑）—— 本轮未要求，避免过度设计。
5. ~~自动化验收浏览器会持续占住剪贴板（harness 行为，非面板 bug）~~ —— **本条已证伪**：
   真机复现了同样的报错，真因是服务器心跳自杀 + 前端谎报（§九），与剪贴板无关。
   当时那句"没有文本或被占住"的兜底文案把真因埋了，才导致误判 —— 别再按这条排查。
6. **不可断行的长词会被断行**（用户 2026-09-21 看图时发现，**用户明确决定先记不修**）：
   表 10.1 首列 `Maihuenioideae`(63pt) 装不进 56.5pt 的列，被断成 `Maihuenioide` / `ae`；
   同类还有 `Inflorescence`(55.1→58.5)、表 10.2 `Martínez-Peralta`(73.8→76.5)。
   根因：`mk_appendix.py` 的 `widths_for()` 只看"整格最长内容"，没区分中英文断行能力
   （中文可任意断行，拉丁词不能）。修法：分配宽度时先给不可断行串留足，再分剩余。
   合计仅约 0.4cm，**版面本身没超宽**（表格网格 774.0pt 精确贴合可用宽 773.9pt）。

### 十一、v28.50 界面收尾三件事（任务下拉 / 删剪贴板按钮 / 雾面）（2026-09-21）

真机用起来后按观感收的三笔，都是**界面层**（v3 的延续），后端逻辑一行没动：

1. **原生 `<select>` 换成自定义下拉。** 真机展开任务下拉：深色主题下弹层是**白底黑字**，
   颜色不统一；改成实心 `option` 底色后颜色对了，但**弹层是操作系统画的，圆角改不了** ——
   和液态玻璃方框格格不入。原生 `<select>` 的弹层形状/毛玻璃**不可定制**，只能自绘：
   胶囊按钮 + 绝对定位圆角玻璃列表（14px 圆角 + `blur(20px)` 毛玻璃 + 悬停高亮 +
   选中蓝色 + 箭头翻转 + 点外部关闭）。浏览器代理实测：两项完整可见可点、零 JS 报错。
2. **删掉「从剪贴板读入」按钮。** 真机上它连点多次读不到（旧窗口连的是已自杀死服务器，
   页面又把一切失败报成"剪贴板没有文本"），而手动 Ctrl+V 一直好使 —— 用户判定它多余，
   按钮 + 前端处理 + 后端 `/api/clip` 路由一起删。② 栏只剩「清空」。
3. **下拉列表改雾面。** 玻璃底 `rgba(30,41,59,.66)` 太透，底下卡片的字会透出来；
   改高不透明雾面 `--dd-bg`（深 `rgba(22,32,47,.94)` / 浅 `rgba(248,250,252,.94)`），
   边缘保留毛玻璃模糊过渡，不再透字。

**顺带修一条真问题**：退出契约里 `/api/bye` 是"收到即退" —— 但 `pagehide` 不只在
真关窗时发：**F5 刷新**、**验收用的自动化浏览器先关**也会发。收到告别立即退，会把
还活着的窗口晾成死页面（踩过：验收浏览器先关，面板跟着自杀）。改为**告别后 15s
宽限期**：期内有新心跳（F5 后新页面起来 / 还有别的页面活着）则告别作废，否则才退出。
冒烟验证：告别后新心跳 → 活着；告别后无心跳 → 15s 后退出；心跳持续 → 永不误杀。

**验证**：告别宽限期冒烟 4 项全过（F5 场景不误杀 / 真关窗仍退 / 心跳持续不杀 /
POST ping 的测试脚本自身 bug 已修）；浏览器代理实测自定义下拉；`run_all.py` 21 套件全绿。

**教训**：① 浏览器原生效应的"可定制性"有硬上限 —— 弹层形状是操作系统画的，
`option` 颜色只部分可控；要圆角玻璃质感，原生控件就别纠结了，直接自绘。
② `pagehide` 不等于"关窗"，别用它做立即退出的触发点 —— 刷新和标签页关闭都会发。

### 十二、v28.51 整本书核 + 表注注入 + 交付说明（2026-09-21）

三缺口收尾（整本书核 → 表注进主 PDF → 交付说明），对应交付物
`out/成品/Cactaceae2009_中文版.pdf` + `appendix_tables_zh.docx` 两件套的最终核验与归档：

1. **整本书核（缺口 2）**：`post_check.py` 对最终成品逐页对账（对比页数 34），
   **门禁 PASS（可交付）** —— 全篇汉化率 **35.9%**（阈值 18%），无 `{vN}` 残留，
   无渲染残渣；第 10–14 页（1 基）汉字占比 < 5% 仅**提示**不判 FAIL：这 5 页是
   引擎按规定不翻的整页表格，已人工对照原文确认。结论：主 PDF 表格页英文原版
   **是设计决策不是漏译**（碎片字形排版无法干净替换，见 §十 table_zh 未采用），
   表格中文统一在附录 docx。
2. **表注注入主 PDF（缺口 3）**：表 10.1 / 10.2 的英文表注（含脚注与全部代码缩写
   释义）位于表格顶部碎片排版区，无法就地替换 —— 以「表注（中文）」标签追加到
   页底：第 3 页（0 基 2）表 10.1、第 9 页（0 基 8）表 10.2，6pt 两栏、标签 7pt，
   字体 NotoSerifCJKsc-Regular 子集（45.29MB）。注入前先备份
   `_bak_表注前.pdf`。验证：无溢出（assert r>=0）、无重叠、中文渲染正常、
   注入后 post_check 重跑 PASS（35.9%）。
   技术点：`d.save(原路径)` 抛 `ValueError: save to original must be incremental` →
   存 `OUT.tmp.pdf` 后 `os.replace` 覆盖；一次 `PermissionError` 因用户开着 PDF，
   关掉后 `Move-Item -Force` 完成。
3. **交付说明（缺口 1）**：写 `out/成品/交付说明.md` —— 两件套配套关系
   （表格页英文原版是设计决策 + 表格中文在附录 docx）、表注注入说明、post_check
   PASS 结论、文件清单、已知挂账（`Maihuenioideae` 断行先记不修）。
4. **清理**：删除 8 个一次性 `_*.py` 探测/注入脚本 + 2 个测试 PDF
   （`D:\zotero-pdf2zh-eval\logs\table_appendix\`）；方法均已固化为本条目。

**验证**：注入脚本断言无溢出；`_verify_final.py`（已删，见上文）确认页数 35 /
表注标签 True / 重叠 0；post_check 最终报告
`server\translated\review\翻译后质检_20260921_144813_*.md` PASS。

### 十三、v28.52 正文任务挂进翻译中继面板（2026-09-21）

豆包通道收口：整篇论文正文回包不再走手动脚本链，与表格/表注一样粘进面板
（analyse → 沙箱门禁 → 确认 → 落盘 → 注入 → 重渲染），一次点击出稿。

1. **任务注册表加第三条**（`watch_clip.JOBS`）：正文任务 manifest=inbox/payload_<name>.manifest.json、
   载荷=inbox/payload_<name>.txt、回包=payload_<name>_response.tsv、门禁=seg_import
   （回锚对账）、出稿=seg_inject（注入缓存）+force_rerender（重渲染，1.x 服务端 8890）、
   产物=<原文 stem>-mono.pdf。路径约定沿用 engine.py/adopt.py 同一套环境变量
   （P2Z_PROJ / P2Z_INBOX / P2Z_BODY_NAME / P2Z_BODY_PDF）。
2. **识别与对账通用化**（`watch_clip`）：新增 `manifest_path` / `_body_blocks` /
   `manifest_units` / `manifest_ids` / `available_jobs`；`parse_units` 支持 TSV
   （k001\t译文）与块（#S1\n译文）双形态；`write_resp` 按任务形态落盘；`run_job`
   返回产物路径 out；`check_workdir` 按 available_jobs 判定。正文 manifest 的 items
   只有坐标，原文从载荷 txt 的 #S 块补全（与 seg_import 的 KEY_LINE 同一口径）。
3. **正文管线脚本路径修正**：seg_import / seg_inject / force_rerender 在 tools/ 根
   而非 table_pipe/，JOBS 的 cmd/after 统一走新增的 TOOLS 常量。
4. **侧车认领加覆盖度校验**（`body_sidecar`）：优先按原文 PDF 内容散列认领归档件
   pdf-<md5[:16]>.jsonl，认领后按 manifest 页码集合校验 —— 实测 Lee 2026 归档件只有
   3 页而 manifest 引用 1-11 页，回锚一碰第 7 页就 KeyError 崩；覆盖不了就回落
   latest.jsonl，让门禁报清晰缺页错误而非静默错锚。
5. **面板接线**（`panel`）：`sandbox_gates` 支持绝对路径 manifest（只读不拷）与正文
   沙箱隔离（P2Z_PROJ=tmp，防门禁真写 out/ 与返工单）；`analyse` 用 job 对象 API +
   available_jobs；`_send` 空守卫 + 绝对/相对路径 + n 按 manifest_ids；`/api/state`
   只列 manifest 在位的任务；`_commit` 打开 `run_job` 返回的 out（不再写死 DOCX）；
   页面标题「表格翻译中继」→「翻译中继」；PH 正则补 `{vN}`；main 用 check_workdir。

**验证**：panel.py --selftest 三条任务双回归全过 —— 表格正文 115/115、表注 3/3、
正文 mock 137 段（每段「模拟译文」+ 载荷原块、含全部字形值）门禁全过，沙箱产物
只落 tmp（真实 out/ 与 inbox 零污染）；tools/tests/run_all.py 21 套件全绿。




### 十四、v28.53 两趟回路默认开 + 换篇自动切换（2026-09-21）

1. **两趟回路默认开启**（`server.py` L59-62）：`PAUSE_TRANSLATE` 从「默认关」改为
   「默认开」—— 提交即走**提字**（零 LLM，落骨架行 + 侧车），停在「待译」等交件。
   想回官方直译设 `PAUSE_TRANSLATE=0`。**动因**：用户关闭了 LLM 供应商后提交论文，
   直接走官方翻译必然报 `Unsupported translation service`，且连提字都跑不了。
2. **force 请求旁路两趟**（`server.py` L1409）：`force=true`（译文已回灌的重渲染）
   直接走渲染，不被卷进「提字 → 等交件」，也不会把注入的译文覆盖成骨架行。
3. **载荷 manifest 自带原文 PDF**（`seg_export.py` L258-262）：新增 `pdf` 字段，
   让面板能凭载荷本身锚定同篇 PDF，不再依赖服务端 history 推断。
4. **面板自动切换最新论文**（`watch_clip.refresh_body` + `panel`）：扫 inbox 取
   mtime 最新的载荷（有配套 manifest 才算，手动 `payload_<名>` 与两趟自动导出的
   `<原文stem>` 两种命名都认）；`/api/state` 把「正文」任务排最前，前端下拉默认
   选中正文而非第一个表格任务；`_send` 未选中时也优先回落「正文」。
   显式设 `P2Z_BODY_NAME` / `P2Z_BODY_PDF` 则不覆盖（手动指定优先）。

### 十五、v28.54 侧车按内容身份锚定 + PDF 由侧车反查（2026-09-21）

**根因**：服务器重启后 history 变化 → 面板回落推断把原文 PDF 错认成 Lee 2026 →
侧车按「页码覆盖度」选时**打平**（Lee 侧车 11 页 ∩ Li manifest 1-7 页 = 7，与 Li
自己侧车 7 = 7），`max()` 平局拿了 Lee 的侧车 → `seg_import` 回锚全错 → 门禁未过。

1. **侧车选择改「内容身份优先」**（`watch_clip.body_sidecar`）：枚举 segflow 下
   **全部**归档件（pdf-*.jsonl）+ latest.jsonl 作候选；从载荷 txt 采 8 行较长原文
   （跳过 `#`/`[`/`【` 规则行）作指纹，侧车必须真的含有这些原文（`_sidecar_identity`
   命中数）才认，身份为 0 的直接淘汰；页码覆盖度降为次排序键。
2. **PDF 由侧车反查**（新增 `_pdf_for_sidecar`）：由归档件名 `pdf-<md5[:16]>.jsonl`
   反扫 `server/translated` 下 PDF 算内容散列对出原文（排除 -mono/-dual/-cut/-compare
   产物），对不上才回落 `_latest_pdf()`。彻底摆脱对服务端 history 的依赖。
3. **`refresh_body` 执行顺序修正**：必须先更新 `BODY_NAME` 再调 `body_sidecar()` /
   `body_manifest()`（后两者读全局 `BODY_NAME`）；侧车反查 PDF 的 md5 扫描只在
   **换篇时做一次**，否则面板每 5s 轮询会反复散列 translated 下所有 PDF。

**验证**：全新启动自动锚定 → 侧车 `pdf-01bb63619e2b315e.jsonl`（Li）✓、
PDF Li 2026 ✓、`analyse` 门禁 `结论: PASS` ✓。

### 十六、v28.55 出稿工序惰性求值修复（2026-09-21）

**根因**：`watch_clip.run_job` 里 `job["cmd"]()` 调用了 lambda，但 `job.get("after")`
直接 `enumerate` —— 拿到的是 **lambda 对象本身**，抛
`TypeError: 'function' object is not iterable`。表现在面板上：③ 检查通过（81/81）、
点 ④ 确认后连接被服务器单方面断开（`RemoteDisconnected`），出稿永远失败。

**修复**（`watch_clip.run_job` L576-577）：`after` 与 `cmd` 同等待遇 ——
先 `after() if callable(after) else after` 再遍历。

**验证**：Li 2026 全链路走通 —— 检查 81/81 全过 → 注入缓存 → force 渲染 30 秒
（5/7 页全缓存命中，**零 LLM 调用**）→ 出 mono.pdf(7163 KB) + dual.pdf；
`post_check` 判定 **PASS**（全篇汉化率 61.9%，阈值 18%；引用 109/109 完整；
无 {vN} 残留；文献区禁汉化通过；无渲染残渣）。

### 十七、v28.56 返工单活过沙箱检查（2026-09-21）

**根因**：`panel.sandbox_gates` 用 `P2Z_PROJ=tmp` 隔离正文门禁，`seg_import` 的
返工单因此落进 `tmp\inbox\<名>.rework.md`，而检查结束 `shutil.rmtree(tmp)` 立刻
把整个目录删掉 —— 面板提示的路径成了**死链**（实测：提示
`返工单: ...\Temp\p2z_panel_nd1vxe14\inbox\....rework.md`，用户按它去读时文件
已不存在）。这恰好卡住「报错后用户怎么修」这条路：单子正是该给豆包的东西。

**修法**（`panel._persist_rework`）：门禁退出码非 0 时，把沙箱里的
`*.rework.md` 搬到**真实 inbox**（`wc.INBOX`），并把日志里 `返工单: <tmp路径>`
那行改写成持久路径（附「已保留」字样），末尾追加一句修法指引。
沙箱原则不破：zh TSV / check_report 等门禁产物照旧只落 tmp 随目录删除，
只有这份**给用户/豆包的产物**必须活过这次检查。PASS 时真实单子由确认阶段的
非沙箱 `seg_import` 自己删（原有逻辑），不会留过期单子误导豆包。

**验证**：构造 #S45 字形破坏（`C∈{2,4}` 改写成中文说法）→ 检查报
`FAIL #S45（第4页） 高价值字形未回锚: ["{v42}='C∈{2,4}'"]`，日志指向
`D:\zotero-pdf2zh\inbox\<名>.rework.md`，文件确实存在（3191 字节，含「只改点到的段」
纪律 + 缺失字形 + 原文位置）；run_all 21 套件与 panel --selftest 全绿。

### 十八、v28.57 报错明细摊到面板 + 责任归属 + 提示词补实测反例（2026-09-21）

v28.56 让返工单活了下来，但用户还得自己去 inbox 里翻文件；而免费版豆包没有桥、
读不到文件，只能粘贴文本。本版把这条回路补完 —— **具体报错直接摊在面板上、一键可复制**，
并把实测反复踩的错误写进整段翻译提示词。

1. **面板新增「⑤ 报错明细」卡片**（只在门禁 FAIL 时出现）：显示返工单正文
   （哪几段、缺哪个字形、原文里它在哪），带「复制给豆包」按钮 —— 走
   `POST /api/copy` → 服务器端 Win32 写剪贴板（与「① 复制待译文本」同一条路），
   用户粘给豆包即可，不必去 inbox 找文件。检查通过/报错/重新装配时自动收起。
2. **责任归属判定**（`panel.blame_of`）：把失败分成「豆包的问题」与「工具/环境问题」，
   徽章分别用暖色/红色，并按归属给不同指引（归豆包 → 复制单子返工；归工具 →
   明说交给豆包没用、找开发者）。**判据顺序要紧**：先看有没有「带 FAIL 前缀且行内不含
   工具标记」的译者侧 FAIL 行，再看工具标记 —— 反过来先全局扫工具标记会误判
   （正常 PASS 路径日志里也有「读 manifest 完成」这类行，与 FAIL 行同时出现时会把
   豆包的错报成工具故障，实测踩过）。工具标记：未接线 / 找不到载荷原文 / 读不到 /
   Traceback / 退出码 / manifest / sidecar / 无法校验。
3. **整段翻译提示词补实测反例**（`seg_export.RULES` 第 7 条）：把真实踩过的两种写法
   写成反例 —— `C∈{2,4}` 被拆成两截并插中文、`(0.69/0.63` 丢块首左括号；附逐字符
   自查办法。**编号不动**（1-9 的编号被术语子项与返工单引用过）。
   注：反例里的花括号须写成 `{{2,4}}`，否则会被 `RULES.format()` 当占位符吃掉。

**验证**：`analyse` 对破坏回包返回 `blame='豆包'` + 1814 字符返工单；`blame_of` 六例
（译者侧 FAIL / 未接线 / Traceback / FAIL+manifest 混排 / 缩进 FAIL / 找不到载荷原文）
全对；`RULES.format()` 后 `C∈{2,4}` 存活且无 `{{` 泄漏；**浏览器实测**：
面板加载正常、无「连不上」假故障，破坏回包 → ⑤ 卡片自动展开、徽章「豆包的问题」、
明细定位到 `#S45` 与 `C∈{2,4}`、「复制给豆包」写入 1814 字符并记日志；
run_all 21 套件与 panel --selftest 全绿。

### 十九、v28.58 实测教训自动回流进提示词（2026-09-21）

v28.57 的实测反例是我**手工**从踩过的坑里挑出来写进 `RULES` 的 —— 下一篇若踩新坑，
还得靠人再挑一次，既慢又会漏。本版把这条回路改成自动的。

**新增 `tools/lessons.py` + `tools/lessons.tsv`（人可读可改的 TSV 台账）**

- **写**：`seg_import.write_rework_note` 判 FAIL 时，把这次踩的坑记进台账 ——
  字形未回锚记 `GLYPH`（证据 = **该原样出现的字形串**，即门禁的 ground truth）、
  ⋮ 错位记 `MOVE`、编号/空段类记 `OTHER`。写失败一律吞掉（台账是辅助，不能反过来卡门禁）。
- **读**：`seg_export` 导出载荷时把台账渲染成 `[实测反例]` 块接在规则 9 之后、
  `#S1` 之前；控制台打印「提示词已附带 N 条实测反例」——否则换台机器或改了台账，
  载荷差异无从解释。

**三条硬约束（不满足会反噬，都写进模块头）**

1. **只记译者侧的错**：台账由 `write_rework_note` 调用，而那是回锚失败/编号断点失败才
   走的路径 —— 工具/环境故障压根到不了这里，不会被误记成「豆包的教训」。
2. **只记正确形态、不记错法**：记的是原文里**该原样出现**的字形串，不是豆包写成的样子 ——
   把错法写进提示词等于教它照着错。
3. **限量**：提示词每加一行都在稀释前面的规则。文件上限 50 条（新条目顶掉最旧的）、
   注入上限 6 条 / 约 700 字符，超出直接截断。

**已知取舍**：检查阶段（沙箱）也会写台账 —— 沙箱"不落文件"的原则在此让位于"教训必须留得
下来"（用户手抖打错字也会记一条，但记的内容是该字形**应当原样出现**，仍是真命题；
靠 (类型,证据) 去重 + 50 条上限兜住噪声）。台账删行即不再注入，清空即回到纯静态规则。

**验证**：破坏回包（拆 `C∈{2,4}`、改写 `(0.69/0.63`）→ 台账自动落 2 条 GLYPH；
同载荷连跑 3 次台账仍 2 条（去重生效）；灌 12 条假教训 → 文件 14 行、注入仍 6 条 /
402 字符、最旧两条被挤出注入（限流生效）；真实导出 Li 载荷 → `[实测反例]` 块落在
第 2946 字符处、控制台报「已附带 2 条」，载荷其余部分与之前一致；
run_all 21 套件、panel --selftest、镜像 13 PASS 全绿。




### 二十、v28.59 ⑥ 语义审核交给硅基流动（门禁之外那一层，只审不改）（2026-09-21）

**缘起（用户拍板）**：门禁（`seg_import` / `check_job`）只判**机器判得了**的东西 ——
编号齐不齐、字形/占位符有没有落点、⋮ 对不对。语义错译（半句丢失、否定颠倒、数字
单位错位、术语漂移）它一概够不到；面板的「待看清单」也只给**读数**（长度比、末条
偏短、一致性），是提示不是判断。这类错目前只能靠用户通读整篇去发现。用户要求把
审核者交给**硅基流动**，落点选**面板新增一个环节 + 手动点按钮**（不点则零成本）。

**三条边界（都写进 `reviewer.py` 模块头，照 `relay_spec.md` §4 红线）**

1. **审核者必须与翻译方无关** —— 翻译者是豆包，审核走硅基流动（DeepSeek-V3.2，付费）；
   被翻译方不得自校，所以绝不把译文再喂回豆包问"你译得对吗"。
2. **只审不改** —— 审核者只报问题，**绝不产出改写稿**（生成式改写不可信，是润色钩子
   退役时立下的规矩）；改不改、怎么改由用户裁决，要返工再由用户把清单交给豆包执行。
3. **不参与放行** —— 门禁仍是硬门，存疑清单只给人裁决。`panel._review` 是**只读**动作：
   不落任何产物、不动 `STATE["checked"]`（审核结果不改门禁结论）。

**新增 `tools/table_pipe/reviewer.py`**

- 审 6 类：漏译 / 错译 / 数字 / 术语 / 指代 / 表达；逐段「原文↔译文」对照。
- **明确不报**编号/占位符/⋮/标点（那是机械门禁的判据）、不报"可以更通顺"这类改进意见、
  不许凑数 —— 报了就是噪音，会把自己报出的真问题淹掉。
- 密钥口径与 `strategist.py` 一致：`SILICON_API_KEY/SILICON_MODEL/SILICON_BASE_URL`
  优先，否则读 `server/config/config.json` 的 `translators[silicon].envs`；缺密钥时
  `audit` 返回 `{"ok": False, "err"}` 而不抛。
- **零依赖**：stdlib `urllib` 直连 OpenAI 兼容端点，不引 `openai` 包 —— 本件要被零依赖的
  `panel.py` 直接 `import`。`enable_thinking: False`（思考 token 既慢又会让 JSON 被
  `max_tokens` 截断）。
- **整篇切块并发**：按「原文+译文」字符预算 14000/块、3 并发；超预算的单段自成一块
  （宁超不切段，切了审核者就看不到完整上下文）；**单块失败只计数不拖垮整篇** ——
  审核结果是给用户看的参考，不该因一次网络抖动整篇作废。
- 回包**宽容解析**：围栏/前后废话都容忍；编号不在本块范围内、说明为空的条目直接丢弃
  （宁可少报，不可把幻觉的段号写进清单）；同段同类型同说明去重；说明/原文片段定长截断。
- 结论落 `server/translated/review/语义审核_<时间>_<任务>.md`（与体检/质检报告同目录），
  报告头写明范围/块数/耗时/模型与「只审不改」声明。

**面板接线（`tools/table_pipe/panel.py`）**

- 新按钮 **⑥ 语义审核**（手动触发，付费调用前会明说）+ 独立卡片显示存疑清单（▲高/●中）、
  模型名、耗时、未完成块数、报告路径。
- **一键复制给豆包**：拼的是「只核对我点到的段，其余段逐字符照抄上一版」的提示词 ——
  返工口径与采纳管线一致，不给出整篇重译的机会。
- **不跑门禁**（审核不该等沙箱）：只要求编号齐全，因为编号不齐连段都对不上、审出来的
  条目落不到任何一段上；原文取自载荷 `#S` 块（回包里只有译文，没有原文就无从比对）。
- 抬头 `[文档]` 行连同术语表一起注入提示词（给审核者篇名/学科先验）。
- 一改粘贴区即收起卡片（结论是针对**旧文本**的，留着会误导）。

**测试**：新增 `tools/tests/test_reviewer.py`（47 例，**全离线**，替身注入 `_chat`/
`_audit_chunk`/`split_chunks`，不打真接口不花钱），锁死密钥三级回落、术语表注释行不可见、
切块保序不丢编号、提示词排除机械门禁判据、回包宽容解析与幻觉丢弃、多块聚合与高在前
（稳定排序保块序）、单块失败不拖垮、报告落盘、面板接线（原文取自载荷/抬头/不跑门禁/
编号不齐即报错）；已挂进 `run_all.py`（21 → 22 套件）。

**验证**：`test_reviewer.py` 47 PASS；`run_all.py` **22 套件全绿**（镜像 13 PASS / 0 SKIP）；
`panel --selftest` 115/115 门禁全过；真实调用烟雾测试（硅基流动，3 段 18 秒）——
植入 `0.69 -> 0.63` 被精确抓成「数字·高」，另两段无问题**未误报**，JSON 解析与端点/密钥
口径实跑通过。

**附带修（与本版无关的既有红灯）**：`test_seg_export.py` ⑦「S1/S2 编号守恒」持续失败，
根因是 v28.58 的实测反例注入 —— `tools/lessons.tsv` 那条占位符不守恒的教训**原文里就带
`#S3`**，而该断言拿**整份载荷**（含抬头）判"没有 #S3"。改为只看**段表区**（首个 `#S1`
起），断言意图不变。

**已知边界（有意不覆盖，别再重新起头）**：全文级跨段指代、接缝断裂、文风突变是军师层
（`strategist.py`）的活，本件不重复；拉丁碎片保真已实测否掉（误报 105/115，字面上分不开
"该保留的学名"与"该翻译的英文词"）。




### 二十一、v28.60 面板兜底退出改成用户可控（头部倒计时 + 一键暂停）（2026-09-21）

**缘起（用户提出）**：面板靠"心跳静默 1800s = 窗口已关"自灭，可这只是个**估计**。估紧了
会把"切去豆包翻长文"误杀（早期 30s 版实测踩过，前端还把"连不上服务器"谎报成"剪贴板里
没有文本"，比不退出更难查）；估宽了则窗口真死了还霸着 60642 半小时，下一次启动只能回落
随机端口，而用户手里那扇旧窗连的却是死服务器 —— **同一小时里刚踩过一次**。阈值怎么定都
有代价，用户要求：**把时间显示在面板上，并允许自己暂停**。

**改动（`tools/table_pipe/panel.py`）**

- 后端：`STATE["idle_off"]` + `idle_state()`（返回 `{off, left, limit}`，`left=None` 表示
  "数不出来"：要么已暂停，要么还没收到过任何心跳）+ `set_idle_off()`；新路由 `POST /api/idle`。
- 倒计时数据源**只有服务器**：`GET /api/state` 与 `GET /api/ping` 都捎回 `idle`。心跳本来
  每 5s 就在打，不另开一条轮询；前端只在两次心跳之间做**本地递减**（否则数字每 5s 才跳一
  次，看着像卡住），剩余秒数以服务器为准（前后端各算一遍会漂）。
- 看门狗：`if p is not None and not idle_off and now - p > IDLE_LIMIT` —— **暂停只停这条
  闲置腿**；关窗的**告别腿照走**（15s 宽限不变），否则"暂停"就成了孤儿进程制造机。
- `set_idle_off(False)` **把心跳时钟归零**：暂停期间的心跳早已过期，不归零就是"用户点了
  继续、服务器当场自灭"。
- 暂停态**只在内存、不落盘**：崩一次后还留着"已暂停"的旧状态，正是上面那个端口被占的坑。
- 前端头部：`自动退出 29:58` 倒计时（等宽数字，免得每秒跳字整行抖）+「暂停自动退出」/
  「继续倒计时」按钮；暂停时转琥珀色显示「自动退出已暂停」，未收到心跳时显示
  「等待窗口心跳…」（不吓唬人）。暂停/恢复各有日志说明清楚后续怎么退出。
- **修掉一条过期注释**：模块头"退出"契约里还写着 `IDLE_LIMIT(600s)`（常量早已是 1800），
  顺手把 600s→1800s 与这次的可控化取舍一并写全；启动横幅也补上"面板上可暂停"。

**测试**：新增 `tools/tests/test_panel_idle.py`（19 例，已挂进 `run_all.py`，22→23 套件）。
看门狗那条腿没法靠"等 5 秒"验四条语义，测试把 `panel.time` 换成不睡的替身，让循环在 1 秒
真实窗口里空转出结论（**把"等得久"换成"判得准"**）；另起一个**真面板子进程**打 HTTP，
锁死 `/api/state`、`/api/ping` 捎回 `idle`、`POST /api/idle` 暂停/恢复、继续后倒计时满格、
页面含倒计时位与按钮、未知路径仍是 404（防止把 do_GET 的兜底分支改没了）。

**验证**：`test_panel_idle.py` 19 PASS；`run_all.py` **23 套件全绿**（镜像 13 PASS / 0 SKIP）。

**有意不做**：不提供"自定义时长"输入框 —— 用户要的是"自己控制暂停"，不是调参；真要调
再改 `IDLE_LIMIT` 一个常量即可，加输入框只会多一个需要落盘、需要校验、还会互相打架的状态。




### 二十二、v28.61 补一份面板操作手册（`relay_spec.md` §9）（2026-09-21）

**缘起（用户提出）**：面板攒到 ①–⑥ 六步、三种任务、两条退出腿、五个环境变量之后，**没有一处
写"怎么用"** —— `relay_spec.md` 讲的是定位/契约/权限边界（给设计看），`panel.py` 的模块头讲的是
设计取舍（给改代码的人看），都不回答用户的问题"我现在该点哪个"。v28.59/28.60 又添了 ⑥ 与
倒计时，更是只在代码里。

**改动（纯文档，不动代码行为）**

- `relay_spec.md` 新增 **§9「已实施件: panel.py(中继面板)用法」**：启动（含 `P2Z_TABLE_DIR` 与
  端口回落、`--selftest`、退出码 2 的含义、环境变量一览）；**① → ⑥ 逐步表**（每步做什么、产出
  什么、"一改粘贴区即作废"这类容易吃亏的规矩）；头部控件与任务行的位置；退出两条腿 + 用户开关；
  **出问题时先查什么**（旧页面连死实例 / 端口被占回落随机地址 / 剪贴板被占 / ⑥ 的密钥口径 /
  ③ 过了 ④ 却拒收 = 指纹对不上）；**面板不做什么**（不翻译、不放宽门禁、不改判据、⑥ 不产改写稿）。
- `panel.py` 模块头加一行指路：逐步手册在 `relay_spec.md` §9，本文件头只讲设计取舍与契约。

**验证**：照文档逐条核对代码 —— ① 的返回（单元数/字符数）、③ 的沙箱只读、④ 的 sha 指纹与 409、
⑤ 的责任归属两分支与文案、⑥ 的"只审不改"、`check_workdir` 失败时的提示与退出码 2、端口回落
与启动横幅两行文案 —— 均与文档一致；`panel --selftest` 复跑 PASS。




### 二十三、v28.62 剪贴板这条路不绑豆包：翻译方可换 + 同源拒审 + 换家台账（2026-09-21）

**缘起（用户提出）**："其实我们的网页Ai剪切板翻译，可以交给不同种类的网页Ai"。

机制上**本来就行** —— `watch_clip.py` 开头那句"手工复制这条路换任何 AI、任何版本都不会坏"
说的就是这件事，真契约只有两条（**编号守恒**：发出去的 `#S45`/`k001` 一条不少回来；**占位符
原位**：`{v29}` 不丢不挪），载荷自带抬头（文档名/术语表/逐段规则）所以换家不用改提示词。但
"能换"从没落到配套上：面板从头到尾写死"豆包"，别家的代码围栏与首尾客套会被当成**译文**收
进来，换家之后"翻译方与审核者同族"这条红线（`relay_spec` §4.5 被翻译方不得自校）没人查，
出了事也看不出是哪家翻的。**这一版把这三件事补齐。**

**改动（6 文件）**

- **`tools/table_pipe/watch_clip.py`（接得住别家的"包装"）**：`parse_units()` 三处收紧 ——
  ① 整行是代码围栏（``` / ```markdown）则跳过**围栏行本身**，围栏中间的内容照收；
  ② 编号行容忍行首包装（`_LEADER`：`-`、`*`、`•`、`>`、`1.`、`(1)`、`1、`）与粗体/引用符，
  照常认出来；③ 新增 `_trim_tail()` —— 只清**尾部**，且客套行必须**同时**满足"与译文隔空行
  **且** ≤40 字 **且** 起首即客套词"才丢（`_SMALLTALK` 词表刻意写窄：光有"以上是…"不算，
  必须接"译文/翻译/全文"这类**元词**，防把正文里的"以上是结果分析"误吃）。**三处都只动包装、
  不碰译文一个字。**
- **`tools/table_pipe/reviewer.py`（红线判据的唯一来源）**：新增 `_FAMILY_KEYS`/`family_of()`/
  `conflicts()` —— 按模型名或厂商名认族（`deepseek`/`深度求索`、`doubao`/`豆包`/`seed-`、
  `gpt-`/`chatgpt`、`claude`、`moonshot`/`kimi`、`qwen`/`通义`；**中文名也认**，认不出返回空）。
  审核报告抬头增一行"翻译方: …"，让事后能看出翻译方与审核者是不是一家。
- **`tools/table_pipe/panel.py`**：头部加**「翻译方」下拉**（7 项，选择落 `.panel_vendor`，
  重启仍在）；③④ 各往 `vendor_ledger.tsv` 追加一行（`时间/阶段/任务/翻译方/单元/待看/结论/
  备注`，每次③检查、④出稿各一行，回包连编号都不齐时也记一行）；`blame_of()` 由写死"豆包"
  改为"翻译方"（具体是谁由 ⑤ 按用户选中的翻译方补上）；**⑥ 同源拒审**——服务器端在 `review()`
  入口拦（**拦在联网之前，不花这笔钱**）；新路由 `POST /api/vendor`；`GET /api/state` 捎回
  `vendors/vendor/rv_conflicts/rv_model`；文案中性化（按钮"复制给豆包"→"复制给翻译方"、② 标题
  「把**翻译方**的回复粘到这里」、粘贴区 placeholder 补"代码围栏与首尾客套会自动剥掉"）。
- **`tools/table_pipe/relay_spec.md`**：新增 **§10「换翻译方: 这条管线不绑豆包」** —— 真契约
  两条表、10.1 只改一处（下拉）、10.2 包装清洗表 + **刻意不做**的两件、10.3 同源红线与
  "要换审核者就改 `SILICON_MODEL`"、10.4 台账与"比一家的正确姿势"（同论文同载荷比③结论+待看
  条数，别拿不同论文比）、10.5 现实约束（**网页输入框长度上限是唯一硬约束**）。
- **`tools/tests/test_webai.py`（新建，51 例）**：锁死上面三件配套，**并把"不许动"的反例写进去** ——
  裸文本一个字不改、正文里"以上是结果分析"不误吃、超长客套行不洗、译文自身的项目符号保留、
  认不出的翻译方不拦、`rv_conflict` 与拒审文案、台账 8 列与制表符替换、报告抬头溯源、前端
  `id="vendorSel"`/`/api/vendor` 接线（并断言"前端不自己认族"）。
- **`tools/tests/run_all.py`**：挂进第 24 套件（连同把 v28.59/28.60 两个套件补进 docstring 编号）。

**验证**：`test_webai.py` **51 PASS / 0 FAIL**；`run_all.py` **24 套件全绿**（镜像 13 PASS /
0 SKIP）；`panel --selftest` PASS（表格正文 115/115 门禁全过）。面板在 60642 用新代码重启后
冒烟四态：`/api/state` 回 `rv_conflicts: [DeepSeek 网页版, 硅基流动 API(DeepSeek)]`；切
"DeepSeek 网页版"→`/api/review` 返回"红线…同源"；换回豆包→转为"编号不齐"（正常接到门禁）；
乱填→"不是已知的翻译方。"；`.panel_vendor` 落盘正确。

**有意不做**：不剥**内容行**里的项目符号（译文本身可能就是列表，剥了就是在改译文）；不管
**首部**客套（编号行之前没有"当前段"，那些行天然落不到任何一段上）；不重排、不补编号、不纠错
译文 —— 清洗只碰包装。台账是**旁证不是门禁**，写不进去只吞掉，绝不影响主流程。

**已知残留**：客套**紧贴**末段（中间没有空行）时不会洗，会留在末段译文里 —— 那属于"人该看
一眼"的范畴（⑤/全表看得到），好过把末段真句子误删。

**已知边界**：同源判据刻意粗糙（按名字认族，**宁可多拦一次**），认不出的组合不拦，只在报告
抬头写明双方；真要用 DeepSeek 系当翻译方，就得把审核者换成别家（改 `reviewer.py` 的
`SILICON_MODEL`/`SILICON_BASE_URL` 或同名环境变量）。




### 二十四、v28.63 排版伤治伤 + 超链接四件套第一次上成品：把"看着不对"变成一次可复跑的体检（2026-09-21）

**缘起（用户提出）**："渲染排版还是有问题，然后关于原来的超链接该怎么处理"。GeoTLM 成品
（`server/translated/Li 等 - 2026 - GeoTLM …-mono.pdf`）有两类问题：**版面有伤**（文字没错，
排版坏了）与**超链接没了**（译文页点了不动）。这一版把两件都做实，并把判据变成可复用门禁 ——
下一篇不用再靠"人眼发现"。

**一、`tools/heal_render.py`（新建）：三类伤，只有两类真治**

| 类 | 症状（实测 GeoTLM） | 治法 |
|---|---|---|
| R 旋转文本退化 | p1 arXiv 水印、p3 `CLS Token`/`Token interface`、p5 `avocado/hammer/wrench/cylinder` —— 原版旋转 90° 的图内标注与页边水印，译文里丢了旋转被拆成**一列单字** | 该区 redact 后 `show_pdf_page` 贴回原版 |
| B 文献区 `[n]` 错位 | p7 `[23]`~`[20]` 落到 `[1]` 之上、`[7]` 骑在条目文字中间（条目本身是英文，禁汉化） | 整区 redact + 按**原版坐标**贴回 |
| 页眉 | **无病**，实测译文页眉与原版逐像素差 0.00 | 不治（曾经凭"看起来像"立过判据，撤） |

- **判据必须看布局，不能看文字**：退化竖列 `a\nrX\niv\n:2\n6…` 去掉空白后与原版
  `arXiv:2606.15909v1 [cs.RO] …` **逐个字符相同** —— 用"文字相等"判会**全部放过**（实测
  放过了 p1 整条水印与 p5 三条图注）。故 `keeps_rotation()` 看的是"该区域还有没有 `dir`
  非水平的行"；文献区同理看 `count_labels()`（`[n]` 开头的行数），不看文本。
- **擦哪儿盖哪儿（不变量）**：R 段 redact 要外扩 1.5pt（退化单字会探出原版行框），
  **补盖必须同样外扩**。曾经"擦 `rp`、盖 `R`"，被擦白的那一圈没人补 —— 实测 p5 右侧
  1pt 带与原版差 **3.23**，同尺寸补盖后 **0.48**。外扩会吃到译文正文（环内有汉字）或
  越出页面时**退回原框不外扩并记账**。
- **判断不了的一律不猜**：该处译文已成句中文（>2 汉字）则**跳过并记账**（`跳过 N 处`），
  绝不把译文正文盖回英文。
- **链接保护（必须）**：`apply_redactions()` 会清掉**与 redact 矩形相交的链接**
  （实测 p7 一次吞 60 条），且调用后 `page.get_links()` 会抛 PyMuPDF 内部错（链接表指向
  已消失的 xref）→ 只能"快照 + 相交即删预测 + 盖完回插"，落盘后**重开文件核对**。
  因此本工具**必须跑在 `relink_pages` 之前**：先搬热区再盖区域，热区坐标就失真了。

**二、超链接四件套（工具早已有，这是第一次在成品上跑通并出体检）**

顺序不可乱：`heal_render` → `relink_pages` → `resolve_links` → `verify_links` → `style_links`
（heal 会 redact，若先挂链接会被吞）。mono 读数：

| 步 | 做了什么 | 读数 |
|---|---|---|
| `relink_pages` | 锚文本热区搬到译文新位置 | 跳过 44 / **重定位 80** / 未命中 3（`'I)'`、`'[15]'`、`'[39]h'`，留清单） |
| `resolve_links` | NAMED → 显式 GOTO（Edge 等简易阅读器不认命名目标树） | **NAMED→GOTO 145** / 行序映射 6 / 回退坐标 0 |
| `verify_links` | 端到端落点语义校验 | 见下 |
| `style_links` | 锚文本原位重绘蓝色（可见性恢复） | 118 段 / 191 字重绘，跳过 32（回填页 `[3,6]`） |

**结果**：mono 链接总数 **146**，逐页 `[10, 32, 5, 7, 13, 15, 64]` —— 与**治伤前逐页完全一致**
（`[: 治伤不得损坏链接]` 这条不变量实测成立）。

**三、`tools/verify_links.py`（改）：补上"空洞绿"这个洞**

原判据是"锚文本里的作者+年份出现在落点页"。GeoTLM 是交叉引用篇，锚是 `Fig. 3`/`Sec. 4.4`/`[20]`
这类，**一条都验不了**，旧版却打印"失配 0" —— 这是**假的绿**。三处收紧：

1. 新增 `--original` 走**落点页一致性**判据（与锚文本内容无关）：原版页的 NAMED → 解析目标页
   → 对成品 `ratio*i` 页的 GOTO **按矩形就近配对**（按序配会因 relink 挪了 1pt 假报 27 处失配）。
2. 新增 `cite_number_anchor()`：引文号锚（`[20]` 被 `style_links` 叠绘成 `"2020"`）按**矩形宽度
   放不下 4 个数字**排除，单列 `疑似引文号锚跳过 N`。
3. **一条都没验时打 NOTICE 说破**："本判据一条都没验…请用 `--original` 走落点页一致性"。

实测 mono：`语义可校验 0 | 非年份锚跳过 144 | 疑似引文号锚跳过 1` → NOTICE 触发 →
`落点页一致性: 可校验 145 | 命中 145 | 失配 0 | 条数不等跳过 0 页`。**这次是真绿。**

**四、门禁：体检与治伤同源（`--dry-run` 兼任体检）**

`heal_render.py --dry-run` 升级为**带退出码的排版伤体检**：有伤未治 → `体检 FAIL` / 退出码 1；
无伤 → `体检 PASS` / 退出码 0（治后再跑必为 PASS，即**幂等**）。当场读数：

```
治伤前备份 : R 4 处 / B 1 页        -> 体检 FAIL, rc=1
治伤后成品 : R 0 处 / B 0 页        -> 体检 PASS, rc=0   (mono 与 dual 同)
```

- **为什么不把判据塞进 `post_check`**：`post_check` 走 pypdf **文本层**，而这两类伤**恰恰是几何伤**
  —— 文本层看着一切正常（退化竖列的文字甚至与原版逐字相同）。两处各立一套判据必然口径漂移，
  故让**治伤工具自己的判据当门禁**。
- 跳过处（该处译文是中文，需人眼判）**不算 FAIL**，与 `post_check` 的"提示级"同口径 ——
  退出码只留 0/1 两态，免得 `!= 0` 的调用方把提示当事故。

**五、dual 现状（用户拍板：本轮不动，只写进文档）**

- dual 每对页 = `[原版页, 译文页]`。实测 `dual links [10,1,32,0,5,0,7,0,13,0,15,0,64,0] = 147`
  —— **链接全在原版页上，译文页一条都没有**。这是双语版的既有结构，不是本次治伤造成的。
- `relink_pages` 对 dual 会 `FAIL 页数不一致（14 vs 7）` —— 四件套在 dual 上**不是"跑一下"，
  而是要新增能力**（译文页热区来源、跨半页目标映射）。
- 本轮 dual **只治排版伤**（p2/p6/p10 R、p14 B；p13 是原版页，`[n]行 36/36` 天然正常，跳过），
  链接结构保持原样。要"双语版译文页也可点"需另立条目。
- **交付口径仍是 mono**（dual 作对照版）。

**六、测试与回归**

- **`tools/tests/test_heal_render.py`（新建，14 例）**：合成 PDF，锁 4 段语义 + 2 条工程不变量 ——
  ① 干跑体检 FAIL/rc 1；② 治伤 rc 0 且**链接计数与命名目标不变**；③ 幂等（治后再干跑 PASS/rc 0）；
  ④ 像素：治后该列与原版差 **0.00**，并**断言治伤前 > 5.0**（证明那条 0.00 不是空过）；
  ⑤ 文献区 `[n]行 N/N` 对齐；⑥ dual 页映射不越界 + 译文页出现 `avocado` + 再干跑 rc 0。
  **陷阱也写进用例**：合成"退化侧"刻意做成"去空白后与原版相同"，即锁死"不许用文字相等判"。
- `tools/tests/run_all.py` 挂第 25 套件；**25 套件全绿**（镜像 13 PASS / 0 SKIP）。
- `panel.py --selftest`（正文回包 81 段）**PASS**：81/81 门禁全过、0 条待核。

**有意不做**：不动页眉（无病）；不碰判断不了的区域；不改 dual 链接；不重排、不纠错译文 —— 本工具
只做"把原版本就正确的形态盖回去"，一个字的译文都不改。

**已知边界**：① R 段外扩 1.5pt 是实测值，外扩会被正文/页边顶回原框（记账 `不外扩 N 处`）；
② `show_pdf_page` 会把原版图片资源带进译文页（实测 p5 图片对象 6→18），逐页核对**差异严格落在
overlay 框内**（`changed_box` 落在 R 框里），**无可见副作用**，判定可接受；③ 落点页一致性判据要求
成品与原版页数整除，不整除则该判据不适用（照旧报 NOTICE）。




### 二十五、v28.64 dual 双侧链接：双语版两侧都能点，且不跨侧（2026-09-21）

**缘起（用户提出）**："请帮我处理 dual 模式下译文页的链接跳转问题"。v28.63 只把 dual 的**排版伤**
治了，链接结构原样留着 —— 实测 dual 的链接**全在原版页上**（145 条 NAMED + 1 条 URI），
**译文页除 1 条 URI 外一条都没有**：中文页点引用完全没反应。而四件套（relink_pages /
resolve_links / style_links / verify_links）都假设"成品与原版 1:1"，`relink_pages` 对 dual 直接
`FAIL 页数不一致（14 vs 7）` —— dual 不是"跑一下"，是要**新增能力**。

**口径（用户拍板）**：译文页点引用 **跳译文侧**（阅读连续，`Fig. 3`/`Sec. 4.4` 落到中文图表标题）；
原版页跳原版侧（顺其原语义）；两侧各自成对，**不跨侧**；译文页锚文本**变蓝**（可点击的视觉提示）。

**一、`tools/dual_links.py`（新建）：两条腿**

| 侧 | 症状 | 治法 |
|---|---|---|
| 译文侧（0 基奇数页） | **零 GOTO** | **不重新定位，直接搬 mono**；只改落点页 `d -> 2d+1` |
| 原版侧（0 基偶数页） | 145 条 NAMED，Edge 等简易阅读器点不动 | NAMED -> 显式 GOTO，目标取 **dual 自己的**命名树（已是 dual 坐标，一律指向偶数页），y 从 PDF 底部原点转 fitz 顶部原点 |

- **为什么搬而不是重新搜锚文本**：实测 dual 译文页与 mono 对应页**是同一份排版** —— `rect` 逐页
  相同（612×792），文本差异 **100% 来自 mono 的 `style_links` 叠绘**（`[1]` vs `[1][1]`，
  `SequenceMatcher` 只有一个 non-equal 段且全是这种重复）。故 mono 已 relink 好的热区坐标在 dual
  上**原样成立**；而在译文页重搜 `[1]` 会命中一片（dual 的锚文本没被叠绘）。
- **同源门禁（不满足则 FAIL，不猜）**：逐页比对 translate 侧字符计数，dual 译文页的每个字符在
  mono 对应页里必须**至少同样多**（mono 因叠绘只会更多）。不满足说明 mono 不是这一版 dual 的 mono
  —— 搬过去就是错的，**门禁拦下时文件一个字节都不动**。
- **译文侧是"清空重建"**：先快照原有的 URI/LAUNCH，再清空按 mono 重建，最后把 mono 没搬来的
  URI/LAUNCH **补回**（否则重建会把译文页原有 URI 抹掉）。
- 落盘后**重开文件核对**逐页条数 + 跨侧落点计数，退出码 `1 if (插入失败 or 命名缺失 or 跨侧) else 0`。

**二、`tools/style_links.py`（改）：加 `--dual`**

原版页**本来就是蓝字**，对它再叠绘一次既无收益、又白白污染文本层（原版页的文本层原本是干净的）
—— `--dual` 时只处理译文侧（0 基奇数页）。跑在 `dual_links.py` 之后。

**并修掉一处坐标系不一致**：`--sidecar` 的页码是 **mono 坐标**（1 基），而 dual 里同一页的译文侧
是 `2(p-1)+2`。不换算就会去跳 mono 那一页在 dual 的编号 —— 回填页（自带蓝字）照样被叠绘一遍。
实测修前 `跳过回填页 [3, 6]`、修后 `[6, 12]`（正落在 dual 的译文侧）。

**三、`tools/verify_links.py`（改）：补第三条判据「落点不跨侧」**

前两条判据都**抓不住串侧**：译文页点引用跳到英文页时，**页码完全合法**（就是 2 的整数倍关系），
与锚文本内容也无关。新增 `side_consistency()`：

1. 第 p 页的 GOTO 落点页必须与 p **同侧**（`p % ratio == 落点页 % ratio`）；
2. 同一对页的**两侧条数对账**（原版侧 = NAMED + GOTO，译文侧 = GOTO），不等即记账 ——
   这同时就是"dual_links 跑没跑"的体检；
3. 原版侧还残留 NAMED 时**点名**："…-> 先跑 dual_links.py"。
   仅在成品页数是原版整数倍（dual ratio=2）时启用；`ratio=1` 时"同侧"是废话。

**四、实测（GeoTLM dual，14 页）**

```
dual_links : 译文侧补链接 146 条(落点 -> 奇数页) | 原版侧 NAMED->GOTO 145 条
             | 保住译文页原有 URI 1 条 | 跨侧落点 0
链接核对   : 292 条 [10,10,32,32,5,5,7,7,13,13,15,15,64,64]   ← 逐对页两侧完全对齐
style_links --dual : 134 段/239 字重绘, 跳过回填页 [6, 12](换算后), 原版侧未污染
verify_links --original : 落点页一致性 145/145 命中 0 失配
                          落点侧不串 跨侧 0 | 残留 NAMED 0 | 两侧条数不等 0 页
```

逐通道核对蓝色像素：译文侧链接矩形由 `0.000` -> `0.212/0.230`（变蓝生效）；
原版侧保持 `0.000`（未被叠绘污染）。备份 `…-dual_bak_链接前.pdf`。

**五、测试与回归**

- **`tools/tests/test_dual_links.py`（新建，18 例）**：合成 orig(3 页，带命名目标树)/mono(3 页)/
  dual(6 页) —— 锁落点映射 `2d+1`（逐条比对 rect 与 mono 相同）、原版侧 NAMED 全转且 **y 未镜像**、
  译文页 URI 活下来、跨侧 0、**同源与页数两道门禁**（rc 1 且文件 md5 不变）、幂等、
  以及 verify_links 第三判据的**正反两面**（好 dual 全绿 / 人为造一条串侧 -> 报 `跨侧 1`）。
  - **合成命名树有个非显然的坑**：目标数组里的页引用必须是**间接引用**（`%d 0 R`）—— 写裸数字
    时 reader 解析出的页号是错的（实测 2 页文档解出 `page=6`），**且不报任何错**。
- `tools/tests/run_all.py` 挂第 26 套件；**26 套件全绿**（镜像 13 PASS / 0 SKIP）。
- `panel.py --selftest`（表格正文 115 段）**PASS**：115/115 门禁全过、0 条待核。

**有意不做**：不改 mono 的链接（mono 走的是另一条已验收的路）；不重排、不纠错译文；不给 dual 重跑
`relink_pages`（dual 的热区来源就是 mono，重搜只会更差）。

**已知残留（待处置，非本次引入）**：

1. **24 处热区只剩方括号** —— mono 与 dual 译文侧各 24 处链接矩形的宽度是 **3.3pt**、框内文本只有
   `[` 或 `]`（点括号能跳、点数字点不到）。原版**同一批 146 条里 0 处**（原版最小宽度 5.3pt），
   即这 24 处是 `relink_pages` **重定位时收窄**产生的（原版 7.0pt 的 24 条 -> 成品 7.0pt 只剩 4 条）。
   dual 译文侧与 mono **逐条同病**（因为就是搬过来的）。修它要动 `relink_pages` 的重定位逻辑。
2. **dual 回填页多叠绘了一遍蓝字** —— 本次 style_links 跑 dual 时 `--dual` 与 `--sidecar` 的坐标系
   还没对齐，p6/p12（1 基）两个回填页被叠绘。**外观无差异**（它们本来就是蓝字），代价只是那两页
   锚文本在文本层重复一次。代码已修（见上），**但已落盘的 dual 里那一遍擦不掉**（叠绘是加法），
   要干净得从 `_bak_链接前.pdf` 重出。
   → **已在 v28.65 从 `_bak_治伤后-dual.pdf` 重出结清**（见下一节）。


### 二十六、v28.65 热区"只剩方括号"治本：碎片命中先并框 + 挂账逐条结账（2026-09-21）

**缘起（用户原话）**："全部进行" → 澄清为"加上表里能动的代码项"。逐条核对后发现上一轮列的挂账
**多为陈账**，真正的活只剩一处 —— 故本轮 = 该处的根因修复 + 一份**结账清单**（哪条已结、哪条
正式撤账、哪条仍阻塞）。

#### 一、结账清单

| # | 条目 | 去向 | 依据 |
|---|---|---|---|
| t1 | `server/utils/execute2.py` / `execute3.py` 重复实现清理 | **已结**（早已删除） | 本节 L3858 / L3915「✅ 本次删除」：547 + 215 行，全仓零引用 |
| t2 | `logs/launch.ps1` 启动前清场（治 SO_REUSEADDR 静默双绑） | **已结**（v28.36 落地） | 本次只读复核：先杀"根"（解释器/uv + 8890 监听者）→ 按 ParentProcessId 递归收子孙 → 等 8890 释放 → 再清日志 |
| t3 | 24 处窄热区（热区只剩方括号，点数字点不到） | **本轮修复**（见下） | 读数 94 → 70，差额**恰好 24** |
| t4 | 返工单加「只列计数不列明细」开关 | **正式撤账** | 已被 v28.36 的**无条件瘦身**取代（只列前 `HINT_MAX` 条 + 报计数），且 `test_seg_rework.py` ⑧ 锁住现文案；再加一个"默认不改行为"的开关＝死开关 |
| t5 | 初译路径占位符多重集校验 | **正式撤账**（已拍板"先不做，只记账"） | 且 prompt 属**缓存键**的一部分，动它＝全体缓存失效，风险与收益需单独评估 |
| t6 | `shift_bands` 弱签名漏报治本 | **正式撤账**（定标后判**不改**） | v28.36：加了逐字保留记号后判别力大涨（弱签名 61.9% → 8.6%），但**跨语言稳定性只有 ~30%** |
| t7 | 回归（27 套件 + 面板自检） | **本轮全绿**（见第五节） | |
| t8 | 结账清单 | **本表** | |

> t4 / t5 / t6 都是"已拍板不做"的历史项，被上一轮**误当活账**重列了一遍。撤账理由写在这张表里，
> 免得下一个人再捡一遍。

#### 二、根因：`search_for` 在碎片排版页上**逐字返回**

成品页每个字形单独定位，故 `search_for('[5]')` 返回的**不是一个整框**，而是 `[` / `5` / `]`
三个矩形。实测 p1：

```
[   x=[411.60, 414.92]  w=3.32
5   x=[414.92, 419.90]  w=4.98     数字基准线比括号低 ~2.93pt
]   x=[419.90, 423.22]  w=3.32
```

首尾**严丝合缝**（间隙 0.00）。而原实现 `min(hits, key=dist2)` 挑的是"离原框中心最近"的那**一个**
矩形 —— 于是热区只剩 3.32pt（一个方括号宽）：**点括号能跳、点数字点不到**。

#### 三、修法（`tools/relink_pages.py`）

- 新增 `merge_fragments(hits, gap=0.6, ytol=1.0)`：**先并碎片、再挑**。判据 = 同一行（y 区间重叠）
  且水平相邻（间隙 ≤ 0.6pt）。空格宽约 2.4pt，故 0.6 只并"被拆开的同一个锚"（实测间隙恒为 0），
  不会把隔一个空格的下一处引用吞进来；迭代到不动点（并一轮后可能出现新的近邻）。
- 新增 `pick_hit(hits, center, occupied)`：`并框 → 排除已被同页前面链接占用的出现 → 取离原框最近者`；
  返回 `None` = 未命中（进报告、保持原矩形，**不死链，只是热区偏**）。
- **踩到的坑**：并框循环里写 `o |= b` 是**错的** —— `Rect.__or__` 返回新对象，`o |= b` 只重绑循环
  变量，并出来的框被丢弃（表现为"并了等于没并"）。必须 `out[i] = o | b`。

#### 四、真件重出（GeoTLM）

mono 从 `_bak_治伤后-mono.pdf` 重跑（事前已验证它是合法的 relink 输入：NAMED 矩形**多重集
145/145 与原版一致**，只是 heal_render 回插改了顺序），dual 从 `_bak_治伤后-dual.pdf` 重跑：

```
relink_pages     : 跳过(回填页/短锚) 44 | 重定位 80 | 未命中 3   （与修复前逐项一致）
resolve_links    : NAMED -> GOTO 145 | 目标页内重定位 139 | 行序映射 6 | 回退坐标 0 | 目标树缺失 0
style_links      : 142 段/228 字重绘（跳过回填页 [3, 6]）
dual_links       : 译文侧补 146 条 | 原版侧 NAMED->GOTO 145 条 | 保住译文页原有 URI 0 | 跨侧落点 0
style_links --dual: 142 段/228 字（跳过回填页 [6, 12]，已跳过原版侧）
```

| 读数（GOTO 宽度 < 6pt） | 改前 | 改后 |
|---|---|---|
| mono | 94 | **70** |
| dual 译文侧 | 94 | **70** |
| dual 原版侧 | 70 | 70 |

**70 就是原版固有的那一批**（最窄样本 `[5.31, 5.31, 5.48, 5.48, 5.48, 5.48, 5.98, 5.98]`，与
dual 原版侧**逐值相同**）；改前那 94 条里多出来的 24 条，最窄样本全是 `3.32`（一个方括号宽）。
**94 − 70 = 24**，与 v28.64 的目测"24 处只剩方括号"逐数吻合，且这批窄热区**确认是本步引入的**
（原版同一批 0 条 3.32pt）。

**验证**：`verify_links` 全绿 —— mono：`落点页一致性 145/145 命中 / 失配 0`；dual 另加
`落点侧不串: 跨侧 0 | 残留 NAMED 0 | 两侧条数不等 0 页`。蓝像素（110 dpi）：新 dual 原版侧
`0.0000`（未被叠绘污染）/ 译文侧 `0.0005`（变蓝生效）—— 同时把上一节的"已知残留 #2"重出结清。

备份：`_bak_窄热区前-mono.pdf` / `_bak_窄热区前-dual.pdf`（用户过目后删）。

> **口径对账**：修复前有一版草稿说"relink 后 < 6pt 的 49 条"，那是**合成页面**上的拟真读数；
> 真件重出后实测是 **24 条**。以真件为准 —— 合成件与真件的字形碎片化程度不同。

#### 五、测试与回归

- **`tools/tests/test_relink_pages.py`（新建，17 例）**：锁"碎片命中必须先并成『一次出现』的整框"。
  合成碎片排版的手法是**实测出来的**：逐字 `insert_text`，且把锚**内部**字符（非括号）的基线
  压低 3pt —— 单纯逐字插入、或走 `TextWriter` 都**不会**触发（退化成整框命中），测试就锁不住这条。
  另含端到端：碎片锚的热区必须**覆盖整锚**、同页两处取近者、短锚跳过、未命中进报告。
- `tools/tests/run_all.py` 挂第 27 套件；**27 套件全绿**（镜像同步 PASS / 0 SKIP）。
- `panel.py --selftest`（表格正文 115 段）**PASS**：115/115 门禁全过、0 条待核。

#### 六、有意不做

- 不给 dual 重跑 `relink_pages`（dual 的热区来源就是 mono，重搜只会更差）；
- 不追 dual 译文侧文本层那约 345 字的叠绘残差（外观无差异，见 v28.64 已知残留 #2）；
- t4 / t5 / t6 三条**撤账**，不再开工（理由见第一节表）。
```

---

## 2026-09-22 v28.67 遗留问题结清：并发提交污染 / import 即改真库 / 页码口径 / 回归不改工作区 / 润色路径

> 全部来自 v28.66 的 D 项审查风险榜（`D:\zotero-pdf2zh-eval\project_map\00_总览.md` 的 P1–P11）。
> 本版逐条**结清** P1 / P3 / P5 / P11 / P4 —— 每条先复现旧行为、再改、再上锁（新增 3 个套件）。

### 一、P1 提字档不再写进程全局：改成"每任务子进程环境"（并发提交污染）

**旧行为（可复现）**：`PAUSE_TRANSLATE`（第一趟提字档）与 `P2Z_DOC_PDF`（按文档归档用）
是**在父进程 `os.environ` 上设**的，隐含假设"同时只翻一篇"。而服务端是
`Flask(threaded=True)` —— 并发提交两篇**文件名不同**的 PDF 时，后一篇会读到前一篇留下的
开关：一篇进提字趟、另一篇跟着被暂停，**静默出一份全英文产物**。

**两道"看着能兜住"的闸其实都不兜**（v28.66 已给代码证据）：
`_SUBMIT_LOCK` 只护**同名**文件的登记（`_INFLIGHT_FILES`，文件名不同即放行）；
`_sole_task` 只服务失败日志的根因归因（`_failure_brief`），不阻止任务并发。

**改法（基础设施 + 去掉壳子）**：
- `server/utils/execute.py`：`execute_with_progress(..., extra_env=None)`；注入点**压在
  `managed_python_env(final_env)` 之后**（后者是从 `os.environ` 拷的，先注入会被盖掉）。
- `server/server.py`：`translate_pdf(..., extra_env=None)`；`child_env = {"P2Z_DOC_PDF": 本篇绝对路径}`
  ＋调用方传入的专属开关，**两次** `execute_with_progress`（含 `--skip-subset-fonts` 重试分支）都带它。
- 第一趟提字改传 `extra_env={"PAUSE_TRANSLATE": "1"}`，父进程 `os.environ` 全程不动。
- **删除 `_two_pass_adopt` 壳子**：它存在的唯一意义是"进门记下运营开关、出门原样还回"，
  开关不再落地全局后，这层进出还原就是死代码。
- 子进程读得到（已亲验）：`patches/pdf2zh_translator.py::_pause_translate` 的读序是 **env 优先**
  （`os.environ.get("PAUSE_TRANSLATE") or (self.envs or {}).get(...)`）。

### 二、P3 去叠清扫 `dedouble_sweep.py`：import 无副作用 + 路径全参数化

**旧行为**：脚本是"顶层执行"式（`import` 即连真库、即改真库），路径/指纹焊死在文件里
（单篇论文的作用域），换一篇就是静默改错库。
**改法**：收进 `main(argv=None)` + `if __name__ == "__main__"` 守卫；`--sidecar / --db /
--fingerprint / --manifests` 全部改成必填参数，`--dry-run` 只统计不落库；**实跑写库前先落
`cache.v1.db.bak-<时间戳>`**。去叠规则 R1/R2/R3 与 `renumber_map` / `trailing_run` / `sweep`
一字未改，只换外壳。

### 三、P5 `relink_pages` 页码口径统一到**真实页序**

**旧行为**：`zero_pages()` 拿侧车的 `page`（`receive_layout` 的**回调计数**，图形对象也各占一号）
去和 `pno + 1`（真实页序）比 —— 图多的论文整体漂移，于是**跳过错的页**：该重定位的页被当
"回填页"跳过（热区留在原坐标），真正的回填页反而被重定位（白搜一遍）。与 Melhani 的
"真实 43/44 ↔ 回调 55/56"同源。
**改法**：新增 `true_page()`（`pageid + 1`，与 `seg_export.py` / `backfill_pages.py` 同一口径）；
`zero_pages()` 改为**按真实页归并**（一个真实页可能有多条记录：页面回调 + 若干图形回调，
只要有一条含可译文字，这页就不算零可译）；老侧车无 `pageid` → 回落 `page`（不误伤归档件）。

### 四、P11 回归不再污染工作区：教训台账路径可覆盖

**旧行为**：`test_seg_rework` 走真门禁 → `write_rework_note` → `lessons.record()` 把教训记进
**入库文件** `tools/lessons.tsv`（去重时还会把日期刷成今天）—— 跑一次回归就脏一次 `git status`。
**改法**：`lessons.PATH` 支持 `PDF2ZH_LESSONS_TSV` 覆盖；`run_all.py` 跑门禁时把台账指到临时
文件（子进程继承），被测代码路径一行未改。
**逐条反证**：不带 env 单跑 `test_seg_rework.py` → `lessons.tsv` MD5 `04B3511A…` → `7993332F…`（**被改**，
36 passed）；带 env → MD5 不变、临时台账生成（36 passed）。全套 `run_all.py` 跑完后
`lessons.tsv` 仍是 `04B3511A…`。

### 五、P4 润色路径：example 不带本机盘符 + 缺路径响亮报错

**旧行为**：`config.json.example` 是 POLISH* 键的**托管事实源**（`server/utils/config.py` 在插件
推送配置时从它无条件回填），却写着本机路径 `D:/zotero-pdf2zh/server/glossary/terms.csv` ——
公开克隆/换盘后就是一个不存在的路径；而译端对缺失文件只 `logger.exception` 之后**静默降级**
（`_load_polish_glossary` 返回 `{}`），钩子**无声失效**；启动自检也只打 basename，看不出文件在不在。
**改法**：
- `config.json.example`：四处路径改为**相对仓库根**（`server/glossary/terms.csv`、
  `server/glossary/guideline.txt`、`server/translated/review`）；
- `server/utils/config.py`：新增 `polish_backfill_value(key, value, repo_root)` —— 相对值按仓库根
  锚定成绝对路径、绝对值原样保留，**文件类**键当场查存在性，缺了就 `⚠️` 点名"会无声失效"；
  **目录类**（`POLISH_REPORT_DIR`）缺失不算错（落盘时才 `makedirs`）。
  本机回填结果与改前的绝对路径**逐字节等价**（换写法不换值）。
- 未动 `config.json`：它在 `.gitignore` 里，且锚定结果与现有值一致，无需同步。
- 未动 vendored `pdf2zh` 的润色自检：路径锚定与告警放在**托管回填处**，译端一行未改。

### 六、验收

| 项 | 结果 |
|---|---|
| 回归套件 | **30 套件全绿**（`run_all.py`：`回归结果: 全部通过 ✅`，0 SKIP） |
| 新增套件 | `test_env_isolation.py` **13 PASS**（P1）/ `test_dedouble_sweep.py` **19 PASS**（P3）/ `test_polish_paths.py` **20 PASS**（P4） |
| 扩写套件 | `test_relink_pages.py` **17 → 24 PASS**（P5：漂移侧车报真实零页 / 同页多记录按页归并 / 无 `pageid` 回落 / 端到端 `--sidecar` 跳真实零页且非零页照常重定位） |
| 镜像门禁 | 改过 `server/` 三个文件后重跑 `tools/sync_patches.ps1` → **13 PASS / 0 FAIL / 0 SKIP** |
| 工作区洁净 | 全套回归后 `tools/lessons.tsv` MD5 未变（P11 生效） |

### 七、边界 / 有意不做

- P1 的测试用**真起子进程读自己的环境**（不是读 `Popen(env=...)` 参数）—— 后者只能证明
  "我们传了这个 dict"，证明不了它**到达**了子进程；另配 AST 静态守卫：`server/server.py` 内
  禁止 `os.environ[...] =` / `pop` / `setdefault` 写开关，且 `translate_pdf` 必须保留 `extra_env`
  并交给 `execute_with_progress`。
- P3 的测试全部在临时目录的**合成库**上做，绝不碰真实 `cache.v1.db`。
- P4 的"目录类不告警"是刻意口径：报告目录缺失是常态（`_polish_report_append` 里 `makedirs`），
  对它告警会变成噪音，反而稀释真告警。
- 风险榜里其余项（P2 已撤销、P6–P10 为观察项）不在本版。

---

## 2026-09-22 v28.66 把话带回上游：核对过的反馈清单 + 两条已备好的上游 PR + 全项目代码串联审查

> **本版不改任何运行时代码**，只改文档 + 在 `D:\zotero-pdf2zh-eval\`（仓库外）落反馈产物。
> 起因：被问"我们到底解决了官方的什么问题"，第一次回答把两层"官方"混成一层，
> 还把上游**已经自己修好**的条目当成现存缺陷。本版先核对、再定稿。

### 一、A：`PIPELINE_STORY.md` 新增第十二章

- **两层"官方"分开**：上游本体 `guaguastandup/zotero-pdf2zh` v4.1.7（我们 fork 它、深度改它）
  vs 翻译内核 `pdf2zh` 1.x / `pdf2zh_next` 2.9.0（我们只调用、不打补丁）。
- **反馈清单 11 条**（带"已复核/实测/推理"状态），全部来自
  `git diff 35af40f HEAD`（`35af40f` = `refs/remotes/fork/main` = 上游 v4.1.7 原版树；两树无共同祖先，
  故这份 diff 正好等于"我们对上游的全部改动"）。
- **另列 6 条"扫描到但未逐条复读"**，在 issue 里标"待确认"，**不进清单表**。

### 二、B：`D:\zotero-pdf2zh-eval\feedback\issue_上游缺陷报告.md`

按上游 `.github/ISSUE_TEMPLATE/问题反馈.md` 的结构写全（含执行环境段）：
11 条已复核（每条给 `file:line` + 机理 + 期望行为）+ 6 条待确认表 + **2 条主动撤销** + 最小复现脚本。

### 三、C：`pr_upstream_fix.patch`（两条真缺陷 + 8 条回归测试）

| 项 | 内容 |
|---|---|
| 分支 | `D:\zotero-pdf2zh-eval\upstream` @ `feedback/upstream`（commit `773bfa5`，基 `35af40f`，**未推送**） |
| 补丁 | `pr_upstream_fix.patch`，8038 字节 / 3 文件 / +151 −6；`git apply --check` 在 `main` 上通过 |
| 改动 1 | `utils/execute.py::_decode_pty_utf8`：真非法字节不再永久卡在 `leftover` 头部（旧实现 → 进度解析永久冻结 + 内存无界增长） |
| 改动 2 | `utils/venv.py`：引擎改由**可执行文件位置**判定（新增 `_engine_from_command`），不再被输入路径/`--output` 目录里的 `pdf2zh_next` 带偏 |
| 测试 | `server/tests/test_pty_decode_and_engine_selection.py` 8 条；上游 CI 已有 `unittest discover` **自动收集，无需改 workflow** |
| 先红后绿 | 本机 Python 3.12：打补丁后 8/8 绿；`repro_old_behavior.py` 实测旧行为输出 `leftover 30 字节 / 后续进度行未被解析到 / 引擎选错` |
| 复现脚本 | `D:\zotero-pdf2zh-eval\feedback\repro_old_behavior.py`（**仅标准库**，任何环境可跑） |

### 四、有意不做：第九章"四条建议"里的第 2、3 条不走 PR

**补"渲染残渣"断言**、**表页零汉化视为预期** —— 上游本体**没有门禁层**，
作为 PR 提交过去只能是**没人调用的死代码**（表面功夫）。这两条真正的归属是
`tools/post_check.py`（第 5 断言 v28.17 / 表页豁免 v28.46 均已落地），对外以"建议 + 实测证据"进 issue。

### 五、口径纠正（三条，落进第十二章第五节的"必须记住"）

1. **"`.example` 会覆盖用户配置" —— 上游已自修**：`server/utils/config_migration.py` 在文件已存在时
   走 `_merge_defaults(defaults, current)` 以用户值为准（`:29/125/155`），坏文件先 `_backup_invalid`；
   `server/server.py:1244-1246` 写明 `# Never overwrite an existing user config with the .example template.`。
   → 从"官方缺陷"清单**删除**（与我们的"用户值优先"补丁**同向**，不是对立）。
2. **"`task_manager` 返回内部字典活引用" —— 不成立，主动撤销**：`task_manager.py:123-130` 两处都是
   `list(...)` **浅拷贝**，列表结构安全。原判据是扫描器误读。
3. **"next 分支把主 `config.toml` 交给子进程会被回写" —— 不成立，主动撤销**：读已发布内核
   `pdf2zh_next 2.9.0` 源码可证 `--config-file` 只读 —— `config/main.py:587-596` 读完即
   `del merged_args["config_file"]`；全包唯一写用户配置的 `write_user_default_config_file()`（`:615-628`）
   落点固定为 `~/.config/pdf2zh/config.v3.toml`（`const.py:8-10`）且**只在 GUI 路径被调用**
   （`gui.py:911/3472`）。原判据只看到"pdf2zh 有副本、next 没有"的不对称，**未验证内核是否真的写回**。
   → 从 issue"待确认"表与关系总览风险榜中一并撤下（不对称本身保留为观察，非缺陷）。

### 六、D：全项目代码"从里到外"审查 + 串联（**只读**，产物在仓库外）

**方式**：全程只读（`Read/Grep/Glob` + 只读 git + AST 静态分析），未启动服务、未跑翻译、未调用 LLM。
**产物**：`D:\zotero-pdf2zh-eval\project_map\` —— `00_总览.md`（六层全景 + 三条链路 + 跨层契约表 + 风险榜 P1–P10 + 测试空洞）、
`10_tools层.md`（工具层 38 个源文件 / 24124 行）、`20_server与patches层.md`（server 20 + patches 16）。

**规模**：`tools/` 顶层 29 + `table_pipe/` 9 = **38 个源文件**；`tools/tests/` 28 个 `.py` = **27 个套件** + `run_all.py`；
`server/` 20 个 `.py`；`patches/` 16 个（其中 13 对纳入 MD5 镜像门禁）。

**跨层风险榜（P1–P11，按跨层影响排序）**：

| 级别 | 风险 | 一句话 |
|---|---|---|
| 🔴 P1 | 进程级环境变量当每任务状态 | 并发提交两篇不同 PDF → B 被 A 的提字趟逼成暂停档 → 出全英文产物 + 侧车归档到错误散列。**亲验两道"看着能兜住"的闸都不兜**：`_SUBMIT_LOCK` 只护同名登记、`_sole_task` 只服务失败日志归因 |
| ⚪ P2 | next 侧无 `.task` 副本 | **已核销撤销**（见第五节第 3 条） |
| 🟠 P3 | `dedouble_sweep.py` import 即改真库 | 无 `__main__` 守卫，`shutil.copy2`+`sqlite3.connect`+UPDATE 全在模块顶层，且写死绝对路径与单篇指纹 |
| 🟠 P4 | POLISH_\* 以 `config.json.example` 为唯一事实源 | 插件 UI 改了不生效；example 里硬编码 `D:/zotero-pdf2zh/...`，换目录部署即失效 |
| 🟡 P5 | `relink_pages` 页码口径与其他工具不一致 | 用侧车 `page`（回调计数）而非 `pageid`（真实页序）→ 含图论文上 `zero_pages` 跳错页 |
| 🟡 P6–P9 | 明文密钥驻盘 / 面板本机 API 无 Origin 校验 / 14 个脚本 import 时重包 `sys.stdout`（**是发生过的故障**，`adopt.py:136-139` 已绕开）/ 两处"静默回落"与 docstring 承诺矛盾 | 详见 `20_...md` |
| 🔵 P10 | 死代码与文档漂移 | `environment_lifecycle.py` 不可达约 61 行、`term_verify.write_glossary_updates` 全仓无调用、`table_zh.py` 自述弃用、`UPGRADE_RUNBOOK.md` 说"patches/ 8 文件"而实际 16 |
| 🔵 P11 | 跑一次回归就改动仓内台账 | `lessons.py:25` 写死仓内 `tools/lessons.tsv`；`test_seg_rework.py` 走到 `record()` → 7 行日期被刷成跑测当天，`git status` 凭空多一个改动。已改回、不进提交 |

**测试覆盖空洞**：**16 个源文件零测试**（含 `table_pipe/check_job.py` —— 长文自述的对账门禁）；
另有 2 个"看着有测试、其实没测到本体"（`verify_render`、`force_rerender`）。

**审查过程中主动撤销/降级的判据（3 撤 1 降）**：撤销 `task_manager` 活引用、next 主配置写回、
上游 `.example` 覆盖；降级上游 `-p 1-5` 单 token（实测**碰巧能用**，非故障）。

**回归验证（本次实跑）**：全 27 个套件 **全部通过 ✅**；镜像门禁 **13 PASS / 0 FAIL / 0 SKIP**。
（注意跑法：`test_mirrors_sync.py` 需指向 `zotero-pdf2zh-venv`；用 conda base 解释器会得到 6 个**假 FAIL**。）

### 七、边界

- 本版**未改**任何运行时代码、未改 `patches/`、未改 `config`，因此 `sync_patches.ps1` 镜像仍一致；
- 反馈产物（patch / issue / PR 描述 / 复现脚本 / 关系总览）全部落在
  `D:\zotero-pdf2zh-eval\`，**不进本仓库**；
- 上游浅克隆 `D:\zotero-pdf2zh-eval\upstream\` 用后可整目录删除，与本地仓库无任何 git 关联。
















