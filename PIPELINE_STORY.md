# 框架纪事：一条论文翻译管线的完整演化（2026-08-29 → 2026-09-19）

> 本文回答四个问题：**我们做了什么、发现了什么问题、怎样解决、结果是什么。**
> 材料来源：`改动记录.md`（26.6 个版本全量日志）、`patches/PROTOCOL.md`、各工具 docstring、M1 验收日志。
> 配套仓库：https://github.com/ice917/zotero-pdf2zh （doubao-pipeline 分支，AGPL-3.0）

---

## 第〇章 框架总览

最终形态是"**四层 + 一管线**"：

| 层 | 职责 | 关键组件 |
|---|---|---|
| 环境层 | 冻结一切自动更新，保护自研补丁不被覆盖 | venv.json 钉死、五道更新闸门 |
| 版面层 | 把 PDF 解析成 34 页/154 段的"槽位图"+ {vN} 字形表 | 上游 converter + 自研渲染整形补丁 |
| 翻译层 | 产出定稿译文并注入缓存 | ~~润色钩子（已退役）~~ → **豆包采纳管线**（M1） |
| 术语验证层 | 冷门术语联网查证，供给裁决证据（用户提案，上游无此能力） | term_verify（可插拔源：Zotero 本地库 / OpenAlex / 维基百科 / 维基数据） |
| 质量门禁层 | 翻车自动报警，不靠人眼 | pre_check / post_check / verify_render / 91 项回归 |

---

## 第一章 奠基期（2026-08-29 前后）：润色钩子的提出

### 做了什么
- 接入硅基流动（DeepSeek-V3.2）为翻译主引擎；修复中文方块字（Windows 下 Noto 字体路径无效——**中文乱码的根源是字体路径**，不是模型）；公式保护改用 pdf2zh 1.x 真正认识的 `-f/-c` 参数；`--skip-subset-fonts` 防字体子集化公式乱码。

### 润色钩子为什么被提出
初译的三大顽疾：**术语前后不一致、指代混乱、观感糙**。逐段独立调用 LLM，每段都是"失忆"的。
解法：在 `BaseTranslator.translate()` 之后挂一个确定性钩子 `polish()`——带 3 段上下文窗口（译名/指代一致）、术语表强制注入、`{vN}` 占位符保护（润色破坏占位符即回退初译）。

### 六次迭代（每轮都是"实战发现问题 → 当日修复"）
| 轮 | 发现的问题 | 解决 | 结果 |
|---|---|---|---|
| v1 | 初译糙 | polish() 钩子 + 术语表 + 上下文窗口 | 观感提升，但 LLM 润色可能改坏占位符 |
| v2 | 润色盲目 | 反思模式：审校批判（忠实性/流畅性两组）→ 修订；"无明显问题"短路 | 批判为空时省一次调用 |
| v3 | LLM 擅改有争议的术语 | 【存疑】组：术语表与语境冲突时**不自动改**，落盘人审报告交人工裁决 | 机器+人分工确立 |
| v4 | 钩子因环境重装**静默失效**（发生过） | 启动自检 print 可见化；上下文窗口线程隔离；存疑清单独立小文件 | 静默失效变成开机即知 |
| v5 | 批判者审不出确定性错误 | **锚点断言**：润色后做不经 LLM 的校验——数字保真、不译词保留，违规回退初译 | LLM 的错用确定性代码兜底 |
| v6 | 实战误报（文献引用区间 `[15–17,24]` 被当数字篡改） | 引用括号剔除；数字缺失降级弱信号只记录 | 误报大幅下降，回退只留给真错 |

### 同期发现的其他大坑与解法
- **POLISH 配置每翻一篇就被清空**（官方逻辑按插件请求重建 envs，未携带的 key 一律删除）→ 白名单回填补丁。注意：当时以为修好了，真正的元凶 9-03 才落网（见第二章）。
- **老 PDF 抽不出文本**：`C{xx}` glyph 名没被 pdfminer 认识 → 全变 `(cid:N)` → 被当公式不翻译 → 输出仍是英文 → `encodingdb.py` 加 C{xx} 回退解码。
- **抽取层系统性错字**（±→I、−→)）：Elsevier 老 PDF 自定义字体编码，损伤发生在抽取层，翻译层无解 → `pdffont.py` 字体字符校正表（JSON 规则，只在"当前解码==记录的错误解码"时替换，防误伤）。
- **环境冻结五道闸**：官方更新会覆盖自研补丁——server 更新检查关、环境更新询问冻结、venv.json/requirements 全量钉死、config 用户值优先。**原则：深度补丁环境，禁止一切浮动。**

### 结果
管线从"能用"到"能看"。已知残留：Æ 乱码、数字标点错位、少量 (cid:) 残留——挂账。

---

## 第二章 质量门禁期（9-02 ~ 9-03）："翻车自动报警"

### 发现的问题
1. 润色钩子再次静默失效（POLISH 键被置 null）；
2. 参考文献页双栏解析失败、任务卡死 16 分钟——**都是翻译完才发现**；
3. 全项目代码审查发现 **89 项问题**（严重 5 / 中等 27 / 轻微 57），Top1 是 config.json 并发读写无锁——正是润色配置事故的根源。

### 怎样解决
- **pre_check.py 翻译前体检**：参考文献页/扫描页/公式密集页/加密检测，坏 PDF 在提交前拦截（依据 BabelDOC ACL 2026 论文的质检指标）。
- **post_check.py 质检门禁**：汉化率、引用完整性逐页对账、占位符残留三项断言，PASS/FAIL 退出码供自动化。
- **server.py 自动质检钩子**：翻译成功后 daemon 线程自动跑双检——把"要记得检查"变成"不用记得"。
- **POLISH null 真元凶落网**：加锁、原子写、预置注入全都拦不住——最后发现是 **pdf2zh 子进程的 ConfigManager 启动时把主配置规范化写回**，POLISH 正是被它抹掉的。终极修复：给子进程传 `config.json.task` 副本，主配置永不被子进程触碰。
- P0/P1 批量修复：进程锁+原子写、迁移异常分类（不再把 OS 异常当配置损坏覆写用户配置）、同名任务幂等去重（重复点击不再互相覆盖）、venv 操作 RLock、skip-last 豁免参数化（文献页失败不再静默放行）、🟡26 项。

### 结果
门禁体系上线即立功：正向（合格产物 PASS）+ 反向（伪造英文译文 FAIL）双向验证。管线从"能跑"到"**自证清白**"。

---

## 第三章 深水期（9-06 ~ 9-07）：自己挖的坑与机械伤四层根因

### 自己挖的坑（三次事故，教训比修复更值钱）
| 事故 | 根因 | 固化下来的教训 |
|---|---|---|
| 插件升级后翻译**启动即崩**（`Event has no acquire`） | 9-03 把 threading.Event 当互斥锁用；当日只做了 py_compile，**静态检查测不出运行时错误**，埋雷三日 | 关键路径必须真实调用验证 |
| 每段译文 LLM 成功后统一报 `takes 1 positional argument but 2` | 插入 `_clean_cjk_typography` 时恰好插在 `@retry` 和 `def do_translate` 之间，**retry 错装到清洗函数头上**，staticmethod 语义被破坏 | 对装饰器函数插桩必须检查装饰器归属；验证要覆盖"实例调用"与"类调用"两种形态 |
| 两处修改并行提交互相覆盖（后写者胜） | 同文件并行编辑竞态，磁盘短暂处于"混合状态" | 同文件多处修改一律串行 + 逐字节读回断言 |

### 机械伤四层根因（一个"观感糙"，挖出四层叠加的病）
1. **第一层·译文清洗**（9-03）：PDF 断行连字符烤进中文（"鱼类-式"）、半角标点残留（"变形,产生"）→ `_clean_cjk_typography` 确定性正则（幂等、负面清单防误伤：小数点/数字范围/{vN}/纯英文），以 `typo_clean=v1` 进缓存键，旧缓存自动失效。
2. **第二层·润色路径绕过**（9-06 14:50）：清洗只保护初译，**polish 的返回值（最终入库稿）绕过清洗**——润色 LLM 重写时重新引入 868 处半角标点 → polish 返回后 + 缓存命中路径补清洗；关键设计：`typo_clean` 不升版本（升了会作废全部缓存），已缓存段落零成本修复。
3. **第三层·纯标点占位符**（9-06 19:30）：连字符清洗后纹丝不动（410→414），顺藤摸瓜——原书 PDF 的标点以 `(cid:NN)` 被解出，命中公式保护首条规则被判成公式包成 `{vN}`，**渲染时原字体标点回填到汉字之间，翻译层/清洗层/缓存层全部不可见** → converter 在翻译后、排版前剥离孤立标点占位符。**"翻译后修改"设计：缓存键不变，重翻 11 分钟全缓存命中，零 LLM。** 验证：半角标点 868→**0**，连字符 410→**0**。
4. **第四层·叠加伤**（9-10）：占位符标点 × LLM 自加标点——缓存"滤波器{v100}（EKF{v101}）"渲染成"滤波器**（（EKF））**"（双括号 ×50）→ 渲染层半/全角等价比较去重。
5. **同期的"硅基准"事故**：术语表只进润色 prompt，初译对术语一无所知；润色被占位符保护回退后，终稿落回"从未见过术语表"的初译（标题 "in-silico benchmark"→"硅基准验证"）→ **初译 prompt 也注入归一化匹配的术语条目**（`In{v3}Silico` 剥占位符后能匹配 `in-silico`）。
6. **vfont 一字之差**：公式字体正则 `.*Ital` 误吞 NimbusRomNo9L-ReguItal 正文斜体段（十余处整段英文未译）；对照上游发现官方是 `.Ital`（单字符前缀，re.match 锚定开头）——**抄配方抄错一个星号**。v21 连带排掉 `CM.*` 会吞 LaTeX 正文 CMR10 的地雷。

### 另一场代价高昂的实习课：bing 静默回落事故（9-10）
渲染验证时 JSON 漏传 config 字段 → service 缺省回落 **bing** → 341 段缓存键全新 → 静默重译 4.5 分钟，术语退化为未翻译。教训固化两道闸：service 缺省回落**大声告警** + force 直通道工具化（`force_rerender.py`）。

### 结果
机械伤四层防线闭合；Melhani 净稿三关全过（门禁 PASS / 双清零 / 页对齐）；v21 架构级修复 P0 五项（含 API 直提任务 Zotero 秒挂载——查 history 复用 taskId）。

---

## 第四章 命中率与工程化期（9-10 ~ 9-12 上半）

### 发现的问题（灵敏度测试实测）
- 术语表**加 1 行 dummy** → 前 3 页 23 段全部缓存失效（整表指纹进键）；
- vchar **加 1 个字符** → {vN} 全局编号级联平移 → 同样全 miss。
结论：**想"改字不重译"，先得让缓存键对扰动不敏感。**

### 怎样解决
- **v23a 占位符编号规范化**：{vN} 全局编号 → 段内出现序规范编号，缓存键与全局编号解耦；命中后按双方 seq 重映射译文 token；4171 条旧条目惰性索引迁移，**零重译成本**。
- **v23b 术语表按段指纹**：只哈希"本段命中的术语"——表变更后，含该术语的段落才重译，其余继续命中。
- **工程化三件套**：42 项测试入库（v23.1）、协议契约文档 + 镜像同步脚本（v23.2）、force 直通道 + 跨页断句根因确诊与缓存手术流程（v23.3）、前瞻上下文页内版落地（v23.4，跨页版确认架构性不可行）。
- **v24a 文档摘要前置 / v24b 军师层**：把"通读全文"意识工程化——文档级通读校对 + 人工裁剪精准写回，v24b.2 占位符图例 + 报告/修复分离，v26-L1/L2 两类可机械识别漏损压缩，v26.2 接缝运维工具化（体检→台账→专项审查→渲染验收），v26.3 悬空功能词硬信号。

### 结果（也是转折点）
Cactaceae 实战修复全部落地。但军师层的实战同时暴露了天花板：**本地逐段润色 = 39 分钟渲染 + polish 反复破坏占位符的回退噪音 + "通读"只能靠摘要模拟**。真·通读需要一个能吃下整篇的引擎——这直接引出第五章。

---

## 插章 术语验证层：AI 联网搜索（用户提案，上游没有的能力）

### 提案背景
润色钩子 v3 的【存疑】组把术语冲突"交人工裁决"——但**人工裁决也需要证据**：一个冷门术语
到底怎么译，拍脑袋和查语料是两回事。上游插件（乃至上游整个生态）对此没有任何工具，
裁决全靠人脑。**用户提出：用 AI 联网搜索给裁决供给证据。**

### 落地：tools/term_verify.py（离线批处理，零侵入只读）
读取润色管线产出的「存疑清单.md」→ 提取存疑术语 → 直连公开检索源检索该术语的真实
用法证据 → 输出"术语 + 证据"报告，辅助人（或 LLM）裁决译法。
默认源 **OpenAlex**（2.5 亿条文献记录，标题级，零 Key、日配额 10 万）。

五条设计原则 + 一条形态判据：
1. **直连公开 API、刻意绕开本地 MCP 服务器**——规避三条耦合风险：共享缓存污染、熔断级联、外部限流竞争，对主管线零影响；
2. **检索源可插拔**（v26.14）——每源一个函数，统一签名 `search(term, max_results, timeout, context, max_retries) -> 契约`，注册进 `SOURCES`；用 `--source` 选择（逗号多选 / `all`）。**新增一个源 = 写一个函数 + 注册一行 + 补一句描述**。判定层 `llm_judge_relevance(term, titles, ...)` 只吃字符串列表，对来源无感，故加源不碰 LLM 逻辑；
3. **开跑先预检**（v26.14）——用探针词真实调一次各源，剔除不可达源并**在报告里如实写明**，避免每个术语在死源上空转几十秒；
4. **优雅降级**：单个术语、单个源查询失败只跳过该项，不拖垮整体；全部源不可达则不产出报告、退出码非 0；
5. **只读**：不改任何翻译产物、不写术语表，只出报告；
6. **按术语形态选源**（v26.15，40 术语实战）——中文术语不走 Zotero（其对汉字是字符级松散匹配，只产假召回），短缩写附"改用全称"提示，库内**译文** PDF 的命中标记为循环证据；跳过了什么写进报告头，不假装查过。
7. **即席查证入口**（v26.16）——`--term "xxx"` 随手查一个词，不必先有存疑清单。动因是**先量了再动手**：存疑清单的术语产出 09-06 见顶（59 个）后归零（09-07~09-12 共 18 份仅 5 个术语），"翻译后自动查证"收益趋零且会把白跑记成 QC FAIL，故不做自动挂钩，改做按需入口。
8. **把工具交给 LLM 自己调**（v26.17）——原始诉求"给 LLM 一个类似 tavily 的搜索工具"的真正落点不是新起服务：那个 LLM 就是豆包，而它的入口 `doubao_bridge.py`（零依赖 STDIO MCP 桥）本就在仓库里，加一个 `search_term` 工具即可。前提是先核实 `term_verify` 的 23 处 `print()` 全在 `main()` 内（否则一行 `[INFO]` 就会毁掉 JSON-RPC 通道）。

已接入的源（`--source`）：
| 源 | 给什么证据 | 前置条件 |
|---|---|---|
| `zotero` | 论文**标题（本地库）** + 命中处**原文上下文摘录**（术语在自己收藏的文献里怎么用） | Zotero 运行中 + 开启本地 API；**真零网络** |
| `openalex` | 论文标题 + 被引数（术语在学术文献里怎么用） | 无，国内可直连 |
| `wikipedia` | 条目名 + 条目摘要（术语**指什么**，定义性语境） | **需代理**（`en.wikipedia.org` 遭 DNS 污染） |
| `wikidata` | 规范实体 + 别名 + **中文标签**（直接服务译名裁决，如 herkogamy → 雌雄异位） | **需代理**（`wikidata.org` TLS 握手被阻断） |

> 唯一**真正零网络**的源是 `zotero`：走本机回环（`127.0.0.1:23119`），不发一个外网包，
> 且它的证据质量最高——`qmode=everything` 命中**本地 PDF 全文索引**，返回的是原文句子
> （截术语前后各 150 字），而不是标题。代价是覆盖面取决于你自己的库。
> 实测一本正在翻译的专著（Cactaceae）直接命中。

> 红线：维基数据的中文标签是**证据不是裁决**——质量参差（繁简混用、地区词差异、
> 冷门概念根本没有中文标签），写入术语表前仍须人工确认。

### 实战教训（七条）
1. 审校报告 humanize 改版后解析得 0 个术语 → 切块与存疑块正则**双格式兼容**（v18 兼容）；
2. OpenAlex 的 `--context` 必须用**短英文领域词**——传长中文描述会污染全文检索词，
   实测 5321 条召回直接归 0；
3. 超时/URLError/连接重置也要退避重试（2s/4s/8s），旧实现只对 429/5xx 重试；
4. "零部署"不等于"零障碍"——维基系零 Key 零部署，但在国内网络**不可直连**
   （DNS 污染 / TLS 阻断）。选源时网络可达性要和接口形态一起验，否则写完才发现白做。
5. **"术语形态"和网络可达性一样要预判**（v26.15，40 术语实战首轮）——Zotero 全文索引
   对汉字是**字符级松散匹配**（单独查 `拓扑优化` 返回 10 条、9 条完全无关），中文术语
   已改为不走该源；短缩写 `CM` 召回 ICD-9-CM 疾病编码这类高被引噪音，改为在报告里
   直接提示换全称；库里 pdf2zh 产出的**译文 PDF** 会被召回，命中标为"循环证据"不予采用。
   首轮实测：zotero 条目 128 → 63（−51%）、耗时 146.8s → 126.0s、2 个假术语被丢弃。
6. **工具"能跑"与"会被调"是两件事**（v26.17）——把自研能力交给 LLM 时，`description` 就是接口。
   首战豆包**没调** `search_term`：它用自身知识 + 自带联网搜索答了 herkogamy（还引用 CSIRO / PMC）。
   原描述只说了"什么时候可以用"，没讲清**这件事它自带的能力干不了**——在它眼里自研工具
   只是一个可替代的竞争项，于是被跳过。补上"你自带的联网搜索拿不到用户收藏的 PDF 原文，
   别用自身知识替代它"+ 触发词（查证/找证据/看原文怎么用）后，二轮实测**调用了**，
   并主动说明"OpenAlex 因 429 未返回，不影响本地证据"。协议层能跑（MCP 握手、stdout 洁净）
   只是入场券；决策层要靠措辞赢过幻觉替代品。
7. **限流要听服务器的，别自己拍退避阶梯**（v26.18）——OpenAlex 实测返回
   `Retry-After: 56941`（≈15.8 小时）时，原实现的 2s/4s/8s 重试纯粹空撞；
   现已改为「`Retry-After` 超过 60s 直接放弃并把等待时间回传」。同一次探针还澄清了
   一个易误判的问题：**限流可能与你无关**——带邮箱、换浏览器 UA、裸请求三种身份
   全是 429，故"配个 `OPENALEX_EMAIL` 进礼貌池"这类做法救不了 IP 级配额耗尽。

### M1 范式下的演进：检索源扩编 + 人机分工
- 翻译引擎侧：豆包协议要求"学名/人名/机构**给出确定译法，不确定的标 ？**"——查证在翻译时一次完成；
- 复核侧检索源：**Zotero 本地库**（已接入，唯一真零网络，全文命中可给原文上下文）、
  **OpenAlex**（已接入，学术标题级）、**维基百科 / 维基数据**（已接入，需代理）、
  **Tavily / SearXNG**（未接入，SearXNG 需容器、占内存）
  ——豆包标 ？的条目与不放心条目才触发，不做全量检索；
- 分工原则：语境依赖惯用语（`successful gamete→可育配子`）进**文档级台账**，不进全局术语表。

### 意义
术语裁决从"拍脑袋"变成"查语料"。这是上游完全没有的能力，且对主管线零侵入。

---

## 第五章 范式转移期（9-12 下半）：润色钩子退役与豆包采纳管线 M1

### 动机（润色钩子的局限清单）
1. 39 分钟渲染、polish 破坏占位符→回退的反复噪音；
2. "通读全文"在本地架构里只能模拟（文档摘要/前瞻窗口），不能真通读；
3. 用户的核心洞察：**豆包翻译更好（整篇通读+中文润色强+免费），我们的价值在版面保真与验证——"我们只做采纳"。**

### 决策（架构含义）
- 润色钩子**从主路径退役**（采纳模式下 polish=off）——外来定稿文本再过一遍 LLM 只会引入风险；
- 术语表从"强制翻译"降级为**事后校验清单**——呼应豆包自家翻译技能的同一原则（"术语表只能约束译法选择并用于译后一致性核对"）；
- 语境级译法（如 `successful gamete→可育配子`）只进**文档级台账**，不进全局强制。

### 侦察与设计
- 逆向豆包桌面版：Chromium 147 内核 + 本地 Agent 平台（109 个内置技能包、mcp_helper.dll、自定义连接器 HTTP/STDIO）→ 判断"它不是聊天软件，是能读写本地的 Agent"；
- 额度实测：基础对话免费不限次；工作任务（Agent）模式额度极稀缺；连接器只在 Agent 模式挂载 → **主通道 = 基础对话 + 剪贴板**，连接器做备用；
- 协议三设计：**#S 编号行**（对齐键）、**字形还原**（{vN} 先变真实字形再喂，数字/拉丁名照原样）、**⋮ 跨页断点**（豆包自己放对了位置）。

### 三道工序（全部固化成可复跑工具）
`seg_export`（编号段落包；断词连字符"后随大写保留/小写删除"分治；跨页续接自动合并——中间隔一整页表格也能抓到）→ 豆包 → `seg_import`（回锚：**只锚含字母/数字的高价值字形，纯标点按渲染契约静默丢弃**——否则找不到本尊的 '-' 会偷走后面真标点的位置，光标级联冲过头；核心命中后向两侧吸收全角兼容的邻接标点）→ `seg_inject`（页级→段内重编号 + **文档指纹作用域**——"Introduction" 原文匹配会命中 13 行横跨 13 个文档，指纹缩到 1 行 + 前置断言"缓存现行译文==侧车 trans"）。

### 隐私审计（开源前，拦下两个雷）
1. 完整 git 历史里躺着 xpi、旧路径、旧文件 → **重建为单提交孤儿分支**，推上去的只有干净快照；
2. 密钥全树扫描扫出**自己的活 DeepSeek key**（藏在两个配置文件里）→ 剔除 + ignore + 建议轮换。**未 push，零外泄。**

### 结果（M1 验收，2026-09-12）
- p2–p4 端到端：6613 字符 payload 一次喂完 → 豆包 2133 字译文 → 9/9 段回锚 PASS → 注入 13 行 → 45 秒渲染 → **verify_render 13/13 全过**；
- IUCN 59 种 / CITES 64 种数字归位（比此前手工手术更干净）；Fisher 3:2 = "平均拥有三个可育配子"；术语全线一致；
- **改字成本从 39 分钟/有 LLM 风险 → 45 秒/零 LLM**；翻译成本 0 元；
- 按 AGPL-3.0 开源：https://github.com/ice917/zotero-pdf2zh/tree/doubao-pipeline

---

## 第六章 成果总表

| 维度 | 数字 |
|---|---|
| 版本迭代 | 26.6 版，每版对应一个真实缺陷 |
| 测试 | 91 项回归全绿（v23.1 起 42 项起步） |
| 机械伤 | 半角标点 868→0，连字符 410→0（四层根因全确诊） |
| 版面对齐 | 34 页 / 154 段一一对应 |
| M1 回锚 | 9/9 段 PASS，数字与拉丁名 100% 原位 |
| 渲染验收 | 13/13 断言全过 |
| 改字成本 | 39 分钟 → **45 秒**，0 次 LLM |
| 翻译成本 | 0 元（豆包基础对话） |
| 术语验证 | term_verify.py 629 行，OpenAlex 2.5 亿条零 Key 检索（上游无此能力） |
| 开源 | AGPL-3.0，单提交干净快照，三层隐私审计 |

## 第七章 润色钩子小传（专题）

**生**：2026-08-29，为了治"逐段失忆"的初译。
**盛**：六次迭代——反思模式、人审报告（存疑组人机分工）、并发隔离、锚点断言（用确定性代码给 LLM 兜底）、锚点调优；配套打掉配置覆写、子进程写回两大静默失效根因。它把管线从"能跑"带进"自证清白"。
**死**：它的两个先天局限——逐段失忆（只能靠摘要模拟通读）与生成式改写的不确定性（破坏占位符回退噪音）——在军师层实战中被彻底暴露。当出现一个"免费 + 能真通读 + 润色更强"的外部引擎时，它的主路径地位就结束了。
**遗产**（全部活在后续架构里）：
- 锚点断言思想 → `verify_render` 的 13 项断言与 M1 回锚校验；
- 术语表指纹 → v23 缓存键工程；
- 反思模式（批判-修订）→ 军师层与"豆包出稿 + 本地机械校验"的人机分工；
- 它的墓志铭：**"润色破坏占位符即回退初译"这条保护规则，反过来证明了生成式改写不可信——于是有了采纳管线。**

## 第八章 未竟事项

- 整页大表格保持英文（翻译表格 vs 保版面，待决策）；
- 双标点微瑕（吸收算法待改逐字符增量）；
- 纯拉丁段落不经 CTK 清理规则；
- 剔除出公开分支的 seam 工具与测试套件待脱敏后回归；
- 上游 9 月重构在即，patches 重放将面临实战考验（UPGRADE_RUNBOOK 已备）。

---

## 第九章 官方引擎隔离评估（2026-09-19）：BabelDOC 2.9.0 同题对照

> 缘起：用户要求"官方新出的插件可以装，但要**隔离**——用容器跑一遍，看他是不是我们想要的效果"。
> 结论先行：**官方初译质量已与我们同一档，但它的"第二公里"（可维护、可验证、可追责）仍然空白——
> 我们的线不动；换产品不换，引擎可择机换。**

### 隔离方式（零接触，不碰现有产线）

| 隔离项 | 做法 |
|---|---|
| 运行环境 | 官方镜像 `awwaawwa/pdfmathtranslate-next`（pdf2zh-next **2.9.0**，3.3 GB），容器内运行，**不进我们的 conda 环境** |
| 文件 | 只挂载 `D:\zotero-pdf2zh-eval\official-next\{in,out}`；**不挂本仓库、不挂 `~/.cache`** |
| 端口 / 缓存 | 不发布端口（与我们 8890 无冲突）；用容器自身可写层，`--rm` 即弃，与本机 pdf2zh 缓存互不可见 |
| 引擎 | `--siliconflow --siliconflow-model deepseek-ai/DeepSeek-V3.2`（**与我们同模型**，译文可直比） |
| 样本 | Cactaceae 专著 `--pages 1-3`（只译这 3 页，其余 31 页保留原文）；mono；无水印 |
| 产物 | `...zh-CN.mono.pdf` + 官方**自动抽取术语表** `...zh-CN.glossary.csv` |
| 获取路径 | Docker Hub 直连被墙 / daocloud 白名单不放行 / ghcr 需鉴权 → 走 `docker.1ms.run` 镜像源；`~/.cache/babeldoc` 资产随镜像打包，**不联网下模型** |
| 耗时 | 3 页 ≈ 108 秒（含 ~30 s 预热；ONNX 走 CPU） |

### 同题对照（第 1–3 页，汉字数 / 拉丁字符数）

| 页 | 原文 | 官方 monos | 我们的 mono |
|---|---|---|---|
| p1 | 2320 / zh 0 / lat 1802 | 936 / **zh 508** / lat 215 | 1224 / zh 387 / lat 524 |
| p2 | 3300 / zh 0 / lat 2612 | 1144 / **zh 723** / lat 164 | 1212 / zh 688 / lat 238 |
| p3（整页大表格） | 1786 / zh 0 / lat 1332 | 1759 / zh 9 / lat 1302 | 2999 / **zh 0** / lat 1332 |

- 双方在表页**都保留英文**（官方 `table_mode` 已退役；我们 `translate_table_text=false`）——口径一致；
- 官方门禁同题同判：用我们的 `post_check` 跑官方产物与我们的产物，**p3 都判 FAIL**（同为"整页表格页零汉化"误报，见下"遗留"）；
- 关键词验收（我们的 `verify_render`）对官方产物 6/6 PASS：`仙人掌科`（p1×5/p2×4/p3×1）、`雌雄异位`、`雌雄异熟`、`近交衰退`、`传粉者` 全部落页。核心术语（herkogamy 雌雄异位 / dichogamy 雌雄异熟 / 近交衰退 / 交配系统 / 自交不亲和 / 传粉生态学）**双方一致**。

### 官方三处硬伤（我方实测，可复现）

1. **自动术语抽取会吞引用与人名**——148 条自动条目里，**14 条是引用人名/机构/出版信息**：
   `Anderson→安德森`、`Guzmán et al.→古兹曼等人`、`Mauseth→毛瑟斯`、`Darwin 1876→达尔文 1876`、
   `Universidad Nacional Autónoma de México→墨西哥国立自治大学`、`Springer-Verlag Berlin Heidelberg→施普林格出版社柏林海德堡分社`、
   `Desert Plants→沙漠植物`、`México, D.F→墨西哥联邦区` …… 而同一批里 `Brown 1990`、`Waser 1983` 却是原样保留——
   **同一类字符串两种处理，说明这层不可控**。这正是我们第 4 断言（文献区禁汉化）要防的事故类（EgoPhys 174 处）。
2. **渲染层 IL 残渣泄漏**：p2 页面上真的印出了 `</style\x01id='25'>`（16 个码点，中间夹控制符 U+0001）。
   我们的产品 34 页扫描 0 处。**我们的四断言目前抓不到这一形态**（现有断言不含"渲染残渣"项）。
3. **引用串空格丢失**：`Waser1983`、`Nobel1986`、`Arias等人2005a`（同段 `Brown 1990` 却有空格，故非纯提取噪声）；
   我们的产物为 `Waser 1983` / `Arias et al. 2005a`。

### 我们自己的可见缺陷（同题对照照出来的，属内容侧）

- p1 署名块被**音译**（`玛丽娅・德尔・卡门・曼杜哈诺…`），官方保留拉丁原文；机构/出版行我们保留英文（官方翻成中文）——**两种取向都不是 bug，是需要定策**；
- p2 括号错位与半角残留：`特有种占 （84%；Arias et al. 2005a）。`、`（Rzedowski 1991).`、`Table 10.1).`；
- 术语取向上我们表里是"繁殖生物学 / 繁殖系统 / 雌雄同株"，官方是"生殖生物学 / 繁育系统 / 雌雄同体"（hermaphroditic 译"雌雄同株"在植物学上易与 monoecious 混，**待裁决**）。

> 这三条按角色分工走**内容侧杠杆**（通用规则 / 质检报告 / 给豆包的指令），开发者不手改单篇文本。

### 项目价值定位（一句话 + 上限）

**一句话**：我们不造更好的翻译器，我们造**论文交付的第二公里**——
第一公里（把英文变成好中文）交给引擎（官方 BabelDOC / 豆包）；第二公里是**这篇论文在任何一天被再次修改时，能用 45 秒、零 LLM、不破坏版面地改对，且每改必验、每验留痕**。

| 层 | 谁负责 | 产出 |
|---|---|---|
| 引擎层 | 上游 1.x / BabelDOC 2.9.0 | 版面解析 + 初译（可替换） |
| 定稿层 | 豆包（免费、能真通读） | 中文化定稿文本 |
| **采纳层** | **我们** | 回锚 / 注入 / 渲染：段号守恒、数字与拉丁名原位 |
| **门禁层** | **我们** | 18 套件回归 + 四断言 + 13 项渲染验收 + 侧车台账 + 复核报告 |

**上限（诚实的三条）**：
1. **版面解析上限不在我们手里**——解析伤（跨页断句、cid 字形、表格页）随引擎走；换引擎＝换掉一批旧伤、**继承一批新伤**（官方这次就交了 `</style>` 残渣与术语污染）；
2. **内容准确性的终审仍须人**——我们只能把证据（术语查证）与门禁（断言）做扎实，不能替人下判断；
3. **"通读全文"这类全局能力，本地架构模拟不了**——所以定稿层继续外包给能吃下整篇的引擎，我们不重复造。

### 给官方的四条可实行建议（成本视角）

1. **自动抽取与强制注入解耦**：抽取结果先过一道类目过滤（引用串 `作者+年份`、机构名、出版行、表内缩写），否则会把文献人名汉化——这是一条低成本、可写测试的改动；
2. **补一条"渲染残渣"断言**：IL 标签/IPL 残渣（`</style…>`）、U+FFFD、U+0001 这类控制符出现在页面文本即 FAIL；
3. **表页零汉化应视为预期**（`table_mode` 退役后表内文字本就保留原文），门禁对"整页表格页"应豁免，否则合格产物被判失败；
4. **术语抽取结果按文档指纹复用**：同一份 PDF 重复翻译时不必再抽一次——这是官方流水线里唯一"每篇固定多付一次"的 LLM 开销（我们的静态术语表是 0 成本）。

> 性价比打通：**模型可切换是官方优势，但"每篇都全量 LLM"是成本**；我们的线把昂贵模型限定在初译与返工，
> 之后全部走缓存与机械校验——于是可以**用更强的模型做初译，同时把维护成本压到零**，这正是"高质量 + 可维护"的组合。

---

## 第十章 豆包进入管线（2026-09-19，v28.23）：一个任务内的两趟回路

> 缘起：此前豆包只能**事后"修一篇"**（缓存手术）——用户点一次翻译，机器翻译就把钱花掉了，豆包稿只能是事后补丁。
> 诉求：让豆包进入**"不走 LLM 的翻译产物"**这条路径，且钩子必须落在**服务端能停下来的位置**。
> 结论先行：**"人 + 豆包"从外挂变成了管线里的一个状态**——第一趟只提字（零翻译调用），任务停在「待译」等交稿，
> 交稿后回灌、第二趟重渲染全命中缓存。**这一趟跑完，LLM 在管线里从"必经的翻译工人"降为"可绕过的初译"。**

### 钩子为什么只能落在服务端

| 候选位置 | 为什么不行 / 行 |
|---|---|
| 引擎子进程内部 | 引擎是**逐页交替**（一页进去就在 `receive_layout` 里提段→翻译→排版回写），切片（页）是它唯一的空间参照系；进程内部没法"等一个人交稿" |
| HTTP 请求内部 | 请求是同步语义，等人会卡死界面 |
| **服务端 worker** | 新插件是**异步协议**（`_client_wants_async_job` → worker 进后台线程，POST 立刻返回 `accepted`，进度走 `/tasks` `/events`）→ **worker 可以中途等人，界面不卡** ✅ |

另一个边界值得写死：MCP 反向语义里**没有"唤醒 agent 干活"**的标准通知，所以豆包触发只能是**我们轮询交件**
（`out/<名>.doubao*.txt` 落盘），不是服务端推送。免得以后再设计一次。

### 一个任务，四个状态

| 状态 | 谁在动 | 产物 |
|---|---|---|
| 提字中 | 引擎（`PAUSE_TRANSLATE=1`：只提字，返回原文，**零 LLM 翻译调用**） | 侧车 → `inbox/<名>.txt`（编号段落包 `#S1..#Sn`，`⋮` 标跨页断点） |
| **待译** | **豆包（人）** | 定稿译文按段号写回 `out/<名>.doubao.txt` |
| 回灌重渲染 | 我们（`deliver → import → inject`，UPDATE-only 改写库行） | 缓存里该篇的段落译文换成豆包稿 |
| 完成 | 引擎第二趟（关掉开关重跑） | 中文 PDF，**全命中缓存、不再调翻译 LLM** |

### 三个非显然的设计决定

1. **暂停档不能"一列不写"**：`seg_inject` 是 **UPDATE-only**（按"原文 + 文档作用域"找行，找不到就 FAIL）——
   这是它防"注入进别的文档"的**核心安全设计，不能削弱**。所以暂停档必须落 **raw→raw 骨架行**（`translation == original_text`），
   且位置要放在 `cache.get()` **之前**：命中旧世代缓存的段落同样得留下本次键形态的骨架行，否则该段不在本篇作用域内。
2. **骨架行必须能止损坏**：新落下去的骨架行若没人改写，就会渲染成"整篇英文"。故新增止损 `rollback`
   （只删 `translation == original_text` 的行——骨架行的定义就是这个，不可能误删真译文），
   三条失败路径（导出失败 / 交件阶段失败 / 等待超时）统一走 `abort()`：**先撤骨架行，再回落成正常翻译**。
   补的一处漏洞：导出阶段就失败时台账无 export 条目 → 作用域会退回全局"最近渲染过的那一篇"（并发时会误伤别的论文）
   → `rollback --pdf` 认领**按文档归档件**，让作用域变成确定性的。
3. **指纹不该从库行反推**：库行是"翻过的产物"，骨架档恰恰没有产物 → 反探必然 0 命中。指纹本就在引擎缓存参数里，
   **随侧车带出**（`doc_fp`）最省事也最权威。

### 真机连撞三个洞（都是"看着正常、实际没走回路"）

| 洞 | 症状 | 根因 | 修法 |
|---|---|---|---|
| ① export 必然 0 命中 | 服务端**静默回落成付费机器翻译** | 指纹靠"库内命中行"反探，暂停档一列不写 | 指纹随侧车带出 |
| ② 运营开关被当一次性令牌 | **第一个任务跑完，后续任务静默退回机器翻译**（第二个任务照样烧钱，日志全绿） | 第一趟 finally 里 `pop("PAUSE_TRANSLATE")` 顺手把运营开关也 pop 了 | `_two_pass_adopt` 拆成"开关还原壳"：进门记录、出门原样还回 |
| ③ `inject` dry 演算 FAIL | 豆包稿无处可写 | UPDATE-only 找不到可改的行 | 暂停档落骨架行（见上） |

### 结果（真机，非离线）

- **正常回路 22 秒**：`提字中(0-6s) → 待译(8s) → 交件 → 回灌重渲染 → 完成(22s)`；三条硬判据：第二趟**无任何 siliconflow 请求**、
  `verify_render --expect` 查到豆包稿特征串 **p1×4 / p2×1**、库行由 UPDATE **就地改写**（本轮零新增原文）；
- **超时兜底**：2 分钟到点 → `abort()` → 骨架行 **5 → 0** → 回落成机器翻译，任务 `success` 且产物是正常中文 PDF；
- 回归全绿，镜像 11/11 PASS；开关默认**关**（`PAUSE_TRANSLATE` 未设时行为与过去完全一致）。

> 一句话：**这一章把"第二公里"从口号做成了状态机。** 第一公里（英文变好中文）交给引擎或豆包；
> 我们要的是"这篇论文在任何一天被再次修改时，能用 45 秒、零 LLM、不破坏版面地改对，且每改必验、每验留痕"。

---

## 第十一章 从"自用工具"到"别人能装"（2026-09-19，v28.24 / v28.25）

> 缘起：用户问"**我们的项目是否落地了？**"。诚实答案分两层——
> **作为你自己的工作流：落地了**（真机跑通、有产物、有台账、有回归）；
> **作为"别人能装能用"的产品：还差一步**（硬编码 + 从未在干净环境验证过）。这一章补的就是那一步里**能在代码里解决**的部分。

### v28.24 硬编码收口：唯一的功能性阻塞点在引擎补丁

扫全仓（`*.py` / 配置模板 / 脚本）后结论很清楚：工具层早已环境变量化（`adopt` / `seg_export` / `seg_import` /
`doubao_bridge` / `server.py` 全走 `P2Z_PROJ`），**唯一的功能性硬编码残在引擎补丁** `patches/pdfminer_pdffont.py` 两处
—— 字形校正表与字体降级台账焊死 `D:\zotero-pdf2zh`。

| 维度 | 说明 |
|---|---|
| 危害形态 | **不是报错，是静默**：读取处是 `except Exception: pass` → 换盘符后校正表为空、**字形校正无声消失且零报错** |
| 修法 | 新增 `_proj_root()`（`P2Z_PROJ` 优先，未设退回本机默认）——与 `tools/*` 和 `server.py` 同一套约定，不引入新的路径探测方式 |
| 顺带收口 | 校正表加 `PDF2ZH_FONT_FIXES` 覆盖；`except: pass` 拆成**缺文件静默**（允许不部署）+ **读不动出声**（JSON 坏 / 权限 → stderr 告警） |
| 为什么引擎侧不用额外接线 | `server.py` 本就把 `P2Z_PROJ` 注入它拉起工具的子进程环境，引擎侧用 `os.environ.copy()` 继承 → **在启动服务端的窗口里设一次，服务端 → 引擎一路到底** |
| 配套文档 | README 第 3 步删掉"**路径必须是 `D:\zotero-pdf2zh`**——配置文件里写死了这个路径"，改为两条可操作项（配置模板里的绝对路径 + `$env:P2Z_PROJ`） |

### v28.25 无用代码二轮清理：判据从"文件名"换成"是否入库"

上一轮（38 个文件）删的是"一看就是垃圾"的（macOS 解压残留、一次性实验脚本）。这一轮换判据：
① `git ls-files --others --exclude-standard` **为空**（没有"既未跟踪也未忽略"的真缺口）
② 已跟踪的 **81 个文件**逐个过三类检查（ruff pyflakes 规则 / AST 全仓引用扫描 / 资产引用核对）。

| 发现 | 处置 |
|---|---|
| `relink_pages.py` 的 `kept`、`converter.py` 里双指针方案的残留 `lo, hi` | 删（死变量不会自己消失，只会骗下一个读代码的人） |
| `tools/tests/test_strategist.py` **被跟踪**，但它 import 的 `tools/strategist.py` 未开源 | `git rm --cached` 取消跟踪（本地文件保留）——**别人 clone 下来这个测试必然跑不动**，入库就是坏的。(v28.30 收口：`tools/tests/` 曾**整体**一并不入库，比这一条更宽；本轮脱敏后整体入库，仅 `test_strategist.py` 仍排除，`run_all.py` 对它自动 SKIP。) |
| `server/README-v4.0.1.pdf`（5.2 MB）与 v4.1.7 并存、全仓零引用 | 删旧留新 |
| **上游死代码 18 处**（`io` / `datetime` / `needUpdate` / `source_mode` …） | **不动**：特征是在上游底座与我们的镜像里**成对出现** = 上游自身的死代码，改它等于往"最小可审计补丁集"里掺非自研改动 |
| AST 函数级孤儿扫描（460 个 py/md/ps1） | `tools/` **0 命中**——我们的工具没有"定义了没人调用"的函数 |

### 结果与未竟

- `sync_patches.ps1` **11/11 PASS**、`tools/tests/run_all.py` 全绿（含军师 49 项单测，证明取消跟踪没动本地能力）；
- 本轮**只消除了已知的阻塞点**。真正的验证是"**换个目录从 README 第 1 步走到最后一步**"——本次未做，仍是挂账。

> 一句话：**能让别人装 ≠ 已经验证别人装。** 这一章把"装不上"从"未知"变成"已知且已修"，
> 但"别人装完能不能跑通"这句话，只有在干净目录里真跑一遍之后才配说。

### v28.30 对外分发：工具链入库 + 只装插件时的等价校验

上一章留下一个悬案：`tools/tests/` 整体不入库，于是公开分支里 `run_all.py` 必然跑不动 ——
**自己的回归门禁对外是句空话**。本轮把这条收口。

**为什么之前不入库、这次为什么可以。** 之前的理由只有一条：含本机路径。逐文件体检后，
`tools/tests/` 21 个文件里**没有人名、没有密钥、没有论文原文**，只有 10 处「一行式」本机默认值：

```python
_VENV_SITE = os.environ.get("PDF2ZH_VENV_SITE", r"D:\Users\<user>\...\Lib\site-packages")
```

`run_all.py` 的解释器默认值、`sync_patches.ps1` 的 `$venvSite` 同类。这类默认值**不需要删掉，改成派生即可**：

| 位置 | 改后 |
|---|---|
| 10 处 `_VENV_SITE` 默认值 | `os.path.join(sys.prefix, "Lib", "site-packages")` |
| `run_all.py` 解释器默认值 | `sys.executable`（用哪个 python 跑 runner，就用哪个跑套件） |
| `sync_patches.ps1` 的 `$venvSite` | `$env:PDF2ZH_VENV_SITE` → `$env:PDF2ZH_PYTHON` 同级推导 |

本机行为逐字不变（用 venv python 跑，`sys.prefix` 就是那个 venv），换台电脑自动跟随。
反过来说，**已入库**的 `adopt.py` / `doubao_bridge.py` / `post_check.py` 里本来就公开写着
SILAGE / wang2026 / EgoPhys 这些论文名与事故背景 —— 所以 `tools/tests/` 入库**不引入任何新的泄露类别**。
契约写进 `.gitignore`：**入库脚本里不得出现 `C:\Users\<某人>`（用户身份）或焊死到某个 venv 的绝对路径**；
项目默认根 `D:\zotero-pdf2zh` 作兜底缺省是既有约定（有 `P2Z_PROJ` 覆盖），不算违规。

**入库清单与 SKIP 契约。**

- 入库 `tools/tests/`（20 套件 + `run_all.py`）与 `tools/sync_patches.ps1`；
- 唯独 `tools/tests/test_strategist.py` 仍排除 —— 它的被测对象 `tools/strategist.py` 属"含本机路径、本地保留"；
- 于是 `run_all.py` 新增 `OPTIONAL_SUBJECTS`：**套件文件缺、或缺被测对象 → SKIP 并单列，不算失败**。
  不这么做，别人 clone 下来一片红，回归就从"门禁"退化成"噪音" —— 这正是上一章那条教训的正面做法；
- 顺带给 `sync_patches.ps1` 补了 UTF-8 BOM：PS 5.1 在**无 BOM** 时按 ANSI 解码，中文字节错位会
  吞掉后面的引号 → 报"字符串缺少终止符"（本轮真踩到，改完才发现）。

| 验证 | 结果 |
|---|---|
| 本机全量回归 | 20 套件全绿、**0 SKIP** |
| 模拟公开 clone（临时移走 `strategist.py`） | `SKIP 本机未部署: tools/strategist.py`，退出码 **0** |
| `sync_patches.ps1` | 11/11 PASS，`patches/` 无改动（仍一致） |

**只装插件/服务、没有开发工具链时的等价校验。** `run_all.py` 是**开发侧**门禁，普通用户不需要跑它。
对应的运行期校验是现成的、**不依赖 `tools/tests/`**：

| 关注点 | 开发侧套件 | 运行期等价做法 |
|---|---|---|
| 服务起得来 / 任务状态机 | `test_failure_brief` / `test_progress_log` | `GET /health`；`/api/history` 看任务状态 |
| 译文没被漏译、文献区没被汉化 | `test_post_check` / `test_pre_check` | 翻译后**自动**落的 `server/translated/review/`：`翻译后质检_*.md`（门禁判定 PASS/FAIL）、`翻译前体检_*.md`（推荐 skipLastPages）——判据与这两个套件同源 |
| 交件本身合格 | `test_adopt`（六道内容门禁） | `tools/adopt.py deliver` 的台账 `logs/adopt/<name>.json`；被拒时看 `review/门禁拒收_*.md`（逐段清单）；`--dry` 先演算不落库 |
| 补丁与镜像一致 | `test_mirrors_sync` | `tools/sync_patches.ps1`（11 项 MD5，任一不一致退出码 1） |

> 一句话：**开发侧的门禁是"改代码前后必须跑"，用户侧的门禁是"每次翻译自动跑"。**
> 后者不依赖 `tools/tests/`，所以工具链此前不入库，也**没有**让用户侧失去保护 ——
> 丢掉的只是"别人能自己复核这套判据"的能力，本轮补回。

---

## 第十二章 把话带回上游（2026-09-22，v28.66）：核对过的反馈清单

> 缘起：有人问"我们到底解决了官方的什么问题"。第一次回答把两层"官方"混成了一层，
> 还把**上游已经自己修好**的条目当成了现存缺陷。本章是**核对之后**的版本：
> 每条都标复核状态，**被推翻和撤掉的两条单独列在末尾**，不允许再混进"官方缺陷"清单。

### 一、先把两层"官方"分开

| 层 | 是谁 | 我们与它的关系 | 反馈往哪送 |
|---|---|---|---|
| **上游本体** | `guaguastandup/zotero-pdf2zh` v4.1.7（Zotero 插件 + Flask server + 环境管理 + 启动/更新器） | fork 它、深度改它（`patches/` 就是改动镜像） | 提给本仓库 |
| **翻译内核** | `pdf2zh` 1.9.11 / `pdf2zh_next` 2.9.0（BabelDOC） | 只调用、**不打补丁**；`--skip-subset-fonts` 这类是上游自己就有 | 提给对应内核仓库 |

判据（可复现）：`refs/remotes/fork/main = 35af40f` 就是上游 v4.1.7 的**原版树**，
两份历史**没有共同祖先**，所以 `git diff 35af40f HEAD` 正好等于**"我们对上游的全部改动"** ——
反馈候选只从这份 diff 里取，不从"感觉上官方应该有问题"里取。

### 二、反馈清单（按严重度，带复核状态）

| # | 上游位置 | 症状（一句话） | 复核状态 | 去向 |
|---|---|---|---|---|
| 1 | `utils/execute.py:145-151` | PTY 出现真非法字节后 `leftover` 永久卡在头部 → 进度解析**永久冻结**、内存无界增长 | **已复核**（脚本实测） | **已备 PR** |
| 2 | `utils/venv.py:358` | 引擎由整条命令行子串判定，输入路径含 `pdf2zh_next` 就选错环境，报错方向完全误导 | **已复核**（脚本实测） | **已备 PR** |
| 3 | `utils/cropper.py:195-254` | `merge_pdf` 异常路径不关句柄 → Windows 上产物被占用、"重试也失败" | 已复核 | issue |
| 4 | `utils/cropper.py:298` | `pdf_dual_mode` 丢弃 `merge_pdf` 返回值，返回一个**不存在**的路径 | 已复核 | issue |
| 5 | `utils/deepseek_thinking.py:98-118` | `toml.dump` 全量重写 `config.toml` → **抹掉用户注释**与键顺序 | 已复核 | issue |
| 6 | `utils/execute.py:454-473` | "句柄有效"≠"是控制台"（stdout 重定向/服务化）→ 监视线程**静默退出**，进度恒 0 | 已复核 | issue |
| 7 | `utils/execute.py:252-274` | tqdm 分支未 `return`，会被 legacy 正则用**另一个分母**二次覆写 | 已复核（结构） | issue（先补日志） |
| 8 | `server.py:1036` | `-p 1-5` 塞进单个 token；实测**碰巧能用**（argparse 前导空格 + `int()` 容忍） | 已复核（实测，**非故障**） | issue（低优先，一致性） |
| 9 | `server.py:1086-1088` | `--pages 1-{end}` 无 `end < 1` 守卫，`skip_last_pages ≥ 页数` 时生成非法区间 | 已复核 | issue |
| 10 | `server.py:1050` | `basename(...).replace('.pdf','')` 不是去扩展名 → `Paper.PDF` 时产物路径对不上 | 已复核（推理） | issue |
| 11 | `utils/execute2.py` / `execute3.py` / `record.py` | 4.1.7 仍保留、全仓**零引用**的孤儿文件（各带一份旧的 `_execute_with_inherit`） | 已复核（全仓零引用） | issue（建议删） |

另有 6 条属"扫描到但我方未逐条复读"（`utils/config.py` 字体键早退、空值键被清理循环删掉、
`example` 当托管事实源、TOML 写字符串 `"null"`、
`print(self.__dict__)` 泄密、`environment_lifecycle.py` 不可达代码）——**在 issue 正文里单独列表并标注"待确认"**，
不进上表，避免把没核实的东西说成结论。

### 三、两条已经备好的 PR（只含这两条改动）

落点：`D:\zotero-pdf2zh-eval\feedback\`（**不进本仓库**）

| 交付物 | 内容 |
|---|---|
| `pr_upstream_fix.patch` | 8038 字节 / 3 文件 / +151 −6；`git apply --check` 在 `main` 上通过 |
| `PR_描述_上游修复.md` | 提交信息、改动前后代码、复现输出、8 条测试覆盖表、影响面、一键提交命令 |
| `repro_old_behavior.py` | 只用标准库的最小复现：输出 `leftover 30 字节 / 后续进度行未被解析到 / 引擎选错` |
| `issue_上游缺陷报告.md` | 11 条已复核 + 6 条待确认 + 2 条撤销，按上游 issue 模板填好执行环境 |
| 上游克隆内的分支 | `D:\zotero-pdf2zh-eval\upstream` @ `feedback/upstream`（commit `773bfa5`，**未推送**） |

两条 PR 改动都做了"**先红后绿**"：本机 Python 3.12 下新增的 8 条回归测试全绿，
其中 4 条在旧实现下必红（`_engine_from_command` 不存在时收集期即失败，行为级红证据在 `repro_old_behavior.py`）。
测试文件放在 `server/tests/`，被上游 CI 已有的 `unittest discover` **自动收集，不需要改 workflow**。

### 四、这两条**不**做成上游 PR（有意不做）

第九章"给官方的四条建议"里的第 2、3 条 —— **补"渲染残渣"断言**、**表页零汉化视为预期** —— 我们**不走 PR**：

- 上游本体**没有门禁层**。这两条是"给一条质检流水线加断言/加豁免"，上游没有这条流水线，
  提交过去只能是**没人调用的死代码**，属于表面功夫；
- 它们真正的归属是**我们的 `tools/post_check.py`**（第 5 断言 v28.17、表页豁免 v28.46 都已落地），
  对外只适合以"建议 + 实测证据"的形式写进 issue，让上游自己决定要不要建这套东西。

### 五、本章的口径纠正（三条，必须记住）

1. **"`.example` 会覆盖用户配置"——上游已自修。**
   上游 v4.1.7 已有 `server/utils/config_migration.py`：文件已存在时走 `_merge_defaults(defaults, current)`
   **以用户值为准**（`config_migration.py:29/125/155`），坏文件先 `_backup_invalid` 再恢复默认；
   `server/server.py:1244-1246` 也写明
   `# Never overwrite an existing user config with the .example template.`。
   → 本条从"官方缺陷"清单里**删除**（我们的"用户值优先"补丁与它**同向**，不是对立）。
2. **"`task_manager` 返回内部字典活引用"——不成立，主动撤销。**
   复核 `task_manager.py:123-130`：`get_active_tasks_list` / `get_history` 返回的都是 `list(...)` **浅拷贝**，
   列表结构本身安全；被共享的只是元素对象。原判据是扫描器的误读。
3. **"next 分支把主 `config.toml` 交给子进程会被回写"——不成立，主动撤销。**
   原判据只看到"pdf2zh 1.x 有副本、next 没有"的不对称，**没有验证 next 内核是否真的写回**。
   复核（读已发布内核 `pdf2zh_next 2.9.0` 源码）：`config/main.py:587-596` 读完 `--config-file` 即
   `del merged_args["config_file"]`；全包唯一写用户配置的 `write_user_default_config_file()`（`:615-628`）
   落点固定在 `~/.config/pdf2zh/config.v3.toml`（`const.py:8-10`）且**只被 GUI 调用**（`gui.py:911/3472`）；
   `_update_version_default_config()`（`:269-281`）写 `~/.config/pdf2zh/default/2.9.0.toml`。
   → 传入的配置文件是**只读**的。**保留的观察**：不对称本身仍值得记（若未来 next 引入写回，本侧无保护），
   但当下**不是缺陷**，已从 issue 与风险榜中撤下。

> 教训落地：**"先核对再反馈"不是谨慎，是必需**。这几条如果不核对就发出去，
> 官方只要回一句"我们早就修了 / 你读错了"，整份反馈的可信度就一起打折。

