# zotero-pdf2zh 项目代码全面检查报告

- **检查时间**: 2026-09-03
- **检查方式**: 四路并行逐行审查（服务主程序 / 配置环境核心 / 执行工具链 / 工具脚本与补丁），关键疑点均做运行时实证（pypdf 加密行为、正则实际匹配、并发模型取证、git 跟踪状态、JSON 合法性、PS 5.1 实测）
- **检查范围**: server/server.py、server/utils/ 全部 14 个模块、tools/ 全部 3 个工具、patches/ 补丁备份及 venv 部署版 translator.py、启动脚本（launch.ps1 / start_all.ps1）、requirements.txt、config.json / config.json.example
- **发现问题**: 共 89 项 —— 🔴 严重 5 项 / 🟡 中等 27 项 / 🔵 轻微 57 项
- **密钥安全**: 报告中所有密钥均已掩码

---

## 一、🔴 严重问题（5 项，建议立即修复）

### 🔴1. config.json 并发读写无锁 + 非原子写入 —— POLISH 置 null 事故的根源
- **位置**: server/utils/config.py L210-308（pdf2zh）、L318-427（pdf2zh_next）；伴随 server.py L1070/L1136
- **类型**: 并发竞态 / 数据损坏
- **描述**: `update_config_file` 对全项目共享的 config.json 做"读→改→写"，全程无锁且 `json.dump` 非原子。Flask `threaded=True` + 每任务一线程，并发提交真实存在。确切竞态窗口：预置回填补丁只作用于**本次请求的 service**，其他 translator 的 null 值从旧快照原样带过——任何针对其他服务的并发请求都会把已修复的 null "复活"。双线程交错写还会造成文件偏移错位混写，下次启动时 config_migration 会把文件**重置为 example，用户全部配置丢失**。
- **修复**: 模块级 `threading.Lock()` 包住整个 update_config_file；写入改用 config_migration 已有的 `_atomic_write_text`（temp + os.replace）。约 10 行改动。修复后对 silicon 发起一次翻译让回填自动修复 POLISH=null。

### 🔴2. venv.py VirtualEnvManager 完全无线程同步
- **位置**: server/utils/venv.py 全文件
- **类型**: 并发 / 环境损坏
- **描述**: 单例被所有任务线程与自动质检线程共享，但全文件**连 threading 都未导入**，无任何锁。两个任务线程同时进入 `ensure_env` → `install_packages` 会并发对同一环境跑 uv/pip 安装 → site-packages 损坏；`_resolved_command` 每次翻译都调 `ensure_env`，check-then-act 竞态真实存在。
- **修复**: 类内加 `threading.RLock()`；`ensure_env` 全程持锁，`install_packages` 内做双检锁。

### 🔴3. config_migration.py 把一切 OS 异常当"配置损坏"，用默认配置覆写用户配置
- **位置**: server/utils/config_migration.py L113-123、146-153
- **类型**: 数据丢失 / 逻辑漏洞
- **描述**: `_migrate_json/_migrate_toml` 的 `except Exception` 把 PermissionError（Windows 文件被占用）、磁盘 IO 错误全部当解析失败 → "备份 → 恢复默认配置"，用户的 apiKey、翻译器设置从生效配置中静默消失（翻译突然全部失败的元凶之一）。这就是历史上"用户配置被覆写"事故的根源之一。`server.py:1313` 调用处无兜底。
- **修复**: 只捕获 `json.JSONDecodeError / toml.TomlDecodeError` 走"恢复默认"；OSError 类跳过迁移并告警，绝不动原文件；`_backup_invalid` 单独 try 包裹。

### 🔴4. server.py 同名任务并发时输入/输出文件互相覆盖
- **位置**: server.py L279-280（上传写入）、L1002-1020、L609-619、L944-962（确定性产物名 remove/copyfile）
- **类型**: 并发竞态 / 文件损坏
- **描述**: 上传与所有产物使用确定性文件名，无 per-task 隔离。用户重复点击翻译（异步协议下常见）时：后到请求会截断正在被子进程读取的输入文件；产物同名互相覆盖；Windows 上撞句柄抛 PermissionError。产出损坏 PDF 或随机失败——今天重复提交反复出现，风险真实。
- **修复**: per-task 工作目录（translated/.work/<task_id>/），完成后移入最终产物；或相同 fileName 的任务排队/拒绝。

### 🔴5. post_check.py "原文保留页"豁免漏洞 —— 参考文献页翻译失败会被误判 PASS
- **位置**: tools/post_check.py L116-120
- **类型**: 逻辑漏洞（质检门禁误放行）
- **描述**: 豁免判定仅凭**译文输出特征**（行首 [n] 密集 + 无汉字），并不知道任务是否真的配置了跳页。两种场景撞豁免：① LLM 对文献行原样回显英文；② 用户跳页数小于实际文献区页数——未被跳过的失败页同样豁免。且全文引用对账因"标号完好"也通过。门禁在最经典的参考文献页事故的一种主要形态上静默放行。
- **修复**: 增加 `--skip-last N` 参数（服务端把任务实际使用的 skipLastPages 传入），只豁免"末尾连续恰好 N 页"；无参数不豁免。

---

## 二、🟡 中等问题（27 项，按模块分组）

### server.py
| # | 位置 | 问题 | 描述与建议 |
|---|---|---|---|
| 1 | L176-180 + task_manager L123-126 | 并发 | `events()` SSE 对活动任务 dict 做无快照序列化，`complete_task` 在 worker 线程新增键 → `RuntimeError: dictionary changed size during iteration`，恰在任务完成瞬间打断 SSE。修复：`get_active_tasks_list` 返回 deepcopy（一行） |
| 2 | L650 | 逻辑漏洞 | 自动质检钩子 `endswith('-mono.pdf')` 不匹配 pdf2zh_next 的 `name.{lang}.mono.pdf` 命名 → **next 引擎质检静默失效**。修复：`endswith(('-mono.pdf', '.mono.pdf'))`；另 QC 线程无并发上限，建议加 Semaphore 限流 |
| 3 | L102、L254-282 | 安全/DoS | 未设 `MAX_CONTENT_LENGTH`，base64 全量驻留内存；`--host 0.0.0.0` 是受支持用法，远程可打爆内存。且解码后未校验 `%PDF` 魔数。修复：设 500MB 上限 + 解码后魔数校验 |
| 4 | L1101-1103、L1153-1155 | 边界 | `skip_last_pages` ≥ 总页数时生成 `-p 1-0` 等非法参数；`'-p 1-5'` 单 token 内嵌空格属脆弱写法。修复：`end<1` 抛业务错误；改 `cmd.extend(['-p', f'1-{end}'])` |
| 5 | L1117、L1185 | 边界 | `.replace('.pdf','')` 替换所有出现，与 stem 语义在 `a.pdf.b.pdf` 类文件名上分歧（next 引擎受影响） |
| 6 | L559-566 | 运行时 | pdf2zh_next 只产出 mono/dual 之一时 `retList[1]` IndexError，报错信息无业务含义 |
| 7 | L507-508 | 资源 | 校验失败路径留下孤儿上传文件 |
| 8 | L272-273 | 健壮性 | data URL 前缀仅识别精确小写形式，变体会整串进 b64decode |
| 9 | L1471-1474 | 健壮性 | 启动交互 `input()` 未捕获 EOFError（L1493 处已处理，此处遗漏） |
| 10 | L853/L930/L1055/L541 | 规范 | 冗余布尔判断；babeldoc 时 compare 被静默跳过无日志 |

### utils/config.py
| # | 位置 | 问题 | 描述与建议 |
|---|---|---|---|
| 11 | L216-218 | 逻辑漏洞 | bing/google 等无 config_map 的服务提前 return，跳过 NOTO_FONT_PATH 全局字体写入 → 用户为 bing/google 配置的字体被静默忽略。修复：字体写入提到判空之前 |
| 12 | L168-170 | 运行时 | `pdf_w_offset`/`pdf_h_offset` 等数值参数无 try/except 守卫（threadNum 有），非数字 → 整个请求 500 |
| 13 | L260/L399 | 运行时 | apiKey 掩码对非字符串值 `len(value)` 抛 TypeError；应复用 `_safe_log_value` |
| 14 | L361 | 逻辑漏洞 | 向 TOML 写字符串哨兵 `"null"`（TOML 无 null），任何新消费者直接 int() 即崩 |
| 15 | L290-296 | 设计确认 | 回填无条件覆盖 POLISH_*（插件 UI 永远改不动这些键）——属有意托管，建议注释明示 |
| 16 | L420/L200 | 规范 | 遗留调试输出 `print(translator.keys())`；`print(self.__dict__)` 有未来泄密风险，建议删除 |
| 17 | L220-221 | 健壮性 | 运行期配置文件损坏无自愈（迁移只在启动时），json.load 异常 → 请求 500 |

### utils/venv.py
| # | 位置 | 问题 | 描述与建议 |
|---|---|---|---|
| 18 | L357-358 | 逻辑漏洞 | 引擎识别靠全命令行子串嗅探 `"pdf2zh_next" in " ".join(command)`——文件名含 "pdf2zh_next" 的 PDF 会让任务误判引擎；非 str 元素 TypeError。修复：调用方显式传 engine |
| 19 | L269 + env_lifecycle L193-207 | 性能 | conda 用户每次翻译请求 fork 一个 `conda info --json` 子进程（数百 ms~秒级），且超时误判环境不存在可能误入重装。修复：缓存命中降级为路径存在性检查或加 TTL |
| 20 | L348-355 | 规范 | `get_conda_bin_dir` 返回 False/str/None 三态，统一 Optional[str] |

### utils/（执行与辅助）
| # | 位置 | 问题 | 描述与建议 |
|---|---|---|---|
| 21 | execute.py L337-362、709-715 | 资源/可用性 | 子进程执行**完全无超时/看门狗**——网络停滞挂死时任务永久 running，只能重启服务（今天 16 分钟卡死就是此问题的温和版）。建议加总超时或"无输出 N 分钟"空闲看门狗 |
| 22 | auto_update.py L468-527 + server.py 启动路径 | 启动阻塞 | 启动时同步网络检查最坏阻塞 1~2 分钟（GitHub 被墙环境每次启动都卡）。建议通知/更新检查放 daemon 线程、超时降到 5s 级 |
| 23 | execute.py L379-581 | 并发 | Windows 控制台进度监控多任务共享同一屏幕缓冲，进度串扰/卡旧值；无控制台运行时 SSE 进度恒 0%。建议并发任务用 execute3 式管道隔离 |
| 24 | cropper.py L202-254、298-299 | 资源泄漏 | merge_pdf 异常路径不 close 文档句柄（Windows 下句柄占用后续 PermissionError）；TB→LR 分支不检查 None 返回值 |
| 25 | execute.py L145-151 | 逻辑漏洞 | PTY 解码器遇非法字节 leftover 永久卡死（仅 POSIX 路径触发）；execute2/3 的增量解码器写法正确，应回移 |
| 26 | execute.py L242-264 | 逻辑不一致 | tqdm 分支缺 early-return，legacy 正则可能二次覆写进度 |
| 27 | config_migration.py L20-26 | 用户数据 | 托管包版本约束每次启动被 release 默认覆写，用户手动锁定的版本丢失 |
| 28 | deepseek_thinking.py L98-118 | 用户数据 | strip_thinking_fields_from_config 全量 toml 重写丢全部注释，建议 tomlkit 或只告警 |

### tools/ 与补丁
| # | 位置 | 问题 | 描述与建议 |
|---|---|---|---|
| 29 | config.json L10/L26/L33 | 安全 | 三个真实 API Key 明文落盘（git 已排除无仓库泄漏）。建议条件允许改环境变量（set_envs 本就优先读 os.environ），并视泄露面轮换 |
| 30 | tools/pre_check.py L267-273 | 边界 | 加密 PDF（用户口令）在 len(pages) 处直接抛 FileNotDecryptedError，"加密预检"分支不可达，报错文案不明确。建议捕获并给定向提示 |
| 31 | tools/post_check.py L258 | 退出码 | 页数计算在 try 块外，PDF 异常时 traceback 退出码 1 冒充 FAIL（应为 2）。自动化会把基础设施错误当质量不合格 |
| 32 | patches/pdf2zh_translator.py L449-507 | 缓存失效 | POLISH_MODEL/DOMAIN/GUIDELINE/FORMULA_MAP 及 terms.csv 内容不进缓存键——修改配置或编辑术语表后重翻命中旧缓存，"改了配置没生效"。建议整体指纹登记为单个缓存键 |
| 33 | tools/term_verify.py L264-281 | 网络健壮性 | 重试只覆盖 HTTP 429/5xx；超时/URLError/连接重置立即放弃。建议对超时类也做 1-2 次退避 |

---

## 三、🔵 轻微问题（57 项，摘要）

**server.py（8 项）**: data URL 前缀大小写、`input()` EOFError、孤儿上传文件、`config.babeldoc == False` 规范、winexe 缺失静默回退无警告、冗余布尔表达式、`.replace('.pdf','')` stem 语义、`-p` 参数拼接脆弱。

**utils/config.py（4 项）**: 浅拷贝共享嵌套结构（当前无害的陷阱）、遗留调试 print、TOML null 哨兵、运行期损坏无自愈。

**utils/venv.py + environment_lifecycle.py（6 项）**: env_lifecycle L901-963 冻结补丁后约 60 行死代码（含交互式 input 状态机）；conda 环境名大小写敏感匹配；rmtree 静默半删后 uv venv 失败无诊断；read_versions 失败误报安装失败；specifier.contains 未传 prereleases；get_conda_bin_dir 三态返回。

**utils/（执行链，12 项）**: **execute2.py / execute3.py / record.py 为死代码**（全仓库无引用，execute3 管道模式还缺 PYTHONIOENCODING）——建议删除，维护面减半；execute.py INVALID_HANDLE_VALUE 判断失效（有兜底）；final_cmd 全量打印建议复用 safe_cmd 掩码；宽度守护改用户终端尺寸未声明副作用；大段注释掉的调试代码应删除（git 已有历史）；task_manager 同 id 静默覆盖、失败态可被改写；cropper 零除/混合页尺寸/先删后写；package_network 截断误判；auto_update zip 无校验和、回滚不清理空目录；deepseek_thinking `_option_value` 不检查 flag 边界、能力缓存无锁。

**tools/ 与补丁（27 项）**:
- pre_check.py: MATH_CHARS `∂` 重复；`[\u0391-\u03c9]` 会把希腊语原文误判公式密集；REF_HEADING_WORDS 含"文献"且无锚点（文献综述页误报）；末尾页解析失败破坏"后缀"判定；0 页 PDF 给绿灯；同秒重跑报告覆盖
- post_check.py: 页数不一致仅中级仍 PASS（建议差值>1 判 FAIL）；汉化率分母含数字（数字占比>95% 的纯数据页误报）；同秒报告覆盖
- term_verify.py: docstring 与实际默认值/行为不符（delay 1.0→2.0、失败仍出报告）；死参数 `context`、死函数 `write_glossary_updates`；open 未用 with；`urllib.error` 隐式导入；seen 去重丢段落归属；markdown 表格未转义 `|`
- patches/pdf2zh_translator.py: prompt 中 `{{v数字}}` 双大括号表述与实际单大括号不符（LLM 实际不受影响，易误导维护）；`_polish_report_seq` 无锁（并发报告排版交错）；上游遗留 ZhipuTranslator except 分支引用未绑定变量 `response`
- 脚本: launch.ps1 uv 路径硬编码无 Test-Path、无端口防护；**start_all.ps1 L105 注释断言"PS 5.1 不允许 -WindowStyle+Redirect 组合"是错误的**（launch.ps1 实测正常，勿据此"修回"）；start_all KEY 探测正则未锚定行首（注释行误报已配置）；`Get-Command python` 无守卫；固定 sleep 3s 单次探活（冷启动误报失败）
- 依赖: requirements.txt 的 `argparse` 不应列为依赖（标准库）；requirements.txt 与 patches/ 下副本重复维护必漂移；config.json.example 缺 NOTO_FONT_PATH 顶层键、与 config.example.json 两份示例并存易漂移；config.json 的 POLISH/POLISH_REFLECT 为 null（与 example 相反，如非有意请恢复 "1"——🔴1 修复后会自动治愈）

---

## 四、已验证无误的关键点（对照）

- 全部子进程调用为列表参数 + `shell=False`，**无命令注入面**
- 密钥日志脱敏规范良好（config.py `********`+尾4位、winexe 命令行掩码、server.py 不落密钥明文、term_verify 不打印 key）
- task_manager 锁设计合理，complete_task 幂等防覆盖成功态；history 上限 200，无内存无限增长
- config_map.py 与 config.toml.example 的 21 个服务段逐项核对**完全一致，无缺失无拼错**
- pre/post_check 行首条目正则、占位符正则（单大括号）与 converter 实际生成方式一致
- 润色钩子 polish 异常处理正确（兜底保留初译、记全栈、照常入缓存）
- environment_lifecycle 的 Windows/conda 路径差异处理、原子写、分层健康检查是全项目最健壮的部分
- start_all.ps1 自我复制 bug 确认已删除；config.json 与 example 均为合法 JSON
- cropper 无临时文件泄漏；大 PDF 内存峰值 O(单页) 可接受

---

## 五、修复路线图（建议顺序）

| 优先级 | 事项 | 工作量 |
|---|---|---|
| P0 | 🔴1 config.py 加进程锁 + 原子写（复用 _atomic_write_text），随后触发一次 silicon 翻译自愈 POLISH | ~10 行 |
| P0 | 🔴3 config_migration 异常分类收窄（只对解析错误恢复默认） | ~8 行 |
| P0 | 🟡2 质检钩子兼容 pdf2zh_next 命名 + 🟡1 SSE deepcopy 一行修 | ~5 行 |
| P1 | 🔴2 venv.py 加 RLock；🔴4 任务级工作目录或同名排队；🔴5 post_check 豁免参数化 | 中等 |
| P1 | 🟡21 子进程看门狗；🟡22 启动网络检查线程化 | 中等 |
| P2 | 🔵 死代码清理（execute2/3、record.py、注释块）；🟡11-14 config.py 小修；tools 阈值打磨 | 低 |
| P2 | 密钥迁环境变量 + 视需要轮换；config 双示例合并 | 低 |
| P3 | 长期: 迁移 pdf2zh 2.0/BabelDOC 内核（见 UPGRADE_RUNBOOK.md），从根上解决版面解析容错 | 大 |

---

*报告由四路并行审查汇总生成；生成工具: TraeCode。任何修复后请同步更新 改动记录.md。*
